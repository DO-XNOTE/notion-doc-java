---
title: 014.用户模块前端页面接入
---

# 014.用户模块前端页面接入

好，同学们，那我们继续进入接下来的课程，在刚才的课程当中，我给大家介绍了使用 spring MVC，分别实现了用户注册、登录以及登出，还有获取当前用户信息对应的一些接口的后台的实现。那在接下来的课程当中，我将带大家一起将用户相关模块的一个前端 html 文件做一次对应接入。
为了节省大家的一个课程的学习时间，我已经将对应的一个 static 的目录下面新建了相关的一些文件夹，我们可以看一下之前的课程当中只有 test 和 index 点 h t， m l，那我在这边新建了 CSS 的文件夹，其中包含了我们所需要用到的前端的 CSS 文件，已经在这边贴给了大家。


由于我们对应的课程主要还是以搜索推荐的后台服务为主，那前端的页面更多的只不过是一个样式的渲染以及产品化的一个流程。那我就不带着大家在这边一行代码敲了，而是直接将已经调完的 CSS 的样式写在了对应的文件里，同学们可以直接下载源码，里边有包含了对应的工程的一个前端的目录结构以及前端的样式文件，那我们其实可以看到在这个前端的文件里包含了 API 点CSS、 boot strap 点CSS、 common 点 CSS 以及对应的 swiper 点CSS，这里边都包含了在了。


对应的前端的页面元素当中还有这个 image 文件夹，里边包含了我们的课程当中所需要用到的所有的一些图片，都在这里边，然后我们的 script 目录下放了对应的 Javascript 文件，包含了一些 ADX 的 file 的 upload API，waterstrip， swiper score 以及结 query 和 swipe 点js。那这些 GS 文件我们也可以直接在课程当中直接的拿过来使用，那我们要做对应的一个模块，首先来看一下刚才所改造的一个首页的 index 点 html 文件。那我们刚才可以看到在 index 点 h temple 文件，原本我们只不过是书写了一个最简单的一个 templates index，并且接入了对应的一些操作。


那在这节课程当中，我已经将 index 点 h 天猫文件填充上了我们所需要的内容，那我先带同学们浏览一下 h 天猫文件的大概的结构。那骑手这些 header 文件主要是用来引入工程当中需要的 static 目录下的一些 c s 文件，然后我们做了一个文件的一个内联的一些样式，将对应的样式直接写在了文件的头部。我们接着往下看，这个样式还是比较多的。然后我们进入真正的一个 h 天猫的勃列题里边。我们所有的薄蒂体都是分为header、 may 和 footer 三个模块， header 也就相当于是我们一个 H5 文件的一个 navigation bug，一个导航栏。然后 footer 的话主要是一个底部的 time bug。那我们可以看到 footer 的话，我们模仿大众点评有四个Tab，分别是首页、团购、发现和我的，然后我们所有的页面的中间元素都放在了 may 对应的这个框架下。那我可以先启动一下应用，给大家先大概的看一个样式的效果。启动应用在 8010 端口，我们打开Chrome。
Local host Bali yielding Static Index.


点 h t l 然后由于我们对应的一个应用是以 WAP 应用，也就是一个 H5 在手机端的应用为主，那我们直接通过检查页面元素，点击对应的这个button，可以做一个 PC 端跟手机端各页面布局样式的切换。我们现在就将对应的一个页面元素切换到了手机端，那我们其实就可以看到整个一个手机端的页面布局元素，上面是点评的一个 navigation bug，下面是一个 tap bug，分别用橘红色的一个底色背景，再加白色的一个底色背景，组成了整个的一个点评的一个框架性的结构。那我们其实可以看到 header 这边有一个定位是我们现在目前是在上海，当然这个定位的话，目前我在代码当中是写死的，那没有关系，我们后面的话可以接入百度地图相关的一些服务去改变我们这个定位。


我们这边还有一个登录的button，也是我坐在这边的一个 right title。那我们就从用户的登录注册的一个模型开始，那首先我们从产品逻辑上面来讲的话，需要在用户的首页就做一个判断。如果说当前的用户已经登录过了，那这边应该显示的并不是登录按钮，而是注销按钮。如果说当前的用户是未登录的状态，那我这边就要显示一个登录的按钮，引导其去做对应的登录。那我就带着大家使用起 query 的方式去做一次服务端接口的一个前端页面的交互，那我们先停止读物。然后我们移到写 query document ready 这样的一个地方。所有的对应的一个 g s 的操作都应该在 DOM ready，所有的 DOM 渲染完成之后才去做。那我们在这边可以定义一个全局变量叫 current user，它默认的话是一个null，那我们将使用这个 current user 配合后端的一个 get current user 这样的一个接口去判断当前的用户是否是登录状态。


我们直接在 document ready 的时候调用点 r Jax 方法，我们将向后端发起一个对应的一个阿嘉克斯的请求。这个 r tax 请求是用于询问当前用户的一个获取当前用户的信息。那我们就可以直接写对应的 AGX 请求，这个请求的话是一个 post 方法， have 是一个post，然后我们对应的一个 content type。是一个 application json，并且 correct set 等于 UT F 杠8。然后我们所访问的一个 UIL 就是我们对应的一个 user get current user。好，那当这样的一个 r 加 x 请求发送出去之后，会有一个 success block，然后还有一个 fail block，那我们先看一下对应的这个 success flow block。


对应的一个实现，我们需要做一次判断，看一下我们对应的这个 user Controller。当获取当前用户的一个信息的时候，会从 h t p servers request session 里边去获取对应的一个 current user session 的一个变量，那有可能当前用户未登录的情况，那这个变量自然而然返回的就是个null，那当前用户已经登录的情况，那返回的就是当前用户登录时候的那个 user model。


因此我们在这边只需要做一个简单的判断， if data 首先判断后端返回接口是否是 success 状态。这个时候我们刚才所用到的通用的一个返回处理的方式在这边就生效了。我们可以非常简单的去判断当前接口的一个响应的状态，那如果说是 success 就判断对应的 data 是否为空，如果说 data 不为空的话，我们就认定当前用户没有登录，那自然 current user 还是取值是null？如果说当前用户已经登录这个data，其实会返回对应的 user model 的对象，那我们直接 current user，就等于 data 点data。


这个时候我们的同学要注意一下，这边对应的这个 data 是已经被 json string five 反编译之后的，也就是说后端返回的是一个json。但是在解 query 内回调函数这边已经将它转成了对应的一个对象，那其实就是这个 user model 对象，因此我们拿到的这个 current user 其实就是一个对象。


好，然后我们做完了这一步之后，我们需要去判断一下，如果说 G 杠 current user 等于 now 的时候，不好意思，我们对应的这个 writer right title 点 text 就应该显示一个叫登录，引导起去点击登录，如果说已经登录的话，那我们就显示的是一个注销。


那我们现在处理的是这个 success 的情况，那有可能后端的接口响应异常是一个 fail 的情况，那这个时候我们就需要用 error code 跟 error message 的方式去解析，那我们直接 alert 一个叫请求失败。原因为我们加上这个元音其实就是解析 data 中的一个error， message 直接抛在前端页面上。这个 block 的话处理的是请求服务端有正确的响应，这个正确的响应的意思是说我对应服务端是调通了，但是并不代表我对应的业务逻辑响应是正常。业务逻辑响应正常对应的这个success，那我在这边还需要处理，一个是说我如果说服务端异常了，那这个时候就是 ADX 的一个 error block。这个 error block 有可能是网络异常，也有可能是别的异常，也会有一个 data 的回调。那我在这个回 data 的回调这边也直接打出来一句话，叫网络请求失败，原因为这个时候我只需要将 data 点 response text 打在对应的 alert 上面即可。
好，我们使用这种样子的方式就完成了对应的一个获取当前用户的信息。那除了这个之外，我们还需要去改变一下 right about title 的一个状态，也就是说我们对应的这个 right title 其实是可以被做事件响应的，也就是当用户 click 的事件上去之后，我们需要做一次对应的一个判断响应，那它分为两种。


第一种情况是仍然是拿 current user 的数据模型，如果说这个 current user 等于null，那不好意思，我们就需要引导用户跳转对应的一个登录界面。 window 点location，点 HREF 等于static，我们在这边新做了一个 login 点 h t l 那我们等会儿去看这个 login 点 h t m l 并且 return force return false 的意思就是防止对应的时间冒泡，还需到上一层，在这一层就处理掉。如果说是 else 的话，那就说明当前的用户已经登录过了。


那我们就需要调注销接口，那我们直接将对应的这段代码可以直接 copy 过来。我们直接将这段代码可以 copy 过来。调用注销接口，那注销接口的调用方式我们只需要改一下，从这边改成logout。然后如果说对应的注销是成功的，那我们直接让其业务逻辑上让其跳转到首页 static index 点HDML，相当于注销成功。如果说注销失败的话，仍然是一样的一种报错方式。当然我们在这边 on CLICK 的之后，得需要把这个 return force 移到最后。好，然后我们启动一下我们的应用，看一下启动效果是否如我们所预计的一样。


打开浏览器，然后点击刷新，我们在这边开一下network，然后选择这个 x h r，也就是只过滤 r 加 x 请求对应的一个请求。好，我们可以看到这边 get current user 已经有一个正确的返回，返回的是now，那就说明他没有登录过。好，这个时候没有登录过，照道来说，点击这个登录 button 会给我们跳到login，点html，让我们尝试着点击一下。


好，我们其实可以也可以看到它跳到了这个 login 点html，那我们首先看一下 login 点 html 做的元素，其实也比较简单，登录的一个 title bug。然后手机号跟密码的一个 form 的表单输入，然后这边有引导用户点击登录按钮，或者引导用户点击注册按钮，然后我们看一下对应的代码，先将服务停掉。然后我们看一下 login 点 html 这个代码的实现也是在对应的 static 目录下一个 login 点html。好，我们看一下代码的结构，对应的样式。不看了，仍然是有 first header，上面是一个登录的span，然后对应的一个这个第四个头部我们可以删掉，先不用管。然后这边是一个may，也就是我们的一个 may 的一个容器的DIV，那我们先毙掉最下面的话是一个footer。好，那我们来看一下对应的这个 may 里边，它其实就是一个 log in the form，有对应的手机号，然后密码，然后有input。两个 input 分别有自己的一个ID，这个 ID 一个是telephone，一个是password。我们可以通过解 query 的方式将对应的这个 input 里边的内容给取出来。


最后一个登录的button，一个注册的button。好，那我们就进入 document ready 的方式去实现一下我们对应的一个登录跟注册的功能，那我们首先当用户点击注册 button 的时候， register button 点 on CLICK，当用户点击注册 button 的时候，那其实很简单，我们只需要使用微动点 location 点HRF，让其跳转到对应的一个 register 点 h 天猫当中，并且 return 一个false，代表已经处理完了。然后当用户点击 login button 的时候。我们点击 unclicked function。然后这个 login button 的话，处理方式的话，我们首先需要取得用户的 telephone 等于。 yeah value。然后我们还需要取得用户的 password a sword 点value，然后我们就去调用这 query r Jax 去发送对应的一个用户的登录请求。那我们也直接可以先将对应的这个阿加克斯的书写给它拷过来。


好，我们修改一下，首先它调用的请求是 user login，然后我们还需要多传一个data，这个 data 的话我们需要调用json，点 string file 方法去将对应的一个对象给它 json 出来。 p 是telephone， value 是telephone，然后对应的 key 是 pass 我的 value 是 pass 我的。使用这样的一个方法，再调用 json 点 string five，将其作为一个 post 的一个 body 的 data 传给了服务端，那跟我们服务端我们来看一下我们的服务端定义的这样的一个 login 的接口，其实是一模一样的，需要调缝跟password。好，如果说登录成功的话，那我们直接可以跳转首页，如果说登录失败，那就报对应的一个错误。当然这边我们还需要一个 return false，所有的 button on clicked 都需要 return false，代表处理完。


我们先回到首页，仍然是 get current user，返回是个null，那我们点击登录好，这个时候我们输入一下我们的要的手机号，1312345678，这是我们之前注册过的一个手机，然后 123456 密码点击登录。好，我们其实可以看到这边已经发生了一个重定向，直接重定向到了首页，那就说明登录是成功的。然后我们看一下 get current user 返回的对应的一个数据，就是我们用户信息相关的一些数据。好，那我们尝试着点击注销，把它退掉。好，这个时候注销完成之后， get current user 返回的就是一个now，那这个时候我们继续看一下对应的一个报错。如果说我们输入了一个12345678，我们故意将密码输错，点击登录看到请求失败，原因为手机号或密码错误。那这个时候我们其实就可以看到其对应的一个操作是一个 error code 跟一个 error message 的返回，那这个时候我们继续做对应的一个注册的功能。那当我们点击注册的时候，我们其实可以看到跳到了注册的这个 form 上面需要用户填写手机号、密码、昵称跟性别，最后点击注册。那我们来看一下我们对应的一个注册的form。打开好，这边有一个 register 点html，那 register 点 html 具体的就不细看了，其实本质上也是一样有一个注册。然后这个第四个头部我们先删掉，然后有一个 may 里边有手机号、密码、性别、昵称，还有对应的input。


最后一个注册的button， register 的button，那我们直接实现一下这个 register button 即可。那我们其实大同小异，我们把对应的 login 的这个 button on clicked 给它 copy 过来。register。好，我们需要取得的变量就稍微有点多，除了 telephone password 之外，我们还需要取得nickname。然后我们还有一个卷的gender，这边的话有一个post，我们需要调用 register 方法。然后这边的话有几个，一个是nickname，一个是gender。好，这个时候若用户注册成功，那我们直接在这边可以抛一个 alert 叫注册成功，并且重定向，我们把它重定向到 login 点html，让他去登录。如果说注册失败，那不好意思，我们将错误抛送在对应的一个界面上。好，我们点击启动。


来到这个注册页面，我们直接刷新对应的注册，先搞一个错的12345678，我们注册重复的手机号，然后这个随便填注册，我们可以看到这边的代码有一点问题，我们需要找一下这边这个 button 写错了，这个没改。 register button 好，重新启动一下。我们将对应的这个页面重新刷新看一下，启动好了，刷新好 13 12345678，然后随便输。这边已经说用户已经存在对应的请求发送失败了。好，那我们注册一个好的号，C9，密码123456，昵称 IMOC another one，有点长，然后性别我们把它改成女的，点击注册，注册成功，好。然后我们 12345679 密码123456，好，点击登录，好，登录成功。


好，那我们现在就完成了前端的用户模块的一个界面的接入，整体来说还是相对来说比较简单。我们使用了 BUG x phone post，完成了对应的 user Controller 相关的一些登录、注册、注销以及获取当前用户信息接口的一个前端接入。

