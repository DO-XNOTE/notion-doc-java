---
title: 005.开发工具介绍
---

# 005.开发工具介绍

好，同学们，那我们接下来就进入基础型的项目的一个搭建的过程，那首先我们来看一下所使用到的开发工具，大家都知道工欲善其事必先利其器，那因此我们对应的选择了一个 Java 和其相关的框架的开发工具非常的重要。首先我们现在大部分的公司都会采用 Java SDK 1.8，也就是 1.8 版本的一个JDK，做 Java 开发语言的一个基础。那接下来我们会使用阿帕奇的Maven，我这边选择的是 3.2 的版本， Maven 主要是用来做包的项目的一个管理。那等会儿我会跟大家详细的介绍对应的环境搭建的一个过程，那最后我们会使用 Intelligent 的一个 idea 工具做 IDE 的开发环境。


好，先来看一下JDK，大家都知道 Java 自从上个公司退出被 Oracle 收购之后，一直由 Oracle 公司维护。那目前 WSE 的版本是出到了12.0.1，但是我们现在大部分的公司里边还是会采用 Java 8 版本去做对应的一个开发。那道理也很简单， Java 版本更新虽然说比较快速，但是其实除了 1.7- 1.8 推出了像 LAMDA 表达式 stream API、 OPTIONAL 对应的一些技术之外，其实在 1.8 之后的一些版本并没有对 Java 做一些特别出彩、特别实用的一些更新。因此许多公司考虑到自己本身的一个中间件部署平台的一个稳定性，还是会暂时性的选择 GDK 1.8 版本做对应的开发，那我们也随大流，我们可以看到对应的这个 Oracle 的一个 Java SE 的 download 的下载页面。我们往下拉这边会有 Java 8 版本的一个download。那我们其实可以看到对应的一个 Java 8 版本仍然被 Oracle 放在了 DOWNLOAD 的比较靠前的一个首页位置，那就说明 Java 8 还是非常受欢迎的，那我们直接可以选择这边的download，它会提示我们根据我们自己操作系统的不同去选择对应的一个 JDK 的版本。


那下载完了对应的 Java SE，并且安装了环境之后，我们可以通过我们的命令行工具简单的来看一下 Java 的一个安装版本的情况。当我们安装完之后，我们只需要使用 java 杠 version 命令就可以看到当前这台计算机安装的就是 DDK 的一个 1.8.0 杠 25 这样的一个version。我们也可以通过到我们对应的一个用户的根目录下去执行点 bash 杠profile，可以看到我们之前已经设置了 Java home 的一个地址。
那接下来我们需要解决的下一个安装的一个问题是阿帕奇的Maven，大家都知道 Java 的一个依赖选项，也就是一些第三方的库都是使用架包的方式去做对应的库的一个管理跟控制，那阿帕奇的 Maven 是使用 palm 文件外加约定的方式做安装仓库去管理对应的一个依赖的价包，并且在代码内通过像 Java 的一个目录以及 resources 目录等等这样的环境去做包管理。
结合工程的以部署方式的总体的一个解决方案，那我们可以看一下我们对应的阿帕奇的 Maven 的一个安装方式。这边我们可以进入阿帕奇 Maven 的一个官网，阿帕奇 Maven project，进入这边的一个 download 的菜单，我们就可以看到阿帕奇 Maven 已经进化到了 3.3 以上，也就是 3.6.1 的阶段， Maven 3.3 亿以上版本就必须得要做 JDK 1.7 的一些支持。那我本地因为之前就已经装过是 Maven 3.2 的一个版本，但是其实这边使用 Maven 3.6 这样的一些小版本的更新是完全是作为一样的配置使用即可。


那我们来看一下，直接在这边下载，对应我这边因为是 Mac 的一个系统，那我直接可以下载 binary 的一个踏包，然后下载下来解压完成，解压到一个固定的目录即可。那我来看一下我对应的一个 Maven 的，是安装在 library 下面有一个阿帕奇 Maven 3.2.5。然后我们来看一下 Maven 安装完之后，其实就是本地的一些配置文件，包括一些可运行的一个程序。那我们需要着重关注的是这个 conf 的配置，因为在这个康复的目录由我们做整个的 Maven 管理仓库的一个 setting 点XML，那我们来看一下 setting 点 XML 文件。那我们可以看到里边最需要注意的是我们往下我们其实可以看到最最需要注意的，其实就是我们这个镜像文件的配置地址，那我将一些没有必要的一些注释先全部都删掉。这些都是我们之前用来做各式的一些注释，我们将这些注释全部都删掉，删掉之后我们其实可以看到由于梅文默认的一个镜像管理的地址是基于梅文 repository 的 org 仓库的。


那我们在国内访问的话其实相对来说会偏慢一些，因此我们国内的大部分用户还是推荐使用对应的这个，没问点阿里云. com 的 Nexus 的私服配置，这样的话我们就可以从国内的一个阿里云的一个镜像去下载对应的 Maven 包的一个地址，那然后我们点击保存。


然后我们一样的在我们对应的一个 bash 点 profile 当中需要去设置对应的一个 m to 这样的一个home，那我们可以看到这个 m to home 是 library 阿帕奇Maven，这就是我对应的 Maven 的一个安装路径，然后我在这边 export 出来。那这样的话我们就可以做到是说当我执行 Maven 命令的时候，比如说 maven 杠version，它就可以帮我找到这个 Maven 命令的一个 version 是基于 3.2.5 对应的这样的一个版本，然后我们对应的 Maven 的一个默认仓库是安装在我们这个用户点 m two。用户目录下，点 m two，我们可以看到点 m two 下面眉毛安装完之后会默认开一个 repository 这样的一个仓库，那我们进入repository，我们可以看到在这边是我之前做项目的过程当中所需要的，所有的 Maven 的一个安装仓库都会在这边。


所以说我们对应的价包都会在这边做团理，好比如说我们进入CDORG，然后这边可以看到我们找一个 Java 的话，我们会用到一些 Java time 等等这样的一些时间，然后我们会用 my betas，那我们可以进入 my Betis，我们可以看到这些，再进入一个 mybadcase CD 到mybadcase，然后这边有我所有依赖的 mybadcase 的版本，我进入一个3.5.1，然后我们就可以看到 3.5.1 对应的 Maven 的 palm 就在这边。


如果说我们之前有下载过对应的依赖包，那这边就会有一个 mybadcase 的点价，对应的这样的一个文件。那OK，然后我们有了JDK，有了Maven，那最后我们要看一下我们要接触到的集成的 IDE 的开发工具，也就是intellige。我们的 intelligent 打开之后可以直接 create 一个 new project。


然后我们在这边选择 project 的 SDK 是基于我们本地安装的 ADK 1.8，然后由于我们是直接使用 spring boot 做对应的一个项目的搭建，那我们可以采用 spring initializer 直接新建我们对应的一个 spring boot 的初始化的项目，那我们点击next，然后我们在这边可以将名字改一下，我们对应的这个项目就叫点评，然后 type 它本质上是一 maven 的一个project，使用架包的方式做打包，并且使用 Java 8 和 Java 作为主语言。那接下来对应的一个 group ID，我们设置 com 点IMOOC。然后 ARTIFACT ID 是点评，然后这边的这个快照的 version 号暂时无所谓，那我们这边 description 改一下点评 spring boot Java project。


然后大家注意一下，我们在这边默认选用 spring boot 2.1.6 对应的这个version，那大家要注意，一旦我们这边选择了 spring boot 对应的一个 version 之后，就必须得要确保跟 spring boot 相关的一些依赖的价包都是这个版本，那不能随意的去切换，因为不同的 screen boot 对应的版本跟版本之间对应的一个兼容性其实是做的并不是特别的到位，就导致会产生许多莫名其妙的问题，因此大家最好是跟我这个课程当中所选用的这个 spring boot 2.1.6 的 version 一致。然后我们点击next，然后可以在这边去做对应的一个 project name，那我这边改一下路径。最好我们平时在做设计的时候，不要放在一个带中文路径名的下面。那我们直接搞在 Desktop 上面，然后我们 project location 点评好，然后我们点击finish，点击，OK，那这个时候 spring boot 会为我们做对应的一个点评， project 的一个创建。


然后我们来看一下我们对应的这个点评， project 生成了一些工程类的文件和霉文的一个管理，这些都不重要。然后我们可以看到这边的source，它为我们做了对应的有 Java 的一个文件，目前就是一个简单的类叫点评application，启动了一个 spring boot 的项目，还有 resources 文件，也就是我们 spring boot 的一个 application 点properties。好，那在我们做所有的操作之前，我们在这边看一下我们对应的一个 Maven 的设置，我们打开 challenge 的preference，然后找到Maven。好，我们可以看到我们这边默认使用的是 Maven 3，那我们其实可以看到应该使用我们自己的一个Maven，我们将路径指向到我们自己的一个 library 的 Maven 下面。还记得我们刚才装在对应的 library 这个 Maven 下面，然后我们需要选定 Maven 的一个setting，这边要点一个 override Maven 的setting，就可以选到对应的library，下面这边有一个阿帕奇Maven，这边有一个conf，然后我们选我们安装目录下的这个 Maven setting，因为在这个 setting 里边我们指定过对应的一个阿里云的一个 Maven 仓库。
点击OK，然后这个 repository 我们可以保持不变，就选择本地的一个就可以了。然后我们点击OK，好，这个时候我们基础的一个开发环境就已经配置完成了。

