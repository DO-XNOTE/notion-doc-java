---
title: 2-3 Netty编解码技术之Marshalling（下）
---

# 2-3 Netty编解码技术之Marshalling（下）

OK 小伙伴们，我们来继续课程。上节课我们对于 server 端整个的代码进行一个简单的编写和梳理，也简单的去跟了一下它的底层的代码。希望小伙伴们对它整个的模板代码编写的方式有一个了解，对它每一步骤做什么有一个深入的了解。 OK 我们接下来其实要我们还没有去对 server handler 具体的实现做一些编码，因为我们现在还不知道我们的实际的数据是什么样子的，我们只有先把 client 端编写完毕，发送数据到 solo 端，我们才能去处理 message 对吧？所以现在我们先直接 new 一个 client 端，我们来编写一下我们自己的 client 端的代码。老师现在还是用这种主函数的方式去做，因为不着急对不对？我们用主函数的方式去写一个最通用的这么一个逻辑。


后面我们在做项目的时候，我们再去把它跟我们的 super boot 去整合在一起。对于 client 而言，它只需要一个 group 就可以了，我们的 n i o event loop group 一个就可以。为什么是一个，因为它不需要去承接连接，它不是一个父子 react 模型，它就是一个什么做处理任务的。


OK 在这里我们的 server 端启动的时候，它是用的是叫做 server boost Chop。我们的 client 端启动的时候肯定是用 client boost Chop 对吧？但是它其实没有 client boost Chop，所以它就用到 boost trap 就可以了，等于利用一个我们的 boost trap 类，有了 boost trap，它就可以继续去组织了。


我们 b 加 group 就是做一个关联，这个关联很简单，就是一个引用。接下来他需要去调 channel 方法，他需要用什么channel，叫做 NIO socket channel 就可以了，因为它不是server。 socket channel 对应着这种 n i、 o 的编程模型。这就不用我再过多介绍了，我相信小伙伴们都理解。这里边我们还是按照逻辑去把 channel 的 init Lazer 找出来。在这里边还是要写 socket channel，把具体的实现类搞定。 OK 最后结束。这就是它最简单的一个编程模型。我们来看一看。其实是一样的。我们只需要把这 3 行话 copy 过来即可。首先还是加入我们这 boss 玛莎铃的 decoder 跟encoder，但是就不是了。变成什么？变成我们自己的 client handler，我们去把 client handler 也创建出来。当然在这里面我们去 create 出来一个 client handler finish 好了。这个 client handler，sorry，它应该是我们的 s c 对吧？ client handler 同样也要去实现一个接口，继承一个类。OK，这个类就是我们的 channel inbound handler adapter，我们去exist，它。好，我们继承这个类。以后我们要把对应的我们想去重写一个方法。这里边报了一个错，我们把它引进来就好了。 OK 现在我们对应着继续往下去走。


初始化完毕之后，很简单，我们就去做建立连接的工作了对吧？建连接很简单，我们就直接调用 b 点 connect 跟我们的 server 地址产生连接。在这里我就用我们自己的幺二七点零点零就可以了。幺二七点零点零点 1 或者 local host 都可以。绑定的端口号是 8765 对吧？做完这个事情之后，调用新客方法同步阻塞。这里边需要返回，也需要抛出异常。我们直接往出throws，它返回。是一个什么？ channel future 对象，我们拿到 channel future 对象就可以去发送消息。我们叫CF。拿到 channel future 对象，我们就可以发消息了。怎么发消息？比如现在我想去发 10 条，或者是发很多条数据过去，发完了之后我们的 server 端就收到了。在这里我们简单写一个小的逻辑，比如现在我 for 循环 Int i 等于0， i 小于100，花 100 次 i 加加。


OK 在这里边我们就要真正发数据了。我们想发的数据是什么？小伙伴们还记得吗？我们之前对我要发的数据肯定是 request 对date，对 request date 我们肯定得先弄出来。OK，我们叫做 r d，等于 new 一个 request data。对于 r d 的 set ID，我们默认叫做加上 i name RD 点 site name，我是消息，可以再加上i。接下来还有一些额外的属性，比如 set 叫做 request message。这里边我就简单写一个内容了，哈，内容再加上i，继续往下。
还有一个比较关键的是什么？这个是一个二进制的贝特自约数组，是不是 set attachment？老师在这里边已经提前准备好了一些对应的什么，一些对应的文件，比如现在我想去在 source 里边，我们来看一下source，这个 source 里边比如有好多图片是吧？比如 001 点 g p g， 002 点 g p g， 003 点 g p g 对不对？有这么多图片，我现在就想传一个图片到另外一端，可以吗？这是可以的。


首先你应该找到这个图片所在的位置在哪里，对吧？这个图片所在的位置，我们其实可以通过什么可以通过最简单的这种 Linux 无论是操作，你是 windows 还是 Linux 它都支持的那种叫做 file 点 sample 方法，我们先把文件的 pass 路径找到。这个 pass 我们应该怎么去找？很简单， system 点 get property。我们写 user d i r 用户的目录对不对？再加上什么？再加上我们的具体我们用 IO file 类，它有一个常量。你的文件分割符是什么？在这你如果是 windows 系统，正斜杠反斜杠是吧？正斜杠或者是两个反斜杠对吧？如果是 Linux 的，应该是反斜杠，两个正斜杠，哈，再加上我们的什么目录叫做 s o u r c e，对不对？所以 s o u r c e 就是目录，再加上我们的分隔符，无论是 Linux 还是windows，他们帮你去做判断了哈。再加上 001 点GPG。


OK，好，我们对应的文件的目录 pass 地址。这个我就不用再说了哈。我们看到它会根据根据操作系统，它会帮你去定位它到底是 windows 还是 Linux 会帮你去解析出来。这是两个反斜杠，这是一个正斜杠，对吧？我相信小伙伴们都应该很熟悉了，老师就不做过多啰嗦了。 OK 接下来我们路径找到了，我们直接通过我们的 java i o 下面的 sell 去把它创建出来就好了。哈，我们等于 new 一个cell，然后把 pass 陆旧传进去。


好，接下来我们干什么事情？对于file，它是一个文件，我们想把它转成一个

什么，一个压缩格式的文件，这怎么去做？老师在这里就采用 file input stream 就可以了。 file input stream 去读出来，我们叫做 f i s，等于 new 一个 file input stream，把 file 扔进来。这个都是最基本的 i o 编程了，我们去直接对于，因为它是一个字节数组， b y t e，我们叫做data，等于 new 一个 b y t e，它的长度就是我们 f is 它的 available 对吧？这个就是它的长度。找到它的长度之后，我们直接把它读进来就好了。
第二， read 一个字节数组，把 data 读进来。读完了之后，我们去做 close 对不对？ f i s 点close。读完了之后怎么办？我们的数据现在是从我们的文件读到我们内存，又放到 data 里，最后又装到我们的 file input 里面， OK 这样又装到我们的 data 里边， OK 这样。


我们现在要做的事情就是对贝塔字节数组想要对它进行一个压缩。压缩老师在这里已经准备好了一个类，叫做 g z i p util 丝，我们就直接用 g z i p util 丝点什么？点 g z i p 方法，它就能把一个字节数组，就是我刚才从 input stream 里面读出来的一个贝塔字节数组，给它进行一个什么，进行一个压缩，就这么一个逻辑，非常简单。我们这里边有一些需要抛异常的，我都throw，我就 throw 一个最大的exception，因为要不然他太多了。好。搞定这个事情之后，现在我们已经做完了。相当于我已经把数据格式进行一个压缩。我们对象都已经填充好了以后，最终我调用这个方法对不对？其实我可以把 channel 拿出来，点channel， OK 它返回的是一个 channel 对象，真正是一个连接的channel，会发通道 c 等于它。我们去 channel 了，我们直接通过点 rat and flash 就直接给它写出去了。 Rat and flash？什么？注意？现在我们直接把叫做 request data。对象直接写出去是吧？直接写出去好了， OK 其实我们的逻辑现在就已经差不多搞定了。我们绑定完了之后，最后别忘了你要做什么。 channel future 你要调用channel，调用它的 close future 方法同步去监听关闭是吧？好，别忘了把资源最终要释放一下，纱藏优雅的释放。 OK 好了，现在我们的代码就真正的写完了。


关于 g z i p，它怎么去把数据进行压缩？在这块老师提供了一个工具类，大家可以看一下。其实就是使用了我们 g d k 底层的 bit array out put stream，以及我们的 g z i p output stream。当然这是非常简单的，这也是我们 Java util 给我提供的，可以把这 beta 这些数据做了一个压缩，你可以去对比一下。



老师在这里有一个小例子，就是原始文件的大小是什么样子的，压缩之后的大小还可以去做还原，就是 n g c i p，压缩就是 g c i p。其实就是这两个静态的方法。在这里我就不做过多的演示了，老师已经给你了这个小的demo。 OK 我们回过头来。现在我们 client 端真正的已经把数据把我们的一个对象 request data 负循环 100 次发过去了。
接下来我们现在是不是可以对于我们的 server 端怎么去接收 date 对象做一个处理了？很简单，我们已经使用了 g boss 的玛莎脸编解码器，所以它就能够对序列化的加码对象做一个识别。我们在 server 端运行的时候，真正处理业务逻辑的这个类就是我们的 server handler。所以针对于 server handler 里边，我们直接就可以去进行编码了，我们现在已经知道了，那个类就是我们的 request data 对象，所以我们拿过来直接可以转换，非常非常的方便。如果是 Java 跟 Java 之间相互通信，我建议大家采用我们的什么，采用我们的 g boss 玛莎莉，这是非常方便的哈。


我们叫 r d，等于 request data，把它进行一个强转。转换完了之后，对于这个数据其实你就知道了，你就知道这个数据是什么了。我们说这个数据它里边有一些属性，我们来打印一下它里边都有哪些属性，比如它当前的 ID 是什么，我们就写 RD 点 get ID，还有 name 是什么，对不对？当然还有一些其他属性，加上 r d，点 get name，再加上还有message，是不是他这个 message 叫做 request message。 request message 好，我们直接 RD 点 get request message 就可以了。这个就是打印出来的最基本的一些内容，我们用这一行表示。


接下来还有一个就是我们特殊传的拉缩格式的贝特字节数组。那个东西怎么办？那个东西我直接就可以取出来吗？点 get attachment，当然它是经过压缩之后的，你想去还原，必须要调。刚才老师所说的那个方法叫做 g i p you tell 里边的安 g i p 方法。把真正的数据进行还原之后，当然它会有抛异常。我们在这里直接往出 slow 吧。


through exception，它真正的返回就是一个字节数组了，我们直接可以b，y，t， e 用一个 attachment 或者是 data 都可以去接收接受回来。比如接受完了之后，我把它再输出解压完了之后，你还原的到底对不对，我不确定。所以老师在这里又提供了一个文件夹，叫做reserver。我们只需要把原来的图片再输出到 reserver 的目录下，这两个图片数据都能够展示出来。哈，是一张图片就证明它的还原跟压缩是没问题的。 OK 所以我们现在很简单就做这个事情。我们把之前的路径也 copy 过来一份，因为我不想重新再去做了，没什么太大必要。


Ctrl c 在我们 server handler 里边，我们直接拿过来，直接把这个东西变成receiver， OK 好了。现在我们有了路径之后，现在是相当于我们读出来的流。想把它写出去，你就用 file out 或者 stream 对吧。 f o s 等于 new 一个 file out consume，只需要传一个 pass 就可以了。非常简单，我们调用 FOS 点 right right 方法去往出写这个东西。写完了之后把流 close 掉。 OK 搞定。好了。现在证明我已经接收了。是不是？当我处理完客户端的请求数据以后，我接下来做什么事情，我是不是会有一些响应？比如我们之前所定义的一个 response data 对不对？OK，我们其实这里边你就可以认为什么认为我接收请求，接收 request 请求对不对，并进行业务处理。我的业务处理很简单，就是打印一下，把原先这个东西再转一下，再还原一下，写到我们的 reserver 文件夹下。


接下来就是回送响应数据，对吧？回送响应数据很简单，就用 response data，我们叫做 response data，等于又一个 response data。可以了。 response data 我们去设置一些简单的东西。 ID 我们就拿 request date 的 ID 就好了。 name 也是一样的哈，点 get to，加点东西，反正它都是字符串的哈，我们就加响应response。为了做一个区别，空格加上一个ID。 response 空格加上一个name，还有一个是它的数据response。点 set response message，我就直接写响应信息。好。响应信息好了，最终我要把响应信息刷出去，刷给我们的克莱姆端作为回送的 ACK 对吧？所以我们还是使用 c t x 对象。


第二， rat and rush 把我们当前的 response data 刷出去就可以了。好了，现在最后一个问题就是当我们刷出去之后，谁去做处理？接下来就是我们的 client 端里边的 client handler，是不是这里边还是没有做实现的，他现在要真正的去处理服务器端给我的响应了。这个更简单了，我们就用最简单的方式去写 try finally 固定的格式。为什么要用 try finally？在 TRY 的时候我们直接把 response data 是不是我们叫 r d 等于一个强转？因为他回来的时候也是一个 Java 对象，只不过现在的 Java 对象换成 response date 了，对吧？最后我们去对它做一个输出。我们说输出什么输出？服务器端响应内容。好，我直接第二 get ID 就好了哈。


OK 在 finally 的时候，我们必须要做什么事情，因为我们在这里。在使用 net 的时候，你一定要注意。 buffer 我们实际的缓冲数据。它虽然是经过举报司马沙岭的一个编解码了，但是它其实也是一个buffer。我们一定要做一件事情，释放资源。如果不释放，可能后面你的资源会有问题，叫做reference，我会有资源撑爆的那一天。 reference concentrate，加入 list 释放。作好必须要做这件事情。


小伙伴们，现在整个我们的编程模型就已经搞定了，我们现在可以来启动做一下试验。你现在启动一下。首先我把我的 server 端启动起来，之后接下来把我们的 client 端也启动起来。右键我们 run s 加application。来看一看到底 o 不 OK 哈。


这里边有一个错误，叫做 none source，系统不能找到指定文件的路径。哈， OK 我把它停掉。为什么是none？我们来跟一下。很简单，可烂的端报的错误开始没有找到是吧？这行代码很简单，在使用它的时候可拉你端就在这里。 User dir sorry。这个应该是 get practice， get property，而不是 get 那个东西对应着我们的 solo handle。里边是不是也有，我再看一下，OK，也不对是吧？它应该叫做 get property，而不是 get ENV。


好了，再次运行一下右键，我们启动服务。接下来我们去启动client，小伙伴们请看。现在我们最开始是我把它打开哈。最开始的逻辑是我们的客户端跟服务器端建立连接，发送 100 条 request data 对象对吧？每一个 request data 对象里边都肯定会做了一个什么文件的压缩，给它传出去了。好了，我们的服务器端接收对不对？服务器端接收接收他的信息，这些东西接收信息对吧？接收完了之后做了一件什么事情，我们来看一下。


接收完了之后，我把每一个文件都做一个输出，输出到 receiver 下面，叫 001 点 g p g 是不是？接下来我们看一看？当然，每次输出的都是同一个文件，就是同一个，它只生成一个。我们点开看这张图片，没问题，跟我们之前 source 下面的 001 也是一样的。所以这也证明了我们自己去做这种压缩，对贝塔这些数组做一个压缩跟团圆也是完全没有问题的。 OK 整个的逻辑。其实为什么我现在想跟大家去讲这个案例？很简单，我想跟大家说一件事情，如何去实现一个真正的消息通信，它都是要经过至少两次处理的。为什么这么说？我们通过一幅图来跟大家去画一下，让大家对这个概念更清楚。


好了，打开我们的画图工具，跟大家简单说一下。现在我们肯定会有两个端，一个是我们的 server 端，一个是我们的 client 端。就是我们的 server 端，我们的克兰端。 OK 我现在把字体调大一点。好。其实我们第一件事情做的什么？我们第一件事情是我们的 clan 端。当然我们的 solo 端一定要起来哈。我们的 clan 端主动的向 solo 端发起一个请求，对不对？主动的要建连接发送的数据就是叫做 request data 对吧？好， request 内塔是我自己定义的数据，它期间经过什么？它期间经过了编码解码。首先它一定要经过了编码。原先是一个 request data 的 Java 的人，你可以认为在我 Java 的里边，它是一个 request data 对象。但是我要做一件事情，什么？因为我有 g boss 玛莎铃的编解码器，所以他第一件事情肯定是把 request data 对象做网络传输，做序列化了。也就是他在传给 server 的时候，他第一件事情是把它进行一个编码编程，二进制的。二进制的数据 OK 传过去了，我们的server，是不是他自己内部也会有编解码器对不对？ g boss 马萨令的解码器就是我们的 decoder 是吧？他会把什么？他会把二进制的数据，解码乘什么解码乘 request data 对象？我不知道这个逻辑小伙伴们有没有理解。


其实这个逻辑它真正在实际的 server handler 去做， server handler 处理的时候，它可以直接处理我们的 request data 对象，而不是在说我在需要做什么辩解码了。不需要他就可以直接去处理中间对象的转换跟二进制转换。我们靠 g BOSS 的玛莎令已经帮你去做了这一个过程，对吧？同理，我现在在 solo 端，我要做完处理之后，我要给他回送一个响应，响应的时候我响应的是什么对象？是 response 对他对象对吧？ raise BOSS data 对象。


response data 对象也是一样的，是不是他也是传过去给我们的克兰端之前他肯定也要把他自己进行一个编码，把他自己序列化成二进制的数据，这样网络上才能传输，因为网络上肯定他识别不了我们 Java 对象对吧？他是不是肯定得需要什么二进制的数据进行解码成什么？解码成 response data 对象对吧？肯定得是做这个事情，由他自己的什么？当然我可以再画大一点，我把它放进去算了，不太好看哈，我就放到这了。


最后由他自己的handler，叫做 client handler。是不是 client handler 直接可以处理我们的 response data 对象对吧？这个就是整个的逻辑。说白了逻辑。其实同学们，你有没有想跟老师在这里额外添加的 attachment 对象，其实也是比较类似的对不对？我们的客户端先把文件读取出来，从磁盘中读取出来，进行一个转换成字节数组，之后做了一个压缩对不对？做压缩之后为什么要做压缩？压缩之后它的数据包更小，传过去的速度更快。


到服务器端做什么？肯定得把原始数据拿回来之后，它是经过压缩的，我把它解压缩，解压缩之后我再对照一下，把它输出到另外一个目录，看看我的输出的压缩和解压缩这个过程到底发没发生数据的更改，发生错误这么一个事情。 OK 其实我们现在已经对编解码有一定的认知了，核心思路就是把原来的对象 Java 对象转成二进制数据，以便网络传输，最后通过编码器、解码器进行对应的一个编解码操作就好了。 OK 这节课我们已经对齐 BOSS 马杀令整个的编解码做一个完整的解读。感谢小伙伴们收看。

