---
title: 3-3 商品评价 - 编写自定义mapper查询
---

# 3-3 商品评价 - 编写自定义mapper查询

上一节我们是把好评度进行了一个编写，现在已经是 OK 了。这节接下来我们应该要开始去把我们真实的一个相应的评价内容，我们都要去查询出来。从这个列表里面，其实我们就可以看得出来，评价其实是涉及到了，也是一个多表关联，本身它会有一个评价表。另外用户的头像，用户的一个昵称，虽然它是脱敏了，但是其实它也是应该要查询出来，再去做脱敏。所以在这边我们会涉及到两张表的一个查询。


好，我们现在打开数据库，我们可以先把自定义的一个 SQL 语句，我们先去编写一下，在这里面新建一个查询。先保存保存，取一个名字，这是商品评价。商品评价，查询商品凭证，点击确定好，OK，可以开始写了。写一个select，咱们放大一些。好，我们先 select 信号from。我们是查询从商品的评价表里面的，所以是 items comments，把它定义为一个别名，叫做 i c。好。再来一个 left join，我们要 join 的是用户表，所以我们使用users，把 users 取一个别名，叫做you。好，OK。当我们把 left 装写完之后，不要忘记还要有一个on， on 是 i c 点，有一个 user ID 等于 u 点ID。好，这样子两张表就关联起来。


随后来一个 where 条件。首先第一个条件我们肯定是要去查询到它是哪一个商品的，所以它会有一个 i c 点。 item ID 等于比方现在的一个我们的数据是在哪里？是这个吧，是在里面的数据，也就是 k 1001。把 ID 可以先拷贝一下。拷贝到这里，我们可以先来查询一下，可以查出很多的内容。当然这里面的数据是全部都会查询出来，因为我们现在还没有分页。但是没有关系，这其实是我们的一个主语句。另外它还会在后面去加上一个 and and，其实就是我们的一个评价的等级 i c 点level。比方我们来一个1，这是好评。再来一个查询，现在我们查询的数量就会减少了。好，OK。在这里。其实我们查询的时候我们是不建议去写信号的，写信号会把两张表里面的列全部都取出来，这样子其实会有一定的性能的影响。


在在我们的页面里面，我们往往都是秉持着用到什么去查什么，我们要以这样的原则去进行查询才是最好的用心。一般来说我们仅仅是用于做测试，去测试的时候随便去看看是无所谓的。你要去写到代码里面，肯定是要把它具体的一个列名要去写上的，我们可以先来看一下。在商品里面，我们先把页面打开，这里面像商品的一个等值，我们是要去查出来。另外商品的具体的内容也是要的。还有商品的对应的规格，它当时评价的时候规格叫什么，我们也要拿出来。随后就是一个另外一张表，也就是用户表的一个头像和昵称，我们是要拿出来，当然不要忘记它还会有一个具体的评论的时间，好在这里我们就可以去把相应的内容给写上了。


第一个是一个level，取一个名字叫做 comment level，使用驼峰慕名就可以了。把 l 改为大写好。下一个是 i c，点是它具体的内容。 s 我们就直接使用 content 就行了。再来一个 s p c s e p c，也就是规格名称，驼峰式命名写好。再来一个 i c，点评价的时间，也就是创建时间。 s create a type t 改为大写驼峰命名好。现在我们就需要去关联到用户，把用户的内容拿出来。一个是用户的头像 as 一下，取名叫做 user face。再来一个用户的昵称 Nickname s Nickname。好，OK，我们再来查询一次。好，现在显示的列名就比较少了，只不过我们当前用户的头像。用户的昵称是没有关系，因为我们在做查询的时候，其实用户他的一个ID，因为我们的 ID 其实是生产环境里面的表，它所对应的用户 ID 在我们当前的数据库里面没有，所以它的一个 user face 和 name 都为空，但是没有关系，我们后续可以去增加的。


我们先把社会语句全部都拷贝。我们先可以放到咱们项目里面的自定义 map 文件里面去。我们把项目打开，先找一下。我们先要找到 map 这一层，在 map 里面我们找到items，有一个 items comments，其实相应我们是可以放到 item comments 的一个自定义 map 里面，也可以放到商品它自己的一个自定义的 map 里面，其实都是可以的。


在这里我们统一都使用 items map OK？所以我们在这边需要把 items map，我们先要去拷贝一份，把它作为自定义的 map custom 好。先双击一下，以后把它所继承的通用 map 我们先去掉，因为不需要下一个，就应该要去把它对应的 Map 叉 mail 好，拷贝一份，定义为 custom 好。在这里面我们就要去做一定的精简了。这个 base result map 是不需要的，它所对应的 name space custom 写过来。


好写进来以后，现在我们就可以在里面去写一个 circle 了。我们写一个 select ID，比方我们先来一个 query item comments。好，要传入一个东西。传入的参数我们在这里直接使用 map 放的参数，可以类型多一些。再来一个 result type，这个应该是一个VO，我们先没有，可以先不选。在这里面我们就可以把这一段的 circle 先拷贝进来，好贴进来。格式我们稍微优化一些。好，这样子我们一整段的查询语句在这个位置，我们今天可以删掉，因为这是用于去做传参的。在这里会有一个 and 条件。 and 条件其实我们也是应该要去做判断的。所以在这里我们先预先写好一个if，先把 if 写上，我们要去做一个判断。在这里面会有一个 test 是必须要填的，这是它的一个条件语句条件相应的内容是在这里面要去写。这个条件其实就是根据我们的一个 level 具体的值要去做的一个判断。OK，这一节我们先写这些，下一节课我们会配合 Ctrl 和 service 一起的。再把这一段的自定义 map 再来完善一下。

