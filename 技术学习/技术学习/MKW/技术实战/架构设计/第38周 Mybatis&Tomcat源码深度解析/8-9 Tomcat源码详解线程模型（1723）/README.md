---
title: 8-9 Tomcat源码详解线程模型（1723）
---

# 8-9 Tomcat源码详解线程模型（1723）

同学们大家好，这章节我们开始学习汤姆 cat 的核心源码解析的第六部分，也就是汤姆 cat 的线程模型。在汤姆 cat 的各大版本中，所支持的线程模型也发生了一步的演变，一方面我们直接将默认线程从 BL 改成了NL，另一方面在后续的几个版本中加入了对 AL 和 AP 二线程模型的支持。这里面要注意一下，它仅仅是支持，并非是默认线程的一个模型。


我们先看一下他们开的在历史上是支持的这几个模型。首先是BL，那么BIO，它是主色词IO，他们开的 7 之前它是一个，它是默认选项，采用传统的 Java IO 进行操作。在这个模式下，每个请求都会创建一个线程，使用于并发量小的一些场景。由于它每个请求都要创建一个线程来处理，所以说线程开销比较大，它不能处理一些高并发的场景，性能也比较差。


BIO 的模式现在已经被他们开的抛弃，代码都已经清理干净了。NL，它是同步非主射，它比传统的 BL 能更好地支持一些大并发。他们开的 8. 0 以后就成为默认采用该方式。 NIO 是加 1. 4 以后提供的一个新的 IO 超出方式，它是基于缓冲区病，能提供非主式的 l 超度的价API。它拥有比传统的 BL 更好的并行的运行性能。在各个线程模型中， NL 作为目前最实用的一个线程模型。此目前汤姆派的默认的线程模型，比如说课程，我们也会对 IIO 折中进行讲解。


下面是APR，那么 APR 是阿帕奇 portable runtime，也可以理解为是阿帕奇的可移植运行词，它是阿帕奇 STP 服务器的一个知识库，需要编译安装 APR 库。接着说我们的 i o 操作的部分是直接调用 native 代码来实现的，可以理解为他们看将以 JNI 的形式调用阿帕奇 STV 服务器的核心动态链接库来处理文件读取或网络传输的操作，而大大提高他们开的对静态文件的处理性能。他们开的 APR 也是在他们开场运行高并发应用的一种首选方式。


下面是 a i o 的方式。 AIO 我们称之为异步非主射，也是 N I O 二，那么他们开的 8. 0 后提供了支持，主要用于链接数目多且链接比较长的一些重的一些操作架构，比如说像相册服务，充分调用 OS 参与并发操作，编程的相对来说比较复杂。那么接下来我们来先看一下 BL 它的工作原理。在 BL 模型中，主要拆的角色是 acceptor 和对应的handler，那工程线程词基于前面对 B1 的介绍，它们的分工是这样的，accept，它这个线程是专门负责建立网络链接，也就是 accept 接收请求。新链接创建成以后，它会交给我们的 handler 线程池进行处理。 handler 它主要是针对每个请求的链接，它为工作进行一个线程的分配，执行后面所有的步骤，这里面主要涉及到像read，decode，process， encode 和cent。那 read 和 sent 是面向网络 IO 的，在等待读写就绪的过程中其实是 CPU 阻塞的。因此 Handler 工程它的每一个线程都会因为 l 组织而空，等待造成一些浪费。


那么接下来我们来看一下 NL 的模型。汤姆开的 NL 模型相比 BL 是多了一个我们这个 Polar 一个角色，这里面我们看它是acceptor。 Polar 和 handler 作为我们的整个工作线程池，如果大家熟悉这个 react 模型的话，这三个角色就比较熟悉了。如果说我们将 Polar 换成rector，那么这里面就是一个 record 模型，他们看的 NL 模型，它的确就是基于我们的主从 recorder 模式，只不过将 recorder 换了一个名称而已。


我们先看这个acceptor， accepted 线程专门去负责建联网络链接，这里面跟 BL 是很类似的。那么新链接创建以后，它并不是直接使用 worker 线程去处理请求accept，它并没有直接去创建我们的 worker 请求，它是将我们的请求先发送给我们的 Polar 进行一个缓存机制，也就是缓冲Polar，它在处理的过程中维护了一个 select 对象，当 Polar 从缓冲队列中获取出链接以后，它会注册到 select 中主设等待读写就绪的操作。这里面分别表示有读写就绪、 rate 就绪和 cent 就绪。那么后来我们看worker，它主要是为来遍历我们的selector，找出其中就绪的 l 操作，并进行我们的一些 worker 线程的一些处理。我们 read 内存读了我们decode，process， encode 和 send 内存写这样一个操作。


如果整体对比的，对于 BL 模型，它一个线程对应一个请求链接的完整过程，因此汤块的服务能处理的最大链接数就和最大性能数是一致的，通常默认是200。那么对于 NL 模型里面，在一个请求链接中对应一个工作线程，它只处理IO，读写后续的一些非主持的过程，因此汤姆开的服务能处理的最大链接数要远大于它的线程数量。接下来我们来更详细去了解一下汤姆开的 NL 的线程模型。


对于 tomclaude 里面， Connector 在启动的过程中会启动一主线程，用于不同阶段的请求处理过程。首先我们看到是 acceptor 线程组，那么 acceptor 它主要是用于接收新链接，并将我们的链接封装一下。选择一个powder，将新链接添加到我们 powder 的事件队列里面。那么 power 线程组它主要用于监听 SOCKET 4 件，当我们的 socket 可读或者可写的时候，将 socket 封装到添加了 worker 线程池的任务队列之中，那么 worker 线程组就用于处理我们的请求，这里面会包括请求的报文，并创建 request 对象，调用我们容器的我们的 pipeline 进行一个处理。这里面像acceptor， power 和 worker 他们所有的线程池都维护在我们的 NLN 的 point 里面，这里面我们可以从代码里面看一下。


对于我们的 NLN 的 point 里面，我们去看一下这里面的starter，我们看 starter internell，我们看一下这段代码里面的一些情况。我们看到这里面我们有一个是我们 get corrector，那么 get corrector 它是去获取我们的 worker 线程池，那么这里面如果说它不存在的话，这里面就会创建。我们看 create accurator，这里面我们创建一个 Toss Queen，通过 TOS Queen，这里面再创建我们的 Tock third factory，这里面会创建我们相关内容。我们可以看到对于所有的我们的工作线程，它我们可以看到它是ESEC，那么这里面我们也能看到我们在 debug 的过程中关于 ESEC 相关的一些线程词。这里面我们可以看到是 h t c P N L 8080， E N E C 这里面是 1- 10 十个线程，我们可以看到工作线程末日创建了 10 个，这是我们的一个工业线程，那我们再回头看一下，这是工作线程，创建完成以后，这里面会创建我们的power。


那么我们可以看创建 power 的过程时，我们直接你有一个 power 对象 new 完成以后，这里面把这个 power 体现在一个线程里面进行启动，那么这个线程我们可以看到它是 kind of power，我们这里面也能看到，这里面也是默认的一个线程，我们可以看到在这里面我们能看到是我们的看到 Polar 是处理 Polar 轮询的一个操作。
那么后面我们来看一下，这里面是 start accept THREAD，也就是说启动我们的 accept 线程，那么我们进来看一下，在这里面首先创建一个 accept 对象，那么 accept 对象它也构建指定一下它的线程名称，它的 third name 是对应名称的，我们的看看这里面是acceptor，从这里面我们可以看到对应的我们当前这个 HDPNL 8080 accept 这个线程，这里面也能看到它线程的一个运行情况。


这里面对于几个线程组，它的各种参数设置都可以基于它进行一些特殊的一些变量的设置，那么我们回到这里面，那么现在我们大概了解对于我们的accept、 power worker 三个线程柱，它是在什么阶段进行启动的，那么这里面启动的阶段，也就是我们的 start 启动的阶段。


STARTER 启动阶段，也就是我们可以看到这里面是 start internal，我们知道 start internal 是直接的被或间接的被 STARTER 方法来启动，这也是他们开的模板方法里面的一种结构。接下来我们来看一下 acceptor 执行的这样一个过程，对于 acceptor 的话，我们可以看到它是直接会启动 Server socket accept，我们也是切换到我们的源码里面看一下，这里面是我们 acceptor 相关的一些操作，在 accept 操作里面我们看到它是在问方法，我们知道 accepter 它是会在几个线程里面进行执行默认，我们知道线程是通过润方法作为我们的一个入口，那么在启动的过程中它会作为一些状态的一些判断。


那么这里面我们可以看到它其实最重要的一个操作就是我们这里面 Endpoint Server SOCKET accept，这里面是什么意思？ Server SOCKET accept，其实它是调用了 NL endpoint 里面的操作，这里面的 Server SOCKET accept，也就是说我们这个端口开始监听我们的请求，我们可以看到这里面 Server SOCKET 上相关的操作，这里面有多个实现，我们这里面默认选择是 NLN 的point，这里面也就是它的操作，也就是获取对应的 Server SOCKET 进行 except 操作，也就是说开始进行一个监听的接收，那么监听接收完成以后，接下来我们看是 set sock 的iperson。


也就是说对于这个 socket 进行一些默认值的一些设置，那么得到对应的 socket 的操作以后，我们看到下面的一些配置，我们可以看到这里面对于我们这个 socket 进行一些配置信息。同时这里面 set socket opposite，它一方面对于这个 socket 进行配，在另一方面进行我们对于这个数据的一些处理的一个过程。


我们可以切进来看一下，选中我们的 NLN 的point，我们可以看到这里面它是特别处理我们一个特定的一个链接，并且这个参数是socket，这里面也就是我们对应的 socket channel 它的返回值的情况是什么呢？我们看如果是true，那么这个 socket was 也就是正确的配置，并且处理完成以后，那么我们可以进行继续。如果说它为 false 的，也就说明这个 socket 它需要直接去立即中断了。


我们可以简单看一下这个 socket 里面的它的一些设置的一些内容，我们看这里面的代码内容，主要是判断一下我们的 NL channel 是否存在，那么如果说 NL channel 它不存在，我们会去创建我们的 NL channel，这里面创建的过程它会依赖到我们的 buffer handler，我们可以看到如果它是 SL 支持的话，它会创建一个 secret NL channel 如果说默认情况下它就是一个普通的 NL channel。


从这里面我们也可以看到在 in the point 里面，接下来它会通过 new 的方式创建我们的 NL channel，那么创建完成以后，接下来去把我们的 AI channel 进行一个 register 以注册，那么我们可以看到后面它一个注册的过程，这里面注册的操作是怎样的？它是基于 power 进行一个注册，这个注册的过程它并没有直接把我们的 NL channel 注册，它是构造出一个 SOCKET wapper，一个包装器，那么这个我们看到这个 SOCKET wapper 的包装器的一个情况，它其实是基于我们能看到此 NL SOCKET wapper 把我们当前的 channel 和我们的 in the point 对象，我们当前这个 this 就是 in the point 对象进行包装进去，包装进去以后我们在这里面通过 power resist，接下来我们就可以 YouTube true 的形式去返回正确的结果。


好，我们可以看到在注册的过程中其实还是比较复杂，注册是依赖的 power 进行一个注册，我们可以看到注册它里面的操作是刚才也看到它是包装成一个 NOL 的 soccer wrapper，进行一个注册的操作，那么我们可以看到这个wrapper，我们看它里面注册操作的过程。


我们可以看到这里面的主要操作也是我们创建我们的 Polar event，它的对象，这里面的 socket wapper 和我们的 OP reject 操作构建出一个这样的一个 event 对象，同时把这个事件 at event 添加到我们的队列里面，我们可以看到添加 event 的过程中，其实这里面会有一个特殊的操作，这里面通过 select 而对应的一个 wake up 操作，我们可以看到。


对，这里面，对在 Polar 里面进行 wake up 操作，那么我们看 wake up 它是什么逻辑意义，从这里面我们可以看到它的一个加 doc 的注解，我们可以看一下它的一些中文翻译。这里面是如果说当前的三位返回第一个选择操作，那么它会立即的一个返回。如果当前在选择操作中阻塞了另一个线程，那么该调用的将会立即返回。如果当前没有正在进行的选择操作，那么下一次调用选择操作会将立即响为，除非同时调用了 select null 或者是 select null 进行 consumer 的消费操作。在任何情况下调用返回的值都可能是非 0 的，除非我们在此期间的再次调用的方法，否则随后的一些选择操作像往常一样进行注射。在这两个连续的操作之间，多次调用值方法它只调用一次具有相同的效果。


这里面我们能看到对于 NL 线程里面，我们通过 acceptor 它执行的过程中到我们的 power wake up 这样一个过程的一个执行的情况。那么接下来我们来看一下我们基于 power 作为我们的出发点去分析这个过程。 power 它作为一个线程在执行，所以说我们可以看一下它对应的方法，我们还是通过我们的 rain 方法来进行一个分析，那么在这里面我们看它是我们通过 select 方式进行主设到这里面这个 slack 默认是 1000 毫秒，也就是一秒，在这里面它会主摄等待woocap，或者说对于一秒的这个 time out 超时间，这里面我们可以看一下对于我们这里面对于 select 进行一个 select 操作，这里面制定了一个 time out 的时间，这里面是默认是1000。


那么执行到这里面我们可以看到后面的处理操作是 event 操作，我们可以看到后面会对于一个close，如果是已经 closed 或者是另一个我们的 key count 等于 0 的情况下，我们会处理一个疑问操作，那么我们看一下 event 它做的事情是什么？这个 event 这个方法它主要通过将艾特 event 方法添加到事件队列中的socket，注册到我们的 selector 对象实现里面，当我们的 socket 可读时，我们的 power 才会对它进行一些处理，这里面我们看到它会遍历 event 里面的一些事件，进行一些操作。


我们执行疑问的 pool 的方法去获取里面的信息，那么接下来我们来看一下它的一些执行的，这是整个我们可以看到执行的过程，后面我们可以看到会执行到 PROS key，那么这里面还是回到 in the point 里面。它的润方法我们可以看一下，这里面我们看它会执行下面的 PROS k，那么这里面是做什么操作呢？他会在一直在我有迭代的过程中去监听我们的信息的一些处理，那么我们可以切到进去，我们看一下 POS k 相关的操作在这里面做了什么事情？这里面会涉及到 proxkit Pro SOCKET，我们找一下Prosocket，这是我们的 Prosocket 的操作跟进期，我们看一下对 SOCKET 处理的操作是如何进行的。在 Pro SOCKET 里面我们可以从这里面去看它在 Pro SOCKET，这里面对应 Pro SOCKET，在这里面我们看它对应 event open read，我们监控 read 就绪的情况。


这里面我们会创建，我们 create socket process，创建一个处理器，这里面处理器创建的过程也是指定我们 socket 的一个包装器和我们的一个四件对象，创建出这个 socket process 对象以后，这里面我们是启用 accuator 来进行执行，那么 accuator 其实这里面它其实也就是调用了我们的 process socket 对应的 run 方法，我们可以从这里面跟进去看一下润方法执行的过程。因为对于我们的 SOCKET process，它执行的过程里面，这里面实行了多润方法的操作读准方法我们可以跟进来看一下，这里面我们可以看到对于 SOCKET process 它也是在不同的 in the point 里面有它自己的实现，这里面我们选择 NL in the point。


在这里面做的主要工作是什么呢？这里面主要是获取不同的 handler 来进行我们请求的处理，我们可以看到这里面是 get handler 进行一个 post 操作，那么这里面的 handler 我们可以看一下 post 操作它的一个具体的实现，我们可以看这里面是 abstract portrait，也就是说我们各种不同的我们的对应的我们的协议的实现。


那么因为我们对于当前我们选择的 NLN 的point，它对应的协议是什么呢？这里面也是对应的是 h p 11 N I O 的PORTAL，那么它执行到这里面去执行的过程中也就执行到了我们的工作线程，那么对于Polar，他把我们这一棒交到我们的工作线程，那么这部分工作就完成了。


那么我们回头来再来看一下我们对于 NL 线程模型，对于他们看的 NL 的线程模型，这里面会涉及到 accepter 线程，这里面的 power 线程以及 worker 线程。对于 accept 线程，它主要是接收我们的请求，那么对于Polar，它主要是监听 socket 事件，当 socket 可读或可写的时候，我们把当前的这个对象赋值到我们的worker，我们的工作线程。那么关于他们开的 NL 的线程模型，我们就先介绍到这里，同学们，我们下一章节再见。

