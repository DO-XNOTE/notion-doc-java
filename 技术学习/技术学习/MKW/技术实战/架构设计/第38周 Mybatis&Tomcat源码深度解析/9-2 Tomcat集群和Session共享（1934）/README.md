---
title: 9-2 Tomcat集群和Session共享（1934）
---

# 9-2 Tomcat集群和Session共享（1934）

同学们大家好，这一章节我们开始学习汤姆开的应用技巧的。第二部分，汤姆开的集群和 session 共享。那么这里面我们要想一下我们为什么要做汤姆开的集群？汤姆开的机群能给我们带来什么呢？首先汤姆开的集群它可以提高我们服务的性能、并发能力以及它的高可用性。另一方面，汤姆开的集群可以对我们的项目架构提供一些横向的扩展能力。还有一个更关键的一点，他们开了集群，它可以给我们带来一个高可用。


那么假如说我们只有单个节点的话，我们系统进行部署运维的过程中，那么它就需要停机，那么我们基于集群的方式，我们在发布的过程中是一部分停机，这样保证我们的服务是进行一个高可用的，那么另外对于我们使用汤姆 case 进行集群的时候，也会给我们带来一些问题。比如说对于汤姆开的session，那么我们原来是基于汤姆开的存储的 session 呢？在这里面就不可用了，所以说我们这里面需要改进成我们的一个集群session，那么这里面集群 session 的话，我们可以基于 Redis 进行了我们的 session 的一个共享。


好，那么这里面我们大概知道他们开的集群和 session 共享要介绍的内容，这里面我们通过两部分来介绍，第一部分是他们开的集群，第二步是他们开的 session 共享，那么我们使用什么方式去做他们开的集群以及他们开的集群带来的一些 session 的问题，所以说我们要解决他们开的 session 共享的问题。


那么首先我们来看一下瘫痪的集群，他们开的集群碰到第一个问题，我们需要跟大家解释一下为什么需要他们开的集群。刚才也跟大家简单的介绍了他们开的集群能给我们带来哪些好处。第一它可以提高氟的性能、并发能力以及高可用。第二提高项目架构的横向扩展能力。第三它解决了我们汤姆开的我们高可用的一个部署方式，也就是说我们部署的过程中不需要停机。那么首先我们来通过提高服务性能的方式来去跟大家分析实际上公司线上的生产环境，它都会通过一台机器去做我们的负载均衡，另外我们去多个澎湃的集群进行访问，为什么这样做？我们想对于一台机器的话，它通常是有一些性能瓶颈的，我们在一台机器上部署汤姆派的，它只能使用这个机器上的一些资源。通常如果说我们为了更充分利用一台实体机的资源，我们可以通过虚拟化刀客的方式在这一个机器上部署多个实例。那么其实对于每一个实体机，它的资源的分配也是取代不均的，比如它的网卡、内存和磁盘 IO 都属于共用的。


这里面如果说我们要跨实力跨机器的去扩展我们的机群，我们就需要在多个机器上进行部署，这样的话一方面我们能充分利用每一台实体机的资源，另一方面我们也可以基于我们的业务能力进行一个横向的不限的扩展，这里面就是提到了所以说我们提高服务集群需要进行扩展的方式。另外还有一个并发能力，对于我们提高我们整个机群的并发能力也是比较容易理解的。


一台Tomcat，它的 LTP 线程值是有限的，实际运行中最大并发数与硬件性能和 CPU 的数量有很大关系。我们如果使用更好的硬件，更多的处理器，都会使汤姆开来支持更多的一个并发数。汤姆开的默认的 STP 实现的是采用主式的 SOCKET 通道，那么后来我们换成 NL 的通道，也就是说每个请求它在使用的线程是有一定的限制的，这样的话我们就是说我们一个汤姆开始它能承受为多少并发，跟我们的硬件 CPO 是有很大关系，我们分配 j v m 内存越多的话，它的性能有益好，但它同时也会增加我们 g seed。


附带我们来简单介绍一下汤姆克的集群它怎么做到高可用性。通常我们在部署汤姆克的集群的时候，前面会有一个NGX，那么也就是可以理解为 NGX 下面挂载了多个汤姆克的节点。当我们有一个塔姆克的节点挂掉，或者说是我们在进行发布的过程中，那么我们可以先把这个节点从 NGX 负载均衡的他们的集群配置中移除，那么对于请求 NX 它还会打到我们的汤块的服务器上，那么对于这些节点的话，它对于下线的节点就不会有新的请求，我们可以对这个下线的节点部署新的应用，也就是更新我们的版本，那么进行上线。


那么这个节点上线完成以后，我们同时把它的配置注册到我们对于 NGX 的它的负载均衡列表里面，这样我们每一台接着替换，这样能做到我们对于整个 NGX 下面的他们开的集群的节点进行替换，在这个替换的过程中我们可以理解为是个平滑过渡，那么对于我们的用户来说，因为通过 NGX 的一层代理在进行访问，我们并没有感知到我们的服务器进行了一个版本的迭代和升级，所以说这就是我们可以理解为基于 n g x 集群带来他们开的集群的一个高可用性。


另外我们再说一下横向扩展，假设对于一台服务器，我们通过不断升级它的 CPU 内存、更换硬盘等等，我们认为这是一种纵向的去提高机器的配置，来达到他们开的所提供服务的性能，那么随着硬件不断的提高，成本也会指数级的上升。其实也有人会描述我们去扩展硬件配置的方式去增加性能，就像是一种吸毒的方式，我们可以快速短暂时间内达到我们性能的一个提升，但是它是不可持续的，因为我们基于现在的技术发展，它的单台机器的硬件性能肯定是有一个限制的。而对于横向扩展，它其实是不一样，比如说我们对于通常的一些双十一或 618 的活动，在机器的硬件达到天花板的限制的时候，我们再进行扩展，我们就需要扩展机器了。另一方面因为它平时的访问量可能并没那么高，到了一些节日大促的过程中，我们需要特别多的机器，那么这种情况下我们需要去增加我们机器的一些节点数。那么瘫痪的机群完成这样的一个扩展的话，它可以在一个低成本的方式迅速的把我们的可以承受的并发数达到一个很高的一个标准。这样的话只要我们增加一些相对便宜的机器实体，部署汤姆克的节点就可以了。


那我们现在了解了汤姆克的集群，它能给我们提高服务的性能，扩展我们的并发能力，也解决我们高可用和横向扩展的能力，那通常我们如何去实现他们他的集群？这面刚才也跟大家介绍好多次，那么我们会基于NGX，像早期的话，我们对于 Java 的部署，我们可能是阿帕奇加塔姆cat，那么随着 NGX 的用户量越来越多，它的性能也非常好。所以说我们现在通常是基于 NGX 加塔姆克的方式来构建我们的负载均衡的汤姆克的集群，我们可以看一下整个一个效果。


在这里面我们对于一个普通的用户，我们进行一个访问，我们提供了一个加 Web 应用，通常我们不会直接访问 palm cat，那么这里面我们知道如果我们直接访问他们开的，那么进行我们服务节点升级的时候，我们就必须把他们开的停掉，那么他们开的停掉对于用户来说是一个有损的，那么停机的这一刻我们是不可用的。


所以说我们通常是基于 NGX 前面注意成代理，那么后面我们挂上我们对应的汤姆开的节点，来构成一个汤姆开的集群，这当我们汤姆克的一出现问题以后，它可以直接下线，那么我们的请求还可以继续在汤姆克的 2 和汤姆克斯 3 上进行一些，我提供我们的服务，那么发布的过程也是原理，我们通过 123 迭代的进行更新我们的版本，那么进行在这里面对 NGX 它的负载均衡列表上执行一个下线升级版才上线的一个处理，这样对于用户做到一个无感的效果。


或者我们用 NGX 去部署他们开的集群，那么现在 NGX 对我们来说也是越来越重要，我们有必要去深入的了解一下 NGX 的一些工作原理。那么严格来说， NGX 它仅仅是作为一个反向代理使用的，像普通的一些负载均衡软件，比如说像RVS，它实现的功能只是对请求数据包的转发。所以说从负载均衡下面的节点来看，我们接收数据请求是来自负载均衡的客户端的真实用户。


返乡代理，它是有些区别的，返乡代理接收到访问用户情况下，会代理用户重新发起请求，代理到下面的节点服务器，最后把数据返回给客户端使用。从节点，也就是从我们汤姆开的节点服务来看访问节点服务的这些客户端，它其实是我们的这个返现代理服务器，而并不是我们真正的一个网络用户了。


其实对于这个返乡代理，我们现在也不用纠结我们的请求是来自于 NGX 还是来自于用户，那么现在我们可以理解为我们的整个NGX，它一呈负载均衡和这种网关的承载逐渐的理解为透明的。那么现在我们就会想 NGX 是如何去做到反向代理和复产均衡，这里面它会用到了一些 NGX 的一些主键，比如说我们实现 NGX 负载均衡的组件，这里面会涉及到像 NGX IGP proxy model，还有 NGX ITP upstream model。那么像 proxy 代理模块，它用于把请求抛给我们的服务节点，或者说 upstream 的服务池。也就是说我们 Nginx 基于代理的方式去访问我们具体的一些节点。


另外像 upstream model，它可以理解为测负载运河模块，我们在这里面需要配置对应的一个 upstream 节点，那么我们基于这个实现我们的负载均和及节点的一些节前健康检查。那么这里我们可以看到我们在对应 NGX 的 config 文件里面，我们配置 upstream 节点，这里面我们给它起个名称，这里面我们是 time k plaser，一个 time 开的集群，在这个集群下面我们配置了 3 个Server，那么这三个 Server 我们还配置了不同的权重。


通常如果说我们的Server，我们的实例节点如果是配置一样的话，我们把它盘权重的配置成一样的，那么这样的话，如果说根据不同的权重的话，我们可以看到它会执行整个权重的一个占比，比如说像我们这个整个占比它是比如是50，那么这里面据第一个节点是占比是20%，那么后面两个节点分别是40%，那么这是我们 stream 的一个配置。


那么这里面我们对于 server 进行一个验证，那么也就是说当我们在访问我们这个 my APP com 这个域名的过程中，它的请求会转发到我们对应的汤姆开的 class 这样一个集群，这样我们可以看到我们配置，嗯，他们开的负载均衡的房子是这样，但我们会想另一个问题，假如说我们某一个节点它已经失效了，那么这个节点失效以后我们的请求会是不是还会打到这个他们的节点上？如果说会打上下面的话，它就会我们带来一定的损失，也就是说明明这个汤姆开的已经失效了，这个负载均衡并不能把这个节点移除。


当然 NGX 是可以支持这样的，我们可以看到另一个胖斯的配置，其实我们可以在对应我们 Upstream 的节点上面增加配，这里面是 Max Fills 以及我们的 FILES time out，那么这里面它这些配置是 Upstream 模块所支持的一些 Server 的标签，那么它是什么意思？像 Max Fills 它是指我们NGX，它会去尝试访问对应的汤姆开的节点，那么访问汤姆开的节点如果失败的话，那么这里面制定一个失败的次数，也就是说我们访问下游他们开的实例的过程，如果失败两次的话，那么后面的请求他就不会再访问这个节点了，那么他不再访问这个节点，是不是这个节点永远不可用了？也不是。


那么这里面有一个 FILES time out，它是什么意思？它就是说当我们发现这个节点不可用的时候，我们通常的请求就不会发出给这个节点，这里面会有一个等待时间，我们这个 failed 也就失败的 time 等待时间就是 10 秒，那么超过 10 秒以后，我再会去尝试去访问他们at，看它是否已经达到一个可用的状态。这里面跟我们的熔断限流的逻辑是有点类似的，那我们访问两次它失败了以后，我就把你这个节点给熔断了。那么我过一段时间以后，我再去尝试去探测一下你刚才这个节点的是否可用，唉，如果可用的话，我把它做一个正常节点，把它部署上去，如果不可用那么继续处于一个不可用的一个状态。


那么这里面大家去理解，这里面我们他们看的集群也可以基于实现这个节点的一个状态的一个情况。那么这里面我们再简单说一下，像 Upstream 的它，我们可以理解为一个负载均衡的结构，那么对于负载均衡的话，它都会有一些支持的一些算法，比如像网关里面我们也会提到像一些轮询或者IP、哈希等等一些情况，那么像 n g x 它也支持轮询，它还支持权重轮询。比如说你看我们这里面基于权重，也就是权重轮询，那么轮询的算法是比较容易理解的，它也是 NGX 的默认算法。


轮询算法就是它按客户请求的顺序，把客户端的请求逐一分配到不同的后端的服务器节点这里面，如果后端服务器节点宕机的话，宕机的服务器会自动的从节点里面节点词里面剔除，这样的话以客户端用户访问不受影响。另外就是权重轮询，那么权重轮询它也就是在轮询的过程中会基于一定的权重优先，得向权重高的节点会分配更多的流量。


还有一种就是我们这里面是对于的是 IP 哈希，对于每个请求，它会按客户端的 IP 进行一个哈希的结果分配。当新的请求到达的时候，先将其客户端的 IP 通过哈希算法计算一个值，然后再对我们的节点的列表数进行一个取模的操作，这样的话映射到对应的机器上，按客户 IP 哈希值相同的，它就会分配到同一台机器。那么这两个算法其实它有一些，比如对于轮询来说，它可以让我们的请求在各个节点部署的会更均匀一些。


IP 哈希的话，假如说我们要去解决我们的 session 共享的问题，其实相当于是我们主要是相同的IP，我们都会达到相同的节点，这样的话即便我们不解决 i session 共享的问题，也不会出现 session 获取不到的情况，所以这里面的话，但是通过 IP 哈希会有一种情况，如果说我们的节点比较少的话，那么它会对于这个分配不是很均匀。所以这里面我们可以就是说基于一定的规则去选择我们的一些算法，通常还是建议大家采用轮询的方式。 IP 哈希的方式其实主要还是因为它那个均匀性做得不太好，所以说大家其实选择的方式会少一些。


现在我们了解了我们基于 NGX 去做负载金宏来部署他们开的集群的方式，那么接下来我们来看一下我们在他们开的集群过程中，我们引起的会话共享的一些问题，那么我们来看一下他们开的 session 共享如何做到 session 共享，那么这里面我们涉及到了多个 timecase 的实例，那么这样的话，如果说当我们请求的会话能保持得到一致的信息，这里面通常会有三种方案。


这里面就是说我们先说一下这个会话共享，就是为了是这个 Web 能适应大规模的防疫，就是让我们的他们开的节点无限扩展，需要实现集群的部署，那么集群坠雪的方案前面是我们的负载均衡的方案，实现负载均衡就是每个请求大家都可能会分配到不同的这个服务器上，这样我们首先要解决一个 session 的统一，来保证无论请求转发到哪个服务器上都能保证用户正常使用。


其实对于绘画，通常我们一个购物场景就是购物车的场景，我们这里面是需要一个 session 的一个共享机制，通常对于 session 的共享机制会有三种方案。第一个就是我们的对于请求的精确定位，这里面请求订阅定位怎么理解？我们主要是基于 IP 的哈希策略，当我们的用户请求的过程中，比如说对于张三，他每次访问的过程中，他都能访问到他们 case 一，李四他每次请求都会访问到他们的。二，这样的话我们对于 session 的话，它就没有存在共享的情况，但是它解决了我们会话不一致的问题。这里面会有一个缺点，也就是说当我们他们开的一这个节点失败，也就是说它宕机以后，那么张三在访问他们开始一的过程中，他会发现他会访问不到，那么访问不到的话，他通过我们的 IP 的哈希策略，他可能会访问到他们的2，也可能会访问到他们的三，那么这种情况下它就会存在 session 绘画丢失的情况。


所以说对于这种情况，对于 IP 哈希刚才也提到我们还是不推荐我们使用IP。哈希的这种策略就是说另一种方案就是我们的 session 复制共享，我们知道如果说我们只有一个谈不开的节点的话，会在我们的 Gam 内存里面分配一片空间去存储我们的 session 的会话信息，那么这里面我们就是说因为每次都是同一个唐派的，所以说它可以从内存获取到相同的信息，这里面我们对于多个节点进行复制的话，如果要做 session 共享的话，我们可以做的一种方式就是说我们不依赖外部的存储空间，我们只是在汤姆开的把这块存储空间进行一个复制。


也就是说汤姆开的一，这里面的这个 season 占的内存空间同时复制到汤姆克的2，汤姆给的 3 上面，这样我们能做到 session 绘画的一致，也能做到访问不同机器的时候能得到相同的一些 session 信息。但是这里面的话，我们可以看到，如果说我们通过 session 复制的这种机制有一个明显的缺点，也就是说这个 session 占用的空间是受限的，这样的话当我们在扩展节点的过程中，因为每扩展节点它 session 绘画会支持更多的绘画，所以说它这里面占的内存空间会更大，所以这样的话就给我们带来一个限制。也就是说我们的扩展受 session 空间在内存的影响，我们这个扩展是其实受限的。


当然如果说一个服务器发生故障的情况下，这样基于负载均衡，我们依然能寻找到对应的绘画节点，但这里面的缺点就是我们扩展的过程中，它占用我们的内存空间，所以说这种情况下其实也是不推荐使用的。但如果说我们的摊不开的节点非常少，比如就两个节点，我们主要是在做一个高可用，那么这样的话，我们为了减少外部的依赖，我们可以引入这种绘画机制，但其实我们更推荐的一种方式，我们利用第三方的cats，也就是说其实我们的绘画基于第三方的 case 去存储。


那么比如说汤姆凯特一请求的过程中，我们并不在汤姆克特一内开启这片内存的使用空间，我们直接把我们对应的信息存储到 Redis 里面，那么汤姆克特2，汤姆克特 3 也是他获取信息的时候直接通过 Redis 获取这些会话信息，这样让我们的应用做到一个无状态，当 template 依托 template 二三任一个机器宕机的过程中，我们请求直接重新映射，那么我们都可以从 ready 里面获取到正确的会话信息。


其实这也是我们现在对于互联网公司在去做多节点部署的时候比较推荐的方式，但其实还有另一种我们推荐的场景，也就是说其实我们在使用的过程并不推荐我们使用绘画，使用session，那么 session 本身就是有状态的，它是依赖这个状态的定义，所以我们要做无状态的情况下，通常在请求过程中不建议我们开启我们的session。
不开启 session 如何去识别这个用户？我们通常会基于 JWT 表示一个token，通过 token 的方式去通过 Redis 或者是 memory cats 里面去获取我们的一些信息，其实它原理是一样的，只是在汤姆 cat 城，我们不依赖 surprise 容器它的绘画机制。


我们基于自己的应用程序，我们去定义自己的一个 token 和我们 Redis 共享的一个空间做了一个分布式缓存的使用，这样的话我们的应用程序做到了真正的无状态，也就是说不存在一个会话共享的情况。我们基于 JWT 或者是其他的一个 token 的生成方案来去通过 token 局唯一标志当前这个用户，去做到我们基于 Redis 的一个信息共享的一个情况。好了，关于我们汤姆克的集群和三层共享的内容，我们就先介绍到这里，同学们，我们下一章节再见。

