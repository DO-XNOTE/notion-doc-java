---
title: 4-2 扩展Mybatis缓存支持分布式Redis缓存（0940）
---

# 4-2 扩展Mybatis缓存支持分布式Redis缓存（0940）

同学们大家好，这一章节我们跟大家介绍一下，我们通过扩展 mybatcase 缓存支持分布式的 Redis 缓存。我们知道对于 mybatcase 它支持一级缓存和二级缓存，但是很遗憾的是默认的 my better 的一级缓存，二级缓存它们都是一个 local cats，也就是说都是我们存在我们 JUM 对应的哈希 map 里面的这种方式的一个缓存。那么因为我们随着现代分布式系统用来越来越广泛，我们通常对于一个实例会有多个节点，那么假如说我们每一个实例都会纯占用的我们内存的空间去做缓存的话，可能我们在里面对于这个经济性不是很好。


那么如果说我们基于 Redis 作为一份共享缓存，那么我每个实例数据都会放到对应的 Redis 里面，我们获取缓存是从 Redis 里面获取缓存，这样减少我们 JAM 实例里面对应内存的占用，也可以做到我们不同实例里面他们的缓存做到一致性。这样的话，基于这一点的话，我们可以进行一个改造，我们买 Badcase 的二级缓存，把它改造成支持 Redis 的缓存。


那么接下来看一下我们怎么去操作。首先在这里面我们需要做的事情，需要打开我们的二级缓存方式，就是在这里面我们设置 setting 我们的 cats enable，它的 value 为true。那么我们设置完打开这一步以后，我们去定义我们的 Redis cats，我们可以看到这里面我们定义的 Redis cats ready 的case，它的操作，它是实现了我们这里面的 case 接口，其实我们在学到现在经历了很多 case 接口，包括 spring 下面的cats，我们或者说是 EC cats 等等这些cats。


那那么这个 case 接口我们可以看一下，它对应的是 i badcase 开启下面的接口，那么基于这个 case 接口它的一些操作，我们可以看到这里面有 get ID 铺到 black 的 get 文档，这个跟大家去介绍过，它其实对参考了对于 map 接口的，对于类的获取和实现，那么我们可以看到它有一些默认的实现，那么这些实现我们都可以理解为它是一个本地的缓存。那么唯一我们这里面定义的 Redis case，它是做到了一个分布，从而那我们看一下 Redis case 具体的一些实现，在这里面我们看一下几个关键方法。
这里面首先我们可以看到我们的puddobject，那么 puddobject 的操作我们怎么操作？我们可以看到我们首先去获取到我们的 Redis template，那么对于 Redis template 里面，我们可以看到我们对应获取到一个 OPS for value，也就是我们对于这个直进一个操作，对于它我们可以把我们的 k 里 value 进行一个设置，那么设置完成以后，这里面还指定了一下我们的过期时长，那么过期词它对应的参数是一个 manage 分钟级。我们可以看到这里面也就是我们的缓存默认在 ready 里面存放 30 分钟， 3 分钟我们不处理的话它就过期失效了，这是我们 put 缓存的一个操作。


另外我们可以看到 get 缓存的操作也是我们使用 Redis template，我们这里面通过 Redis template 获取到我们的 OPS for value，也是对于一个值的操作，这里面我们通过 OPS for value get k 获取到我们这个对应的值的对象，那么这样看的话，这是最简单的两个方法，也就是说我们把我们的 KV put 到对应的 Redis 集群里面，或者是我们获取我们的object，把我们通过 k 获取到我们对应的value，也就是从我们 Redis 里面获取到对应的值。那么这样的话看起来非常简单，但我们这里面实现了很多东西，我们看一下做了哪些事情。


首先我们看一下我们的 Redis template 是如何注入的，那么对于 Redis template 我们可以在这里面我们需要了解一下对应的，也就是说 spring 的生命周期。这里面我们在 get redis 他们这个过程中，我们先判断一下这个属性是为null，如果等于 null 的话，我们这里面使用了一个 application context holder 去 get in。如果说当前 redis template 等于 null 的话，我们通过我们的 application contact holder get been 的方式获取到 re redis template。
那么这里面大家要注意一下，通常我们在写我们的 spring 程序或 spring boot 程序的时候，对于这些 web 依赖，我们都是通过注解的方式，我们通过 where 制动注入的方式把它注入进来，那么在这里面我们可以看到我们并没有使用对应的属性，使用 and where 的方式把这个 red template 注入进来。为什么不能这样做呢？这里面给大家简单介绍一下。


其实我们要知道可以注入 b 这种情况，通常它是都是存在我们 string 容器里面，那么对于我们当前这个 Redis cats，它的实力其实并没有在 string 容器里面，它的 case 构造方式，它是基于 mybadcase 实现实例构建的，也就是说当在我们解析对应的 user map 的过程中，我们得到一个 cat 标签，那么得到 cat 标签以后我们要做的事情需要实例化这样的 Redis cat。那么这个实力化的过程，因为买 at 的本身它是不依赖 spring 的，所以说在这里面买 Beta 是感知不到 spring 的存在，所以说它只能通过加 2 反射的方式，我们这个 Redis cat 进行实例化起来。


那么实例化起来以后，我们知道如果说 Redis cat 里面没有 Redis template 进行支持的话，它断然不可能把数据放到对应的 Redis 集群里面，所以说我们这里面必须提供出 Redis template，那么我们这里面提供的方式是怎样的？我们可以看到我们是使用了一个静态方法，也就是我们 application context holder 去 get been 的方式获取到，那我们看一下这里面的实现，通常在 string 程序里面是不建议大家使用静态方法去获取 bin 的，因为这里面会给我们带来一定的不确定性。比如说当我们使用容器在初始化完成之前，如果调用这个方法，它肯定获取不到我们想所期望的对象，所以说这样的话通常是不建议使用的。


但在这里面我们为了解决 string 容器的拼音和 my betas 自己创建的这些实例之间这些对象的关系构建，那么我们不得已在这里面使用了 application connect HOLDER 的静态方法去获取我们必应的一些信息，那么这里面我们获取到必应信息以后，那么就跟我们 Redis case 呢关联起来了，这样我们就可以进行我们的数据的一些存储，那么这里面我们看到整个这个 Redis cats 实现的还是比较简洁的，那么完成这个简洁我们接下来就需要使用这个 Redis 开始。


那么怎么使用？对于通常的二级缓存的话，我们需要在 user map 里面在这里面添加一个 cat 标签， case 标签，通过 type 指定一下我们这个缓存的类型，这是 Redis case，跟我们刚前面讲到的我们这里面默认的二级缓存用到的是这是个永恒的缓存。


接下来给大家介绍一下这里面对于 x 庙这个 case 标签，它解析的逻辑是怎样的。这里面我们对于涉及到 Mapper 解析的话，我们注意一下它是通过 Xmail Mapper build 的方式进行解析的，在这里面解析的过程中，我们特意去这里面看一下 cats element，那么 case element 我们可以看到这里面它会解析 case 标签和 case EF 标签，那么这里面我们重点看一下 case element 在 case element 里面处理的过程，我们可以看到首先它会通过我们对应的 context 对象里面获取我们的 type 属性，那么如果 type 属性它默认的是 property 对应的这个内容的话，也就是给我们一个默认的值，如果说没有指定的话就是采用它作为我们的一个默认标签。这里面我们可以看到它会获取对我们这个 Tab 进行解析，解析出对应的类型来人，对于这个类型通过我们的类型别名映射器，也就是解析出它对应的类型，后面我们看它去获取我们的case，也就是过期的一个策略。


这里面我们默认只是LRU，后面是我们是也是通过我们对应的一个别名解析器，把对应 LRU 它解析出对应的一个实现class，其实后面的处理逻辑也是类似的，我们看这里面是 Flash interview，也就是说我们刷新的时间周期，这里面获取我们的size，这就是通常我们在定义 catch 里面的这些标签。那么我们可以看到最后这里面会涉及到一个 catch 的实例化，这里面我们可以看到是通过 use new cacts，你们这里面把参数传入进去以后，我们怎么去创建这个 new cats？对新的一个 case 对象，这里面会用到了一个 case builder，那么这里面的 case builder 我们看首先指定一下当前的 name space，这里面我们指定我们的实现类，那么还有我们对应的一些装饰相关的一些操作，这里面我们可以看到是ru， case 等等这些信息。


那么看完这些信息以后，我们可以看到它对应的这个 build 操作。其实我们创建完 case builder 这个实例，那么最终通过 builder 方式构建出我们的 case 对象，那么其实这个 builder 的操作，其实也就是我们最终会通过反式的方式把我们的实例对象勾选出来，我们可以看到这里面是有一个 new base case instance，我们看它是如何实现的，我们可以跟进来看一下。


这里面我们获取到我们的对应 case plus，这里面通过反射的方式找到 case plus 对应的构造方法，那我们通过构造方法的方式，直接你有 instance 构成这个instance。那么对于我们的 Redis catch 的话，这里面获取到的构造方法是什么呢？我们可以从这里面去看一下这里面获取的构造方法，也就是我们对应的有包含一个参数，也就是作为我们的 case ID 的这个参数构造出我们的 Redis case，那么看到这里面的话，我们可以看到这样的话，我们的 case 标签通过解析，最终我们通过反射的方式把我们的 case 对象构造出来，所以说在这里面构造的过程中，这个 case 对象它并没有跟我们的 string 容器里面的鼻音进行一个关联。
所以说在这里面，如果说我们想获取对应的我们的 Redis template，那么我们必须在这里面通过我们的 play case context HOLDER 做一个桥梁来获取对应的 Redis template 信息。那么关于这个，我们通过 Redis 实现这个 cat 的方式就先介绍到这里，同学们，我们下一章节再见。

