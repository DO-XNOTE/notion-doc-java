---
title: 7-4 Tomcat核心配置分析（2105）
---

# 7-4 Tomcat核心配置分析（2105）

同学们大家好，这章节我们来介绍一下汤姆开的一些核心配置的分析，这里面我们主要是去看一下汤姆凯的支持哪些方面的配置，我们对于这些配置项给大家做一些介绍。对于汤姆 cat 我们知道它支持很多方式的配置，这里面我们重点来看一下对于汤姆cat，它的配置文件都在对应的汤姆 cat config 这样一个目录。下面在这个目录里面我们最关心的两个配置文件分别是 configure 的 Server XL 和对应的VCML，对于 VCML 我们改动会比较少一些，那么 Server XL 那么我们可能会进行调整一下，对于这些我们是使用默认值还是需要调整的，这个其实并不是很重要，重要的就是说当我们需要对它进行优化的时候，知道应该在什么地方下手。


对于这几个配置文件，我们还是切到我们的唐门开的目录里面，给大家去了解一下这些配置文件的用法。在这里面我们可以看到对应打config，这里面我们还是重点看一下这些配置文件。我们来先看一下这 Server 点ML，这个 Server 的 ML 的内容跟大家也简单介绍过，但我们并没有逐项逐个标签的去跟大家解释，说明它的意义和逻辑是怎样的。同时这里面也有一个 Server 的Demo，对于 Server 的 demo s now，它其实 Server 我这里面拷贝一个副本出来，在这个副本里面我们把我们关注度比较低的一些内容进行一些清理，其实这里面我们还可以去通过 Server 的 ML 来重点来看这些内容。


从这里面我们看对于这个 XML 它的一个根的节点，也就是我们的Server，这里面 Server 里面内容我们可以看到这里面色指定了port，并且是它的 set down 一个说明，可能我们经常会看到配置的，但我们可能没有去想过它的原因是怎样的。


其实对于这个server，它作为一个标签，其实是代表了一个属性，对于这里面其实有一些隐性的操作，也就是address，如果说对于 address 它默认就是 local horse 的，如果说我们通过 local horse 的去拼 8005 这个端口，它就会触发一个 Tom cat 对应的 start down 操作，其实这里面的输入我们是需要使用 Tarnet 命令。


Tarnet 我们对应的 local house 的8005，它访问到这个主机的 8005 端口，就会触发我们汤姆 case 的关闭 Server 点 Q2 里面它是否还支持一些其他的一些属性？我们可以在这里面简单给大家去进行一些调整，其实这里面还支持哪些？比如说它的 class name，对于 Server 它默认在解析这个标签的时候，会构造一个对应的一个 Server 类，这个 Server 类也就是我们的 stand Server，可以在这里面去看一下，我们可以看到这里面是 standard Server，这个是一个标准Server，对于标准 Server 它其实是实现了我们这个 Server 的接口，这里面对于 Server 接口时提供了这些相应的操作。这里面其实我们比较关注的可以像在这里面我们指定对应的一个 Server 的接口的实现，这里面有对应的 POD 80 零五。


那么如果说我们想关闭这种 set down 关闭的方式，我们把 8005 我们改成一个 -1，那么就关闭了汤姆开的这股自动关闭的这个端口，同时这里面对于 bin 里面的 set down 操作，它也就已经失效了，也就是说我们直接关闭了相关的功能，那么说我们怎么关？如果说 set down 它已经失效以后，我们要关闭它们开的可能就只剩下 q 的方法了，其实这样对于 set down 保留的话，对我们还是有一些帮助的，重点还是聚焦到 configure 下面，接下来我们看一下对 Server 下面的内容，其实 Server 下面的内容包括三类元素，这三类元素分别是listener，我们可以看到这里面有listener。


还有一个是 global naming resource，也就是一个全局的一个命名查找资源，这里面我们看 global z n d i，另一个就是我们看后面是一个service，这个 service 其实是相对比较重要的，我们会重点去介绍一下，那么我们来先看一下这些listener，对于这些 listener 的话我们可以重点一个个去介绍。


首先我们可以看到这里面是一个 version lock listener，这个是比较容易理解的，就是说它在启动的过程中，我们通过日志的方式把我们当前的这些版本信息输出来，这版本信息还会包括像我们的服务器操作系统 JM 的一些版本信息。第二个我们来看一下，这叫APR，我们这是一个执行 APR 的一个生命周期的一个渐进器，它它会在我们的服务器加载销毁你这种服气停止的过程，如果说找不到对应的 APR 库的话，它会输出日志，并不影响汤姆开的那些正常启动。接下来我们看一下这是一个 GRE memory，立刻 print listener。看到这个的话，我们可以想到其实这个 listen 的人，它就是监控我们的 GRE 内存，避免泄露的一个监天器。下面我们可以看到它是一个 global resource 进行一个 life cycle 的listener，它做的事情是什么呢？其实它在我们的服务器启动的过程进行一些加载，和对于我们销毁过程中，服务器停死的过程中进行一些全局的一些命名服务。


下面这个是我们看的是 shut local unique prevent listener，我们可以看到它也是对于我们内存泄露的一个保护，主要是针对我们的 third local 这样的一些信息。因为我们在写加码程序的时候，有时候我们会把一些设的 local 的一些信息存在设的 local 里面，避免我们参数传值。这里面就是要做到当我们程序停止的时候，把我们是 local 的信息进行一些内存回收，避免它进行一个内存泄露。


接下来我们来看一下 global naming resource，也就是我们全局的一个 GNDI 的资源查找器，这里我们看它默认定义了一个resource，这个 resource 我们看它的 name 是 user DATABASE 执行的，对应的 author 是container，它的类型，这里面类型是 user DATABASE，也就是我们定义的一个 use level list 类型。


在里面我们看它有一些描述信息 factory 和 pass name，这里面就是说构建这个对象的时候，会通过 memory user space factory 来构建这个对象。同时这里面做了一些描述，就是说用户的数据更新，这样的话我们可以进行一个更新和一些保存，但因为我们对于他们开展来说，它本身的用户角色管理是比较弱的，通常我们很少使用对汤姆开的默认的用户决策的权限信息，这里我们看指定一个 pass name，也就是 com c i f 的汤姆开的 user 点XL。


这个文件也给大家介绍过，这个文件里面介绍了我们对于他们开的默认的一些用户和角色之间的关系。这个文件其实是经过修改的，且默认我们只有 Tom cat。对于像这个 role 这个 row 角色， row name 对应的 manager GUI 是因为我们特意添加上去的，加上这个 manager g o i 主要是为了解决我们访问 manager app 的权限问题。这里面我们有汤姆 cat 的角色和我们的 manager GOI 角色。


下来我们来看一下我们的汤姆 cat 登陆用户，这里面有汤姆 cat boss 和肉意，这里面对于汤姆 cat 的用户的话，我们这里面特意设定了他的密码也是汤姆cat，并且指定了他的角色，他角色是对应的是汤姆 cat manager guy，我给你指定的这个汤姆开的用户添加上 manager g y 这个角色，它就可以去访问到我们对应这个 APP 的目录，也就是我们访问 manager APP 这个一个页面的功能。


这是我们看到是 Tom cut 的 user 这个配置文件相对是比较简单的，那么我们还回到我们的 Server 文件，这里面我们对应的 Server Demo，我们接着去看，这里面有对应的一个 service 标签，对于 service 标签它是相对比较重要的一个，这里面我们定义了它的名称肯定inner。


如果说我们想知道它一些更详细的配置，我们可以看到这里面有doc，我们 config service itmail，也就是说我们在 Web APP 下面有对应的文档，在这文档里面我们找到对应的 config 目录， config 目录下面有很多配置相关信息的一个介绍，这里面我们可以看到有 server s t l，我们可以打开这个文件去看一下它的一些详细的简单介绍。
这里面我就先不跟大家操作，那我们先看一下 service 的name，对于这里面的话，它的内容还是比较多的，对于 service name 的标签主要是用于创建 service 实例，默认使用的是什么呢？也是 stand service，对于 stand service Tom Pad，它指定了一个 service 的名称，也就是我们这里面的 contain liner。对于 service 它的一些子标签，这里面它的子标签其实包含的东西也很多，比如说listener、accurator。


和 Connector 和engine。可以重点介绍一下这个equator，我们可以看到educator，默认它是在一个注释里面，就是说它是一个没有生效的一个状态，我们可以看到它的一些内容。对于acute，它是用来配置这个 service 的一个共享的线程池，也就是说我们可以基于这里面配置一个ACUTE，跟我们 Java 里面 acute 可以理解为是相同的道理，这里面有一个name，也就是我们的性量池的名称，同时我们对于这里面创建性量池名称的一个前缀，可以看到这里面 e 和 EC 指定杠后面就是我们这个数字加一。
这里面我们看这里面配置的是 Max 的shirt，也就是说我们的最大线程数是150，这里面还有一些最小的一些线程数的一些配置。其实这里面我们主要关心的是对于它默认这个共享线程池，可以基于这种发动机配置，我们可以调整它一些线程值的大小。除了这里面显示的这些属性，它当然还有一些其他一些属性。


其实跟我们 JDK 里面创建线程池的这些核心的参数可以进行一个类比，比如说我们对于创建线量池的一个最大队列，或者说我们线程池的最大空闲时间，比如超过某个时间以后，这个空闲线程就会被销毁，这里面的默认值是一分钟。对于像我们的最大队列，就是说当我们的线程达到了我们当前的核心线程数以后，我们需要把我们的数据向队列里面放，队列也满了的话，就开始创建我们新的线程，直至达到我们的最大线程数。其他的还有一些，比如说我们线程除的一些线程的优先级，这里面的优先级默认值为5，这个值可以从 1 到 10 进行一个配置。


这里面还可以指定一下线程池的一个实现类，如果说没有指定的话，它这里面也是标准的一个 standard 设的 presert activator，也就是说这是汤姆开的基于 cat Liner 实现的一个线程值的一个接口，其实这个这里面的内容就简单，先介绍到这里，对于 a Creator 后面我们再看这是什么是Connector。


对于 Connector 的话，我们可以看到它是指定了一个对应的是端口号8080，那我们知道汤姆 cat 默认启动的端口号是8080，但是我们注意一下， 8080 端口是对应的是 ATP 协议，对于 ITP 协议，这里面还有我们的 connection time out 的时间，这里面是2万毫秒，也就是对应的是 20 秒。


这里面的还有一个 direct port，它是 8 斯闪退是什么意思？其实如果你有音响的话，我们知道 Tom cat 暴露的 at TTBS 协议的端口是八四十三，也就是当我们有一个请求，如果说它是支持 secret constraints 的一个约束，如果是需要 SSL 的这种加密传输，它就会跳转到 8443 这样一个端口。


接下来我们来看一下其他的一些内容，对于我们通常来说谈不开，它暴露的是里面就是 8080 这样一个端口，我们看下面这里面是8443，对于 8443 这个端口默认没有打开，也就是说我们需要一些安全协议，访问的时候我们需要打开。八、四十三、这里面我们需要配置一下我们的一个密钥，对 RSA 的密钥，对指定一下它的一些加密的一些类型。其实我们在定义 connect 的过程中，我们还可以指定一下它的一些线程池的一些信息，比如说这里面我们可以看到它设置了一下 Max straight，也就是我们最大线程数。同时这里面也可以直接指定一个 IQ Ator， IQ ter 就是在我们刚才基于上面的方式定一个iqueuter，在里面进行一个指定，可以类似于在使用的过程。首先定义一个线程池，我们在对应某个方法执行的时候，指定一下我们使用哪个线程去执行，这里面是一些类似的一些操作。


接下来我们去后面看一下这个引进标签，这个标签就是我们的一个 Server 服务引擎的一个操作，这里面也有对应的它的配置文件的说明，这里面是 in 点IXL，就是在我们对应的 config 下面，这里面应该配的文件。对于这里面的配置，它可以标明这是一个 THREAD 引擎，它主要是用于默认的，就是我们这里面是 cleaner decode house，也就是说我们服务的主机名称就是 local house 的，这里面其实它还会支持一些其他的一些属性的一些配置。


不展开介绍了，那我们看一下这引擎下面我们比较关注的一些标签，这里面我们主要关注的标签就是host，我们可以看到这里面 host 的一些配置的信息是企业 name 是 low cost，并且它对应的 base 的路径也就是 Web APP，也就是我们这里面的 Web 这样一个目录，这里面我们看到它有像 unpack wall，也就是说如果用放入的外包在这个目录下面它会自动进行一个解压，进行一个制动的一个部署一个过程。


其实我们可以在 host 里面去再定义一些context，这个 context 概念是什么呢？比如我们指定一下dockbase，比如说我们这个符合这里面加 Web 工程的一个工程目录，这里面指定一下它的路径，同时我们把它的对应的 context pass 标记一下，这样我们访问的时候，比如我们访问对应的 local house 的8080，我们这里面指定 demo 就能访问到这样一个 map 对应的服务的一些信息。同时下面这次 value 这里面注意一下，它跟这个 value 很相似，这里面我们看到定义的 class name，也就是我们的 access lock while，它就是主要是为了记录我们的这些 access log 访问日志，这里面是对应的 LOGO house access lock，这里面的 staff 以及它后缀是一个 TIC 文本格式，这是前缀。


对于它那个日志的内容，这里面我们可以看到它通过这样的通配符进行解析的一些打印，这里面也有对应的各种Y6，它的配置信息，我们有对应这些每一个通配符，它的一个内容的一个解析。进行到这里面，我们看到我们这个标签通过 host engine service Server 进行一个包围的一个操作，这里面对于 Server 的这个配置文件，我们就先介绍到这里，大家也大概了解了这里面有哪些东西，我们可以进行哪些方面的一些配置，这是我们的一个 Server 的配置，接下来我们来看一下对于 configure Web 的配置，其实我们改动的也会比较少一些。


这里面还是重点跟大家介绍一下它里面几个关键的信息。这里面我们看到对于 Web APE，下面注意这个 XMR 的根路径。我们先看最重要的一个配置，就是Threader，这里面也就是说我们的 debug Server letter，像 debug Server letter 这里面有一些隐晦的parameter，就说我们的初始化参数，对于这些初始化参数，它是一个 name 和value，也就是对应的一个 KV 的结构。


对，对于debug，这里面是0，是关闭依次开启，这里面是 listing 默认false，它是什么意思？也就是说我们在默认访问请求的过程中，我们的目录是不是展开？假如说我们访问一个目录，会不会把下面里面的每一项的一个文件全部展开？也就是当我们的一个请求访问到对应的Docs，如果说 Docs 当前没有一个欢迎页 index GSP 或 index mail，它就会把整个这个目录给我们展开，也就是把一些信息暴露出来，这就是 listing 的一个效果。


其实这里面还有其他的一些配置参数，我们可以看到这里面非常多，这里面每一个参数当然都有它对应的意义，我们可以展开去进行一些学习的一些操作，比如说这里面像有对应的 read only，如果说我们打开了 read only 的话，它操作的过程就只能接受这个 get 请求，对于像一些 i GPT 的一个命令，像 put 和 delete 它就会进行一个直接的一个reject。


像其他的一些文件，像比如 send feel size，也就是说我们在请求的过程中，会不会把文件的大小作为一个参数给我们传输出来，这里面还有一些其他的一些配置，我们你就不再一一的去介绍了，这是底票的surprise。还有第二个重要的是什么呢？就是我们的GSP，因为对于 Tom cat，它主要是提供 Thrift 访问和 GSP 操作，这里面 GSP 我们看到对应的所有的星点GSP，它都会进行一个通过 GSP 的方式解析，在这里面打开去看一下对于一个 GSP 是如何解析的，它会通过 GSP sellers 的方式进行解析。通常我们 GSP select 进行一个转换成 Java 代码，对 Java 代码进行编译，转成对应的class，这个 class 就是对应一个 serlight 做一个映射，最终会执行我们一些操作。


对于 GSP 配置操作，我们其实现在设计的比较少了，因为现在我们基于 spring 步的开发的过程中，已经不推荐我们在使用 GSP 作为我们的模板语言开发了。如果说还需要我们后端提供一些模板的话，这里面推荐大家使用像 framework 和其他的一些模板语言。


后面我们来看下面是一些配置的一些信息，这里面是SSI，那么可能很多同学对 SSI 他不太清楚，可以看到它里面对应的 SI 的描述，也就是 server side include，这是什么意思？其实如果大家知道阿帕奇或者是早期在做一些网站的过程中，会涉及到像我们的一些 header 和footer，有我们的网页的头和脚都会通过一个公共的页面 include 嵌入进来。其实汤姆开尔也支持这样一个协议，这里面需要要求就是我们对于一次 SHS t m l 这种情况的话，它可以进行通过 include 标签把一个头和一个尾引入进来。


我们如果说使用 GSP 的话，我们当然不需要用 SS 这种方式，因为 JSP 给我们提供了更多可扩展的一种方式，这是 SII 的，我们就一个简单的一个介绍，后面还有哪些像 CDI 的话，我们就不去做过多介绍了，这里面是对于这些默认的这些 Server light 的一些影射。比如说像 defailed Server light，它的 UR pattern 是我们的一个直接斜杠，也就是缀根的一个路径。其实说如果说它没有其他的 surprise 进行映射到的话，最终它都会通过底票的 surprise 映射完成。这里面对于 GSP 的话，它可以映射对应的像 GSP 后缀和 GSV 后缀的这些请求，这里面是 SS 请求，它是比如这里面指定是 SS t，m， l 这种后缀的，才触发 Server side include 这样的一个 Server side 的操作。
我们继续向后看一下这里面的一些关键的一些内容，这里面有涉及到像 LED header secret 一个filter，这个我们其实使用的是比较少，这也是 Server side include 的操作。我们跳过我们这个其实是我们比较关注比较多的，有可能我们会进行一些重写，但是我们基于分布式的这种多节点部署，我们很少在使用汤姆开的内置的绘画管理。


对于这个session，默认它的 time out 时间是 30 分钟，这里面也就是说我们在请求过程中，如果说有三四分钟不访问的话，它的会话就失效，我们再访问它可能需要我们进行一个重新的登录的操作，但基于我们现在这种微服务的结构的话，我们很少在使用汤姆开的默认的session。


会话管理通常会基于 Redis 来进行一个共享的一个会话信息，进行对我们的 cookie 或一个 token 进行存储，代表这样一个登录的状态。后面是一些 MIME 的一些 Mapping 映射，这里面是对于不同的一个后缀，我们进行一个映射的一个类型。这个前面也跟大家介绍过，这里面我们重点跟大家看一下几个，比如说像对应的XML，我们可以看到这里面是 XML 相关的一些信息，或者是我们在这里面是 h t m l。


这里面我们还能看到 Java 后缀的请求，这样基本上我们到最后我们给大家去介绍的是一个，这里面是 welcome fill list，对于 welcome 也就是我们的一个欢迎页，大家比较容易理解。当我们访问一个路径，我们并没有指定一个 URL 的时候，它会通过当前这个目录里面去查找对应的，像 index 点 s VR， index 点 STM 或 index GSP 作为我们的欢迎页，找到的第一个页面就作为我们访问的路径。所以说有时候我们在访问一个网站的时候，我们通常是只是输入域名，并没有指定这个域名下面的这个具体的页面，那么也就是说它是基于一个欢迎页的逻辑，去给我们查找一个映射的一个默认页面，这是我们可以看到对应是 Web 点 mail 里面的内容，关于他们开的核心配置的内容我们就先介绍到这里，同学们，我们下一章节再见。

