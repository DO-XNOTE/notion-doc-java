---
title: 2-3 Mybatis 缓存实现源码解析（1550）
---

# 2-3 Mybatis 缓存实现源码解析（1550）

同学们大家好，这两节我们开始学习一下买 Badcase 缓存的一些实现，这里面根据 my pattern 缓存的一级缓存，二级缓存分析一下它的源码实现。首先我们来看一下 my pattern 缓存的一个架构，在这里面我们作为一个客户端去发起请求的时候，也就是通过我们买卖的程序，首先我们会使用到开请 IQ Ator，这里面对我们构造了二级缓存的一个适配。执行的过程中，它首先会生成我们一个 cat k，基于 case k 去二级缓存里面查找对应这个 case k 对应的这些参数构造的 case k 是否已经缓存。如果说已经缓存的话，我们直接把缓存的数据获取出来返回客户，这样的话就可以大大的提高我们通过数据库查询想要的一些时长。


如果在这里面没有缓存的话，那么我们需要接着向下走，这里面是ACQ，对于 accurator 我们可以类比对应我们的 SQL session，在这一层的话，我们也会去判断一下执行的过程中会不会有一个 local cats，如果 local cats 存在的话，我们就把缓存直接返回过去，也不会去找我们 MySQL 数据库。这样的话就构建出来我们这里面的二级缓存和一级缓存。


这里面处理的过程中缓存主要就是为了提高我们向阳的一些时间，如果 local case 也没有的话，他就需要去从数据库里面查找了，从数据库外查找出对应的数据，它会同时放到 local cats，那么在这里面返回到对应的 cat a cuter，需要判断一下我们有没有去开启的二级缓存。如果我们二级缓存开启，它就会把当前的这个查询构造出一个对应的 case key，同时把这个对象也放到对应的 case 里面。


这里面要注意一下，对于我们的 ACQ ATOR，它是一个 SQL session 维度的二级缓存，它是对应我们的命名空间，也就是我们 NAMESPACE cats Mapper 所对应的一些配置，可以配置我们的二级缓存。我们要知道一级缓存我们是默认打开的，二级缓存是默认关闭的，所以我们如果说需要二级缓存的话，需要手工开启一下我们的二级缓存进行支箱。


对于一级缓存的话，我们这个 local case 的存储我们就可以理解为它只是在我们程序里面配的一个哈希map，基于哈希 map 去存取这个对象，这里面我们会想这里面 catch 的区别。首先这里面的 logo 开始我们就是纯粹的一个 Java 的原生对象，存在函形 map 里面，根据我们的 KV 结构获取到它的对象，那么所以在这里面获取的过程中，假如说我们用到的是一级缓存获取它的对象，它是一口是相等的，也就是说整个对象是同一个对象。


如果二级缓存的对于 my pass 二级缓存它它实现了特殊的台词，也就是说缓存的序列化，所以说我们通过二级缓冲获取到对象，通常它不一定是一样的，也通常也是不一样的。好，那么接下来我们了解了买 Badcase 缓存的执行情况，那我们通过代码去演示一下，看一下缓存执行的真正情况是怎样的。好，现在我们在这里面看到我们这里面写了个单元测试，就是用来 test local cats，也就是测试一下我们本地缓存。这里面要注意一下，我们在测定本地缓存的过程中，我们在这里面看一下，我们是配置了一个二级缓存，这我们在这里面去定义了一下我们的 cat enable 是false，也就是说我们现在并没有开启我们的二级缓存。同时为了给大家更好的去演示，在这里面我们通过 setting 我们的日志，也就是说会把我们自行的 SQL 打印出来，这样让我们更直白的去感受到这个查询是有没有经过我们的数据库查询。好，在这里面我们之前看一下效果，首先我们直接运行看一下我们打印日志的结果。


好，那么在这里面我们执行完成。首先我们可以看到这里面有一个对应的我们查询语句，这里面的 slack 的查询语句它只发出了一条，同时我们可以看一下在对于 user 进行比较的话，看一下我们会议的 user 和 user 2 这两个对象，我们使用等号甚至都没有一口没有等号去比较的号，看我们得了，对，下次 true 也就是我们返回值是true，这说明什么呢？说明我们当前的在查询相同的条件的过程中会使用到我们的一级缓存，对于一缓存参数条件一样，它在一定的时间间隔内可以获取到相同的一个价格对象。


现在我们来看一下买 Badcase 二级缓存。从这里面我们可以看到，对于 Mybyte 二级缓存，我们是基于开启 AQ Ator 来去实现的。虽然说我们这是 my BYTE 是二级缓存，但是说我们应该能想到，假如说我们二级缓存能命中的话，就不会使用一级缓存了。我们也可以说二级缓存它的优先级要高于一级缓存。


好，那么我们接下来去进行一个代码演示，在这里面我们可以看一下我们需要做哪些事情。首先我们要在买 badcase configure 里面，在这里面通过 setting 去设置我们的 cat enable，我们这个 value 0 false，我们改成true，那么改成 true 以后还要 true 的一件事情是什么呢？我们需要在 user Mapper 里面去定义我们的 case 信息，这里面我们简单说一下，可以看一下我们在这个 name space 为 user Mapper 的 name space 下面，我们定义了 case 的一个标签，在这里面我们指定了一下 cat 的实线是 Pre Pateo cats，那么这里面呢？我们看一下我们的 size 大小是 1024 UREK，同时这里面还有一个对于我们缓存的失效策略，这是我们用的是LRU，大家应该知道这是最近最少的回收，也就是说移除这些长时间不被使用的一些对象，同时还可以支持其他的一些对于一些缓冲回溯的策略，比如说像先进线出，这里面就是 FIFO 新进。
先出是指按照缓存进入的顺序来移除他们，假如说我们的塞子空间不够了，那么新加入一个缓存的话，就把我们最早放进去的缓存再给移除掉。同时它还会有一个soft，也就称之为软引用，也就是移除基于垃圾回收器状态的软引用进行规则的对象，也就是它类似于 GM 我们的内存回收的一个逻辑，对于这些软引用的状态进行一个回收，对应的 soft 软引用。同时还有一个 weak 就是弱引用，那么弱引用它会更积极的移除基于这个垃圾搜集器和弱引用规律的一些对象，同时下面我们可以看到它有一个刷新的时间间隔，也就是说我们缓存多久的时间会刷新一次，它默认的话不是会清空的，通常会设置一个毫秒值，这里面我们设置的值抛开对应的毫秒转换成秒的话，现在是 120 秒，你就对应两分钟，也就是说两分钟的话它的缓存就会刷新一次。


好，那么我们这里面配置完成以后，我们现在通过单元测试去运行一下，看一下我们得到的结果，现在我们自行，看一下我们执行的结果，可以看到这里面 user 得到的结果是false。在这里面这两个对象是不相等的，因为我们开启了二级缓存，对于二级缓存来说它的命中率是高于一级缓存，那么二级缓存它实现的逻辑实现一个case，也就是在这个是存储的过程中，会涉及到一个对象转化，它并不像我们这里面的一级缓存，也就是 LOGO case 里面直接把这个对象放进去，同时获取的过程直接把对象返回过来，它采用的不是这样一个逻辑。


那么好我们来这里面去分析一下，我们看一下它执行了多少次搜狗语句，我们向上点 try user Mapper test 可以看一下它执行的情况，那么在这里面我们可以看到是我们进行了 SQL 的执行，同时我们指定的这个 SQL 语句 select 对应的我们的操作，执行完成以后我们得到了一个优势对象，对于我们的优势对象字 ID 等于5，这里面我们看一下它会有一个 case hit cat 命中率的一个输出。这里面我们可以看的是 0. 5，再向下是 0. 66，也就是说后面我们可以看到它的命中率越来越高。同时我们在去比较两个对象的过程中，我们发现这两个对象它并不是相等的，但具体为什么不恰当，我们待会去研究一下。


既然我们要研究的话，我们可以通过 DEBUG 方式跟一下，跟到我们的程序对应执行二句缓轴的位置，我们可以看一下大家为什么获取的是两个不同的对象，好，那么现在我们来进行 debug 开始。好在这里面我们可以看到我们现在是直接执行到我们 defailed SQL session，在这里面我们可以跟一下跟进源码，好在这里面我可以看到它在里面去进行了一个对于我们收口操作，在这里面我们看也会去构建一个 case k，大家注意一下这个 case k，它属于一级缓存，因为当前我们第一次执行它还没有获取到我们的数据。


看一下我们之前，首先我们构建出 case k 开子k，我们可以看到它的结果是通过我们一些特殊的字符串进行一些组装，其实中间还是通过我们的冒号进行一些隔离，那么这样的话我们得到了这样一个 case k，接下来我们进行一些查询操作，跟进去我们可以看一下我们对应的这个 case 对象，可以看到这里面是我们的 case 对象，它是有值的。


好，我们继续，可以看到现在我通过我们对应的 case 里面获取我们的数据，现在我们获取到的数据以为是now，它就是说根本没有获取到我们对应的数据，现在我们没有获取到我们的历史数据，需要通过我们代理的 delegate 对应的 accurate 来进行真正的我们的数据查找。


现在我们可以根据取用来进行查找，在这里面可以看一下，可以看到这里面获取的方式是通过 base activator 里面的 local case 去获取我们的这些数据对象， local case 就是我们的 19 缓存，从这里面我们能看到这个 local case 的实现就是简单的一个哈希map，在这里面我们这个哈希曼粉去存储一些 case 对象，现在去获取一下它有没有对应的值。


可以看到获取到的 list 它依然是null，也就是当前环境下一级缓存、二级缓存都没有缓存我们的数据信息，所以没有缓存我们的数据信息。现在要做的事情也就是通过我们的 query from DATABASE，也就是通过我们的数据库里面去获取信息。


好，那么现在执行的话，获取到了我们的 list 信息获取完成以后怎么办？可以看一下获取完成以后，他需要把当前的这个信息储存到我们的 local case 里面，也就是我们对应的一级缓存。可以看到在这里面我们首先在执行的过程中得到我们 logo cat 信息以后，它会先把一些 k 的信息储存进来，同时我们可以这里面当前的 LOGO case，把 remove 原来的 object 的信息，同时我们把新获取到的历史对象的信息储存进去，也就是我们通过这样的方式把我们的 local case 进行了一个更新。


这里面我们来看一下 local cache 的实现。首先我们从这里面去看这个 local cats 的对象，大概对应的 list 可以看到 local cats，这里面我们可以看到是 prettile cats，跟我们在这里面配置的这个对象是一样的。好，那我们再回到这里面看一下这个 case 对象的内容。OK，还是回到这里面我们可以看一下这个 case 的实现，其实我们可以看到在这里面定义这个 cache 的实现，它本身就是用的一个对应的Hashmap，我们把这个 Hashmap 的信息存到 CACTS 里面，这里面有对应的 put object，也就是 key value 的一个情况。我们在获取也就是 guide object 的过程中，直接把这个对象获取过来，这样我们获取到对应的一些信息，好，那么我们停掉，我们跟到断点，那么现在我们执行下去，现在我们获取到了这个对象。


可以看到这 TCM 里面也做了一层管理，跟进去看一下这个 TCM 做的事情是什么，什么它是一个 transaction cats manager，也就是我们对于四五的一个管理，我们跟进来看一下，我们可以看到它还进行了一层四五的一个缓存，在这里面的话，在从一个四五里面把我们的这个 k value 也缓存进去，可以看到对应的这个 cat 信息。
get case 能 get 到什么样的一个情况？这里面有一个我们的纯 sex 的 case 的集合，我们从这里面获取到我们的一个 case 信息。我们现在回到我们执行的点，它基于我们的四五缓存进行缓存的一个设置。现在我们可以看到经营执行完成了，那么我们一步一步向上跳出，现在我们可以看到搜索 session template 这一层的字形。好，现在我们执行完毕，跳过断点，可以看到这里面执行到 debug 对应的 SQL session，继续再跳过这一层，可以看到现在执行到我们的 case IQ Ator 在这一层的话，它在执行的过程中其实经过了多层的一个包装。


现在执行到我们的二级缓存对应的点，我们可以看到在这里面它在获取对应的 case 的对象，因为当前的二级缓存的配置的是打开的状态，所以说我们这里面能获取到这样一个 case 对象，这个 case 对象里面的信息可以看一下当前这个二级缓存，去看这里面去判断是不是已经使用缓存了，我们看到在这里面它会发现缓存已经存在。
我们在 TCM 里面 get object 看一下得到的结果这个对象是否为null，我们可以看到它不是null，也就是说我们在第二次执行的过程中，我们发现可以从我们的 TCM 缓存里面去获取到我们对应的信息，那么现在把我们的结果返回出去，现在就得到了我执行的一个结果，我们现在执行完成。


好自强调出整个执行完成，我们可以看一下执行的结果，现在我们发现得到的对象它不是相同的一个对象，这里面对于它是不是相同的一个对象，我们要确认一下，不打开我们的二级缓存，使用一级缓存获取的对象是相同的，打开了二级缓存那么获取的对象它就不是相同的了。这里面我们再回头看一下，我们买它提示缓存实现的整个一个架构逻辑。


首先我们在执行的过程中会通过我们的开层 IQ ter 来进行 2G 缓存的查找，如果我们能从 2G 缓存获取到以后，它就不会去执行一级缓存和我们数据库相关的操作，如果说在我们的 2G 缓存没有找到对应的对象，它就会进行下一层次的执行，在这里面我们进行对于 IQ 的冲我们的 LOGO case 里面去获取网传信息，如果能获取到那么就直接返回，获取不到的话再从我们的数据库里面去查询真正的查询信息。


查询的过程中同时会把我们新查到的数据放到我们的缓存里面，对于二级缓存它执行的逻辑也是一样的，假如说没有从缓存获取到，它在执行出查找到我们的数据以后，会把当前的数据放到我们的二级缓存里面，这样就完成了数据执行的过程，所以这里面我们可以看到它会先执行二级缓存，再去执行一级缓存，二级缓存的优先级要高于一级缓存。那么关于买 Badcase 缓存实现源码解析的内容我们就先介绍到这里，同学们，我们下一章节再见。

