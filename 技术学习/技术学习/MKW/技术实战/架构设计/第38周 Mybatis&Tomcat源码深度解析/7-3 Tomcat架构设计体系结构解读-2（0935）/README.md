---
title: 7-3 Tomcat架构设计体系结构解读-2（0935）
---

# 7-3 Tomcat架构设计体系结构解读-2（0935）

那么我们来看一下对于一个默认汤派的，它的 Webinar 里面的内容是什么，可以简单的快速扫描一下，这里面我看它对于像各种信息做了一些注解性的操作，我们可以看，首先对于一个 Server light 来说，它默认的 Server letter 也就是 debug debuild。


Server letter 对应的是我们的阿帕奇的 Calendar 下面 serverless 的 debug Server letter，也就是如果说我们没有特意指定我们访问的 Server letter 默认，就到我们的 debug Server letter 这里面它配的一些信息，这里面是首先 debug 为0，没有打开是listing。 listing 是什么意思？就是说我们是否会把目录对于我们是进行可见默认是false，因为 listing 如果为 true 的话，很容易把我们的一些信息进行暴露，就是把我们整个目录可以打开，让用户访问的时候能看到，看一下还有哪些配置。


这里面我们看到对于serlight，如果我们访问 GSP 的话，默认会访问到 GSP serlight，它会进行一些操作。接着我们再看后面，应该是一些 Mint INFO 信息了，你可以看到对于我们默认的 Server light Mapping 是默认我们的字撇，执行的时候它会访问对应的底 q 的 Server light。这里面还有对于 GSP Server light，它的映射路径是像星点 GSP 或新的 GSPX 都会执行到对应 GSP 的一个 THREAD 上面。


那么接下来我们再向一下看，嗯，这里面，这里面给我们设置了很多一些案例，我们可以参照这些去进行配置。这里面像 session configure，我们的绘画过期时间，对汤姆开的绘画过期时间默认是 30 分钟，这里面我们当然可以根据我们自己的需求去调整，我们可以看一下这里面还有一些 media type，可以看到对应各种后缀的一些 type 方式，其实这里面是我们很少见的一种方式。我们可以看关注一些比较常见的，像奇瑞，这是一种压缩的那种方式。后面还有一些找一些我们比较常见熟悉的。这里面像 a PK，我们可以看到它 a PK 对应的是 Android 的一个 package 的一个压缩包，这里面对应的 a Tom，我们可以看到 a Tom 的对应的 XMO 的文件，这里面还像AVI，也就是一些 value 的一些文件，就是定义了一些 media type 的一些类型。


我们可以看到这里面定义的内容非常的多，我们需要知道一点，就是对应汤姆开的默认的waptml，其实它里面的内容跟我们应用程序比较， Web l 进行一个配置的一个MOS，达到真正我们应用程序生效的一个 Web l。还有一个值得我们去看一下，是汤凯的user，对于汤姆克的优势，我们可以理解为它是汤姆克的原生配置用户权限的一种方式。在这里面我们做了一些改造，比如说我们在这里面去访问对应的 manager APP 的过程中，看这里面提示需要去修改汤姆刊的user，修改的内容是什么呢？可以看到在这里面我们对于汤姆刊的里面增加了一个角色，这个角色也就是 manager GUI，那么只有我们的访问用户具有了 manager GUI 的访问权限才可以进行访问。我们可以看到这里面对应的是password，我们写的是他们开的，它对应的一个决策是汤姆 cat 和 manager GY，这样的话我们的汤姆 cat 用户就可以访问到对应的 manager 这个路径下面去管理我们的这些应用程序了。


好，接下来我们来看一下我们的 library 库，这里面 library 库都是一些炸包，这些炸包我们可以看到像这里面是有 EL API 和这里面的汤姆开的 API 后，还有一些L8N，就是我们的可以国际化的一些文档，这里面其实都是我们 Java 编译出来的一些信息，其实最大的也就是我们的凯琳娜，这凯琳娜内容是整个 Java 的一些核心。这里面还有一些notation，就是我们根据价格规范定义它引入的一些注解的一些API，这里面有 JSV API 等等这些信息。索拉的 API 这些我们就不再一一去看了。


后面是我们的lock，刚才我们介绍的来对于 lock 的一些日志的一个格子，这里面有 calendar lock，也有 calendar out，也就是我们的calendar，或者说配置按天更新的话，它每一天会生成一个对应的 log 文件。


当前执行的就是我们的凯琳out，这里面还有对应的 host manager，对应的一些 log 文件，这里面有 local house 的文件，这里面是 local house access log，也就是说我们通常的访问日志就在这样对应的 access log 里面，通过这里面我们可以看到我们访问的来源 IP 和访问的相应的状态码，这里面还有一些 manager log，也就是我们的管理空间的一些日志。


其实我们通常线上的话，关心的最多的孩子可能 out 和 local 的 access lock，还有一部分就是我们打印出的一些业务日志，也是我们比较关心的。后面这是 temple 文件，我们可以看到这里面是临时的一些文件，我们这个临时文件具体的内容我们就不再去关心了，下面是我们的 Web AP，这里面 Web AP 默认访问的路径是在 root 下面有我们对应的一个工程可以进行访问，对于 Web ML 里面它有 Web info，下面有对应的 Web ML 配置。


其实我们刚才看到这里面的我们访问的 root 下面的 wepro 里面的配置的一些信息，其实我们看这里面是特别简单的，那么就是这里面的信息跟我们刚才看到的这个里面的 Web ML，它的信息进行 MOS 组成了我们这个应用对应的合并的一个 Web ML 的信息集。


这是我们看到对应 Web APP 下面的内容，这里面有刚才介绍到的example，这里面还有 manager 和我们的 host manager 等等，这里面最大多的内容是一个doc，我们的 doc 文件可以从这里面可以点开去进行一个阅读和看下面这个 worker 文件。


no worker 文件我们可以展开看一下。首先我们看 worker 下面有cleaner，下面有 local house 的loghoust，也有对应的我们的这些各个应用程序的一个目录，那么也就是我们 Web APP 的目录在执行的过程中会映射到对应 low house 的下面的这个目录，里面有一份映射，那么这里面有什么区别？我们可以在这里面去简单打开我们的 root 看一下，在这里面我们有一个对应的GSP，那么如果 GSP 的话，我们可以看到它会生成一个对应的 index GSP 点Java，我们可以看到这里面有我们对应一个 GSP 的文件，那么 GSP 文件在执行的过程，它会生成 index 点 GSP Java。


基于这个 GSP Java 的话，我们可以看它其实就是一个继承了，我们这里面首先继承了我们的一个 LTP GSC base 文件，这里面还实现一个对应的一个接口，这个接口是 GSP source import 等等。最终这个 GS index js Java 它会在编译成对应的一个点 class 类，我们才可以通过服务进行访问执行。这样的话我们可以大概了解了这个汤姆开的它整个目录的一个结构，以及它每一些关键的文件放在一个什么位置。


那么接下来我们来看一下汤姆 cat 的总体架构，汤姆 cat 在运行的过程中，我们可以把汤姆 cat 的整体去进行了层层的一个划分。首先汤姆 cat 它作为一个服务，整个一个 Server 是整个我们的最外层看到的内容。基于 Server 它里面会区分服务，这个 service 就代表我们整个一个服务层的一个内容。 service 里面就区分着我们的 Connector 以及连接器，以及我们的一个引擎，我们的一个处理引擎。这里面我们看这个结果其实跟我们在他们开的 config 配置文件里面， context 配置文件很相似，这里面我们可以看到我们打开 Server 的配置文件，看到最外层是我们对应的Server，我们看在 Server 里面它存在我们对应指定的一个service，那么 service 它的名称是肯定的。在 service 里面我们可以看到它里面有 Connector 和我们的引擎，这里面我们也可以看到它的Connector，指定的我们的端口是8080，下面有对应它的处理的服务的引擎，这里面也叫Canada，并且它 debug host，也就是它默认的它的主机 host 叫 local host。


在引擎下面会再设置其他的一些内容，我们看引擎下面又是 host context 和一个 wapper 包装器，这里面我们了解了它整体的一个结构以后，我们来看一下对于我们做一个Server，它处理请求的过程，此前请求过来以后，它会通过对应的我们的一个 connect 下面的协议，这里面是 ITP 1. 1 版本的协议进行 accept 接收这个请求，那么接收请求再进行一个执行。


执行的过程，这里面会涉及到我们的一个Processor，也就是一个处理器默认我们使用 ST1. 1 版本的协议进行一个处理。处理的过程中这里面进行一个是转换一个 depth 的包装，这里面进通过一些管道处理，我们通过 in sin Y6 进行 host context 和对应的 wapper 进行一个包装的一个过程。接下来他自己在会执行到对应的 Server let，我们的 Server let 引擎， Server 引擎它通过 init 和 do service 的一些处理，可以根据我们不同的请求类型，比如它是 get 请求， post 请求来区分对应处理的transit，这里面对应的就是 do get 和 do post 的操作。处理完成以后，那么我们的响应结果就随着我们的渲染的内容返回到我们对应的浏览器端，这个我们可以理解为汤姆开的它整体的一个结构。其实到这里大家对汤姆开的总体的架构情况已经有一些基本了解。好了，同学们，我们这一章节先介绍到这里，下一章节再见。

