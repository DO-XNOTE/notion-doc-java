---
title: 11-1 手写Web服务器（2005）
---

# 11-1 手写Web服务器（2005）

同学们大家好，这章节我们开始学习它们开的照轮子的部分，手写一个 Web 服务器，它们开的本身它实现了加 Web 服务器，同时也是 serverless 容器。这里面我们照轮子的部分也是通过学习他们开的工作原理，实现一个简化版的加 Web 容器。同时我们也支持 Server 的协议扩展，这里面的 series 协议，通过我们自定义一个简化版的 series 协议，也就是扩展我们的一些请求服务。


我们这里面大概了解我们手写 Web 服务器做的一些工作内容，简单看一下手写 Web 服务器所包含的信息内容，搜写外网服务器。首先我们有一个我们的主类，也就是我们的 my Tomcat，我们通过 my Tomcat 进行音译的 start 操作来完成我们服务器的启动，他们开的服务启动完成以后，我们就要接收请求了，接收请求通过请求会包装成对应的 SP request，响应的就包装成 ITP response。这些类都是我们自定义的一些类。


对于 request response，我们可以理解为它是一个普通的抛出对象，也就是它是传输一些数据的。同时我们在初始化的过程中需要配置我们的 Server let，也就是说指定某个 URL 请求哪一个对应的serverless，对应的class。那么这里面一次像 serverless mapping，它会包括我们的 Server name，我们请求法案的URL，以及我们具体这个 Server light 它的类路径这样一个映射关系，对于 Server net Mapping，它对应的serverless。那么这里面我们看我们定义了一个 serverless 规范，即 Beats surlight 作为一个抽象的 surlight 类可以被继承。这里面我们实现了两个词线类，一个是我的 my cat 和 surprise net 以及 my dog swenet。这样我们基于这两个 Server light 进行一些实现输出，接收我们的请求输出我们的显然信息可以基于这两个 Server light 来实现我们的一些业务逻辑。当然这里面我们并没有去实现特别复杂的业务逻辑，只是做一些简单的输出。


其实我们知道，当我们 Tom cat，我们的 my Tom cat 启动完成以后，我们接收请求，每一个请求过来以后都会包装成一个 survey 的process，基于这个 serverless process 去执行一个工作线程，处理我们这个应用请求的一个过程。这样的话我们大概知道我们手写 Web 服务器它所包含的一些内容。也就是说，首先 my time cat 进行启动，启动的过程中会初始化我们的 Server net Mapping，也就是把我们 THREAD Mapping，也就是我们定义的THREAD。我们想象一下 Tom cat 它的生命周期， Tom cat 启动的过程中，它会去初始化对应的 Server 的 x mail 和 webcase mail，我们会把我们的对应的 Server type 配置在 webadmail 里面，他们看的通过解析 webadmail 去加载我们的 series 的一些配置，通常这些 webmail 里面它的配置内容也会包括对应的URL，以及我们的 serverless 会给定一个名称，这个名称也会去指定名称与具体出来的一个因素关系。


了解这一点的话，就是我们他们开始启动的过程中所要做的一些事情，主要做的事情就是解析Webtml，我们这里面手写 Tom cat 可以不需要去做得过于复杂。通过 Webtml 解析的这些star，我们可以直接通过加 2 的方式把它进行一个初始化构造。就是这里面我们还会涉及到一个 serverless Mapping 的一个映射的一个配置，这是它启动完成以后开始我们接收请求，接收请求的第一步。


其实我们这里面是通过 my tomcat 的 start 接收请求的一个过程，接收到请求以后，首先会把我们的一些请求的信息，这里面对于我们通常的一个 socket 接收的信息，也就是一个输入流，基于这个输入流进行解析，包装成对应的 it request。当然我们对输入流进行包装的过程中，我们是遵循 h t p 协议的，会获取到对应的 get master 访问的 URL 以及它的一些参数信息，基于这些信息我们进行处理，对应的这个 socket 结束完成以后，处理完也要把它的输入输出想象出去，想要它的输出也对应一个输出流。


输出理由包装成，我们在 activity response 里面进行执行，这样执行完成，对于 request 和 response 它进行一个包装，完成以后它会通过 Server 的process，这样会起一个工作线程去进行执行具体 start at 的业务逻辑的操作。


现在我们大概已经清楚了，我们要实现内容，那么我们看一下我们手写 Web 服务器它的一个代码实现。这里面我们看我们首先也定义了一个 Tom cat will，也就是我们 Tom cat 照轮子的一个功能。我们可以看到我们在实现这个工程的过程中，我们依赖了 spring boot，那么有同学会疑问，因为本身 spring boot 已经集成了Timecat，那么我们还依赖 spring boot，会不会跟我们已有的 Timecat 进行冲突？这里面告诉大家不会跟 Timecat 进行冲突的，看一下我们一代的情况，首先看一下我们的 palm 的XL，其实这里面我们构造出来的我们这个照轮子的工程，看一下我们依赖的内容并不是 spring boot Web 或 spring boot Tom cat 相关的一些信息。


这里面引入的英内容是 spring boot STARTER，通常我们做加 Web 开发的时候，我们会引入 spring boot starter 杠Web，或者说是杠g，d，b， c 等等这些信息。这里面我们引用的只是 string 部的starter，我们看还提供了一些工具类，这里面像瓜娃还有 rombook 相关的一些信息。同时我们为了打印日志，这里面引入了 spring 部的 start logging 以及 spring boot start test，这些是我们常规我们开发项目过程中通常都会引入的，唯一的区别就是避免了跟 spring boot 默认集成他们看到的冲突。


我们并没有引入 spring boot STARTER Web，我们引入的是 spring boot STARTER，这个 STARTER 作为一个启动器就足够我们基于 spring boot 它的规范来启动我们的项目。这里面整个我们使用了 spring boot 项目，那么我们就要看一下我们的入口类，我们程序的入口类是他们看的 will application。通常我们构建入口类的时候，我们会基于这个项目的名称，这里面通过 application 后缀作为结尾，我们启动的过程中会通过 string application round 方式去执行我们的程序。


这里面我们看到了我们这里面并没有去手工指定我们汤姆 cat 的启动相关的一些内容，这里面我们是不是也有跟自动装配相关的内容？为了汤姆 cat 的简化实现，我们这里面并没有使用自动装配的方式。这里面对于 my time cat 我们定义了它是一个可以被 spring 实例化的一个病，本质里也可以通过 at component 这样一个注解进行修辞。也就是说我们在汤姆开的 well application 启动的过程中，会把它实现 my time cat 实现成一个b。这会儿我们看到了它的一个执行的逻辑。我们先看一下 my time cat 它做的一些事情是什么。我们可以看到 my time cat 它实现了我们的 insulation b，看到这个接口，我们知道它在容器启动的过程中会调用对应的一个方法，这个方法内容也就是我们的 off 的 prop set，这里面我们看一下 after property set 它的实现。


对于 after property set 也就是使命，容器启动的过程中，首先执行当前方法的隐匿的操作，执行完成以后再执行我们的 start 操作，跟我们在汤姆开的启动的那个生命周期的模板方法有些类似，但是我们这里面并没有做得那么复杂，先看一下隐匿的操作执行的过程中。


首先我们可以看到这里面我们会对我们的 u r serverlight map 进行一个初始化，直接 new 了一个哈希map，我们去获取一下我们 serverless mapping 的一个configure，这个 configure 内容它跟我们 tank 的没有直接关系，它只是方便我们一个配置。


看一下 get Server net Mapping 配置的信息，这里面我们的 configure 类，我们放到了 configure 这样包，下面我们通过一个静态方法直接获取到我们 Server 的 Mapping 信息，这里面的 series Mapping 信息，我们构造了一个 series Mapping 一个集合，基于这个集合来去创建我们 Server 的 Mapping 对象。创建这个对象的过程中，我们看我们传入了几个参数，这里面的参数分别是我们的 Server let name 和URL，以及我们对应这个 Server let name 它的一个全路径的类名。可以看到这是 simple name，这是我们的映射路径，这里面我们是 my dog 和 my cat，这里面对应的我们的实例类，也就是我们看到 my dog surprise，我们获取 get name，也就是全路径的名称。在这里面我们配置了一下我们的 my dog 和 my cat，对应 third light 的一个 third at Mapping，还回到我们的 my time cat，那么进行。这里面我们初始化来我们的 URL surprise Mapping，可以理解为我们在对应的 Web ML 里面配置了一下我们的Serlight，这样程序在启动的过程就知道了我们对哪些 URL 做了一些映射。


这个初始化完成以后，我们需要构建一个线程池，这个线程池是一个工作线程池，当我们请求来了以后，我们基于工作线程池去处理我们这个请求。我们我们可以看到对于这个线程池构建的过程中我们定义的一些参数，这里面有我们的核心线程数和我们的最大线程数以及我们的 cap level。这里面我们看到还有我们的一个队列信息，这些我们都是提取出来我们的一些属性进行判断，可以支持一些配置。


这里面我们的核心线路是10，我们这里面最大限量是200，我们的支持的最大队列是1000，这个我们应该理解对于 Java 里面这个线程池的工作原理，当我们在初始化的过程中，它逐步去创建线程，达到了我们核心线程池的大小，如果请求继续过来继续堆积的话，这样的话它就会放到我们的一个主车队列里面。在主车队列里面它会随着请求的增加，也会很快达到我们主车队列的一个峰值。


达到我们的 p 介词以后，如果还要请求过来，就会创建新的限量池，这个现在限量池逐步的会达到我们的一个最大限量池数。如果我们队列已经满了，也达到了我们最大限量数，那么带来新的请求就会拒绝掉。这里面我们使用了默认的拒绝策略，默认的拒绝策略也就是直接 reject 抛一差，即将不接受初始化我们的线程池，我们把线程池输入除到我们 Tom can 的一个属性 incuter 上面，这里面我们可以看到我们的 Tom can 的 init end，也就是说初始化完成。


初始化完成以后，我们开始做的事情是什么呢？我们可以看到初始化完成以后，开始执行start，也就是启动操作。我们可以看到启动，这里面词现在相对比较简单。首先我们构造一个 Server SOCKET 对象， Server SOCKET 的对象，它是里面监听的端口号，这里面端口号也是支持配置的 Server SOCKET。启动完成以后，这里面我们会打印或日志输出 my 他们开的已启动地址 LOGO house，端口是port，端口号是指定的什么？这里面启动完成以后，我们要做的事情就是去这里面一个 well 循环去监听我们的请求。


这里面我们会监听 Server SOCKET accept，它得到信息以后会构造我们的一个 serverless process，可以看到它的 accept 信息，获取到一个就是 socket 对象，我们接收到一个 socket 的信息以后可以进行一些处理，这里面我们可以看到这里面是一个主式的操作，因为 accept 是一个主式的操作，它有请求以后获取到一个 socket 的一些信息，这个主色操作。所以说这个 word 循环它不会一直空转，大多数情况下会在主色的去接听我们的请求。


这里面我们看到它的参数是 SOCKET 和一个 UR serverless map，看一下这里面的 serverless process 是如何处理的。对于 Server 的process，我们可以看到它支持的参数，一个是 SOCKET 对象，一个是我们的 UR Server 的 Mapping 映射在这里面，首先跟我们属性进行赋值，接下来我们要做的事是什么呢？我们可以看到它是一个异步线程句子形，我们看它是继承了我们 ser 的类，这里面它的启动的过程中会执行一个 run 方法，看一下 run 方法做的事情。


大家在执行的过程中首先会把我们的线程名称打印出来，因为我们还记得在这里面我们在构建线程的时候，特意给他指定了一个线程名称，比如说我们的他们开的 worker 指定一下我们的线程名。好这样一个结构，在这里面处理的过程中，它会得到我们当前执行的线程ID，这样的话我们看到它输出我们当前执行的线程，也就是我们的一个，它并不是我们的 men 主线程，它是一个新建线程池的一个线程。在这里面我们看到它会构建我们的 s t p request 对象和 s p response 对象。可以看这里面构造的过程中它只有一个参数，这里面是 socket get input stream 以及 socket get out stream，也就是我们的输入流是request，输出流是 response 想象对象。


这两个对象构造完成以后，就进行我们的 dispatcher 操作，看一下这里面的 dispatch 操作。我们看到 dispatcher 的话会感到有一些眼熟，比如说我们在学 stream 的过程中经经常提到的 Dispatcher light，在 j to e 的 Server light 规范里面，默认它执行的也是 Dispatcher 类，会基于 Dispatcher 类去分发对应的 master 的类型，去进行do，get，do， post 等等一些操作。


这里面我们执行到 dispatcher 以后，我们要做的事情是通过 request 里面获取到我们的URL，这里面我们得到 URL 以后会从THREAD，我们的 URL THREAD mapping 里面去获取我们当前这个 sir 的类型，得到这个类型以后进行处理。


如果说这个 URL 并没有映射到一个类上，也就是没有映射到一个certain，相当于我们的请求状态是404，抛出404，我们提示未找到对应的throughlet，也就是说没有对应的相关配置。如果能正常找到我们的 class 的话，我们就需要把这个 class 通过反式的方式进行一个实例化，这里面跟大家介绍一些区别。汤姆 cat 在默认实现的过程中，这个实例化是在启动的过程中就完成了，而我们实现的过程是在请求的过程进行完成。这里面有一个区别，这里面做的是线程安全的，也就是每个请求会创建一个不同的 Server let 对象。当然线程安全已有它的代价，也就是每次请求它都会创建这个新的对象。


在我们的使用性能上会有一些区别，这里面我们获取到我们的 surprise net 以后，获取使用这个词来对应的默认的构造方法，进行一个 new instance 的方式进行私立化，得到我们的一个 Server 的对象。得到这个 Server 的对象以后，我们看到我们基于 service 的对象进行一个 service 操作，我们看一下这个 service 操作执行到哪里？这个 survey 操作默认是在我们的 base serverlight，现在已经执行到 base serverlight，它在里面注意的事情是什么呢？首先去判断一下我们 request messed，也就是说我们请求的方法是什么？如果是get，那么我们进行 do get，如果是post，那么进行 do post。我们知道 STP 的请求支持，像 get post，还有delete， put 等等这些，但是我们这里面实现并没有实现那么复杂，只实现了 get 和post。


如果你执行的是其他类型的请求，这里面我们会抛出一个 not support，也就是我们不支持这个方法，这样的话会执行对应的 do get 和 do post 方法，这里面 do get 和 do post 的是实现它具体的业务逻辑，应该有具体的业务 surprise 来实现。


这里面我们可以看一下具体的surprise，也就是我们的 my cat 和 my dog 是我们默认的扩展类型的一个serlight，这里面我们看到我们实现的内容也是比较简单的，这里面我们可以看到对应的我们的 my cat 和 my dog 是在一个 customer 包里面，也就是说这些内容是我们制定的实现跟我们实现了他们看的轮子没有一个非常紧密的关系，可以在这里面扩展我们不同的业务逻辑的Serlight，我们在扩展的过程中我们在这里面实现了 do get 和 do post 的方法。这里面的 do get 和 do post 实现业务逻辑非常简单。


首先打印银行日志，把我们的一些需要响应回去的信息进行print，那我们接下来看我们响应信息 print 输出的流程是怎样的。我们这里面看对应的我们的 response 方法， respond 方法它只有一个 output stream，这里面它做的事情也就是把我们的这个文本内容输出到我们的浏览器，这里面输出的过程我们可以看到实现的相对来说文本内容比较多一些。


首先他会去第一行是 S t P 杠幺200， OK 去标明了一下我们输入的内容，它是 HCP 1. 1 的协议，同时它想要状态是200，OK，这里面进行换行。接下来是 context type，也就是这里面去描述了一下我们的内容类型，这里面是 text i TML，同时它的支付，其次 UTIL 杠8，这里面进行换行。


接下来我们看我们打印了一个简单的标签，这个标签也就是我们这里面 html body，也就是我们的输入内容是在包装上这个 body 里面的，这里面其实并没有去做一个严格的强制要求，其实我们就输入任何内容，它都会在前面去渲染过来。我们这种方式的话机里面我们包装成一个这样一个字符串，这个字符串我们直接写入到输出流里面，写完以后我们把这个输出流进行关闭，这样的话我们这个流程就执行完成了。这个过程完成以后，我们现在来回顾一下，看一下我们整体弹幕开的照轮子的一个结构。首先我们看这里面是我们的启动类，也就是我们基于 spring Boost 的方式进行启动。另外我们的入口类启动类去触发我们 Tom cat 的创建， Tom cat 它会进行一个初始化操作，初始化的过程中会涉及到我们的 surprise Mapping 的映射，以及在映射的过程中会去构建一个线程池。


接着就是启动的过程，其实它就是在监听一个请求的过程，今天请求的过程其实它就是当请求过来以后，它就会把我们的 socket 对象和我们的 URL Server 的 Mapping 构造成一个 Server 的process，也就是我们这样一个工作线程池，那也叫工作线程。


工作线程在执行的过程中，它会去构造实例化我们具体的 Server net 这里面向我们的 my cat Server net 和 my dog servernet，这样的话，它执行完成以后，在构建 survey 的过程中会创建我们的 request response 对象，那么基于对应的业务 survey 的进行执行整个这个过程的话，最终是依赖我们 response 的一个 old out print 的方法把我们的信息输出去，这样我们整个手工照轮子的工程哎就完成了。这里面完成以后可以简单演示一下大执行的效果。


好，那么这里面我们停掉，我们重新去启动一下我们这个照轮子工程，现在开始我们执行启动，那么好，启动完成，那我们看一下前面跟 spring boot 相关的一些启动，我们就先不关注了。这里面我们可以看到对于我们的 my time cat init start，我们 my time cat init end，也就是说这会儿的话，我们程序的初始化工作和我们的启动工作已经完成，当前启动的端口号是80088，接下来去访问一下访问的URL，我们这里面应该注意到我们访问 URL 支持的是 my dog 和 my cat，我们就是 local house 8088 my cat 和 my dog 的效果。那么这里面我们看一下我们执行 my cat，我们刷新一下得到 my cat 的效果，再访问 my dog，我们可以看到它现在是切换了dog，再来切换cat， my cat，那么这两个我们都测试没问题。


那么我们访问一个不存在的，比如说我们 my cat 下划线杠一，我们访问我们看我们 404 没有找到对应的sort，这样的话我们可以发现再去验证我们这个手工写的 Web 服务器的过程中，发现它已经达到了我们的预期，可以看到这里面是我们请求过程中输入的一些信息，可以看到它在执行过程它用到的我们的线程池这样的一个线程信息。这样的话我们这个手工照轮子的写的这个手工写的 Web 服务器。就跟大家介绍到这里，同学们，我们下一章节再见。

