---
title: 3-4 Mybatis缓存应用技巧（0804）
---

# 3-4 Mybatis缓存应用技巧（0804）

你们好，这章节给大家介绍一下 my Beta 是缓存的应用技巧。缓存的内容我们讲了很多，那么这里面我们再跟大家回顾一下 my best 是一级缓存和二级缓存，那么这里面我们在通过用户客户端执行的时候，我们通过 accuator 首先执行的时候会命中对应的开请acutor，那么开请 accuter 去判断一下当前的我们这个 Mapper 对应的命名空间是否支持二期缓存。


如果二期缓存开启的话，那么支持二期缓存，那么执行的过程中我们会优先从二级缓存里面看一下对应的数据有没有，这缓存里面假如没有的话，它会执行 acute 对应我们，比如像 simple acute，它在执行的过程中因为 simple occuter 它继承了我们的 base occur，那么 base occuter 里面有对应的属性是 local cats。作为我们 Mybest 一级缓存，那么 local cats 里面不存在，那么 Q2 就会要从我们的数据库里面去获取我们的信息了。那么这样我们大概重新回顾一下 my best 一级缓存和二级缓存。那么我们来先看你一下对于缓存它的实现的内容，那么切到我们程序里面可以看到对应，嗯， i Betty 下面有一个这样的catch，这样一个包，这里面我们首先看到对应 catch 这个接口。


对于 cats 的话其实大家不陌生，我们不管是使用 my best cats 还是其他的一些 CACHE 的情况，通常 cats 我们可以获取到我们首先是对于我们缓存的隐形 put object，我们对应的 k 和 value 进行一个 catch 的一个设置，那么另一个常用的操作就是获取，我们通过我们的 k 去把我们对应的 value 内容获取过来，那么这就是通常一个 catch 的操作，那么其实跟我们对应的 map 的结构是很类似的。


我们首先把我们对象 put 进去，那么我们获取的用 get k 的方式获取工具，嗯，这里面还提供了像我们remove，也就是指定 k 的一个删除，或者是我们把整个 case clear 掉，也就是把整个清除掉，我们同时也可以获取到一个size，也就是获取一下我们缓存的这个缓存容量的大小。


下面还有我们可以获取到一个读写锁，那么基于这个读写锁进行一些操作，那么这样我们可以了解这个大概 cat 的时间，那么我们来看一下具体这个 cat 的时间内有哪些。从这里面我们可以看到 cache 的实现类很多，这里面比如说像 blocking cats FFO，也就是说先进先出的，这里面是 RRU 的cats。在我们的二级缓存默认我们使用的是 pretty 这样一个cache，这个 cache 我们可以理为永恒的，怎么理解它是永恒的？通常我们在设置 cache 的时候，因为我们的 cats 容量是有大小的，那么基于一个有限的容量，我们在做长续的过程中，我们的数据是不可控，可以离别是无限的，那我们的容量空间占满了我们怎么办？是说我们通常还是建议大家去设置一个容量，这样的话当我们的缓存达到一定介入情况，我们就可以对它进行一些移除的一些处理，这样保证我们的内存空间是可控的。


按这个 case 它的实现，我们看它存储的数据非常简洁，就是基于行情 map 进行一个存储，在里面指定一个对应的ID，那么它的size，也就是我们去获取到我们对应 Hashmap 的size。这里面我们向 put 的过程中，我们可以看到它并没有做任何限制，也就是说我们有多少东西都会 put 进去。这里面我们可以看到 get object，也就是依赖 cats 的 get k，这里面对应的 remove 也是我们映射到对应 map 的一些操作。clear，我们进行一个清空的一个处理等等。我们可以看到对于这个缓存的话，它实现的逻辑是非常简单的，也就是说它其次是依赖我们的行情办法进行一个操作。那我们可以看一下其他的一些缓存，这里面我们对于我们的先进，先出我们的 FIFO 这样一个嗯，case，我们看它的实现逻辑，通常对于这样的一些缓存，它是有大小限制的，也就是说我们的缓存大小限制死定，这里面默认是1024。那我们看一下它的 put 和 get 对应的操作，这里面对于我们的 put object 的操作，我们可以这里面有一个 cycle k list，也就是一个对应一个循环，利用一个循环的一种操作，我们可以看它做了哪些事情。


对于 SQL k list，我们这里面是首先把我们这个 k 放到我们的 k list，我们这里面是 at last，也就是把它放到最后。那么这里面我们看如果说当前我们的 k 的数量如果大于 size 乘以size，我们可以看到答案默认是 102 式，那么大于这个值的时候，它会把我们的旧的，也就是说我们的注意旧的这个 old k 把它给移除掉，那么移除掉以后，同时也把我们 delegate 对应的这个 object 移除掉，那么做完这个操作以后，那么我们要住的才去 put 我们的object，也就是说我们像我们的先进先出的这个 case 它会保证我们的缓存，也就是 case 的 size 的大小。那么其他的也就是说对于 FL cats 的话，它只有在 put object 的时候有一些特殊的区别。


那么对于其他的操作的话，我们可以看到有一点注意的是clear，那么 clear 的操作它需要对我们的 delegate 和我们的 key list 进行一些 clear 清除。所以说对于我们这个先进先出的 cats 的话，相比我们这里面的 propete 这个 cats 多了一个，也就是我们的DQ，也就是我们的 k list，通过 k list 来去维护这个线进线出 k 的列表，这也就是我们去了解这个缓存，会了解这个数据结构的一个设计。


通常现在我们在面试考算法的过程中，如果让你实现一个先进先出的一个 cats from，我们至少需要两个数据结构，一个是我们那个DQ，另一个就是我们类似也可以理为哈希map。好，这是我们先进先出的一个case。这里面我们还可以看一下 LRU 的case，那么对于 LRU cats 的话，它实现的过程也是可以这样理解，它一个在构建的过程中，它也依赖我们对应的一个 case 的delegate，同时还制定了一些 k map，那么我们可以看一下它这里面实现的逻辑有什么区别。


其实这里面在我们可以看到在设置我们这个 set list，它用到了一个 link 的哈希map，那么 LRU 通过 link 哈希 map 去简化这个 LRU 的实现。对于这个 link 的哈希map，它这里面做了一个特殊的实现，也就是我们的这里面实现了 remove at least entry 的方式，去保证我们 LRU cats 里面保留到的对象都是新鲜的对象，把我们最近不常用的对象移除掉。


好，这是我们简单的介绍了我们这里面的一个 FL 先进先出的 case 和一个 Lu 的开次。那么对于我们的开发过程怎么去使用cache，我们可以回顾一下。对于我们的二级缓存，我们需要在这里面去简单去设置一下我们缓存的开启，因为开启 enable 我们指定的值为true，因为它默认是false，所以说我们需要显示的去指定一下。
那么另外我们在 user Mapper 里面，这里面我们定义一下我们的一个 cat 标签，那么标明一下我们这个二级缓存所用的 cat 对象是什么？这里面我们可以看到我们指定了一个 prepared 的puter，一个cache，那么对于它来说，这是 my badcase 默认的一个 cache 实现，我们当然也可以通过自定义的方式换入我们自己实现的一个cacts，那么这里面我们指定一下它的大小1024，它的一个失效策略，也就是LRU，也就是说我们这里面还会涉及到一个 Flash interview，也就是我们的刷新缓存的一个控件。这里面我们可以看到是 120 秒，这里面 read any 哈是false。


通过这种方式我们可以定义出我们的一个开启的情况，那么我们完成这些操作的话，我们在执行的过程中就起会使用我们的一个二级缓存和一级缓存时，默认打开就会使用我们的二级缓存。那么关于买 Badcase 缓存的一些情况，我们就先介绍到这里，同学们，我们下一章节再见。

