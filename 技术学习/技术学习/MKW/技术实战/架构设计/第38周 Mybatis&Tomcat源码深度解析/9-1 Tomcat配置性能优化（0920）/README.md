---
title: 9-1 Tomcat配置性能优化（0920）
---

# 9-1 Tomcat配置性能优化（0920）

同学们大家好，这一章节我们开始学习汤姆开的应用技巧，那么汤姆开的应用技巧的第一部分，也就是我们这里汤姆开的配置的性能优化。那么首先我们对于汤姆开的配置性能优化，我们先针对于 Java 程序来进行一个内存优化，那么对于汤姆 cat 的内存优化，也是对于 JM 的内存优化。那接下来我们跟大家介绍一下汤姆 cat 的线程优化，我们知道汤姆 cat 的线程我们可以经过多方面的一些配置，那接下来我们来去了解一下汤姆凯特 IO 优化，那首先我们来看一下汤姆凯的内存优化，通常我们这里面对他们看的内存优化就是基于 JM 的，我们基于 JM 的一些内存优化，我们通常要配置一下加 option 这样一个参数，那么这个在哪里配置？我们这里面切换到我们汤姆开的目录，那么这里面我们看到汤姆 cat 目录，对应 bin 下面我们看到 cat liner s，在这个里面我们在这里面可以看到一些关于注释的一些信息，我们可以看到这里面自有加 option 这样一个选项，我们在这个注词下面配上我们的加 option 信息。


这里面我们通常配的对应的是 XMS 和叉Max，它是什么意思？它其实就是说我们默认的设置堆的初始化内存大小和我们的堆的最大值。其实这里面可以支持的一些参数还是很多的，都包含哪些内容，我们可以在这里面去看一下，这里面除了我们的 XMS 和XM。


x 我们可以看到比较关注比较多的是 XS s，这个 XS 它意思是什么呢？它就是说我们去设置一个线程栈，它占用内存的大小，当然这里面它不同的操作平台其默认值是不一样的。后面我们可以看到这里面有对于它的XMN，对于x，m， n 它是什么意思？它是设置我们新生代的初始值以及它的一些最大值。下面我们可以看到这里面有对应的我们的 new size。 new size 是什么意思？ new size 它就是设置我们新生代的一些初始值，这里面有对于 Max new size，我们去设置我们新生代的一个最大值。
接着看这里面是 new ratio，它是什么意思？它就是说设置老年代和年轻代的一个比例，比如说我们看这里面是8，那么它是什么意思？也就是说老年代内存比上年轻的内存它是 8: 1，那么换个概念，也就是说老年代占堆内存的是 8/ 9，那么年轻贷占对内的是 1/ 9，我们看这 survivor radio，它是什么意思？它是指我们设置新生态和存活区的一个比例，这里面主要是指两个存活区其中的一个。通常我们的这里面醇活区的值，它设置为8，也就是说我们811，也就是说我们醇活区它是一，那么新生态是8，也就是两个醇活区，也就是说 e 八，这样对应的是一个整个一个十。


接下来看一下是我们的 mini help free retail，它是什么意思？也就是说指 g c 后，如果说我们发现堆内存占到整个预估上限值的百40%，那么我们再去增大这个整个现实质，这里面还有我们的 Max Hive free till，跟上面是一样的一个道理，就是也就是 GC 后如果发现空闲堆的内存占到整个预估上限的70%，这我们去搜说这个估值上限，那么这里面分别对应的默认值就是 4 次和 7 次。


Mint space 它是表示我们这些元数据的初始化空间，那么它达到这个值的话，就触发我们的垃圾搜集，进行一些类卸载，同时 GC 会对值进行一些调整。如果说我们释放了大量的空间，就应该适当的降低这个值，如果释放了很少的空间，那么在不超过这个 Max Meta space 的 size 访问里，应该适当的提高这个值。通常这个值的话，它的变化是比较少的，因为我们在使用 string 的过程中，会使用一些动态代理生成一些加 r 类，所以说现在的话我们对这个空间的使用也是越来越大。那么我们看下面是我们的 Max map space size，也就是跟它是对应的一个最大值的一个上限，也就是我们元数据的空间的上限。那么以上这几个参数其实在我们在设置 gum 的时候，使用的是相对比较多的。


接下来我们看一下这个配置的listener，这个 listener 配置注的事情是什么呢？它是为了解决我们 GRE 的内存泄露的问题，那么如果说我们程序有内存泄露，我们内存射程多大都解决不了问题，所以说这里面他们看提供了一个listener，我们配上这个 listener 就可以解决我们内存泄露的问题。


那我们来看一下是在哪里配置，这里面是在我们的 Server 的XL，我们可以在这里面去看一下，这里面是我们的 GRE memory leak 对应的这个listener，那么这个 listener 它的意义是什么？它主要是用来解决我们内存泄露和一些锁文件。在汤姆开的初始化之前，使用系统类加载了一些类，并且设置缓存属性来达到避免内存泄漏和锁文件的一个目的。


那么对于内存泄漏，基于垃圾回收机制，如果你一个想要回收对象被另外一个生命周期很长的对象一直在引用，那么 GC 是无法回收这个垃圾对象的。还有一种内存泄漏是因为类加载器导致的， GRE 库中的某些类内存运行的时候是以单例存在，所以说我们程序启动和关闭 GRE 库，在使用上下文加载器加载，保留了上下文内加载器的一些引用，就导致了被引用的这些类加载器无法回奏。所以说他们开始在部署多个 Web 应用，使用了不同的三亚纹理加载器，那个旧的三亚纹理加载器它没有办法承受，就会导致一些内存泄露。


所以这里面我们基于这个 GRE 的内存泄露的一个鉴天器可以解决这方面的一些问题，接下来我们来看一下汤姆 cat 的线程优化的部分。 cat 接收我们请求的线程池，通常我们是基于 Connector 进行配置的，我们可以看到在这里面 connect 配置我们设置的几个关键的参数，也就是四个，我们是 Max third，也就是我们的最大线程数。这里面还有我们的一个 Mini spare third 和一个 Max spare 色的，这里面有 accept count 这几个值怎么去理解？首先我们看一下 Max 设的，它是表示我们程序里面对于处理链接的线程是最多是多少，是600，那么这里面的 Mini SPARE 和 Max SPARE 它的区别就是说通常我初始化的时候，就直接会初始化这样一个指定值的一个线程数，那么这个是什么意思？我们的 Max 斯佩尔设的是540，指它跟这个最大线程数的区别是什么呢？假如说我们的请求特别多的话，我们的请求最大可以涨到 600 个，那么随着我们比如说请求的收缩，那么如果说我们的请求量是，比如说是大于500，但是我们实际用不了这么一个情况的话，那么我们就会回收一些这样的一个线程数。


那么这里面的 accept count 它是指什么情况？它是指我们进行一些队列大小，如果说请求量还是过大的话，我们会在队列里面有这样一个排队的一个机制，那这是我们对于线程的一些优化的配置，这个大家可以根据不同的业务场景来进行一些适当的调整。接下我们来看一下汤姆 kid IO 优化，那么对于汤姆克的IO，我们知道它里面对于最原始的 Java 的BL，现在在汤姆克的酒已经被淘汰了，现在我们使用最多的是Java，其他也就是我们的同步非主式IO，那么另一个是我们的加 NL two，那么它也就可以理解为是一个异步非主持的l，也称为AL。另外我们这里面是对于 APR 也是阿尔法奇 portable 的 runtime 的这样一个 i o 模型，对于BL，对于他是他们开的最早采用的这个 l 模型，现在已经在他们的酒被淘汰了。


这里面我们就不过多介绍，那么 BL 它主要是主色词l，我们每一个请求它会需要这样一个线程池，所以说对性能开销是比较大，它不适合这个高并发的一些场景。那么对于一些它的一些优势就是它优点是比较稳定，适合连接数比较小且固定的一些架构。这里面我们先说 NIO 呢， NIO 它是同步的非主设的IO，那么 GDK 1. 4 之后把它实现成为一个新 IO 由，也就称为是 new IO。那么这个模式是基于多路复用选择系监测连接状态，通知线程处理，从而达到了一个非主视的一个目的，它比传统的 BIO 更好的支持并发性的性能。


他们开的 8. 0 以后就会默认采用了NL，这里面还AIO，就是也就是我们的加 NL two，它是异步非主持的 l 是 JDK 1. 7 以后去支持的，它与 NIO 不同的地方，但是它并不需要多路复用选择器，而是请求处理线程的执行完成以后进行一个回调周知，也就是说可以继续执行。后面的操作也是在汤姆开的 8 之后支持的，但是这个并没有作为一个默认的采用。


另外就是说对于APR， APR 它是阿巴基 portable runtime，也可以理解为是阿帕奇一个可移植的运行库，是阿帕奇 STB 服务器的一个支持库，可以简单的理解为他们开的将以 GNI 的形式调用阿帕奇 STB 服务器的核心动态链接库来处理文件，读取或一些网络操作，主要是能体现在性能上会比较好一些，但是我们使用的时候是需要进行编译安装这个 API 库的。关于他们看的配置性能优化的内容，我们就先简单介绍到这里，同学们，我们下一章节再见。

