---
title: 8-5 Tomcat源码详解容器初始化过程（3232）
---

# 8-5 Tomcat源码详解容器初始化过程（3232）

同学们大家好，这一章节我们开始学习 Tomcat 核心源码解析的第四部分， Tomcat 容器的初始化过程。 Tomcat 它作为一个 surprise 容器，可以独立部署，也可以集成到加法程序里面运行。通常我们使用 spring boot 会使用 Tom cat 嵌入式部署的方式，这里面对于独立部署或集成部署，那么最终它构建 Server 的逻辑是类似的，只是这个程序启动的入口有所不同。这里面我们会对两种不同的部署方式都来介绍一下。


首先我们来看一下独立部署方式的启动初始化流程，在这里面我们看到如果简化一下我们这里面初始化的方式，也就是我们对于他们 cat 下载解压完以后，在 being 里面我们启动对应的 startup 这个 cell 文件，我们触发启动，那么这个启动的过程其实就是触发了他们开始启动的过程，那么启动过程它做了哪些事情？其实我们在这里面启动 startup 点 SS 的话，这个里面它其实对于我们的一些变量进行一些组装，最终它做的事情是在间接的去调用对 cleaner 的 SX 这样的 cleaner 的cell，那么它做的这个过程是启动 GM 运行启动这个启动类，也就是 Boost wrapper，那么对于 Boost Wrapper 类，它这里面是有对应的问问方法，我们通过 calendar 的 SS 去启动它的过程，进行我们的 lead 装载。


那么对于 bootstrap 它住的事情是什么呢？它住的事情其实是对，后面就是初始化内加载器并实例化Cliner，那么这里面 Cliner 它做的事情，我们可以看到对于 STARTUP 和 Cliner 的SS，它都是一个启动脚本，这里面还不涉及到 Java 代码，那么基于启动脚本启动 Boost trouble 这个类触发我们 JAM 的启动。
那么在这里面对于 Boost strap 和Cliner，它都是为汤姆开的启动做准备的，那么汤姆开的标记我们可以理解为 Server 是汤姆开的启动的标记，那么这里面 boot shop 它作为启动的入口，那么 Calener 作为一个配置解析的过程，那么 Calener 主要做的是解析我们一些核心的配置文件，像这里面的 Server 的 M 2 解析的过程中，它基于我们的配子情况创建我们 Server 组件。


那么 Server 组件创建完成以后，其实 Server 组件是整个我们服务的一个包装，也就是说我们暴露给外部进行一个对于启停的一个门面的一个服务，那么基于 Server 服务它做的事情，以上是启动我们的service，也就是通过我们的服务组件启动我们的具体的服务。那么对于 Server 下面，它可能会有多个service，通常我们在用的时候，一般线上部署我们都还是一个Server，下面只有一个符。


对于 Server 和service，我们都可以理解为 Tomcat 里面的组件，那么我在组件章节介绍的过程中也提到过，那么在这里面对于 Server 和 service 下面还会有对应的连接器和容器，那么容器下面它还会包括像 context 的等等一些信息，那么这里面我们可以看到它其实通过我们一个启动脚本，那么启动脚本触发我们这个启动类的 main 方法执行启动的过程。


bootstrap，它去通过反射的方式构造出Tina，那么 Calener 在解析 Server 的过程中完成我们 Server 组件的创建，那么这里面也就是我们对于他们看的里面具体组件的内容了。其实我们从 Canada 到 Server 这个过程可以理解为我们启动的过程和主键初始化的过程的一个分水岭，那么我们通过 Server 后面的内容都处于主键初始化的过程，那么在这一部分对于我们不管是独立部署的汤姆 cat 和嵌入式基层汤姆cat，那么对于 Server 后面执行的内容可以理解为逻辑是一致的，那么这是我们初略于看到了这个他们开的启动的过程，那么我们再更细化的看一下它启动的情况。


这里面我们作为一个普通用户，这里面的用户他可能是一个任务去执行脚本，也可能是真正的用户去执行命令，这里面我们并不介意。那么相对于他们 cat 来说，对，对于一个程序去执行弹幕开的启动，那么它都称之为用户。那么用户启动脚本的话，它会执行 main 方法，执行 boot strap 这个入口类的 main 方法。在 main 方法执行的过程中，它会调用两种操作，一个是 init 操作，一个是 load 操作。其实 init 的过程是对于 Boost trouble 的init，它会调用我们 cleaner 相关的一些 load 操作。那么在这里面对于 Canina 执行的过程， Canada 得到以后它会进行对于 x Server 的 XR 的解析，完成以后会得到我们 Server 的这个私立对象，那么这里面对于 Server 进行一个 init 的操作。


Server init 的操作，其实它会像一个链丝响应，类似于核爆炸的一个影射，它会把下面各个组件进行一个初始化的一个调用，因为我们在知道在各个组件它都实现了像 left cycle 这样一个接口，它里面都包括像init， start 等等这些一些方法。


那么其实我们看 Server 启动的过程，首先它是初始化，在 init 的过程中，它会间接的去调用下service， accurator 引擎，host，contact、 Connector 以及它这些 protect handler 这样的一些初始化的过程。在这里面对于 Server 它初始化service，它会去初始化引擎，这里面在 host 和 contact 的这些对象的初始化，同时这些对象初始化完成以后，它会执行accuator，那么 acuator 执行完成以后，也就是初始化完成以后，他会看这里面对于 Connector 和一些 PORTAL Handler，也就是对于协议处理器的一些初始化操作。


我们可以看到在 init 这条线执行完成，那么它还会回到我们的 Boost trub，那么在这里面会进行一个启动的过程，这里面启动的过程我们称之为start，那 start 执行的流程跟上面 init 是很类似的，它也是间接的依赖 Tinder 的start，同时执行出 Server 的 start 也是进行一个链式的一个引用，那么他把这里面下对应的service、 occuter 等等这些信息都进行一个 star 的一个初始化的过程。


那么在这里面完成这个 star 过程的话，其实我们汤姆开的就已经启动完成，这样的话我们监听对应的端口，通常是 8. 8 两端口通过监听端口的方式等待我们请求的过来。这个我们可以看到对于独立部署的方式，它是这样一种执行的一些流程，那么其实不管是独立部署还是集成部署，那么最终对于 Server 后面它执行的过程其实可以理解为是相似的。


接下来我们来看一下他们的源码，这个通过源码来跟一下，看一下它执行的过程。首先这里面我们切到 bootstrap 它对应的方法，这里面我们关注的应该是对应的 main 方法，我们在进行主键介绍的过程中，也跟大家去做了一些引导，比如说它启用的过程，首先是通过这里面的STARTUP，那么 STARTUP 它会执行对应的 cleaner 的SS，也就是通过 cleaner 的 SS 去进行我们启动方式的执行。


这里面我们看到它启动的内容就是我们的 Boost spot 这样一个引导类，那么好我们到 bootspot 引导类里面我们看一下它的 main 方法，对于 main 方法启动的过程，我们看它执行的逻辑是比较简单的，首先是通过我们的方法直接就把我们的 Boost up 对象 new 出来以后，进行我们的隐匿的操作，我们先看它隐匿的操作的流程，这里面我们看对于布斯撒帕它里面隐匿的操作。


好，我们看隐匿的操作的一个过程，好在这里面隐匿的操作它做的事情是什么呢？我们可以看到它在这里面的去获取到一个对应的container，一个类加载器，那么获取这些 load cleaner 这个class，那么获取到以后它进行我们的实例的创建，得到我们这样的实例，他接着会做了一个类型的一个赋值，我们可以看到这里面它对于克里娜它进行了一个方法的调用。


set point class loader，那么这里面我们可以看到对于 bootsharp 的引进的方法，它注的内容并不多，那么在这里面它只是局限于在 Boost drop 里面的操作，同时对 Cliner 进行了一个方法的赋值，那么好，这样的话对于隐匿的操作的就已经完成了，那么我们现在还回到我们的没有办法。


接着向后看他的隐匿的操作完成以后，后面我们看他需要去做的事情就是我们进行一些命令操作，因为我们在启动的过程中，如果说我们是通过 start 点 SS 去执行启动的，那么这里面它的 command 是对应的start，它会执行到我们 start 方法里面，我们可以看到对于这里面的它会涉及到我们的 Diamond load 操作，我们可以看一下对于的 Diamond 它的对象也就是我们的 boot shop 对象，那么 boot shop 对象在这里面我们可以看到它的 load 操作是做什么事情。


好，那么在这里面 load 操作，我们可以看一下对应这里面的 load 操作，它会触发 continue 相关的 load 操作，那么我们从这里面可以看一下，在 load 执行的过程中，我们这里面指定了一下 Ms 的 name 是load，这里面我们可以看到它得到的 cleaner 的 demo 对象进行我们的一些操作，就是说它通过反射的方式调用了我们 calendar 对应的 load 操作好，那么对于 Calendar load 操作我们这里面请应该要切到我们的 calendar 方法里面进行一些执行了。


那么这里面我们切换到 container 的那里面，那么在这里面我们关注一下它的 load 操作做了什么事情好，那么我们可以看一下 load 操作做的哪些人，这里面是 init DIR 做初始化目录 init naming，那么我们接着看一下，这里面是，这里面对于 Catherina 做的事情是获取到 Server 进行一个隐匿的操作，那么我们这里面对应一下。
那么对于 Canina 他做的事情是我们先是 get Server，其实 get Server 的过程中，如果说 Server 不存在，他进行创建得到这个 Server 以后进行 Server 的 init 操作，那么我们可以看一下，在这里面我们先看一下 get Server 操作的类型，我们看这里面是直接去获取这个 get Server 的过程，那么因为在 Calener 它有一个对于我们刚才讲 XL 解析的过程，我们可以看到这里面是有一个 PROS Server XL，那么它在这里面启动解析的过程中其实就会得到一个对应的 Server 的一个实例。


那么在这里面得到 Server 实例以后，我们在这里面进行 Server 的隐匿的操作，那么现在我们可以看到对隐匿的操作默认是对应的到我们的 life cycle 这样一个接口下面， life cycle 接口下面它的内容特别多，默认的实现就是 life cycle base，那么 life cycle base 跟我们构造了模板方法的一个结构性，那么这里面我们要重点去对应到对于的 Server 的实现，那么 Server 实现我们从 calendar 里面去进行拟一个颜色，是对应一个接口。我们也在主键介绍的过程中，也跟大家去介绍过对 Server 它默认的一个 stand Server 的操作，这里面我们还是重点看它的 init 方法。


好，从这里面我们看它 init 操作执行的过程中是通过调用 init Intel，那么对于这里面我们看一下 Intel 它的具体的实现，还是回到我们的 stand Server 这样一个类里面，在这里面我们可以看到它在初始化的过程中，也就是执行的过程中，这里面我们看到它注册的一个 string cat 信息，这里面的是MBN，这里面的是 factory 以及 global name resource。


这里面我们会看到对应其实最关注的也就是我们的 service init 的操作，那么看到这个方法的时候，我们知道对于 Server 它已经把它的初始化的这个 GD 棒已经交到了 service 手里，那么我们可以看到在 service 层里面它做的事情相对比较复杂一些。 service 这里面要初始化对应的实体引擎也要初始化accurate，同时还要初始化Connector，这里面的内容是相对会比较多一些。那我们先看一下对于 service 它的操作，我们可以看一下它的 init internile 的操作内容。


好，这里面对于 star service 它处理的过程，首先我们判断一下我们的引擎，那么我们的 spring 引擎是否存在，如果存在的话进行以及的操作就是这里面。首先第一步要初始化我们的引擎，那么对于引擎初始化的过程中，我们可以切进来引擎，它同时也是使我们可以，对于引擎它也是个接口，那么我们可以看到默认的实现，也就是 stand 引擎，那么这里面我们看到它的内容，我们也重点关注 init it now。


好，这里面对于引擎它的引力的Email，它这里面没有实现太多内容，我们可以看到它其实在我们的 container base 里面做了一些事情，对于我们一些配置进行一些执行器的一些设置。好，那么这里面我们可以看到这里面执行到引擎后面，像 host context 的内容都是我们引擎里面的一部分，那我们再回到 service 的，我们可以看到 service 它会执行 accuator 对应的 Connector 相关的一些操作，我们回到 standard service，好，那么在这里面 Swiss 我们看首先它执行的是引擎，那么接下来会对我们的 Accuter 进行一个初始化操作，那么对于 Accuter 初始化操作完成以后，我们后面是对 Connector 初始化操作好，那么我们先来看一下对于 Accuter 它的初始化操作的内容。


那么对于 Accuter 初始化操作的内容，也就是它这里面在执行，我们可以看到也是对应lifecycle，我们还是切换到 a Creator，那么 acuator 它的实现也是标准的 standard shirt accurator，这里面我们看它的 init 操作，好，我们可以看这里面对于它的操作也是调用了我们的父类的 init internile 这样的操作，那么这里面其实它并没有做特殊的一些逻辑，那么这是acute。


那么我们来接着看一下对于 connect 它操作的内容，我们还回到service，这里面是对应的 Connector 相关的一些操作，那么 Connector 它是一个对应的类，那么对于它一个隐匿的操作可以看一下，我们可以看到对于这里面的对于 connect 的初始化操作，它会首先去判断一下 Protocol Handler，也就是我们的协议处理器是否存在，如果协议处理器不存在的话就会抛异常出去。


这里面我们可以看到它会构建一个适配器，通过这个适配器去解析协议相关的一些内容，这里面我们看对于我们协议的一个 handler 进行一些对于 SCAN Server 相关的一些信息的一些绑定。好，我们可以看到这里面主要是处理了一些跟 PORTAL Handler 相关的一些设置的一些信息，最终这里面还会去触发 PORTAL Handler 的一个初始化的操作，那么这里面我们可以看到它在 connect 操作的过程中，最终会触发我们的 PORTAL handler 相关的一些操作，那么在这里面进行了 PORTAL handler init 相关的操作，这里面 PORTAL handler init 的操作我们可以在这里面简单去看一下，这里面也是对应的，我们可以看一下这个 abstract STB 11 protect 这里面做的事情我们就会涉及的会相对来少一些了。


好，那么这里面我们把初始化的流程简单跟大家去介绍了一下，那么接下来我们来看 start 将也就是启动的流程，那么完成了初始化进行启动，那么启动的话我们的入口还是在 Boost shop，那么我们还是先切到 boot shop 里面，对于 boot shop 我们看一下 boot shop 它这是漏的操作，那么它的 start 操作，这里面 star 操作我们可以看一下，其实也是这里面先是通过 Cliner 里面得到对应的反思类进行 start 操作的执行。那么也就是说它通过 Boost shop start 执行到对应的Canadiner，那我们还是跟进来，那么看一下这里面 cleaner 的 store 的操作，那么这里面 start 操作，它首先会判断我们的 Server 是否存在，如果 Server 不存在的话，之前漏的 Server 来进行初始化，那么这里面最终它还会调用我们 Server 的 start 操作。


那么这里面会有一个如果说发生异常的话，它会进行 Server 的destroy，也就是把这个 Server 对象的进行销毁操作里面，那么我们相同的逻辑跟下去的话，这里面会到我们的对应的 Server 的 start 操作，我们还是直接切换到我们的标准的，Server，我们看一下这里面它对于 start 操作相关做的内容，这里面也就切换成我们的 start internell。


在这里面初始化的过程中，我们看到整个过程其实它也是一个间接的一个调用，这里面得到我们的 service 列表进行 service 调用的一个启动的操作。同时这里面我们启动的过程中，我们看到跟我们的隐匿的操作逻辑是一样的，它都是通过一些链丝的直接或间接的调用启动下面的这些主键的完成。


那么这些主键的内容我们就先简单介绍到这里，接下来我们看一下 spring boot 它嵌入式集成 Tom 凯尔的执行的一些逻辑。首先我们看一下对于我们在使用 spring boot 开发的时候，它如何把它们开的依赖进入的，通常对于我们一个 spring boot 的application，一个应用程序我们会依赖 spring boot start Web，也就是说我们做一个加 Web 工程，通过 string 部的开发加 Web 工程，我们会依赖 starter Web 这样一个我们的 STARTER 启动器。那么对于这个启动器的话，它是直接会间接依赖了对应的 Tomcat 的启动器，也就是 spring boot start Tomcat，对于 stream boot star Tomcat 里面它依赖了我们 Tomcat embed 是car，也就是说我们对于一个可嵌入式的一个 Tom cat 的一个炸包，那么在这个炸包里面会有一个 Tom cat 的这样一个类，那么对于嵌入式的 Tom cat，它是基于这个 Tom cat 类进行一个启动的方式。


那么这里面我们可以看到对于嵌入式汤姆cat，它通过 spring put 是怎么依赖进去的，也就是通过我们的 standard Web 间接的验证， start Tomcat 对应到 Tomcat 的一个集成的一个核心包，那么这里面去引入的过程，那么我们知道 spring boot 它主要是通过自动装配来完成我们的工作，那么它这个启动的流程是怎样的？我们可以看一下。


对于自动装配的逻辑是，我们作为一个 stream 的工程，会引入 stream 的 start Web 或者是其他的，它都会直接或间接地引入死门步的 start auto configuration，也就是说我们自动装配的方式，对于 Tom kind，它作为一个 serverless 容器，那么在 string 布的过程中它会被抽象成一个叫 serverless Web Server，也就是说我们 spring boot 它并不会直接依赖我们的Tomcat。它是通过一层抽象，也就是说 Server light Web Server，也就是说它是基于 Server light 的协议完成的 Web Server 这样一种方式进行构造我们的实力。那么对于 Server at Web Server，它的实现不止汤姆cat，它同时还包括 Jeti 等等其他的一个比较优秀的一个 Server net 容器。那么这里面我们会有 Server 的 Web Server factor auto communication，也就是通过它来进行自动装配，那么它在自动装配的过程中，它会通过我们这里面的 Server net Web Server effective configuration 来构造我们的一个对象，那么这里面会需要依赖我们的上下文环境的一些判断，那么我们需要看一下相比来说，我们在依赖的过程中我们有没有引入我们的唐木开的信息。


那么我们这里面会要判断一下 Tom cat 作为一个当前 3L 的条件是否存在，如果当前 3L 条件存在，也就会触发这里面嵌入式的 Tom cat 的自动装改的字枪，最终会构造出我们这个 Tom cat 的这样一个对象，那么这样的话就把我们的潜入式的 Tom cat 呢构造完成了。


接下来我们看一下基于 spring boot 完成的一个外部工程初始化我们汤姆 cat 的过程。这里面我们简单去跟大家介绍一下，这里面我们是一个汤姆 cat m d 的这样一个 spring 步的工程，那么基于这个工程呢？我们先简单看一下它的依赖，这里面的依赖是非常我们常见的，这里面是首先是 string boot a survip，这里面我们显示地引入了一下 string 部的starter，汤姆kid，这是我们单元词 test 类，因为这个工程我们主要是为了去验证集成唐派的，所以说它里面并没有实现过多的功能。


那么这里面我们对于一个默认的我们的启动类，对于他们看 M8D 的application，那么启动过程中这里面我们暴露出一个UL，就是我们对灯的 hello index 的请求的时候，返回对应的 hello world 这样一个内容，这里面对于我们的信息也没有去做一些特殊的配置，我们就保持 stream 部的它默认的操作类型。那么在这里面执行的过程，我们可以现在是通过 debug 的方式去启动执行我们的程序。


那么在执行之前，我们先给大家去介绍一下对于 Server net Web Server factory auto configuration 执行的过程。首先我们可以从这里面去看到这是一个，我们看到是一个 auto configuration，那么对于 auto configuration 的命名，我们都知道它通常是基于一个自动装配的，那么在这里面我们可以看一下它是在什么位置进行自动装配的，它是在我们对应的 string boot，我们 string boot auto configure 里面这里面去执行的，那么同时我们可以在 Meta INFO 对应的 spring factor 里面找到这样一个配置，我们可以从这里面看到它确实是在我们对应的 spring factor 配置的里面，我们可以看到它对应的k，这里面内容比较多。我们可以找到它对应的内容，也就是 enable auto compaxy，那么通过 enable autocom 作为k，那么对应的对象它作为 value 的这些值，它都会在启动的过程中进行自动装配。


这一点大家了解了，我们可以看到在执行的过程中，那么这里面的类就会引入进来，那么在引入执行的过程中，我们可以看到这里面会有一些这里面有 add import，那么 import 它的逻辑，它会把 import 相关的这些信息作为我们的配置，也就是对应 spring 里面的加 configuration 的配置引入进来，我看到这里面有 embedding 的 Tom cat Jetty under her。这里面也就是说它要有默认它会支持三个 Server 来的 Web Server，那么这里面我们默认的用的是 Tom cat 的，那么我们看到这里面，我们可以看到切进去，我们进去看一下，对于这个它是一个静态的一个 name 类，那么对于 name 类的话，它这里面首先会支持一些 position class。那么首先是满足哪些条件的时候，这个 MRD 的 Tom cat 才会去实例化我们care，首先它依赖了 Server net，同时它在三下文里面也就是能找到我们的Tomcat，我们可以看到能找到 Tomcat 和surprise。首先是surprise，它是说明是一个 j to e 的环境，是加 Web 开发。我们看到Tomcat，我们可以知道我们的上下文引入了我们的集成Tomcat。好，那么满足这些条件以后，那么它进行我们这个 Tomcat Server。


net Web Server 的 factory 的一个初始化，那么这里面初始化的过程，其实我们可以看到它这里面也是直接构造出我们的对象，构造完对象以后把对应的一些配置项，也就这里面我们看了咱们看的 connect customer 的一些配置项和 context customer 和一些 PORTAL handler 的customer，也就是说我们可以通过对定义一些 bin 来装载到始终容器里面。那么当我们的汤姆 cat 的这个 server 在初始化的过程中，会把它注入到我们对应的这个 factory 里面，这样初始化的功能就会对应一个生效。我们看对于这里面我们看到的是他们开的 Server LED Web Server factory。这个 factory 类里面我们重点关注的是什么？是这里面是 get Web Server，我们可以看到 get Web Server，那么看到这里面的话，我们能看到了这个 Tom cat 的这样一个对象，在这里面勾了 Tom cat 的对象，大家应该注意一下，对于 Tom cat 的对象它已经嵌入到，我们可以看到它是在汤姆开的 MID 的call，它已经归到汤姆开的核心包里面的一个内容了，那么其实也就是我们在对应的汤姆 cat Web Server factor 里面去构造了我们汤姆 cat 的对象，也就是说构造出一个嵌入式的一个弹幕开的服务。


那么这里面我们可以看到它初始化的过程中会进行一些信息的一个初始化，那么我们执行到这里，我们大概跟我们 spring put 的关系就关联下了，那么这样我们可以看到我们通过一个 string 部的应用，最终会够让我们弹幕开的这样一个对象，那么现在我们知道它是如何关联起来的，那么我们可以通过 debug 的方式去简单的去跟一下整个程序执行的过程，那么我们现在可以通过 debug 的方式启动我们的 Web 服务，那么我们启动的过程中我们的断点就是执行到这里面。


在这里面我们可以看到这是我们汤姆开的启动的过程，也就是说启动的过程中我们最终关注的是汤姆开的构建。好，我们现在跟进来，现在是构建我们汤姆开的对象，那么这里面我们跟进去看一下它执行过程中做了哪些事情。好，那么接下来我们来继续执行，这里面我们首先它会去创建一个 base DIR，也就是说创建我们的汤姆开的默认的目录，那么这里面我们可以看到，因为我们是基于 idea 进行一个 debug 方式的运行，我们可以看到它最终会创建一个Temple，也就是说创建一个临时目录。
那么我们可以看这里面我们之前看到生成临时目录的结构是怎样的？它的临时目录是对应的 VR Floader，这里面会生成一些相对来说随机的一些字符串，就我们看最后这里面有他们看的8080，也就是说这个目录是服务以他们看的 8080 端口去做一些操作的。


这里面对于把这个绝对路径附属到我们的汤姆开的 base 点m，这里面我们看它会创建一个Connector，那么得到这个 Connector 以后，这个 Connector 对应的 PORTAL 协议，我们可以看到它是 NTB 11 NIO portur，那么基于这个协议它它会把这个 connect 也会加到我们对应的 service 下面，那么我们可以看到对于 Tom cat 它本身是一个Server，那么这里面我们看到直接可以获得 get service，它并没有通过我们的 get service 点 get service 去获取。那么这里面我们看一下它是如何实现的。
其实我们跟进来如果说只是单纯看加 2 源码，我们发现它非常简单，其实他们看 get service 的过程，其实也就是说默认通过 get Server，那么在 get Server 的基础上获取到我们 service 列表里面的第一个元素，也就是说我们获取默认的 index 为 0 的，就是说这个第一个 service 元数，那么这样的话我们进行一些信息的一些赋值。那么对于这个 service 呢？有我们的 control 加到对应的这个 service 上面。


好，那么这里面会对于我们的 connect 进行一些设置，那么我们看一下 connect 会做哪些设置，简单过一下，在这里面执行的过程中，我们可以看到，首先这里面会执行我们的 get part，也就是说我们监听的端口，那么端口通常我们可以通过配置的，也就是说所有的我们可以基于 application 点儿 YML 或者 property 进行我们这些参数配置的内容。它都会通过类似于 customer 一种配置的方式，把这个值影响到我们对应的 time cat，它这些主键里面，我们可以快速的过一下这里面的一些内容。
好，我们这里面加跳过，再来指向。好，这是我们的Connector，那么我们接下来看一下，这里面是对于我们这个汤派的 get host 设置一个 auto deployed 是否自动部署？这是默认是false，同时这里面带去 configure 引擎，这里面 configure 这些引擎的过程我们可以跟进来看一下，这里面会对于不同的，我们这里面是 values 进行一些添加，我们可以看到它是一些管道符的操作逻辑，这样进行一个处理，这里面会对于我们的一个 context 的信息进行一些处理，我们可以跟进去简单看一下它的一些处理的逻辑。


这里面构建完成以后，对于 context 进行一些设置，比如说我们这里面获取到 context pass，也就是说我们在 application 的 YML 里面去设置路径的过程中，最终会通过这种方式把我们这个 context pass 去设置到对应 Tom cat 的组件上，也是这样一个逻辑，这里面会设置我们的 display name，这是我们的pass。同时这里面我看 DOC base，也就是说我们这个 Web context，它指定的默认的目录是什么？我们这里面一看，可以从这里面去看一下对应的我们的 DOC base，它的路径跟我们对应汤开的路径其实是一致的。好，我们继续看，这里面会添加一些监听，对于 life cycle 的一个listener，我们这里面去放置进去。


好，接着我们去执行准备 at local Mapping 相关的一些信息。在这里面我们可以看到，对于 Web APP loader，我们在讲他们开的它的内加载器的过程中，我们有了解到 Web APP loader，它加载的内容是对于不同的 Web 服务，它是进行一个隔离的。


好，这里面我们去看这里面我们看到它上面是有没有加一个 debug Server light，这里面是判断一下是否要加跟 GSP 相关的一些 Server light。因为在 spring boot 它在推进的过程中，在 spring boot 里面已经不推荐使用 j s p 了，所以说对于 spring boot 它默认处理的过程中并不包含 j s p 相关的一些信息。我们这里面就直接跳过，这里面会有一些静态资源的一个 life cycle 的一些监听，后面会做一些 post process context，我们看它做这里面是为空的一些信息，那么执行到这里面的话，我们就已经得到了这个汤姆开的 Web Server，那么得到汤姆开的 Web Server，我们可以看到汤姆开的 Web Server，它是通过我们这边 factory 的 get Web Server 去执行的。


那么我们看一下这上面是对应的是 Creator observer 这个方法是在什么地方执行的？我们跟进来去看一下。好，它其实是在哪？是在 on Refresh 的时候进行执行的，也就是 on Refresh 的过程中，它会去触发我们这个 Web series 的一个创建的过程。


好，那么我们跳过这个断点，现在的话我们相当于是程序的初始化过程已经完成了，那么接下来我们进行我们的汤姆开头的启动工作，那么汤姆开的启动工作我们从这里面去看一下，我们一步一步跟进来，这里面是对于我们的 connect 的进行一些预处理，这里面我们弹幕开始就获取到我们的 connect 信息。


那么看一下，如果 connect 不等于null，选择它那个是一个自动可以支持自动启动的，那么这里面会进行一个自动延迟启动的一个设置。我们跟进来看一下，在这里面我们看到首先唐不开的就获取到我们的host，并且 host 下面的所有的 children 信息，那么在这里面如果说我们判断 children 它是 Tom 开的 embedding 的 config 的一个纸类的话，它会进行我们的这个对于言辞启动的一些设置，我们可以看到这里面是其实他做的事情是什么呢？言辞启动的过程，也就是说我们可以看到这里面会 find children，那么得到这些相关的一些主键以后，进行一个 for is 的 load 操作。因为我们在介绍汤姆开的主键的过程中，它通过漏斗的方式进行一些初始化一些操作的过程，我们这里面去跳过，那么执行到这里面的话，其实我们这些组件的话，它都应该是去初始化准备的一个过程了。


好，那么现在我们可以看到它执行到这里面，它的 started 就变成true，也就是说整个他们开的它这个我们可以理解为它的这个状态变更已经完成了。好，那么在这里面我们是快速的跳过断点，那么这样的话我们这个启动过程就算完成了，那么在这里面我们可以简单的了解了对于我们集成汤姆开的它启动的过程，以及它在初始化过程处依赖的一些信息。好，那么关于汤姆 cat 它启动的内容我们就先介绍到这里，同学们，我们下一章节再见。

