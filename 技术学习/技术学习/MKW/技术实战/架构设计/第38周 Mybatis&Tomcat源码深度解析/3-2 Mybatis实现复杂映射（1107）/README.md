---
title: 3-2 Mybatis实现复杂映射（1107）
---

# 3-2 Mybatis实现复杂映射（1107）

们大家好，这一章节我们来介绍一下 my Betty 是实现复杂映射，那么这里面的复杂映射通常来说并不是说我们通常一个PORTAL，一个表的一个映射关系，可能我们会涉及到一些其他的嵌套，那么这里面我们来去了解一下会有哪些嵌套呢？首先我们对于复杂映们的定义还是基于我们的 resort map 来实现的这些复杂映射，那么因为 my 特产默认的映射优势对象还是进行一个列和属性的一个原生的一个匹配，那么匹配不上，那么值就不会附入进去。


那么如果说我们要去做一些特殊的，比如说像我们对一对象的一个嵌套，那么我们会使用到 a 侧case，另外如果说我们涉及到一对多的这种列表嵌套，我们这里面会用到collection，那么在做复杂映射之前，我们先来看一下 my better 官方对于我们的映射的定义，那么我们可以先来这里面去看一下我们这些 AQ2 映射器。


这里面对于 AQ2 映射器支持的内容会非常多，这里面会列举像cats，result，map，我们SQL，install，update， delete 和select，那么这些内容我们相对来说是比较简单的，我们就不会做过多的介绍。


我们来看一下结果映射，那么这个结果映射就会涉及到一些复杂的结果映射，这里面定义是高级的一些结果映射，那么对于高级结果映射的话，这里面会有一些信息，这里面我给大家看一下这里面，首先如果说我们想在对应的一个 blog 里面，我们包含一个作者的信息，那么通常如果说我们只需要一个作者的 ID 或名称的话，我们不需要这样复杂的映射。那么如果说我们需要把作者的一些更复杂的信息也包含进来，那么这里面我们看到 so case 这样一个注解，我是朱姐这样一个标签，你把我们的信息包含进来，这里面我们拿我们这个 block 和我们的author，也就是说我们的文章，这篇博客文章和我们作者的关系，它肯定是一个对于文章来说是一对一的。也就是说这个文章通常对应一个作者，那么对于这个文章它会有一些post，我们可以理解为评论的，对于文章指定一篇文章，它可能会有多个篇评论，所以说文章和我们的 post 评论是一个一对多的关系。这里面对于一对图的关系，我们会指定一个 claxen 进行一些数据的收集。


那么大家理解这一点的话，那么我们来开始去看我们这里面的一对一的一个嵌套和一对多的嵌套的一种实现的方式。那首先我们来看一下描述一下我们这里面我们的业务场景，在这里面我们可以看到，我们首先来看一下我们的映射对象，在映射对象里面我们这里面增加了一个PID，也就是说对于一个用户来说，我们通常会有一个他的上级，我们可以理解 point ID，对于我们部门的每一个员工他都会有上级的ID，那么这里面我们就会有一点，假如说我们在构建这个对象的时候只知道我们一个 p i d，那是不够实用的。


那么我们应该怎么去做？我们还需要去了解一下我们PI，也就是说获取张三这个对象的时候，同时把他上级的这些基本信息也获取到，这里面的基本信息我们可以包含比如它的名称等等这些信息，那么我们怎么办？我们在我们的堵门层，我们的模型映射是跟我们的 Java 对象与表结构的一个对应的关系。


如果说我们需要这样一个对象，那么通常会构建一个 v o，那么这里面我们可以看到我们这里面更有建立一个 user VO，对于这个 user VO 我们这里面列出了一个基本的信息，比如说我们当前对象的名称，那么同时我们需要一个point，那么 point 我们就用 user 来标明。另外我们还需要children，也就是说假如它有下级的一些信息的话，我们定义为children，那么就是它的即可。你确认 list 一个 user 的一个 list 的集合，那么这样的话我们构建出这个 user VO 就可以去解决我们的问题。


那么如果说我们在进行查询的过程，假如说大家对于 Hypernet 里面的一对一和一对多或者多对多了解比较清楚的话，对这里面是很容易去明白的，因为对于 happy and 它实现的一对一对多或多对一这种方式实现的过于复杂，也因为它这种会带来一些接连测库的一些查询，所以说大家对于 habitat 的采纳使用率远远不如mybadcase。
当然这并不能说明 hypertenite 不好，其实 hapnet 给我们提供了更多的功能，只是我们在使用的过程中可能这一个选择上做得不是很好。那么其实买 Badcase 在设计的过程中，它也有原来简化的 SQL 操作，也做的是越来越复杂，尤其是像 mybeta plus，它做的一些工作也就是为了让我们使用 mybeta 的过程中更像 my Hive net。


那么我们言归正传，我们直接来看一下对于 user VO 这样一个对象，我们怎么去做映射。我们可以看到对于 user Mapper 里面，这里面我们有一个对应的 user view 的一个 result map，那么对于 user view result map 我们这些关键属性，比如说像这里面属性有id， p i d 和name，那么它的映射关系跟我们在上面对应的普通的这个 user 上的 map 很相似，直接映射就可以，那么我们可以关注一下我们对应的parent，那么这里面的 part 我们可以看到它是需要我们对应的一个 user 对象。


那么我们获取 user 对象的话，我们的方式，我们可以利用我们这里面 select user by ID 的方式把它对象获取出来，那么我们可以看一下这里面是怎么去定义的，这里面我们的 Java 类型是我们对应的一个 user 对象，那么这里面还指定了一个 select 的查询操作，这里有一个 select 查询操作，我们是指定的是 select user by ID，那么这里面我们可以看到前面有这个对应 name space 的空间名称，我们需要跟我们的 statement id 进行一个绑定。


同时我们可以看到这里面查询 select user by id，它需要一个参数，那么这个参数我们这里面是对应的PID，也就是我们根据当前这一列的 PID 去查我们的user，也就是 power 的对象，同时它的 fast time，也就是说它去查询的类型是一个，我们可以理解为它是一个急切的，也就是说我们在执行的过程中马上就会把它对象去获取过来，跟它对应的是一个懒加载的方式，也就是它用到的时候再去加载。那么我们这里面是使用可以说恶害模式，就是当需要的时候直接就把它给加载过来。


那么另一个是collection，那么 collection 对应的属性是我们的children，也就是说我们获取当前用户的下级，那么获取当前用户下级我们怎么获取呢？这里面也看，我们这里面是有个 of type，也就是说我们这个列表里面的类型也是对应的user，那么我们在查找过程中指定的关联 ID 是我们的ID，也就是我们当前用户ID，那么这里面会有一个 select 查询条件，我们指定的是什么呢？是这里面是 select user by p i d，也就是说我们获取一个 user 集合，根据我们的case，我们的part，也就是上一级的 ID 获取到当前指定用户他所有团队里面一级对象的这些用户，那么就获取到了。


那么这里面我们可以看到一个相对比较复杂的一个 result map，那么来看一下对于这个旅社的 map 的一些实现。这里面我们看到我们实现了一个是 select use view by ID，其实我们可以看到这个查询非常简单，它只是一个非常简单的根据 it 去查询的一个对象，但是它执行的过程，因为指定的 user view result map，那么在执行的过程我们可以看到执行了多少个 SQL 语句，首先我们来看一下这里面的查询，这里面我们有对应的是 test select user view by ID。


那我们先执行一下，我们看一下执行的结果，好，我们执行，好，执行完成，我们来看一下这里面，首先我们去看一下我们的 user 对象，这里面我们打印出了我们的 user 对象，我们可以看到 user 对象。首先我们当前的 ID 是2，这里面有个 point user，它的 ID 2 的上一级是user， ID 是一，那么我们来再看一下其他的，这是它的普通属性，我们就不太过多关注了。我们重点看一下这里面有个车准属性，车准属性我们可以看到它里面有一个元素，这个元素是 ID 是20，也就是说我们这里面标明 ID 为 2 的，它由它的上级是对应的 ID 是一，它的下级只有一个，嗯，这里面对应的是 ID 是20，那么也就是说它能获取到的上下级。同时我们可以看到 user VO 并不是一个简单对象，它是一个相对复杂一些的一个 Java 对象。


那么来再看一下它执行的 SQL 语句，我们从这里面去看这里面首先我们在执行的过程中是我们 select PID name on cats，这是我们一个简单的一个查询，这个查询就是我们第一个就是 select view 的一个过程，这里面执行的 SQL 语句就是我们这里面 select user view by ID 所指定的 SQL 语句，那么现在我们的参数是2，我们得到了这样一个对象，那么接下来我们来看一下我们第二个，也就是 select press date 这里面也就是我们看到是也是查询一个对象，这里面我们看到是 select from t user where ID 这里面执行的内容是什么呢？也就是我们在这里面指定的获取 parent 的操作，这里面是 select user by ID 的操作，那么这样我们得到我们的 parent 对象，对应我们可以看到它的参数。


parameter 是指定的是一，这个一是从哪来的？我们可以看到这里面的 PID 是对应的一，也就是说它从我们的 PID 里面获取到这个属性作为参数进行一个查找，那么这是第二个错口语句，我们看还有第三个错口语句，我们重点看一下它的 where 条件，也就是 52 条件 PID 等于多少，那么这样 PID 等于这里面获取到一个炒作，那么对应它我们可以看到它的permeter，也就是它查询的参数是2，因为当前的用户他的 ID 是2，所以说他的 PID 也是2。


找 PID 为 2 的他的一些求准，也就是他的下属，那么这里面我们查到一条记录获取过来，这里面执行的就是我们这里面对应的CLAX，就是 select user by PID，同时我们的 com 也就是我们指定 ID 作为我们的参数，那么这样的话我们就会简单去理解，我们在构建这个 user VO 对象的时候，其实我们初源是只查询了一个对象，但是它同时执行了 3 个 SQL 语句，那么同时周过三个词口语句。也就是说第一个我们获取了对应原生对象的这些基本的属性信息。第二个词口我们获取的对应的user，这也就是 part user 信息。第三个词口我们获取到了children，也就是我们下属所有 PID 都是指定当前 ID 的这样的一些信息。那么了解到这里的话，我们可以看到相对复杂的这个 resort map，它的结构大家想必也已经比较清楚了。那么回到这里面，对于 mybadcase 实现复杂映射的内容，我们就先介绍到这里，同学们，我们下一章节再见。

