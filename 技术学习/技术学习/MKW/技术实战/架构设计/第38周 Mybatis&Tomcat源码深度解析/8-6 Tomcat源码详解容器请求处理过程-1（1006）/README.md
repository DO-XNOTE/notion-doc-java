---
title: 8-6 Tomcat源码详解容器请求处理过程-1（1006）
---

# 8-6 Tomcat源码详解容器请求处理过程-1（1006）

同学们大家好，这章节我们开始学习汤姆开的核心源码解析的第五部分，汤姆开的容器请求处理的过程。前面我们介绍了汤姆开的初始化的过程，我们知道像 Server service 和 connect 这些主键的作用，这一节我们就介绍这些主键在处理请求过程中是如何工作的。我们来先看一下对于 Server service 以及 Connector 和引容器，它组成了这样一个结构图，那么我们可以看这面是属于连接器，这面我们属于容器，对于一个服务器，我们这他们开始提供服务的过程中，它需要做两部分工作。


第一部分是建立我们的请求链接，也就是说我们通过浏览器发出请求的过程中，汤姆 cat 作为这个服务是如何接受这个请求？这个接收请求是需要一个连接器来去处理，接收到请求以后如何进行我们对于 Web Server 这一些逻辑的一些处理，像我们对于 host 的 context 和 wapper 对应，就是 supply 它的一个包装器怎样去处置它的过程？这里面我们先看连接器 collector 部分，对于 Connector 部分它是如何接受用户请求？主要是这里面依赖了一个 accept 组件，这个 SF 之间，它主要是用来监听客户端的套接字链接并接收 SOCKET 的请求。它得到 SOCKET 请求以后，它会包装成一个 sock 的 process 作为一个 command 命令，这个 command 命令它会交到这个 IQ Ator 进行一个处理。


IQ2 它是进行多线程的一个处理，这个处理的过程就开始执行这个请求的任务。执行的过程会依赖到Processor。对于 Processor 的这个组件，它主要是读取这些消息的报文，解析请求帖，请求头封装成一个 request 对象，那在这里面处理的过程中会涉及到一个Adapter，这个 Adapter 通常是 Claude Adapter。它的工作是什么？主要是为了对于 Connector 和容器层做一层转换，它生成一个 request 对象和向阳对象来传递到我们对引擎的语气里面。这里面还会涉及到 Mapper 映射的一个过程。对于 Mapper 组件，它是将请求的 URL 值和请求头的 host 值匹配一下。我们要确定我们执行对应的 host 是什么，我们的 contact 的内容是什么？以及我们需要执行的 series 请求是什么？那么经过这些匹配的话，通过 map ince 我会找到对应容器下面的这一条整个链路。


在这个音庆容器中，我们通过 pipeline 的方式进行执行。通过管道执行的过程，像 host contact wapper，它像执行一个管道的阀门一样进行处理各个领域它应该去处理的一些工作。从这里面我们可以看到这是整个我们对于他们开的处理请求的一个比较抽象的介绍，就是我们通过链接器接收请求，那么通过容器来进行处理。
请求处理的过程中涉及到的这些概念主要是对于接口的方式进行一个体现。如果说我们去想了解一下具体圆满是怎么处理的话，我们接下来去看一下，在这里面我们看一下对于请求过程中，他是他们开的实际执行的这些源码处理类的过程。首先在连接器这一层，我们这里面涉及到 NIO 的 Endpoint 及这里面的 NIO 的协议以及我们process，这里面是 colleague Adapter，它进行一门连接器到容器之间的一个转换，这是他真正处理的一些实际的这些API。待会我们先去了解这些 API 的实现过程，以及我们在通过 debug 的请求的过程中，看在什么阶段能运行到对应的这些 day 和方法上面。


对于容器这里面我们看到其实容器的处理的过程，我们首先是对应的是引擎，这里面有对应的 standard 引擎的nvilow，也就是我们对于引擎处理的这个阀门，后面对应的是 standard host，也就是我们的域名下面是 standard context 以及我们的 standard wapper。对于 wapper 我们可以理解为它就是对应一个 spread 的一个包装。
在对应 super let 处理的过程中，我们通常会依赖一个 filter chain，也就是说我们处理这个过滤器的整个链的处理完成以后，它会涉及到serlight，我们是基于 spring boot 对应的嵌入式，他们开的启动的过程中，最终我们找到的 slide 就是我们对应的 Dispatch 时代的。


这里面说到 Dispatch 时代的话，大家应该是非常熟悉了，如果整个这个请求的过程，其实说我们再介绍一下我们通过浏览器执行到我们对应 Web Mac Canker 的执行过程是怎么去处理的，其实我们再加上糖派的圆满解析的过程中，就需要把这一部分也去解析过来了，那么在这里面这是我们请求处理过程中具体的这些实际的类。
再看一下更框架性的，这里面相比上一个图会更直观一些，或者说是它的这个细节它程度会更多一些，这里面大家可以就是说用辩证的方式去看。首先我们对于 Server 是整个汤姆开的一个包装，这是汤姆开的一个服务的理念， Server 下面会具有一到多个service，下面我们区分出了 next 和我们的引擎，对于这里面也就是我们的连接器和我们的容器，这个图它应该是介绍他们现在 6 和 7 的时候的一个图，对于比如这里面是 HPP 11 的 PORTAL 和 GIO 的Endpoint，在他们开的九个版本以后这些已经不存在了。


现在其实我们依赖的默认的是这里面是 NLO in the point，那么这里面是 NL in the point，这里面对于 PORTAL 也是 NL 的 PORTAL 就是 NL 的协议。在 Endpoint 里面我们会涉及到 acceptor 和Acutor， acceptor 是接收我们的 socket 请求， acutor 是作为一个线程池把我们对应的请求进行包装，然后执行的过程。这里面会依赖的 on Processor 进行处理的过程，其实在这里面处理的过程，它会把我们的一些数据进行一些报文解析包装，转化成我们的一个 request response 响应的请求，把它传递到我们的 Adapter 里面。


Adapter 处理的过程中，我们看这里面会涉及到一个 Mapper 的去确认，因为我们在 service 里面去配置的像host， context 以及我们对应的一些信息，基于 Mapper 来确定一下我们需要选择的 host 和context。


在这里面需要选择的过程，首先我们会执行到我们的引擎引进，也就是对应的我们的肯定的引擎，这里面它会基于 pipeline 的一个管道服进行咨询一些信息。对于引擎这一层我们关注最多的一件事情，我们的引擎为了那么引擎value，它对应的也就是我们这里面填写的是 standard 引擎value，在这里面的管道符里面其实它还可以加一些其他的below，这里面我们不会太多的关心。那么接下来我们来看一下host，对于 host pipeline 的话，它执行的我们比较关注度更高的也是 host Valve，这里面也就是我们的 standhost value， context 也是同理，这里面我们关注度比较高的是对应的 stand context value，那么 wrapper 它也就是对应 pipeline 里面引起一个 wiper value，这里面也是 stand wiper value。


对于 stand wiper value，它处理的过程主要是依赖了一个 filter team 里面说到 filter chain，它因为属于我们对应的 Java 的 API 里面的一部分，所以说我们对 filter chat 是比较熟悉的，对于汤开的里面，它默认的一个实现叫 application filter chain，它会把我们的过滤器和我们 Server add 包装起来，包装成一个 application filter chain 过程。那么在执行我们对应的 filter 的过程中，执行完成以后，那么最终会执行我们对应的 surlight 的过程。默认的我们对于 string MC 的包装，我们的 surlight 只有一个是 discussion surlight，那么我们这里面看到了大概的我们他们开的语言码解析的一个过程，接下来我们通过代码来去分析一下它执行的过程。


在这里面我们先看一下对于我们的汤姆克 M8D 的就是我们嵌入式汤姆克的发射，这里面定义了一个 hello Controller，通过我们的 Mapping 设置的话，我们知道当我们访问 hello index 的这个 URL 的过程中，它会请求到这样一个方法里面，请求到我们的 control 之前，也就是说整个这些处理的过程通过一些断点来进行，看一下它处理的一些情况。


好，那么这是我们的情况，现在通过 debug 的方式进行启动，这里面我们启动启动的过程，我们在这里面看一下通过浏览器去发出一个请求的过程，现在我们可以看到现在已经他们开始出一个启动的过程中，因为程序里面有一些断点，我们可以看一下把断点去跳过这个断点。


我们次在 get Web Server，也就是说程序启动的过程中如何去构造我们的 Web Server，这里面构调的过程中，在初始化的过程也跟大家去介绍。通过初始化一个汤姆cat， Tom cat 里面我们设置一下我们的 base DIR，我们的Connector，即把我们的 Connector 组装到我们对应的 service 下面。接着我们对于 host 的一些设置，以及对于我们的引擎相关的一些配置，这样就完成我们对于弹幕开的相关的一操作。


这里面其实比较简，在我们在初始化的过程也知道初始化的整个流程还是比较复杂的，现在我们是跳过这个断点好，那么跳过这个断点，这个断点我们也是跳过断点以后，这里面就看到我们可以看到这是对应的我们的 LN 的point，初始化的过程它需要去绑定一下我们的 Server SOCKET，现在我们知道这行为还是处于一个初始化的过程。这里面是我们在对于初始化，对于 NLN 的point，我们在初始化的章节没有去详细介绍到过程，这里面钻点去跟大家适当的补充一下。


这是其实我们在构建我们 ALOPTND point 的过程中，会涉及到构建一个对应的power，就是一个轮询器，因为我们知道在处理请求的过程中，我们现在不会对每个请求创建一个线程，我们基于一个人群的方式去观察对应的请求有没有数据响应过来。

