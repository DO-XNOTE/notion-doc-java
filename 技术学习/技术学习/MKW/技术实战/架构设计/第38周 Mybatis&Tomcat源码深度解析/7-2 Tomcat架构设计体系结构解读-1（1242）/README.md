---
title: 7-2 Tomcat架构设计体系结构解读-1（1242）
---

# 7-2 Tomcat架构设计体系结构解读-1（1242）

同学们大家好，这一章节我们来开始介绍一下汤姆 cat 架构设计体系的结构进行解读，这里面我们也分三部分跟大家去介绍。首先大家认识一下汤姆开的服务器，其实我们去了解一下汤姆开的目录结构之后，我们来看一下汤姆开的总体的架构。先来看一下汤姆开的服务器，对于汤姆开的服务器可以称之为是 it 服务器，也可以称之为是 Server light 容器。对于 HTP 服务器的话，它可以提供像html，我们的ZSP，或者说图片或CS， GS 的这些样式的一些提供服务。


作为一个 HT 服务器的话，它在启动的过程中是基于 survelight 进行一个引起的操作，操作的过程中，在请求的过程中，它会构建对应的是 survey request 的对象和 survey response 对象。 request 的对象接受我们所请求的参数。 response 对象作为我们返回响应的内容的一个包装对象。


接下来我们来看一下对于一个 server letter 容器的，对于它来说，它可以独立部署，也可以潜入式的继承部署。那么独立部署我们这里面又称之为显示部署和隐私部署。对于显示部署是需要我们手工去配置，我们在 Server 在 ML 里面去添加 context 元素，指定一个pass，那么指定一下它对应的 Web 的一个服务目录，我们就可以通过弹幕开始去访问。


另一种方式我们是经过隐私部署。隐私部署其实非常简单，在早期的话，我们用这种部署方式会更多一些，也就是说我们直接把一个挖包或整个应用程序复制到我们的 Tom cat Web APP 目录里面。对于是一个挖包目录的话，我们复制进去以后，它会进行一个自动的解压，解压的过程，解压完成以后把外包里面的内容解压出来，它符合一个加外包目录的标准结构，就可以启动运行。也或者是我们直接把这个挖包解压后的内容，就是整个应用目录直接拷贝到汤姆开的 Web 目录下面，也是可以正常执行的。


另一种方式就是嵌入式集成的，现在应该 spring boot 大家都使用的越来越多，因为 spring boot 给我们提供了一个嵌入式集成塔姆盖的运行的一种方式，尤其是我们现在在微服的环境下，基于 Docker 进行一些运行的过程中，通常会生成一个炸包，这个炸包里面会集成了一个嵌入式的 Tom cat，这样来让大家去使用起来可以更方便。


首先我们来可以看一下汤姆 cat 的形状，可能大家在使用汤姆 case 的过程中很少关注汤姆 case 本身它的效果，其实启动他们开展以后，我们在这里面可以看到对应的阿巴奇他们开的它的一个版本号，这里面我们可以看到各种对应的链接， document configuration， example 和 wiki 及我们的邮件列表。


在这里面我们看到这是一个默认的访问的页面，像这里面的提示，这里面提示如果你看到这个页面的话，是我们当前汤姆开的已经安装成功了，这里面每一个操作都是我们对于他们开的默认提供的一些功能，可以看到这里面有 Server 的一个运行状态，点进去我们可以看到这里面我们能看到一些信息。


我们看到对应的是 local house 的 8080 manager 个states，也就是我们管理的一些信息，我们可以看到这里面的一些运行状态的信息。好，那么我们再看这里面有我们一个 manager APP，那么我们可以看到 manager APP 里面看到的是什么呢？对于我们当前 Tom cat 部署了哪些Web，这里面我们可以看到首先是默认的我们的Web， welcome Tom cat，另外我们的 dog 还有我们的example，这里面的 host manager，这个 manager 目录等等，我们可以看到这里面其实我们可以通过选择一个外包上传的方式进行我们的一些外包程序的部署，但是我们其实很少这样直接使用，大多数每个汤开的里面只会部署一个外包APP，直接把它通过脚本的方式把它放到目录里面进行启动。


我们的Tomcat，同样这是我们的一个执行的方式，我们也看到了默认安装完Tomcat，这里面会部署了一些应用程序，这里面有 doc example，这里面我们可以看到通过文档提示推荐我们去阅读的一些文档信息。这里面是如何管理我们的应用程序，我们对于一些安全性质的一些考虑，或者是一些关于我们的 Clacson session，也就是如何进行我们的会话复制一种方式。后面我们可以看到这里面当我们默认安装完成以后，其实进行 manager APP 进行管理的过程中，是会提示我们没有权限，这里面会告诉我们如何去配置。我们在这个对应的 cleaner home，也就是Tomcat，在 home 目录里面我们对应的 configure Tomcat， user xmail 里面进行一些编辑操作，也就是加上一个对应的访问用户的角色，那么就可以进行操作了。


这里面也是对于我们的一些谈不开的酒，对应的一些document，这里面我们可以看到谈不开的它的一些叫doc，它的一个bug，这个类似也就是一些问题的反馈，这里面对应的 git reposure 以及 git 库提供的也是在 git Hub 上面，这是我们可以看到对应这个汤姆 cat 默认的一个页面。


这里面我们可以去访问一下它的一些comment，我们可以看到访问 document 的信息，给我们提供了一些 user guide，就是常用的一些信息，大家可以去读一下，这是它的一些文档信息。另外这里面还有一些example，我们可以在这里面贴个example，这里面提供了非常简单的一个image，这是 surprise example，我们可以基于 select example 进行一些执行。


看一下这个操作一个过程，比如说我们之前一个 IQ Ator，这里面得到了你好世界这样一个简单的操作，这里面我们可以看到对应的这个 hello world，它的一个源码信息，我们可以看到如果说它的内容就在这里面，我们简单的去打印输出一下，可以看到这里面我们是实现了一个surlight。


hello world，注意一个 surlight 来进行一个内容的输出，这是我们看到的这 document 的信息的内容。好，我们退回，这里面还有一些其他的信息，比如像 request info，request，head，request，parameter， cookie session。它给我们提供一个 demo 让我们进行了解。现在我们知道这个 Tom cat 默认部署完成以后它的一个效果，那么我们回到 PPT 继续这里面我们了解了汤姆 cat 部署完成的效果。


接下来看一下汤姆 cat 的目录结构，对于汤姆cat，它作为一个价，外部它的启动过程是依赖 DDK 的，它在官网我们可以下载对应的一个唐木开的 GPT 包，那么进行解压就可以启动运行了。对于这个目录里面首先是一个 b 目录，对于通常的我们的应用服务器，它都会有一个 b 目录，这个 b 目录里面存放一些启动器的命令，通常它会有我们对应的点 BAT 结尾的和点 SS 结尾的。点BAT，它是 Windows 下面启动的文件，点 SS 是 Lynx 和 Mac 下面的一些启动文件。同时这里面有对应的 config 目录，它是用来存放汤姆 cat 的一些配置文件，我们比较常用的是像 Server x l Web 的 ML 和一些 context ML，当然还会有一些权限配置的文件。下面是 library library 它是存放汤块的，医疗的一些炸包通常是以 Java 的二进制的方式进行一个存放。


后面我们看log，那对于他们看来执行过程中它都会产生大量的一个 log 文件，那么 log 文件是都会存在我们的log。当然我们也可以去通过配置指定一下日志存放的目录，那对于lock，它产生的日志文件是比较多的，这里面比较常见的。我们对于自 client 的 all 的日志，主要是记录他们开的启动的一些信息，还涉及到像我们的 access log，也就是说我们通过部署完成以后进行访问记录的一些信息。这里面还会涉及到像 local house 的日志，它主要是存放应用初始化那些没有处理的异常的一些信息输出，同时也包含刚开的启动和暂停的一些运行日志。还会涉及到一些像对应的 host manager 信息，主要存放汤姆派的指代的一些 manager 项目的一些日志信息。


其实对于我们来说，我们关心最多的还是对于 Claude 日志和我们的 LOGO house 以及我们对应的 LOGO house access 日志。 access 机对于我们作为一个 Web 服务器的观察来说是非常重要的，它会记录这个每一个请求的访问，请求的时间，它的资源和一些状态码的一些信息。那么下面这个 temple 文件它是很容易理解，它会存放一些临时产生的一些文件，就是我们在过程中产生的一些上传下载，通常会在 Temple 文件生成一个临时文件进行一个存储。


下面是 Web APP， Web APP 是我们在跟唐姆开的接触最多的一个目录了，部署的程序通常都会在 Web APP 下面，我们会在 APP 看一下我们的这些程序部署的是否成功等等这些信息。下面是 worker 目录，它是存放唐开的运行词编译后的一些文件，比如说我们把一个 JSP 文件放到汤姆派的对应的 Web AP 下面以后，它会首先把 JSP 编译成一个点 Java 文件，对点 Java 文件，再进行一个 Java 的编译成一个点 cast 文件，它会进行一个正常的一些运行。


这里面看到了这个唐开的目录结构，其实看到这些名称的话，还是相对来说比较常规的。对于我们的必应 config 力，我们的 lab 库，我们的logs、temp、 Web AP 和worker，大多数我们的一些应用软件面都会包括这些目录。当然 worker 可能会比较少一些，因为 worker 作为一个再次编译后的文件，它确实是相对比较特殊一些。
那么这里面我们切换到我们的汤姆开的目录里面，在这里面为了给大家展示的比较方便，我把这个汤姆开的也导入到 idea 里面，我们在这个结构里面去看它的内容。首先我们看到的内容是什么呢？是这里面的是b，那么在 BN 里面我们可以看到它里面是各种不同的对应的一个启动器的文件。首先像凯特琳娜BAT，那么 BAT 它是 Windows 进行执行的肯定的SH，它是 Windows 启动的一种方式。通常我们获取到这个应用程序以后，我们可以看到都会找到它对应的 start APP，这就是它的启动操作startup，它对应的也是我们 Windows 的和，嗯，对应的是 Linux 版本的一些启动，这里面还有对应的 set down，也就是关闭。这里面还涉及到像我们的 set class pass，如果大家对于这些 link 脚本比较感兴趣的话。


好，我们可以点进去简单看一下，在这里面我们看到它主要是查找一下我们对应的加 home 或者 GRE home 的目录，找到我们执行的对应的加滑程序，那么这是我们对应的毕业目录下面的内容。那么接下来我们来看一下 config 目录，对于 config 目录下面我们可以看到这里面其实我们最常见的像 context 点XL，我们指定对应的我们的目录 Web INFO， Web ML，这里面是他们开的对应的Web。还有这里面其实我们更容易理解的是我们的 Server 点XL，打开 Server 的 ML 里面，这里面的内容非常多，同时我们看到对应这个 Server 它的一个指定一些 port set，这是对应 set down，也就是关闭对应的暴露的一些端口，后面还有一些其他，这里面我们像 service 指定的是凯特琳娜，这里面像 collector 是8080，这个是我们关注最多的。


我看下面其他的一些配置，这里面还会有一些host，我们可以默认它是对应是 local house 的指定的 APP base，有 Web APP，这里面是 unpack 腕，我们可以看到它是既自动解压，我们的腕包是默认是处式，也就是达到一个 auto deploy，也就是自动部署的一个过程。


OK，下面我们可以看到这里面会设置的一些日志，我们可以看到对应的 access log 日志的过程，这里面配置的是 access log value，对应的是director，也就是它打印的目录，也就是对应的locks，这里面我们看它是在我们的 locks 下面，这里面会有一个对应我们这些日志的前缀，是 LOGO house access log。
它的后缀是点text，也就是说我们指定一下它的文件后缀，其实我们可以对应它的格式进行了一些修辞，比如说它的这个partner，也就是它的模式是怎样的？看到这里面有杠h、杠L、U、T，它分别对应的什么意思，大家可以通过文档去查找一下。


好，我们看到的是 Server teml，那么这里面还有一个wireteml。大家对于 Web 的 mail 在做 Java Web 工程的时候，早期是非常熟悉的，因为我们在做 Java Web 工程的时候，对应 Web INFO 下面的这个 Web 的mail，它是加 Web 的一个标准规范，随着 Zara light 3. 0 以后，对于这个 Web 的 mail 它已经不再做一个强制要求了。现那我们都可以通过 Java 的注解配置方式把这些实现。

