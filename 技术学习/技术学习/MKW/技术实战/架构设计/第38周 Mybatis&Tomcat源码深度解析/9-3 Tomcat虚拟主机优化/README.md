---
title: 9-3 Tomcat虚拟主机优化
---

# 9-3 Tomcat虚拟主机优化

同学们大家好，这章节我们开始学习 Tom 开的应用技巧的第三部分， Tom 凯的虚拟主机的优化。那么我们了解虚拟主机的优化呢？首先我们要知道虚拟主机是如何进行配置的，另一方面我们基于虚拟主义的配置来去学习虚拟主机的优化。为什么需要虚拟主机的配置？其实是这样，通常在一些大厂的话，我们很少在用到虚拟机的这种方式，其实有一些小厂它是这样，就是说我们在部署它们 cat 的过程中，它可能没有对这个实体系进行一些拆分，比如说我们进行一些虚拟化。那么对于一个 Tomcat 部署在一个实例上，整体资源实际有浪费的。


那么也有同学说，为什么我们不在一个机器上部署多个汤姆开的，而使用虚拟机，这里面会有一个类似这样，就比如说我们把它虚拟成多个虚拟机的话，它都会有一些资源的占用，比如说我们起一个汤姆 cat 和起两个汤姆 cat 对资源的利用率是不一样的。如果说我们能在一个 Tom 开在里面部署虚拟主机的话，它会对于这些内存的利用率会更好一些，当然它也会给我们带来一些运维上的一些问题。
通常对于大公司很少在使用这种虚拟机配置的方式，那么如果说我们真的有虚拟机配置的这种方式，我们应该如何基于他们开的配置这个虚拟机？通常的一些业务场景是我们在一个服务器上部署一个他们开的不愿意再添加更多的，他们开的去部署不同的实例，而我们只是通过虚拟机的方式去扩展不同的功能，那么这里面我们配置虚拟机的方式可以跟大家简单介绍一下。


那么对于虚拟主机的配置，我们可以看到我们在 Server 的 Xmail 里面添加对应的 host 标签就可以。那么如果有同学要问，那么什么是虚拟主机？我们对虚拟主机下一个概念，其实这也是比较模糊，通常我们这样理解，我们在一台物理机上搭建多个 Web 站点，那么每个 Web 站点它都可以独立运行，互不干扰，那么这些站点我们就可以称之为虚拟主机。


那么对于虚拟主机的实现方式 Tom cat，这里面是我们对于多个域名解析到同一个 IP 地址，在我们的 Web 服务器里面添加多个站点，每个站点设定一个主机名，也就是一个 host 或者是我们的一个域名，那么 ITP 协议请求里面是包含了我们的主机名信息，当 Web 服务搜到访问请求的时候，就可以根据不同的主机名来访问不同的网站。


那这里面我们看一下基于汤姆 cat 的虚拟主题，我们如何去设置，我们可以从这里面切到我们汤姆 cat 的配置，我们看在对应的我们这个汤姆开的目录里面，我们可以看到这里面是 config 信息，在这里面我们如果是需要配置我们的虚拟主机的话，我们需要打开我们的 Server dicner 配置，通常我们在 Server doc 那默认有一个host，它的 name 对应的是 local host。


其实如果说我们不去区分虚拟主机的话，对于这个 name 的它的作用并不是很强，如果说我们要去做我们的域名区分的话，这个 name 就比较重要了，我们必须指定特定的一些信息，那么靠这里面我们看对于 host 里面它的信息，我们关掉我们的locals。


我们来看一下我们这里面配置的两个虚拟主机，这里面我们配的虚拟主机对应两个，一个是 user my APP . com，一个是 product my APP . com，我们可以理解为这是我们的一个网站对应，这是我们用户中心的资质域名，这是我们的商品信息的一个子域名，那么在这里面我们对于这两个不同的模块，我们虽然是单独部署在一个汤姆 head 里面，它为作为不同的站点来独立运行。那么这里面我们看一下它的一个参数。


对于host，这里面我们看，首先这里面有 APP base，也就是说我们的应用它所处的它的录音是怎样的？这里面有个 unpack 的 was 是否默认打开解压我们的挖包，这里面默认是true，这里面还有 auto deploy，就是是否自动部署也是默认是true。也有 XR validation，也就是是否对我们的配置信息进行校验，这里面是false，在里面对于 XR name is where more 也是false，那么这是我们的 host 的一些配置，那么完成了 host 的配置，我们看一下 context 内容的配置。


那么对于 context 的配置，我们看这里面是 doc base，这里面 doc base 和 APP base 我们要有一些区分，这里面 doc base 我们可以理解为我们这个站点的相关主要信息的内容，这里面我们可以看到对应的我们也是 user logo，他们开的 Web APP 的user，也就是说我们关于这个 host 的主机，这个主机的这些网站站点的信息是部署在对应的一个 user 目录。同时这里面我们可以看到它有 pass 路径，也有 reload able，也就是是否可以重新加载pass，我们并没有指定一个特殊的目录，也就是说当我们在访问 user 的 APP . com 这个域名的过程中，不需要添加一个特殊的Paas，就可以直接访问到我们对应 web A p s 下面 user 这个目录的这个 Java 站点，这个加 Web 站点这里面的 dockbase 它并不要求我们跟 APP base 有一个什么相关的关系，我们这里面可以指定任意一个绝对有权限的一个目录就可以。


那么我们了解了 user mypv . com 它的配置，我们再看一下 product mypv . com，那么对于一个这样两个不同的站点，我们这个玉米儿之间它还是有一定关系，但其实这个关系其实非必然的，其实我们可以完全部署两个风马牛不相机的站点，那么这里面我们可以看到对应的它的信息，这里面也是汤凯 VP 我们这里面对应的整个它的区别， v 的区别也就是我们的 doc base 它存储的目录是什么？是product，所以这样的话我们基于这种方式去配置，同时我们需要对应我们的域名映射到对应的服务器的IP，那我们访问 product my APP . com 的时候，会访问到对应 protect 下面信息的这个加 web 的目录，下面，这里面对于我们的数据进行处理，那么这样我们就知道了我们对于一个通过汤姆看的配置虚拟主机的一些关键点是哪些，那么这里面是 context 标签，那么 context 标签里面的内容我们可以基于这里面对于我们的 logo host 下面对于它在配置的一些信息的内容。


进行一些我们制定的一些扩展，那么现在我们已经了解了基于他们看的如何进行配置虚拟主机，那么配置完虚拟主机我们再想一下如何进行一些优化，那么我们来看一下如何进行虚拟主机的优化。那么对于虚拟主机的优化，通常我们是关心他们开的启动速度，我们这样去想，一个他们开的里面部署一个应用和两个应用，它启动的速度肯定是不一样的，因为如果说部署一个应用，它当一个应用启动完成以后，我们服务就可以对外提供服务了。那么如果是多个应用的话，它启动消耗的时间会增加，我们对于他们看的启动速度进行优化的时候，我们通常会进行哪些操作？我们会删除那些没有必要的一些 Web 应用，比如说默认带的一些 sample 或一些等等一个 manager 相关的信息，如果我们不需要的，我们可以把它清理掉。这里面我们可以在这里面去看到，对于我们部署他们开始的过程中，我们只关心我们所使用的目录。
那么在 Web AP 下面，我们看到这里面这几个像Docx、example、host， manager 和manager，它都属于一个加 Web 应用，也就是说我们在启动他们开的过程中，其实相当于同时部署了 12345 个 Java Web 应用。


那么这几个 Java Web 应用都是我们关心的吗？其实并不一定，那么如果说我们确定我们对于像 doc example 这是我们不需要的，那么我们可以把它给清理掉，那么清理掉这些也就减少我们汤姆开的其中的负担，所以说这样它的速度肯定也会快一些，这是一份方式。


另外还有是我们在他们在启动过程，我们可以对于一些不关注的一些特性，比如像 Web SOCKET，那么假如说我们对于 Web SOCKET 我们不需要的话，那么对于这个汤块的下面相关的跟 Web SOCKET 相关的一些炸包，我们也可以把它清除掉，比如这里面涉及到像汤姆开的 OBS OK 的这里面还有没有超开API，那我们把这些炸包移除以后也可以减少对于 JAM 启动过程中对于这些炸包，也就是 class loader 查找的一些时间。


另外因为我们现在的使用汤开的，现在由于使用部的原因也好，随着时代的发展也好，所以我们现在使用 GSP 的功能会非常少，那么如果说 GSP 功能我们不太使用的话，那么跟 GSC 相关的这些大包我们也可以清理掉。那么第一步我们可以基于对于没必要的这些功能依赖把它给删除，可以提高我们的一些启用速度。


另一部分我们来对于配他们开的配置项的优化，我们在前面也介绍了一些他们开的性能优化的一些信息，那么这里面主要是针对我们的 gum 的一些相关的一些配配置，或者说是对于我们的 connect 选择的一种方式，那么我们单纯如果是想对于他们看到一些配置，还会想到哪些？这里面我们更多的去想一些对于 Web ML 和 Server ML 这些配置的一些优化，那么这里面我们可以看一下，我们回到我们的 Web ML，那么这里面我们到 config 下面有对应的 Web ML，我们可以看到 Web l 它的特别长，竟然 4000 多号，那么虽然里面有大量的注解，但我们想其次里面还是有一些比较相对冗余的东西，因为汤姆盖的功能还是很丰富的，我们在使用的过程中并不一定能用到它所有的功能。


说一下这里面 Web M2 里面相关的一些标签，比如像 debug sweat， ZIP sweat 以及 welcome filter list 这些内容，其实我们可以把我们不需要那么的标签去移除掉，那么也可以减少它解析的操作。尤其是这里面对于像 MME Mapping，这里面的信息非常多，这里面对于汤块的默认的话，它应该把整个我们涉及的 mymapping 的都加进去。


但是我们通常我们的 Web 服务其实相对是比较简单的，尤其是我们作为一个加 Web 工程，我们更重要的工作是处理我们对应 strain Mac 的一些应用，响应通常是返回json，所以说我们对这些应用其实很多东西我们并不关心，我们可以把它给直接清理掉好，这是我们对于 Web ML 里面的一些优化。


那么对于 Server 的 XL 里面我们也可以进行一些优化，比如说这里面刚才前面一段也提到，像 connect 我们一定要选择我们 a i o 的方式，效果更好一些。另外我们是否对我们的内容进行压缩，包括这里面我们在启动的过程中，像对于这些 out employee 和我们的一些像 unpack wall 这些功能，我们也都可以进行关闭，也就是减少一些它的一些特性，这么它的启动运行的速度就会好一些。那么这是我们汤姆刊的一些配置项的一些优化，大家可以从这方面进行一些参考。


另外就是我们 GM 内存的优化， GM 内存优化我们不单单针对他们开的，那么我们所有的 Java 程序在运行的过程中，我们都需要对于 d m 启动的内存进行优化，这个我们在前面也介绍过了，对于内存优化，我们优化的方式可以基于这里面对 Calendar 的一个命令行文件进行一些处理。

这里面像 Calendar cell 和BAT，那么 window 环境是我们去修改 BAT 的配置，那么对于我们的 Linux 系统或者说是 Mac 系统的话，我们在这里面去修改，肯定那哎，在这里面去配置一些 Java 的 OPS 相关的一些 j m 的一些参数，那么这里面我们跟大家去介绍了一下他们开的虚拟主题的一些配置方式和一些常见的一个优化项。那好，我们这一章节的内容就先介绍到这里，同学们，我们下一章节再见。

