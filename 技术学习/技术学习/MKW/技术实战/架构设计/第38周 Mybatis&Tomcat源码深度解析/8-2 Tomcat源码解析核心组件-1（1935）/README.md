---
title: 8-2 Tomcat源码解析核心组件-1（1935）
---

# 8-2 Tomcat源码解析核心组件-1（1935）

同学们大家好，这章节我们开始学习汤姆开的核心源码解析的第二部分，也就是源码解析核心组件。那么我们在汤姆开的架构设计的时候，介绍到他们看的里面有几个重要的组件，比如说像Server，service， Connector 以及 container 这些组件的概念。那么现在我们来详细的介绍一下每个组件它的一个工作逻辑。


首先我们来看一下整个这些核心组件，这里面我们看到第一个是bootstrap，那么 bootstrap 它是我们整个 Tomcat 的入口类，也就是说我们通过独立部署的 Tomcat 的过程中，我们启动的过程中会启动。首先是 Boost strap 对应的 men 方法，通过 men 方法调用它的隐匿的操作来创建一个肯定 Liner 的对象，那么肯定 Liner 对象注的事情是什么呢？肯定 Liner 的对象，它是来负责解析他们开的这些配置文件，比如说像最重要的 Server 的XL，它以这种方式来创建 Server 组件并进行管理。


接下来我们来看一下bootshop，那么 boot shop 作为一个入口类，这里面一定要注意一下，它是作为它们开的，独立部署的时候， boot shop 是它的入口类。假如说我们他们看的基于 stream boot 进行一个集成部署的方式的话，那么对于 boot strap Lina 就跟我们他们看到的启动就没有太大的关系了。


那我们来首先看一下 Boost shop，那么对于 Boost shop 这个类，它里面有对应的 men 方法，我们可以在这里面去看一下，因为我们作为一个启动类的入口，肯定是通过 men 方法去启动的，那么我们如何是定位到对于他们开始启动过程？它是执行的 Boost Trump，这里面我们可以看一下在 b 里面它的启动系统文件，那么启动汤姆 cat 最基础的它是基于STARTER，这里面 start up，这里面有 BAT 和SS，那么 BAT 它是 Windows 的启动器，那么 cell 我们这里面是 Linux 或 Mac 都可以用这种方式。
我们先看一下这里面，这里面我们看到它其实跟大家有一个简单的介绍，我们可以看到它里面启动的内容是对应的Canada，我们可以看到其实我们执行 start up 的时候，它并没有去启动真正的启动 come cat，它词里面间接的调用了 container 点cell，那么基于肯定的点 cell 来我们看一下肯定的点 cell 做了哪些事情，这里面我们看一下。
那么我们打开肯定的 cell 以后，我们可以看到这里面有一个非常显眼的几行，我看这里面是对应的 Boost strap，也就是我们的 RG 阿帕奇特琳娜 start up 里面的布斯stop，那这里面它被用了好多次，都是作为我们整个启动的方法，我们可以看到这里面就引用了 8 次这里面。所以说我们可以看到通过这样的话，我们也能定位当唐开的启动的过程中，它是通过我们对应的普斯刷进行我们程序的执行。


好，我们还回到 boot shop，那么对于 boot shop 的话，它启动的过程中会传入一些参数，那么这些参数支持哪些参数？我们可以从这里面去看一下。对于这里面，对于它构造出我们的参数指定为命令，这里面命令有区分。对应的starter，这里面还有是started，这里面是stop，这里面是start。这样一个启动我们可以简单理解，它就是说启动我们的程序和关闭我们的程序等等，这里面还有一些配置的验证等等这些信息。那么从这里面我们能看到我们程序这是个启动的入口，那么在启动的过程中 Boost Java 它会创建我们凯特零点的对象，那么它是如何创建凯特领导者对象？我们从这里面去简单看一下，我们可以看到，首先在启动的过程中会创建一个 boostrap 对象，那么 boostrap 的对象它会执行隐匿的操作，我们看一下隐匿的操作做了哪些事情，通知里面我们只是会看一些关键的信息，如果说我们每一行都去解读的话，这样投入的时间精力会非常多。


其实在启动的过程中，凯迪琳娜和布斯沙巴是做了一个解耦的设计，我们可以看到在这里面 put strap 并没有直接引用 Candy liner 这个类，它是通过反射的方式去加载calendar，那么加载完 calendar 以后，我们可以看到它是通过 Calener 这样一个 loader 去加载对应的 Calener 这个class，那么加载完成以后获取对应的构造方法，对应的 get new intense，也就是说通过反射的方式构建出这个对象，那么构建完这个对象以后下面还做了一些事情。


因为我们这里面通过反射的方式得到了对象，所以说它对于方法的调用也是用到了 Java 的反射，那么这里面加了反射，我们看它是执行对应的 master name 这样一个方法，那么对于 master name 我们可以看到这里面它对应的参数是一个 class loader 这样的参数，这里面我们获取到对应的 mess 的方法，这里面通过 invoke 的方式去执行 cat liner 里面的 set point class loader 这个方法，同时把我们这里面指定的这个 class loader，也就是我们的共享 loader 作为参数传入进去。


这里面我们可以切到container，简单去看一下这里面的方法，我们可以看到这里面到 cat liner，我们看一下 set point，我们可以看到 set point class loader 这样操作，它其实就是通过反思的方式调用了一个这个方法，同时把那个 third lock class loader 传入进来。


好，那么这样的话我们可以看到 boot stop，它这是一个启动初始化的过程，那么初始化的过程还会有一些事情，比如说它初始化的过程，我们可以看到这里面是创建了我们的肯定那操作，那么整个这个过程是不是就执行完成了？其实并不是，我们还回到对应的 men 方法，那么在 men 方法里面我们可以看到，首先我们是通过构造的 Boost shop 进行init，那么 init 完成以后，我们可以看到后面还会有些操作，这里面我们看到如果说我们执行的是start，也就是启动方法，那么这里面会执行一些操作。


首先它会执行嗯， Diamond 一个 load 操作，再进行 start 操作，那么这里面相当于是我们构建完这个对象，那么再通过 start 的方式把这对象启动起来，那么这里面我们看到它是 Diamond 这样一个对象，那么这个对象是一个什么对象？我们来看一下它是在哪里设值的，我们可以看到这个对象其实就是 boot stop，那么 boot shop，这里面我们回头看一下，它首先要进行漏的操作，那么漏的操作做了什么事情？在漏的操作的过程中，它也会把这些参数进行一些序列化，通过把参数这里面执行，把参数设置到对应的操作里面，这里面我们看它其实对于 put drop 的 load 方法，其实相当于是间接的去调用了 cleaner 里面的一个方法， calendar 里面的什么方法也这里面也指明了是 calener 里面的 load 方法。


所以说我们看它的调用过程也是通过反射的方式去调用的，所以说它漏的其次也就是调凯特琳娜里面对应的漏的方法，我们看一下凯特琳娜漏的方法做了什么事情，好，我们看到在启动的过程这里面有初始化我们的一些文件目录，这里面初始化一些名称，这里面就是对于凯特琳达最关键的信息了，它就是解析我们的 Server 点XL，构建出我们的 Server 对象。那么在这里面进行一些复杂的对于 Server Xmail 的一些操作以后，那么执行完成以后，我们从这里面去 get 我们的 Server 操作，那么我们可以看到这是对于 Server 一个 get set 的操作。那么如果说我们得到的 Server 呢？它等于now，那么整个启动就结束了，也就是说启动失败了，它并没有获取到 Server 对象好。


从这里面我们看到其实 Boost 算法它启动的过程中主要是初始化创建一个凯特琳娜操作，那么对于凯特琳娜它的功能，我们刚才也看到了calendar，它是负责解析汤开的配置文件，比如说像 Server 的cancel，其实它也作为一层包装的代理层进行。


我们下面对于 Server 的启动，那么这里面我们可以通过calendar，我们看到通过 calendar 引入到我们的 Server 操作，那么得到 Server 对象以后，下面它对于 Server 进行一个隐匿的操作，也就是说我们程序初始化的过程中层层递进，我们首先通过不算法的音译的操作调用 client 音译的操作其实是 load 操作，那么对于 connect load 操作的话，它会调用 Server 的隐匿的操作，也就是我通过一层一层的调用下一层把整个 Tom cat 里面的组件进行初始化完成。然后再进行启动的一个操作，里面我们看到了肯定 0. 6 的相关的操作，那么我们还回到 Boost shop，那么看 Boost shop 里面除了执行了load。


操作，它其实还有一个操作，也就是我们的 stop 操作。我们可以看到我们方法下面它这里面对于启动的逻辑，这里面还执行了 Diamond start 操作，那么我们看一下它 start 操作做了哪些事情，对于这里面的 store 的操作它也是，所以首先你。


判断一下这里面的肯定的demo，就肯定那这个对象是否存在，如果它不存在的话就执行一下隐匿的操作。其实正常逻辑上我们先执行 load 操作的话，已经把 container Diamond 进行初始化完成了，所以说这里面对 container Diamond 它通常是存在的，这里面我们看到他也去获取对应凯特琳娜的 start 操作，那么我们可以看到获取对应 start 方法，那么还是通过。


反式的方式进行 invoke 执行，那么这里面也会到这里面执行它对应的 start 操作。
我们可以看到这里面 store 操作也是大家首先会判断一下。


get Server 是否存在，如果说 get Server 不存在，那么进行漏的操作去创建我们的Server，那么如果说执行完 load 操作还没有得到一个 Server 对象，那么现在就相当于是我们的程序启动失败，这里面会抛出对应的信息肯定点 no Server，也就是没有一个服务。


后面我们看到它其实做的逻辑，也就是说执行 get Server，点start，也就是说获取到我们的 Server 对象，进行我们操作的启动。其实如果说它在启动的过程中抛出异常了，会进行一个 server 的destroy，也就是相当于是把整个程序销毁执行完成。这样的话，我们对于 cleaner 它 store 的操作的功能就大家有一个简单的了解。好，现在我们知道了 Calener 和 Boost shop 它们的关系，以及 Calener 和 Server 它之间的关系。那么在 Calener 和 Server 之间，我们这里面定义叫lifecycle，很容易理解，它也就是生命周期。


对生命周期的管理大家还有印象，我们在介绍他们开的磁力模式的时候，有注意到对于我们的模板方法，它就是我们介绍的 Lifecycle 的 base Lifecycle，也就是 Lifecycle 它的一个实现类，那么这里面我们切换来看一下，本身 Lifecycle 它是一个接口，这里面对于 Lifecycle 它定义了一些我们的一些状态和一些操作。
那么对于 level cycle 下面我们看它的内容特别多，这里面我们可以看到它对于它们开的下面很多组件，它都是直接或间接地去实现了Lifecycle，那么看到这里面的话，我们会去想一下，那么 lab cycle 下面这些状态常量都有哪些？我们可以看到这里面有，我们看这里面常量是 before init event，也就是说跟我们生命周期变化相关的这些操作的话，都在这里面会有一些体现，这里面是 before int，也就是我们程序初始化之前 off 的init，也就是初始化之后。这里面有start，也就是启动的一个事件，这里面是 before start 和 after start，都是对于这个事件有一个前置的操作或后置操作的话，我们可以通过监听这个事件来完成。


这里面有停止，我们可以也看到对应的是 before stop 和 off stop，这里面同时还有对应的 destroy 相关的操作，也就是我们对于我们的实例销毁的过程，我们销毁之前或销毁之后要做哪些事情，这里面我们看大概了解了这些情况，那我们看一下这里面还有的一些操作，这里面操作我们可以看到它可以通过 life cycle 的方式去添加监听器，那么我们可以在加监听器的过程中监听当前这个主键的一些四件的变化。这里面我们可以看到获取到你们所有的这些 knife cycle 的电器，这里面也有remove，也就是对接电器的一些正常改查的操作。这里面我们看重点的，对于 life cycle 来说，我们这里面有隐匿的操作，还有我们对应的 stop 的操作，以及我们的 stop destroy，那么这几个关键的操作，这里面还可以获取当前 lifecycle 它的一个状态，这是获取我们 state 的一个 name 等等这样一个信息。


那么了解到这些以后，那么我们来看一下 life cycle，它其实可以去触发某个状态的变化，再比如说我们把状态，把我们的程序启动结束销毁这个过程，触发状态变化的过程中，它可以去进行一些事件的发布，那么可以通过它这里面注入的这些 life cycle 的 listener 去监听整个过程事件的发布。那么这里面状态变化。


对于 life cycle 它里面的加 doc 文件里面整了一幅图，看起来还是因为我们这里面的字体放的比较大，所以说这里面看不到一个全局的。那么我们简单来说一下，其实它这个状态的变迁很容易理解，其实就跟我们通常在描述一个状态变现的方式的逻辑是一样的，比如说我们通过首先调用 store 的方法，它会执行隐匿的操作的过程，就相当于是我们把这个对象的同一个 new 对象慢慢的转化成一个isolation，也就是一个初始化的状态。
在初始化的过程中，它会自动变成初始化，完成操作变成初始化完成操作以后，我们会调用它的stop。


方法，那么 stop 方法这里面我们看到它会执行一个 starting Pre，就是说在我们启动，因为它是有一些时间周期的，那么我们首先是预热启动，那么启动的过程接着变成它的一个starting，也就是正在启动中，那么接着它会变成一个started，也就是说执行启动完成。


那么从这里面看的话，我们从一个新创建的对象到它初始化，再到它进行启动，直到启动完成这样一个过程，那么在整个过程中它还状态的变迁，我们可以看到这里面如果说它执行 stop 或沿这条线过来，我们可以看到它走到这里面的是 stop in Pre，也就是说它开始启动的一个预处理，那么接着它会自动跳转到stopping，也就是启动中。


最后在这里面是自动的到启动完成，启动完成以后，那么这是一个状态，那么在这个状态的过程中，我们可以看到它可以去进行start，那么start，也就是说它重复去执行这个 starting 对应 start 完成的操作，或者说是它基于 stop 的它之前一个 destroy 操作。


这里面我们看 destroy 操作的过程中，也就是把我们当前的一个实例进行销毁，实例销毁的追踪状态，这里面我们看到它是destroy，也就是说 destroy 的也就是销毁完成。那么其实这里面我们可以看到整个这些节点之间的状态的变化，在这里面有一些展现，大家可以去看一下，那么我们现在再看一下对于我们的 lifecycle 它里面的这些模板方法的结构，我们可以看到对应它的模板方法，我们可以理解为是这里面是我们的 init start，这里面是 stop 和 destroy 这几个方法是所有它这些实现类都会去实现的一些方法。


那我们来看一下具体这个方法它实现的类的情况，这里面我们，我们看一下这个 init 方法的实现，我们可以看到这 init 方法，这里面我们只看到是 life cycle base 去实现了 init 方法，其实也就是说我们可以看到对于 life cycle base，它实现了lifecycle，那么它是作为一个基础的一个生命周期的一个 base 的管理类，那么这里面它实现的类方法主要是我们可以看到像这里面隐匿的方法 stop 和 stop 都实现了，但是我们可以看它具体的操作。


它在执行隐匿的操作的过程中，它首先去判断一下当前的状态，如果在那些状态不等于 new 的话，那么这里面就会提示 invelop tracing，也就是它是一个不正常的一个状态转换，这里面我们它状态转换，这里面的状态是 before 隐匿的event，也就是在初始化之前它触发了一个错误的状态，那么如果说状态检查没问题的话，我们可以看到这里面会进行一个状态的一个内部转变化。它由我们这里面由它一个 new 的状态转换成一个insulation，也就是初始化中这个执行完成以后，以 slice 初始化中的过程中先来接下来进行真正的一个初始化的操作。


这里面初始化操作是以你搜 it now，那也就是说它内部初始化的逻辑，等这段内部初始化逻辑执行完成以后，他会把当前的状态set，state， internio 改成什么呢？自己 so 的完成，也就是说初始化完成。那我们可以看对于这个方法，它通常是个抽象方法，这个抽象方法由不同的它的子类去进行一个实现，这里面我们可以找一个比较容易理解的，我们可以看到Server，我们看对于 Server 这里面默认的实现是 stand Server，也就是标准的Server。对于标准Server，它在实现这个 init nail 的话，也就是说内部初始化的过程中，它也是首先执行 super 的 init nail，下面也就是处理对于 standard Server 它初始化的一些逻辑。


这会我们先不会重点去看它，那么回头我们再看对应的 life cycle base 里面其它的操作，比如像我们这里面可以看这里面像 stop 操作，也是它会首先判断一下当前的状态是否满足 stop 的，它的状态的转换的、跃迁的过程，如果说满足的话，那么它正常进行一个stop，你这里面也是会执行 internell stop 操作，这里面 stop internell 的操作。那么如果不满足的话，这里面在各个阶段就对应的一些处理，一些异常状态，一些处理，最终执行完成以后，那么它会进行我们这些实例的这个destroy，进行一个死力的销毁的操作。


那么这是我们的对于 life cycle 和 life base 的一些介绍。那么接下来给大家去介绍一下Server，它表示服务器，那么对于这里面的服务器，它表示整个凯迪琳达斯尔赖的容器以及其他的一些组件，它来负责组装并启动斯尔赖的引擎以及他们开的连接器这些信息，那么 Server 它通过实现了这个 lifecycle 接口，提供一种优雅的启动和关闭的整个系统方式，里面主要也是 init start stop 与 destroy 相关的一些操作。


那么这是我们 Server 的一些描述，那么接下来我们来看一下这个 Server 里面包含了哪些信息。那么如果了解 Server 包含了哪些信息，其实我们通过这个 Server 点 Xmail 是比较容易去理解的。我们打开configure，这里面有对应的 Server signal，我们可以看到对于 Server 的XML，它的我们的根的标签，也就是我们的Server，我们可以看到 Server 这里面会标记它的段构号，它是否支持 set down。那么对于 Server 这个组件，它里面会包含像listener，这里面还涉及到 global naming resource，以及这里面的对应Server，下面会支持一些service。

