---
title: 2-5 Nacos核心源码解析-服务注册与发现源码解析-3（1211）
---

# 2-5 Nacos核心源码解析-服务注册与发现源码解析-3（1211）

我们可以看到这里面是忽略掉了我们的返回值，这样对于客户端来说整个这个注册过程就已经完成了。注册过程完成以后，那么我们现在可以看一下对应的我们的服务列表，在服务列表可以看到它应该我们重新查询一下，得到这个新的服务里面我们可以看一下详情，这里面是我们 nicos provided 对应的这些信息，这里面是 10. 27 卓有 IP 8081 是我们端口号，它是一个临死历等等这些信息，这就说明我们这个节点已经注册完成了。


对于注册完成以后，我们看一下整个注册完成以后就是我们客户端的操作，注册完成了，其实我们这会并没有关注到我们的服务端的操作，对于客户端我们还要对于发现，那么我们先看一下对服务端的注册，服务端的注册是执行到 instance 的 control 的 resize 的方法去做的一些操作。对于客户端我们可以通过我们的 debug 方式进行一些代码的跟踪，其实服务端的话，因为我们是通过 Docker 去部署，那么在这里面进行 debug 其实不是很方便，可以怎么做？我们可以在这里面切换到 nicos 的源码，对于 nicos 源码里面，我们可以看到这里面有 naming 对应的操作，我们可以找到对应的方法，可以通过 instance control 找到register，我们可以看到这个操作是对应的这个请求 post Mapping，它的操作，它对应的 control 的URL，它的 URL 是这里面对应主成本对应的instance。


那么我们这里面我们可以看到它有注册和取消注册对应的一个逆向操作，在这种它要做的事情是什么？我们可以看到首先它会把这个参数反序列化到我们这里面的 instance 对象执行，通过 safe manager 进行实例的一个注册，这里面我们可以跟一下这里面，当然我们没有debug，我其实我们也可以通过 debug 的方式进行的，但是相对来说比较麻烦，建议我们的时间我们可以通过代码跟踪的方式去看一下，能达到相同的效果。


那么首先我们在这里面去注册它以后，你可以看到它会通过这些信息创建空的一个 service party service，它的意思是指我们在创建的过程中，这个服务它并没有一个实例，也就是说这个服务只有服务名称，没有对应的 instance ID，就是说它的服务的实例 IP 等等。


我们看这里面，其实它底下执行的操作是 create service if option 的，也就是说当前我们的这个 map 列表里面的它如果说不存在这个服务的话，就进行一个创建。我们看一下这里面做的一些操作。首先它注的操作是通过 get service 的方式，通过我们的 map 里面去查找，也就是说一个字，当前的 name space ID 和 service name 作为参数进行查找。


如果说当前 service 如果不存在，通过对应的那个 space 和 Server name 相关的一些信息去把这个 service 创建出来，当然创建完成以后我们需要对这个 service 进行一些处理，这里面有 service 的validate，还有是 put service，那 init 这里面还有一个 init 初始化操作，我们看一下这个初始化操作是什么呢？嗯，这个初始化操作其实它会做了一些，比如说把 service put 到我们对应的一个 service map 里面， put in 圈注意一些检查，同时还会通过我们获取到这个service。


进行一个 init 的时候，我们看这个 init 操作做的是哪些事情。首先它会去启动一个定时任务，这个定时任务也就是说对于它的心跳数据进行 task 一个check，同时去获取到这个 service 下面这些相关的集群数据，对这个集群数据也进行一些初始化，那么其实集群的初始化的内容也是比较类似的，我们可以看到这里面对应它集群的初始化操作，对应的 init 操作。


看我们这样搜索一下，对于它 init 操作也是进行一个定时任务，已经进行一个 health check 的一个task，也就是对于这个集群服务的一个健康诊断，去做一个check。好，这是我们现在已经到这里面一起操作，就最终我们相当于是创建完这个服务以后，对它进行一些初始化的操作，进行一些处理。这里面还有我们可以看到它是这个数据一次幸福务，它会进行监听这些信息的一些变化，这边构建出这个service，它的名称 name space 作为一个唯一ID。


既然看完以后跳回来我们刚才的一个操作，也就是说我们的 create empty service 的过程中就是一判断，如果说担心这个服务不存在，那我们把这个服务创建起来以后，我们保证在 get service 的这个过程中，我们获取的对象不为now。当然我们可以看到这类如果说 service 为 null 的话，当前就是属于数据不一致，它是有问题的，这里面我们就会抛出一个 next exception 异常。


我们这步完成以后，这里面我们可以看到我们是 add instance，也就是说把我们当前这个实例对象添加到这个服务里面，这里面服务的唯一标志这里面是name， space ID 和 service name 加上我们当前这个实例的对象。嗯，好，我们看一下它的处理的方式。


在这里面操作的过程中，我们看这里面也是先构建出实例的一个列表k，这里面首先这设是临时列表，就是我们的 name space ID，我们的 space name 和临时列表就是临时的这样一个状态，构成一个k，通过这种 k 获取一些相应的信息。我们最终我们会把这个对象，我们可以这个 ins 对象作为我们的一些信息，把它放的通过一次幸福务去放进去，这样的话整个过程我们就看起来注册的过程就已经完成了。


那么其实这里面还有一些事情，就是说当我们把 instance 的这个对象，我们把它放到处置到 consistent success 以后，我们就可以理解为它会进行服务一致性的一些传输，它会向整个集群这 Nicos 集群的多个节点来进行一些数据的传输。对于这个一致幸福务它有多个实现，我们可以进来看一下。在这里面对于我们这些临时节点默认的实现。这个我们到咱们看一下。最终它执行unput，把 KV 放逐到我们的对应的 map 里面，同时通过 DS 创这个协议进行一个 think 的操作，它其实进行这个数据同步的过程，那也是构造出一个 task 任务，对于这个 task 任务进行队列的一个执行。嗯，通过这里面这是我们对于服务注册的整个流程的讲解，现在我们来看一下它这个接受心跳和服务列表这些请求在服务端的操作是怎样的。


这边我们还是到这个 instance control 先看心跳，通过这么我们看到当我们执行心跳操作以后，它会在这里面进行一些处理，其实处理的逻辑也是类似的，首先它会通过 request 的请求里面获取到这些相关的 e 参数，对于这些参数进行一些处理，最终我们它执行的抄入会在这里面去看。


首先我们看一下我们会根据我们获取到的这些参数信息，通过 service manager 里面 get instance，也就是说我们去获取到这个汇报心跳的这个实例，如果说这个实例它没有获取到，现在的流程就已经发生变化了，如果说这个流程没有获取到，发现它这个时令已经有心跳过来了，这说明一个什么道理？说明这个时令可能有一段时间它没有发送心跳，那么在我们的注册中心把这个实例已经移除掉了，对于这些移除掉的实例，它如果有新的心跳过来了，那么我们这会要注的操作就是对它进行重新的一个注册，我们可以看到这里面做了一个重新的注册操作，那么最终注册操作完成以后，它再进行后面的最终心跳的时间的一个更新。


这里面我们看到这样我们最终能获取到 service 的时候，才可以进行后面的操作。如果说 service 最终没有获取到，它也会抛出对应的 next exception，这里面我们可以看到他做的事情，对于我们这个服务进行一个 kind beat，也就是说个心跳的一个处理，心跳的处理的逻辑其实我们想一下也是比较容易理解的，最主要类就是更新一下它这个时长，它当年时间把这个我们之前心跳的时间已经更新完成，那么去之前一个数据同步的一个操作，整个过程也就完成了。


好，那么这样我们来看一下我们的服务列表的操作，对于服务列表操作的话，其实对于服务端内容也是比较简洁的，服务端的内容也是对应我们这里面的 instance list 的操作，可以在这里面看一下对应 list 操作，它也是通过 request 获取到相应的参数，在这些参数里面去通过这些信息最终去执行我们这里面的都S、R、 V I P、x、 t 这样一个超方法来去获取我们的这些信息。


我们其实比较重点关注的就是它对于 IP 列表的一个处理，我们可以看到在这里面它获取到 IP 列表以后，对这个 IP 列表进行一些，比如说它可用性健康检查的一些处理，并且在健康检查处理的过程中，如果说他发现当前如果说他的那个健康的节点比较少的话，它可以进行一个前提是会把这些非健康的节点也会放进去，主要这里面会看这里面构建了两个map，一个 map 是 true 和false，这个 true 和 false 它分别代表是健康节点的 map 和非健康节点一个map。


在这里面进行一些特殊的一些处理，我们可以看到在这里面如果说得到这个健康节点的个数，跟我们的一个总节点支数提个比，小于我们一个阈值以后，它就会把整个我们这些，可以看到这里面是得到的force，也就是非健康节点也会放到健康节点里面直接去传输过去等等。这样实现的一些逻辑。


对于这里面是我们的获取服务列表这个过程，服务端我们就简单介绍到这里，其实这里面还涉及到一个我们服务列表，它的一个对于客户端它执行的情况，客户端执行的情况，我们可以在这里面去通过我们这个 study 模块里面去做一些稳一些处理，在这里面我们写有一个单元测试类，在单元测试里面，这里面是我们通过智通注入的方式，有一个 discover client，我们通过这个服务发现的客户端去获取到我们的这些服务。


这是我们的服务，可以我们通过我们的服务获取到服务实例，这是我们要得到服务的，也可以通过 get 我们的instance，当然获取我们的 instance 的话，是需要我们的一个服务列表的，那么这里面我们可以通过迭代的方式把整个服务列表全部列出来，也就是我们通过这种方式可以把我们的实例去执行一下，那么我们可以通过这种 debug 的方式去执行一下这个 discom client 它执行的流程，那么我们可以也是通过跟踪的方式去看一下这个过程。


其实这里面我们看到它可选的这个其实只有一个 nicos client 是正常的，它是通过 service discovery 盖的实例，那么盖的实例最终的底层的效果，我们可以看到它其实是通过我们也是 naming service 去获取这个instance，我们到 naming service 可以看到这里面我们也是 nicos 那个service，最终我们看它是执行的操作，这里面区分是订阅与非订阅的情况，其实对于订阅情况它只是通过我们本地的缓存里面去获取信息。


对于另一种情况，我们可以看到它通过 get service info direct from Server，也就是说它直接的通过 Server 去获取信息，跟到这里面去看一下，最终它还是使用了 Server Prox 这个对应，我们可以看它的服务名称就是 naming Prox，最终它执行的操作是什么呢？它执行操作 query list，也就是说帮我们把这又回到我们刚才注册的流程，它在 query list 里面首先把我们的参数构造成parameter，最终执行IQAPI，也就是发出我们的 IP 请求。这样的话跟我们就理解它后面的操作，其实也就是对于把 STP 请求发出去，执行我们获取我们服务端的这些服务列表的一个过程。我们对于这个服务注册一发现的内容，我们就先介绍到这里。

