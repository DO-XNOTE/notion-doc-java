---
title: 2-7 Nacos核心源码解析_动态配置-2（0826）
---

# 2-7 Nacos核心源码解析_动态配置-2（0826）

接下来我们看一下这里面动态监听的实现逻辑，其实对于动态监听我们主要就是看到 worker 对于它的实现，那我们来看一下 client worker，首先它的构造方法，构造方法传入的参数，这里面有 LTP agent 是用来去做理解，为一个 TP 的客户端发出 SP 请求。


这里面还有一个 config filter chain manager，其实这个我们关注度比较低，这里面还有一些 property 的一些初始化的一些原始数据。我们看到比较关键的地方，它这里面构建了两个线程池，一个单线程度的一个线程池，另外一个就是你这里面是根据我们当前运行时的就是 CPU 个数，它就是说它获取的一个线程池。


向下我们接着看的话，这里面首先第一个线程池，它会我们相当于是周期性的执行这个 check config info，整个我们这个构造方案完成了。那么对于这个 check configure info，我们可以看到每 10 毫秒执行一次，那我们看一下这个 check config info 做了哪些事情，我们点进去看一下这个 config info，这里面我们可以注意到对于我们构建的第二个线程池已经使用到了。


第二个线程池就是在这里面每一次进行执行，把我们一个长轮询的一个任务放到我们的线程词面进行自相对于这个任务，我们可以获取到的任务 ID 进行一个调配。接下来看一下这个长轮询就是我比较关键的一个实现，其实它就是定义了一个线程池的实现方子，这是它一个 task ID，其实在 run 执行的过程中，也就是说对这个 task ID 进行一个长轮，进行一个监听，这里我们可以看一下他做了哪些事情。对于这里面的一个变更，对，这里面是 check 这个 local 的 configure 它有没有发生变化。接下来后面有一个是 check update date ID，从这里面你跟进去看一下它的实现逻辑，这里面我们会作为 cat z 的作为参数，这里面一些初始化的一个 cat 的list。其实我们看到在后面的这里面有一个字 check update config，其实这就是我们在真正关心它这个变化的过程。对于在这个 check update config 里面，它其实这里面构造了一个长轮训的一个 at teaching，那么我们在这里面 head 里面 put a long pulling time out，设置一个 time out 超时间。


接下来我们看一下它这里面是真正的去发起 at v 请求，这里面是 agent it post 的，这里面通过发一个 post 请求，这个请求对应的请求的 UR 路径就是这里面是 on figure control，对应的 pass listener 跟我们在这里面是对应的路径是一样的，也就是说通过他也去向服务端去发出一个请求，那我们通过反映这个 listener 对应响应的一个结果，如果说它的结果状态是OK，那么我们首先设置 cost check sales Server 为true。同时对于这个结果集进行反序列化的一个解析。


通过解析，我们最终会获取到我们的一个 update list，也就是一个变更列表，把这个变更列表作为我们的返回值，通知我们的我们本地的 client 端，告诉变更列表是什么。我们通过变更列表获取到的一些变更事件，基于这些变更事件去获取最新的一些内容，促进我们这个动态配置的信息的一个更新。那么这是我们这个动态监听的内容。


接下来我们来看一下这个订阅刷新它的一些功能是怎么实现的。订阅刷新主要涉及到三个类，首先是 neckled contact reference，另一个是 Refresh event listener 和一个 Refresh scope，这个是我们先看一下 nicos context Refresh 它做的事情是什么？这是对于 next connect right，它首先是去监听一个词件，这个词件是 application event ready event，也就是当我们的应用程序准备好以后，它就开始去注册一下这个 nicos listener for applications，我们看一下它注的事情是什么，也就是说它要注册一个监听自见，这个监听自见就是我们要监听所有的 next property source repro positive。也就是说我们需要把所有的 nicos property source 进行监听起来，如果是它发生变更以后，我们要触发我们对应的变更事件，接着跟进去看一下在这里面我们做的事情。


注册我们的监听，当它监听到事件的时候，它需要去重新 public 一个event，就是 refresh event，也就是说它监听到我们配置文件的变更，那么呢他去构建出一个refresh，名叫一个刷新事件，把这个刷新事件发出去。


这个刷新事件以后接下来做的事件是什么呢？就是我们看一下 refresh even 的listener，对于这个 event listener 它需要监听 refresh event，跟我们这里面刚好就对接上了，你在这里面去发布一个事件，在这里面我们监听这个事件，同时我们看一下这个监听事件的过程，这里面当触发到是 refresh event 的时候，我们进行一个 handler 操作，这个 handler 操作其实现的逻辑看起来非常简洁，这里面做的事情也就是 refresh 一个操作一个刷新。那么我们看一下其实这个刷新的过程还是在string，它这个构建 bin 的过程设计的还是比较巧妙的。首先它会对于哪些鼻音进行刷新，这里面是一定要指定，指定的是对于 press scope 的鼻音才能进行一个刷新，对于其他的鼻音是不能进行刷新的，所以在这里面首先它会通过这里面获取到所有的这个 request scope，对这个 request scope 进行全局的一个刷新的操作。


执行完这个刷新操作，整个我们对应的这些基于 nicos 配置的这些b，它就可以把这个配置对象进行一个更新了，这样的话就完成了订阅刷新的一个过程。接下来我们看一下 nicos 动态配置服务端相关的操作，这里面不管是获取配置还是动态监听，我们都是 config Controller 对应的类方法，那我们先切到我们对应的 necos 的源码，这里面重点关注一下 configure control，对于 configure control，它其实是在 configure 这个模块下面对应，我们可以看到是 control 包下面在这里面我们关注的主要是两个方法，一个是 get configure，我们先看一下 get configure。


get configure 也就是通过远程获取到我们的一些配置的信息，其实对于这个control，它实现的内容是对于一些参数进行一些预校验，等一般预校验以后，它就通过这样一个 Email 进行了一个督盖的 configure 操作，对应的是 config service survelight，一个音乐，这里面的都 get config 我们可以理解为它就是可以理解为数据库的查询的一个过程，它把数据进行一些包装，再进行一些筛选性的一些处理。不好我们这里面就不会对它里面的细节再做过多处理。


接下来我们来看动态监听，这里面是卡片 control listen 的操作，涉及到对于长轮询的配置的一些处理，还是切换到我们 config control 对应的这个listener，他的监听方法是这样，listener，那么我们看一下他处理做了哪些事情，现在他从这个请求参数里面去获取一下用户比较关心的哪些可能发生变化，并且对它进行一个 MD5 的加密。


接下来他要做的事情就是进行长轮询的一个配置的一个处理，来，这么首先判断一下这个请求是否支持长轮询，其实它定义了一个比较简单的协议，只是看一下 header 里面有没有对应的k，如果有的话我们认为它是支持长轮询，如果没有的话就不支持。接下来它要做的事情就是进行一个长轮询的操作，添加到我们的常用性 service 里面，下面的逻辑就是它对于短轮型的一个兼容的操作，这个我们就不再过多的去接触。接下来我们看一下如何把这个长轮型的客户端添加到这个长轮型的一个服务里面，一个 service 里面，其实这里面他做的事情也比较简单，我们看一下，首先它是获取到我们这个长轮询，需要长轮询对应的一个时间，那么把这个时间在这里面去做了一个处理，做一个处理进行一个 delay time 的设置。这里面我们看一下这个 delay time 默认是 500 毫秒，也就是说把我们的超时间减去 500 毫秒以后，把它作为我们新的这个线程处理的一个超市时间，可以看到这里面胎猫的时间。


最终其实这个方法里面做的事情，也就是说把我们的这个对于一个请求，它封装成一个 client long pulling，进行一个线程值的操作，进行长论区的一个执行，整个这个过程就完成了。这个 NICOS 动态配置源码解析的内容我们就先介绍到这里，下一节再见。

