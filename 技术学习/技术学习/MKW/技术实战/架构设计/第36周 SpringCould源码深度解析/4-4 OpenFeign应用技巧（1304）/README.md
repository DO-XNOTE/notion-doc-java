---
title: 4-4 OpenFeign应用技巧（1304）
---

# 4-4 OpenFeign应用技巧（1304）

同学们大家好，这一章节我们来介绍一下 swing Claude open phone 的应用技巧，那么我们在介绍应用技巧的过程中，我们首先去了解一下，如果说我们需要自定义 open 份的一些各种参数，以及说我们试一下自己的配置这事怎么来弄？如果说我们需要了解如何自定义扩展 open fan 相关的内容，那我们先来看一下这个份 client configuration，我们的改动都是基于份 client 相关的一些改动，那么如果说我们了解清楚了分 client 的 configuring 相关的一些内容，那么说我们可以基于它这些 configuring 里面定义的这些 b 进行自定义扩展，那么就是我们一个比较好的一个切入点，当我们了解了这个 fan configuration 相关的一些内容以后，我们就可以知道我们如何修改 fan 的一些默认配置，以及我们在自定义这个 iPhone can 的这个注解的时候，它对应的 configuration 这个属性我们定义一个 configuring 类之如何定义。
那么基于这两点我们就说我们先认识一下分开的configuration，然后我们就知道如何进行通过配置文件基于 a Pixel YML 的方式去修改 fan 的一些默认配置。然后我们也可以通过 add fine client 里面的 configure 注解自定义之一的注解事件，这样的话让它生效的会更明显。


那么接下来我们先看一下份看的 configuration 它的源码，在这里面我们看对应的这个份看的configuration，它其实是在我们对应的，这是 spring code open phone card 这个包下面，在这个里面我们可以看到它是通过 configuration 去修饰的，也就是说它会在扫描的过程自动装配进来。我们看这里面它是肯定是定义了很多bin，在定义这个 bin 的过程中所依赖的这些属性它都提前注入进来。


这里面简单介绍一点，比如像 HTB master convert，那么这个就是我们 Spark VC 也用到的对应的一些转换的一个功能。这里面是有 annotated parameter process，也就是说我们对于注解的一些参数处理器，这里面是跟 phone 相关的 format 注册器，我们知道对于我们在学 SPV seed 时候也会涉及到像我们的格子化和校验相关的一些内容，都有相对应的注册，这就是我们的一个日志，这个就不太仔细说了。


这里面看我们涉及到 spring day 的 Web property，也就是如果说我们在 open 份在暴露的阶段使用了 spring date 相关的内容，那么我们可以在这里面进行相关的一些配置，这是份 client property，就是说我们基于份的一些配置属性相关的一些内容，这是一个encode，我们譬如说像这里面我们可以看到它的配置相关的信息的时候，其实这里面也主要是构成一个 config 对象，那么这个 config 对象它默认会有 debug 相关的内容，那么也可以去指定我具体某一个服务名称相关的一些信息，那么最终它都会解释成对应的 FIN client configuring 这样一个对象存储起来供后面去使用。好，那么我们接下来去看，这下面会涉及到我们的一些文件的编码，解码一些相关的东西，这里面decode，这里面是我们的encode，这是 encode 进行编码， decode 进行解码。


我们看一下，在这里面涉及到两个 encode 对象，也就是两个编码对象，那么对于两个编码对象，它的条件这里面有一些区别，我们看一下这两个都是跟我们对应的 America date 相关的一些内容，假如说我们当前容器里面是不存在对应的 domain paciable，也就是说如果说我们没有依赖 spring common date 相关的一些内容，那么它就会注册对应的这个encode，我们看是 spring encode 对应的一些信息。那么如果说我们看当前我们的三下文环境里面是能查找到对应的 peaceful 这个类，那么它就会注册的更复杂一些，这里面它就会使用 paysable spring encode 进行构造我们的 encode 这样一个对象，这就是说我们根据不同的上下文环境去初始化不同的 encode 对象。


接下来我们来看一下这个 priori Merry encode，其实这个我们可以看到我们这里面是 condition on class，也就是说我们 payable 是存在的情况下，它才会去构造一个 feasible spring query parameter query map encode 下面是一些contact，那么这里面对于因为是 open fence 跟 spring Claude 集成的这样一个过程，所以说这里面都是使用的是 Smen VC contract，也就是我们约定了一个协议是支持 Spiama c 这种约束的。下面是我们个 formatting convert service，这次跟 Spiama seed 逻辑是一样的。
下面我们可以看到这是一个retry，也就是说支持重试，但是这里面默认的使用的是 never try，也就是不支持重试。下面我们还有这是跟日志 factory 相关的，我们再看分 client configure，这是我们就是说 string encode 的一个实现的逻辑。


下面的内容我们就不去过多的去看了，这里面是构建 s 分builder，以及下面是各种构建分 builder 的一些不同的逻辑，这是是比如支持熔断器的分 builder 下面是还有一些，这里面是比如说我们支持统计的，这是 michmeter capability，就是去做一些特殊的一些逻辑。


其实对于它的话我们后面会介绍到这个主要是对我们所有的这些操作加入了一些监控点埋点，那样的我们可以通过搜集这个监控点获取更多的我们 open 份相关执行的一些信息。那么这里面我们可以看到对于分开的 configuration 的它的内容其实就在这个类里面，主要是跟我们约定配置了一些这些属性和一些依赖的对应的一些bin。那我们了解了这些属性以后，如果说我们需要手工的去创建对应的configuration，或者是修改份默认的配置，那我们怎么去操作呢？我们先回来，那我们在这里面我们去看一下如何去修改份默认的配置。


如果说制定份的配置其实最简单的一种方式，我们是基于 application YML 去执行，那么我们可以看一下，这里面我们写了一些配置的一些示例，那么配置示例我们看对于 FIN 相关的是 FIN client configure，对于 configure 下面我们可以定义不同服务的名称，这里面比如说默认的我们定义 debug 它的属性。这里面比如说 connect time out，我们的 read time out 以及我们的一些日志的级别。


当然我们可以根据我们的服务名称进行扩展，这里面比如像 Gecko 的 provided 2，那么其实特意为了跟我们默认的 neckles provided 进行个区分，当然命名为R1，而数字的结尾命名其实并不是太好，那么我们可以从这边看一下对于这种方式的配置，它下面可以支持哪些属性。


其实我们可以看到这里面的属性，像我们的链接的超时时间，阅读时间，就是读取的超时时间以及我们的日积点。当然我们可以看到下面所有的这些属性都是可以支持自定义配置的，比如说我们 error 的decode，比如说我们发生异常以后如何进行解码，这里面我们都可以自己定义自己的解码，因为我们现在的业务只是为了讲解原理，业务非常简单，我们就不会去一一去实现了。


大家可以根据对应的 d code 这个接口进行实现 g 的逻辑就可以，包括这里面它的一个重试器，这里面重试器我们刚才看到默认是不支持重试的，但其实对于容词器它默认也实现了基于次数的一个冲刺器。这里面比如说我们还有是 defiled query parameter，我们关心哪些值？还比如 debug 的 request headers，就是说我们的请求 head 里面去支持的一些内容，包括我们进行一个请求的一些拦截器。假如说我们涉及到一些安全校验的话，我们可以把拦截器在这里面去注入，包括我们对于 decode 404 是否是特殊的进行解析，这里面我们看还可以支持一些我们 capities 的一些自定义，其实这个 capture 自定义的内容扩展其实是非常重要的，我们可以基于它去进行，在运行的时候修改我们这些愤刊的相关的一些内容。


下面我看这里面还有一个，比如说我们的监控指标信息是否打开，比如说这里面是false，我们可以设置为true。当然如果说我们打开监控指标的话，我们还需要做一些其他的配置，比如说我们还需要依赖进来对应我们的支持监控指标相关的一些份的一些炸包。


好，这是我们看到的，就是如果说我们单纯是为了去修改配置的话，我们可以基于这些在我们的 YML 配置文件里面进行修改，修改的时候可以指定一下我们自定义的实现，那就完成了。那么这是相对来说基于 YML 是比较容易一些，就是容易去配置比较实现的。但大家一定注意一下，其实对于我们很多情况跟 spring Claude 相关的内容，我们都是基于它的，比如说注册的命名空间是在比如 spring Claude neckles，或者是其他的在 spring Claude 这样的命名空间，那么对于 FIN 的话，它其实就直接从根部开始直接分 client 进行一个命名空间的开辟，那么分 client config，那么再发config。


下面这里面会基于显得我们基于不同的这些我们 open phone 的服务进行一个配置。这样也就是说我们比如 debug 是所有都会生效，那么我们这里面它是指定具体的服务，那么具体服务在优先级的选择上我们肯定知道，比如越是具体的它的优先级会越高，比如说我们这里面配置不同的值的话，对于普通的如果没有配置它会显示defailed，那么如果说是对于 neckles provided 2 这样一个配置的话，它会选定对应的这样一个配置好。这是我们对于在使用 open Pen 的时候可以做一些默认配置的一些修改，可以基于这里面去修改。那么另一个逻辑就是比如说我们如何去自定义份 client 相关的一些信息，那么其实我们在使用份 client 的过程中，我们通常是只配置一个名称或者是 service ID，那么如果说我们要做很多特殊的配置的话，我们可以在这里面去配置一个 configuring 这样一个配置类，那么这个配置类可以自定义，所有这里面所涉及到的这些必应我们都可以进行覆盖。


那么好，那么我们这里面去看一下我们这个demo，在这里面我们可以看到在我们的 ecoservice 的时候，我们这里面去指定了一下 my phone configuration，那么我们这里面也是特意只是为了演示逻辑，这里面我们看我们在这里面去自定义了一个bin，当然我们指定 bin 的时候我们可以应该要把这个条件去掉，因为是我们自定义的自定义有优先级最高，那么优先级最高的话我们就不应该需要这个 condition on missing being 了。


那么就是我们就是默认它就是优先级最高的，基于它的话它实现完它就把这个位置占住了，也就是说只要我在运行启动的时候，我就先会把 decoder 这个 bin 注入进去，那么其他的逻辑情况它就会根据当前情况，如果说当前三下文没有的话再去初始化，如果有的话就不初始化。


这样一个过程我们也看，其实在这里面我们并没有去指定 at configuration 这样一个注解，其实这个 my phone configuration 它是基于一个新的对于那个止容器进行加载解析的，它其实解析的逻辑我们再去想一下，是这样一个过程，我们可以看到对于份 client configuring 它是怎么进行执行的，我们看一下它是什么地方使用，大家看一下点开这个引用的地址，只有在这里面，我们这里面是分 context 在这里面去引用了，那么引用了我们点进去看一下它是进行 super 一个它调用父类。


我们看它的父类是 name 的 context factory，这个我们提过好多次了， name 的 contact factory，那么在这里面它对应的就是一个 default config type，也就是把这个 type 注为我们需要注入配置的一个指定的bin，那么也就是说在初始化的时候，它会把这个 debug config type resist 到我们的 string 容器里面，这样启动的过程中就会加载这个容器。我们可以大家跟一下这个 Tab 在什么地方使用了，我们可以在这里面看一下这个 context rejest，也就是注入影像，也就是说把当前的这个配置类注册到我们的一个子的 string 容器里面，这样当这个 contact refresh 的时候，也就是说会解析它里面相关的内容。对于这个 open phone 的话，它解析的内容也就是我们这里面的是 open FIN，对应 FIN client configuring 相关的一些内容。


好，这是我们对应的这个配置的一个逻辑，我们在这里面配置这个信息下，我们只是为了演示这样一个功能而去配置的。那么如果说我们还需要去复写，比如这是解码，如果复写编码的话，我们就可以参考在这里面的实现，去找到对应的内容进行扩展我们的逻辑就可以了。好，那么我们关于 open fun 的应用技巧的内容我们就先介绍到这里，同学们，我们下一章节再见。

