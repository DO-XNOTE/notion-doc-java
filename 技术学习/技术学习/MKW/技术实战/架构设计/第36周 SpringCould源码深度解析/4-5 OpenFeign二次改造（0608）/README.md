---
title: 4-5 OpenFeign二次改造（0608）
---

# 4-5 OpenFeign二次改造（0608）

大家好，这一章节我们来介绍一下 spin code， open phone 的一些二次改造。我们对它进行二次改造，主要还是为了基于对它的这些扩展机制的了解，可以进行一些扩展的一些操作。这里面我们可以参考 open FIN 它自己实现的一个功能，这里面也就是 Macr capaliti。它注的事件是什么呢？它就是在我们在使用 open fan 的地方，它进行对 open fan 这些依赖的 b 进行一个扩展，进行加入一些监控的功能，通过认识 Macr capability，最终去学习一下据 cavey 体扩展的一些机制。


我们先来看一下 make meter cave 它的一些功能和实现是怎样的，先到这里面去看一下对应这个类，其实呢我们看一下它就是实现了 capability 的一个接口，在这接口里面它有一些默认的一些对应实现。如果说我们看一下对 capability 它的一个实现，我们可以能找到对应的 make mirror capability。


如果说大家在跟我相同的操作没找到对应一个实现，那我们需要做的事情是什么呢？我们需要引入相关的一些依赖，怎么找这些相关的依赖？其实是这样，在这里面我们可以看到，对于这是在份 client configuration 里面有 meter configuration，对于这个 meter configuration 它这个 being 的诸如的条件，也就是说首先是两个 condition on bin 和 condition on class。首先我们当前的容器里面是要需要有 meter dresser 这个一个监控，一个主要紫招的一个除射器。另外我们的当前环境里面是需要有 make Meta capability，它是需要我们在 palm 依赖里面引入进来对应的。我们是一个 phone Macmeter 这样一个依赖，那把这个依赖引入进来的话，我们就可以找到对应的这个类了。


在这里面我们可以看到以后还有一个需要我们去开启的，就是肯定是 on property，也就是我们在属性配置的时候，我们需要把份 meter is 的 enable 打开，就是说如果说我们没有配置的话，默认也是true。当然我们刚才在演示的过程中，我们刚才特意配成false，现在我们可以把它改成true，或者把删掉，我们都可以去达到它为 true 的一个效果。


接下来我们来再看这个 micimiter 它的实现的功能，对于它作为实现这个接口它要做的事情。首先我们在构造方法里面，我们可以看到它是通过构造方法注册的时候，你就构造出来一个 simple meter registry，比如一个简单的一个注册器，基于这个注册器我们去做我们相关的一些信息，我们可以从这里面看。


首先我们看我们在获取一个对象的时候，比如说我们获取client，获取一个encode，只有一个增强获取的一个概念，通常它默认的实现，我们看一下默认的实现是直接把原生的对象返回过去，不做任何操作。底下我们对于增强的操作是做了哪些事？我们看一下对于 client 的话，我们是重新进行包装，构建出来一个 matter client，对于 Encode 也是进行一个重新包装，构造出个 matter Encode。也就是说当我们在真正执行 Encode 的过程中，我们看这个 matter in plan 的对于开了它会实现我们对应的 Excel 的一个执行的这个方法。执行这个方法我们可以理解 matter client 它相对于原生的client，也就是我们原生指定的 client 进行了一层代理，这种代理我们可以称之为是静态代理，也就是说这个 Meta client 它只能代理这个 client 相关的一些内容，做相关的一些处理。


这里面其实它只实现了一个方法的代理，也就是一个执行方法，其实我们可以看到在执行的过程中他做的次行是什么呢？就是通过 matter 进行一些记录，记录上我们这里面 Michael 它对应的 name 和Tiger，这里面指定了一下方法名称和我们的份 target 相关的信息，最终它会去进行记录。


其实执行的还是 client execute 相关的一些信息，也就是把这两个参数透传过去，这样我们能看到其实对于我们 Meta cavip 他做的事情，其实就是说在执行的过程中，我给你偷梁换柱，把这个 client 给你重新包装成一个新的 client 的代理，把这新的 client 代理它实现我们特有自己的一些业务功能，这里面我们可以表现这业务功能是增加一个监控信息，这是我们的Claude，比如说我们的 encode 操作我们也可以跟进来看一下。


其实它处理的操作也是，其实也是对应的在 include 操作方法过程中的进行一些包装，我们首先记录一下我们的一些监控信息，同时再去调用 encoder 的 encode 方法实现。是类似的，也就是对于原生的 code 进行了一层代理去执行完成进行一些监控的信息。


这里面如果说再做一些判断性的一些监控的一些扩展，其实其他的我们可以不用一个一个去跟进去看，其他的这几个逻辑也是一样的，我们可以看到对应这个里面Meta，invocation， handler factory，他做的事情也是在做相应的处理的时候，加上对应的一些 matter 相关的一些信息，我们可以看到在这里面去执行的过程中，进行 invoke 的时候我们进行了一些操作的一些记录。那么这样我们基于能看到了这个 micrometer 的capability，这样的话简单已经理解了它的一些扩展机制。


那么我们在想这个 capability 它是在什么阶段进行执行的？大家有没有去想过？其实我们可以看一下，对于这个capability，我们看它是在执行份在执行的过程中，执行 firm builder 的过程中，大家应该还有印象，我们会经过 capability entry 增强的方式去获得对应的信息，那么在这个增强的过程中其实就完成了这个偷梁换柱的替换。
这里面我最终得到一个包装后的一个client，是这样一个过程，其他的像 repair 和我们等等这些可扩展的信息，我们都可以通过这种方式进行一个自定义的扩展。所以说我们在做二次改造相关的操作的时候，只要基于这个 capity 它的实现，我们就可以把 open 分相关的这些 bin 的信息都可以进行一个一代我们自己适应的方式进行一个改造。那么同学们好，关于 open for 二次改造的内容我们就先介绍到这里，同学们，我们下一节再见。


