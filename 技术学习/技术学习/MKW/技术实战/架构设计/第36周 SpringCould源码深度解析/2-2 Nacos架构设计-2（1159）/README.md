---
title: 2-2 Nacos架构设计-2（1159）
---

# 2-2 Nacos架构设计-2（1159）

接下来我们看一下 negos 的源码，那么如果说我们现在对于开源系统的源码，我们肯定是通过 Git Hub 或者是 gidee 上去克隆它的源代码，在这里面是nicos，它本身是放在阿里巴巴这个 group 下面去做的，现在最新的版本是 1. 4. 2 和 2. 0，这相当于是一点 x 的版本和 2 点 x 版同步进行发布。 0. 从这里我们可以看到它现在它这个 star 数量是还是非常多的，包括 Fox 也是比较多的。对于它这里面就划分的这些模块，我们待会可以通过源码的形式去看一下。


那么接下来我们怎么把源码 check 下来，就其实是比较简单的，大家应该都不会有太大问题。首先我们通过 gate 把我们对应阿里巴巴 group 这个 gate 地址的源码克隆下来，我们要做的一件事就是我们通过把它的 1. 4. 2 这个标签 Tiger check 下来，也就是说我们直接克隆下来的内容，它通常是它对应的 master 分支，那么如果说我们要看它一个稳定的分支版本的话，我们需要做的事情就是需要进行一个 check out，对应的一个 tag 版本， tag 版本是对应的一个稳定的版本。


好，那么我们来看一下我 check 下来这个整个 idea 它源码的效果，这里面我们看到整个nicos，它 NICOS 2 下面的内容是比较多的，我们打开它是最根目的这个 palm 文件，可以看到它这里面对于这些子模块的进行一些管理，我们可以看到它这个模块的排序方式我们可以理解为靠前面的，可以理为是最重要的企业模块。


但通常我不会这样去构建模块的关系，我通常的一些构建模块关系更像对于更基础的，我把它放到最上面，比如说像 config 肯定是依赖 core 的，那么 core 我建议放到最上面，下面再放 config 和 naming 这样一个命名，包括这里的common，它通常也是一个供货模块，它的排序我还是建议也是放到上面。这个对于 nicos 它官方的这块文件提的一些我自己一些不同的见解，具体这里面我们可以看到这是它的一些基于 Maven 的这些。


模块的一些实现，我们这会就不会展开源码去详细讲解了，我们下一章节的话会对于几个重点的一些源码进行一些分析。回到我们的PPT，接下来我们看一下 nicos 的服务发现及管理。而使用 nicos 的过程中，我们的第一步，也就是说需要把我们的服务注册到nicos，因为只有 bug 服务注册nicos，那么我们才能通过 nicos 进行发现。服务注册完成以后，我们的 nicos 可以发起对这个服务进行一个健康检测，通常是由服务提供一个对应端口，由 nicos 调own。


接下来也就是我们的消费端去订阅服务列表的过程。这里面为什么不是说是获取服务列表，而是订阅服务列表，因为我们在消费端订阅服务列表的过程中，它需要监听这个列表的动态变化，所以说我们称之为订阅，而不是紧急人获取。我们充值了，还看一下原来我们在这里面订的这个图。


首先我们的服务方它只通过注册服务注册到注册中心，那么消费方它是获取了这个服务，当消费方获取到福利表以后，它可以发起对应的 RPC 调用。在这里面其实我们忽略了一点，也就是说 NICOS 它还可以通过调用服务方的这些特定的接口来检查一下我们这个服务接口，也就是通过健康检查，当我们这里面服务它的健康状态不太好的情况下，直接在服务注册中心就把这个列表下线了。


那么通常我们在注册中心的列表下线是我们的服务主动下线。服务主动下线的过程中，它可以通过注册中心的注册列表移除 IP 和端口号。那么另一种情况是，如果说服务方它的负载压力比较大，自己可能虽然机器还在线，但是它承受不了更多的请求。这种情况下，我们通常是在健康检测的时候发现这个服务的响应非常慢，或者说是这个服务是处于不可用的状态，那 NICOS 注册中心主动的就会把这个服务方的注册服务下线掉。


我们接下来看一下 NICOS 的这些服务结构。对于 Nicos 来说，它对于我们整个配置也好，服务注册也好，区分了几个维度的一区分，比如首先是namespace，那么 name space，它是定义了大的一个空间范围，在 namespace 里面它又区分group，那么在 group 下面我们又定义为service。对于 service 也可以去划分集群，对于集群一和集群2，集群一，集群 2 下面都有对应不同的我们的服务实例。切换到 Nicos 的控制台，体会一下 name space、 global service，它们之间的关系。在这里面我们看到我们现在是在服务管理下面的服务列表下面，这里面首先是区分了一下这里面对应的 name space，比如我们点public，这是 public name space。当我们在切换其他的几个，比如我们这里面是慕课网，我们是一个测试，基于这两个的话，它的一些区别是什么呢？这是对，我们定义了三个 name space，通常我们内部 space 用来做环境的区分，我们可以这样去理解，我们比如说线上环境，贝特环境和我们的 DEV 环境，那么基于这些环境进行区分的话，去发现不同环境上的服务，这样避免一些环境的冲突。


那么我们看一下这里面的 name space，它有一个命名空间管理，那我们可以理解为就是命名空间的 CRUD 就可以了，可以在这里面去编辑删除或者查看详情。那这我们回到服务列表，通过我们看到这个服务它归属的内容是什么呢？它首先是属于我们 public 一个 name space，其次它的 group 是 debug 的group，也就是一个默认的分组，那么我们从这里面看它的详情。首先 name Swiss 是指定了我们的分组，也自定了接下来是我们的服务名称，服务名称后面的话，对于我们集群，我们可以通过添加一些元数据定义我们的集群，最终是得到我们整个集群的它对应的实例，这个实例我们可以通过 it 股端口号来确定。好，那么接下来我们去看一下 NICOS 的动态配置服务。我们知道 NICOS 它提供了动态配置服务，我们是不是应该考虑一下在企业微服务开发的过程中，为什么需要 NICOS 的动态配置？另一方面，动态配置它的实现原理是怎样的？它如何是做到实时的或者准实时的去更新我们的配置？另一方面，对于 Nicos 这些动态配置，它的数据结构是这样的，首先我们来看一下为什么需要 Nicos 的动态配置，这里面的动态配置的更新主要是跟我们在程序里面写的对应的 application property， application YML 所对应的，因为这些我们可以理解为是静态配置。


其实在实际应用中会有一些动态更新配置的需求，比如说我们需要修改服务的链接地址限流的配置。在传统模式下，我们需要手工修改配置文件，并且重启应用才能生效，这种方式效率太低，重启也会导致服务的暂时不可用。另一种方式，其实我们来配置它需要一个集中的管理微服务架构中某些核心的服务，它为了保证高性能，它会部署上百个节点，如果在每个节点中都维护一个配置文件，一旦配置文件的某个属性需要修改，可想而知它的工作量其实非常巨大的。另一方面就是说我们需要对于环境区分和配置内容的安全。首先去说一下环境区分，环境区分通过 Nico 的动态配置可以区分我们的线上环境、配置环境和测试环境。那么配置内容的安全怎么去理解？这要大家了解一下在互联网它开发过程中这些对于涉密的数据的一些存储方式。


首先是这样，通常情况对于开发人员他是有我们的 GitLab 对应的权限，那么对于运维人员，他通常是知道我们的 MySQL 对应的他的一些比如说用户名密码，对于开发人员通常他是不知道线上的用户名密码的。我们如果说使用静态的配置文件，那么开发同学就需要获取到对应用的数据库的用户名密码，这样的话就相当于增加了一个密码泄露的可能。


所以说我们在这里面通过统一的这个配置，那么把这些数据放到我们的 nicos 去配置，只有让运维人员，比如说 MySQL 的 DBA 同学他去配置这个事儿，这样的话就可以保证我们的一些数据安全。另一方面我们前面提到过通过 profile 机制来管理不同环境下的配置，这种方式对于日常维护来说其实还是比较麻烦的。推荐词，用 NICOS 的环境区分，可以做到这些配置信息的一个一劳永逸。接下来我们来看一下 NICOS 的动态配置的一些实现原理，在这里面我们可以看到，其实在 nicos 处理的过程中还是比较复杂的，因为 nicos 动态配置它能做到一个实时或近实时的更新，这整个过程的话，它跟我们这个 client 端和 Server 之间的交互是分不开的。


在这里面我们看，首先我们对于 nicos Server，它作为一个服务我们的业务系统，它会嵌入 nicos Claude 端，它向 nicos Server 进行请求。首先我们知道在一点四几的版本的话， Nicos 判断它是基于 SP 请求进行一些数据的交互，那么这里面 SP 请求我们知道它是无状态的，怎么能做到一个近实时的一个数据请求呢？其实逻辑是这样的，当我们系统起来以后， nicos client 它会向 nicos Server 去发起一个请求，那么这个请求得到 nicos Server 以后，它并不会直接向回来。当它直接回来向阳的话，原因也就是说当我们的数据有改变的话，它会直接向阳。假如说它请求过来发现我们的数据没有改变，那么这会怎么办？通常的 MV 请求，它会直接返回我们的状态未改变就得到了一定要请求的一个结果。


基于 next Claude 端，它是使用了一个长轮询的一个方案。长轮询的方案是怎样的？当 next Claude 向 next Server 发起请求的时候，如果说没有变更，它会在这里面持续等待，这里面等待我们可以看到它等待的是 29. 5 秒，那么等待这样一个时间，假如说等到 29. 5 秒它还没有变更的话，那么它就会把请求响应回去告诉耐克可安德，它并没有碰上电话，同时他搜到响应以后他会马上接着再去发出一个请求，再发一个 request 请求还是循环这个周期去等待 29. 5 秒。


只有我们当配置发生了变更，通过我们的控制台或 API 去操纵我们的配置，操纵完配置开放变更，那么在这里面会触发一个事件，那么这个事件去更新对应的 Nicos client 所关注的这些配置文件，触发它的响应请求返回过去，这样整个这个过程也就得到了这个数据的一个一致性的传输。那么在这里面它能做到一个近实时的一个传输，主要是用了一个长轮性的一个机制，长轮讯的机子，也就是我在请求发出以后没有变更，我就持续等待一会儿，在整个周期里面，如果说有发生变更，那么请求它会马上返回触发的这个事件，让它马上返回。这样就能得到让我们的 nicos Claude 端的基于一个实时和近实时的获取到一个变更的请求。


接下来我们看一下 nicos 这些动态配置的数据结构，在这里面我们看我们刚才介绍服务的时候也提到了，这里面会涉及到 namespace group，那还有service，那对于动态配置的话，这里面的区分是怎样的？它是 namespace group，这里面一个 date ID 这样一个区分，对于通常一个 configure 对象的话，它会包含什么内容？它肯定有ID，它还有它的配置内容。对于 config 还需要一些信息，它可以有一些标签，这个标签的话通常也有对应的 ID tag name， tag type，还有这里面有一个 n ID，这是Tiger。


同时它还有一些历史记录，当我们的 config 进行更新的时候，它会有一些历史记录的变化，比如这里面也是，首先它的历史记录肯定跟 configure 的内容它的结构类似一致的，但这里面会添加一个modify，也就是说修改的时间、修改的 IP 和修改的用户。这样的话我们可以追溯审计的这样一个配置修改的一个过程。那么关于 NICOS 的架构设计，我们就先介绍到这里，接下来我们会看一下 NICOS 的一核心源码讲解。

