---
title: 6-7 Sentinel应用技巧（2558）
---

# 6-7 Sentinel应用技巧（2558）

同学们大家好，这一章节我们来介绍 cycle 的应用技巧。 cycle 和 spring code 阿里巴巴集成为我们的微服务流控带来了很大的帮助。这里面我们介绍 settlement 应用技巧，主要聚焦在 settlement dashboard 上，也就是 settle 控制台，它为我们提供了一些数据监控、心跳采集，我们这些规则的动态配置的功能，我们使用上还是比较方便的。但是当我们深入重度依赖 settle 控制台的时候，会发现 settle 控制台它只能算是一个半成品。


首先， settle 控制台是基于 spring boot 的 Web 控制台，监控数据它是存放在内存中的，并且它监控数据只能保存 5 分钟，那么基因内存它也限制了我们控制它的横向发展，也就是横向我们扩容的方式，随着我们接入三座控制台的服务增加单机控制台同步监控数据就会有问题。


首先我们要解决控制台高可用的问题，这样也会解决 setup 控制台横向扩容的问题。那么另一个问题就是基于控制台配置的规则的执行过程，控制台它可以实时推送流控规则到 settle 客户端，那么默认的控制台它并没有持久化这些规则数据。如果说我们的业务系统进行重新发布或者重启，那么规则就丢失了。


另外一方面，我们基于 HTP 协议通过控制台远程推送到我们客户端这些规则的过程，推送过程避免不了一些失败的情况，如果规则失败，它会造成我们不同机器的规则不一致的问题，那么这也是一个比较大的问题，这就是我们要解决的另一个问题，也就是说控制台规则配置持久化的问题。那么一起来看一下我们如何解决 settle 控制台高可用的问题。


在这里面我们可以通过图里面去看一下。正常情况下，我们通过 settlement 控制台与客户端交互的逻辑是这样的，这是我们的控制台 dashboard APP，那么这是我们对应的业务，我们的业务通常会嵌入我们的一个 settle client 端，作为我们一个程序的一个服务。那么在这里面，对于我们正常的控制台与settle，它的交互逻辑是这样，我们的 host 它会通过定时任务这个schedule，也就是我们也是基于一个定时任务，它去通过我们哨兵的 client 端，就是我们的业务系统里面去拉取什么呢？拉取我们的一些流控的一些数据，这里面主要是去拉取我们当前每一个接口，它当前的QPS，它响应时长等等。也就是说我们对于 static slot 里面搜集的这些数据，它会封装为一个是秒级的尾物，每一秒钟对于一条资源，它会有一条记录。我们这样把数据拉取过来以后，对于这个控制台，它会存储在我们的内存里面。我们知道对于一个单机，它的内存设置是有限的，那么基于这样的settle，它在开源的版本里面做了一个特殊的设置，也就是说我对应的这些监控数据我只保留 5 分钟，那么也就是说它基于限制 5 分钟的保留数据，那样避免把我们的内存挤爆。


那么其实我们在业务使用的过程中， 5 分钟是一个问题，通常 5 分钟我们当发现问题进入解区的话，这个时间差可能就超过 5 分钟，那么我们再进去看的时候，这样可能我们已经错过了最佳的一个监控现场，我们的监控不能向前追溯，这是一个比较大的问题。另一个问题，即便我们的监控只保留 5 分钟，那么随着我们接入的业务非常多，我们的资源数也非常多，那么最终我们内存里面城市还是不够的。所以说这里面我们要解决的一个问题，就是说我们需要把内存的这些数据的存储，把它转移到一个Redis，那么转到 Redis 的话，我们同时也解决了我们多节点共享的问题，也就是说我们要解决控制台高可用。


首先我们要解决到我们内存数据存储的问题，那么这里面哪些数据会存储到内存里面？这里面会包含我们的一些监控数据，我们的一些规则数据，还有我们的一些心跳数据。我们知道对于哨兵 client 端，它启动完成以后，它会向我们的控制台汇报一些心跳数据，那么这些心跳数据它也是基于内存及存储的。如果说我当前汇报了 host 一，那么我们在 host 2 上去看这个机器的情况的时候，可能我看得不到它的一些心跳数据，所以说这是我们需要解决的一些问题，我们要解决赛道控制台高可用。
第一我们要解决的问题就是我们如何把控制台这些内存级的数据，把它统一转存到一个集中式的一个共享数据库，比如这里面我们可以记Redis，把它去存储，为什么我们选用 Redis 而不是选用MySQL？这里面我们当然也会使用到 MySQL 的 1 分的操作。我们解决高可用的问题，我们主要是解决当前这些监控信息，还有一些心跳信息，这样一些信息我们是放到 Redis 是比较合适的。


所以说当然这里面还有一个定时任务的问题，如果说一台机器，我们这个定时任务它是在秒级的去执行，去从我们的哨兵 client 端里面去拉取我们的数据，这个拉取数据的过程中我们可以去定思自相，那假如说我们的控制台完成了高可用的扩容，那么我们的机器的节点会越来越多，那么我们如何去处理这些host？去跟我们这些业务系统拉取数据的一个编排，也是我们要解决的。


原来是一台机器 host 一它会去拉我们对应，像EO、 APP 1、 APP 二、 APP 3，也就是我们不同的服务，那么不同服务里面还有不同的机器，那么都是制态机器去干活，那么现在我们已经扩展到多个控制台机器了，我们需要去动态的去编排。比如说后子翼，我应该拉取的数据是我们对应业务 APP 一的数据，那么拉过来以后我把这个数据存到 Redis 里面，这样对于 host 2、 host 3、 host 4 这样控制台的数据也是可见的。


那么对于我们控制台的 host 2，它是不是应该去拉 EO APP 第二台机？所以说这里面需要解决的问题就是说我们控制台需要在秒级的去轮询我们所有这个业务系统的一些监控数据，那么我们需要通过一种策略把我们这些业务数据的一些节点，我们应该是机器维度，把机器维度的这些监控信息比较合理的分布在我们控制在这些 APP 上，我们控制在机型，这样的话当我们的控制在扩容的时候，也就可以去分担我们业务这些接入的一些压力。


那么了解这些我们可以简单总结一下，如果说我们要解决 signal 高可控的问题，我们首先要解决的是我们对于 host 抓取它的数据，也就是我们控制台抓取的监控数据需要存储到我们一个 Redis 里面，它不能存在内存里面。另一方面我们作为我们的客户端汇报我们的个人的一些心跳信息，我们或者说业务 APP 的核心跳信息，那么他汇报的这些信息也必须存到对应 Redis 集中性存储，这样保持我们控制台多个节点的共享。


另外其他的还有哪些问题？对于我们的规则设置，那么规则设置原来它也是默认存在我们的单机的控制台上，那么它也需要把这些规则也存在我们比如 Redis 或者说 MySQL 库里面，这都是可以的。这里面还有一方面就是说对于我们哨兵的 client 端向我们的控制台汇报我们的信息的过程中，原来我们是汇报对应的机器的IP，或者说我们机器的应用人。那么现在我们如果说是 host 集群的话，我们需要通过 Nginx 做一个代理，通过 Nginx 进行一个那一个负载均衡的分发，那么这样通过 NGX 我们的域名映射，至于这个 NGX 它映射到对应的哪台机器上，不管它的负载均衡策略是什么，只要我们负载到一台机器上就能解决我们的问题。这样的话我们可以去解决 Sensor 控制带高可能的问题。


如果说我们知道了要改动这些内容，它具体改动点在哪里面？我们准备看一下。对我们控制在高可用，我们需要首先对于服务发现的私有化，对应的就是 simple method discovery，也就是服务发现，它会我们的哨兵 client 端会把我们的当前的节点信息汇报给我们的控制台。另一方面就是监控数据的持久化，监控数据持久化也是这里面默认它是对应的接口人 metrics 的reporsory，那么它默认的实现是一个 in memory 的一个实现。后面我们的规则持久化也是对应的规则持久化。这里面比如说我们看到没有 Ruler repository，对应 Ruler repository，它基于我们的流控规则，我们的降级规则它也都是基于 in memory，也就是我们通过对应的 concern 的哈希 map 去存储的。


另外还有一个是监控信息的同步策略，其实对于我们来说，对于我们控制的资源消耗最多的，也就是我们的自研信息的一个同步，也就是说我们把监控信息抓取过来存储，并且我们需要同步的策略是需要怎么去编排，让我们的这些控制台相对合理均匀的分布。那么这样我们就可以在程序里面去看一下这些需要改造的内容。


我们先看这个 metric feature 它改造的内容，对于 metric feature 它做的事情，其实也就是说定期的去抓取我们的一些机器上的一些资源的监控信息，我们可以看到这里面有个 fatal schedule service，就是我们通过这个基于这个定词任务，它进行每一次的去自行，这里面只有一个线程，同时我们可以看到它执行的过程。它执行的过程是我们看一下是一个schedule，它定义的一些频率，我们这里面它会最终会执行 fast RAPP，也就是把所有的这些 APP 的数据都抓取过来，那它抓取过程，我们看它的delay，我们这里面是 10 秒，那么这里面我们时间间隔是什么呢？它是秒的单位，我们时间间隔是 1 秒，这说明什么问题？我们这个定时任务每一秒钟都要执行一次，这样可以让我们看到的监控近似于秒级实时监控。


那么基于这个监控我们可以看到当它每一秒钟去执行一次的话，这里面会 fits all APP，这里面要做的事情是通过我们所有的管理的 APP 列表里面，并且把我们所有的机器机器处理都列出来，这样的话做一个进行一个抓取。那么如果是单机，我们接入的机器比较少的话，这个问题还不大。如果说我们接入的机器比较多，比如说 APP code 几百个，每一个 APP code 它的机器数可能少的几台，多的可能上百台，那么这样的话一个人群的 SP 请求去抓取，我们想想这种事简直是压力是非常大的。
所以说我们要做的事情，我们需要基于这个 metric FACTS，我们进行一个分布式的操作，我们需要把我们所有 APP 的节点和机器进行一个散列，散列到不同的 host 上，也就是说我们在这里面通过我们的一些外部的定制任务的策略，把我们的这机器散离着不同的host，让他们去分工协作进行一个抓取的过程。这是我们对于 match effects 一些处理的一些过程。



另外我们来看一下我们处理的一些repetposry，这里面我们看一下对于我们抓取来的监控数据我们是怎么存储的，我们存储的方式是 metric repository 去存储的，那么存储的内容我们这里面它默认的实现也就是一个 enmemory 的 metric repository，也就是说我们在这里面存储到我们的一个内存里面，它肯定是空间是有限的，我们可以看到这里面它设置了一个最长的一个监控预留的时长，我们看它默认是毫秒，也就是说它只保存 5 分钟，这里面一秒乘以64，一分钟变成 5 分钟。也就是如果说我们只保存 5 分钟的数据，刚才也提到了 5 分钟，当我们发现问题到介入查询也会有一些时间的间隔。当我们在 5 超过 5 分钟的时候，我们来看到监控的时候，发现我们的追加现场已经丢掉了。所以说通常我们在业务上的话，我们这个时间需要放长一些，甚至的话我们可能会对这些监控数据有做一个永久的保存。


当然对于这些流控的监控数据，我们没必要做到永久保存，但是我们比如说存储到天级，或者说一周、两周或者一个月这样的时间的存储还是非常有必要。所以说我们在改造的过程，我们需要重新实现我们的 metric repository，实现我们基于 Redis 的存储或者基于 DB 的存储来做一个这样的一个对应的一个替换，这是我们对于监控指标。


那么还有哪些内容对于我们的，这里面是服务发现，那么服务发现我们刚才也介绍到我们这里面会涉及到用到我们的 APP management，在我们进行我们的 APP 管理的过程中，我们 APP 会上报自己的心跳信息，同时它可能会新加机器节点或减产机器节点，那么这里面我们都是基于 method 我们的 discovery 这里面存储的方式，我们可以看到它数据的存储也是基于一个 simple Max discovery，它也是基于我们的内存在 concern 的 map 里面去存储。


当然对于一个机器的服务发现的话，我们在内存里面存储，当然我们瞬时的存储是没什么问题的，我们机器下线以后就会丢失，当然我们必须还是为了解决高可用，我们需要把它也是存储在 Redis 里面，这里面我们存储的过程我们可以看到把 APPS 这存储的方式由我们的 content map 改到我们的 Redis 里面。


这样一个过程，我们再看对应的规则持久化，对于规则持久化的内容是其实也是非常重要的，这里面对于规则持久化相对比较集中。我们看对应 repository 下面的Ruler，我们的 Ruler repository 它有多个实现，这里面的实现我们可以看到它也都是基于 in memory，或者说我们在包结构里面，这里面也能看到对应的一些存储实现。我们这里面最关注的像 in memory 的 follow Ruler store，其实我们看一下它存储的方式是基于我们内存的存储，所以说我们最终还需要把它迁移到 Redis 上面，这是我们可以明显感知到如果说我们做高可用，我们需要改到的地方，这里面我们在服务这发现持久化的方式。另外我们监控数据持久化的一些存储，包括我们规则持久化的一些存储方式，或者我们监控信息同步策略持久化的一个方式。


当然这个改造其实工作量还是比较大，所以说我们很难在课堂上把我们的代码实现出来，所以说这里给大家提供一个思路，大家可以参照我们的提供这个控制台高频需要改造的内容进行，完成自己控制台高可用的改造。那接下来我们来看一下控制台规则同步机制。其次 settle 官方它提供了三种模式，也就是说提供了三种方案，三种方案对应的也就是我们首先是原始模式或者 pull 模式和 push 模，也就是说我们对于原始模式的话它是这样，也就是说它会把我们的规则推送到客户端，并且在我们的内存直接更新保存，也就是我们刚才提到了我们这些数据是存在内存里面的。


它的优点是什么？优点当然是简单，它不涉及到外部依赖，它跟我们客户端的数据传输，基于 ICP 协议字节传输推送，但是它也有缺点，它的缺点它其实是无法保证一次性的，当然我们的业务端的一些服务重启完规则就会失效了。这刚才我们也提到了不保证一次性，也就是说我们在推送的过程中有失败的话，会对同一个服务里面不同的机器，它们的规则可能是不一样的。我们这是原始模式，当然我们线上不会采用这种模式，这也是我们需要对控制来说改造的原因。另一个就是 pull 模式，我们可以知道对于我们同步的一些策略，我们的客户端可以主动的向规则中心的一些轮询院拉取，它的优点也是比较简单，也没有外部依赖，我们的规则需要持久化。当然缺点是什么呢？我们知道人群我们肯定会想到它，首先它不会做到实时性，另一方面它可能会一些额外的或者一些无效的一些操作。所以说对于这个来的话，它的缺点是不保证一致性和实时性，同时它也会有一些无效的一些消耗。另外就是 push 模式，对 push 模式来说，对于客户端我们需要依赖一些注册中心配置的，比如像 ZKI Nicos，由配置中心进行一个规则的推送。它的优点是我们的规则能做到持久化，一致性也能做到一个近实时性。当然这里面这个实时是需要一个近似时，它可能会有一定的一些言辞。它的缺点是我们所有的业务系统，所有接入哨兵的系统都需要引入一些外部依赖，那么在这里面我们采用哪种方式在企业上应用哨兵控制台，其实这三种模式都不是一个比较好的模式。


其实最我们推荐的方式，这里面我们推荐使用的方式就是我们基于原始模式的持久化，我们可以推拉结合，这会我可以跟大家介绍一下这个过程。首先我们来看一下我们这个控制的配置计划的过程，这跟刚才那个图其实很像。首先我们在配置规则的过程中，我们的规则一定要落库，这个落库我们不管是 Redis 还是DB，我们一定要首先持久化，那么持久化完成以后，我们需要做的事情就要把规则推送到我们的 client 端，推送的只有把我们的规则推送到客单端我们的业务才会生效。


那么在推送的过程中，如果涉及的机器数比较多的话，我们可以基于异步的时候它进行推送，那么我们肯定会有一定的一些重试的一些策略，比如说我们推送失败，那么我们进行重试。当然我们会想到这样一个问题，其实我们在推送规则的时候，我们这些服务它可能处于一个发布的状态，比如说发布 cost 一刚发了最新的版本，那么它启动成功了，那么 host 2 它正在发布，那么 host 3 它还没有发布，那么我们这一刻把规则推送过去以后， host 一得到了中心的规则 host 2 它处于一个不可用状态，不可用状态它会基于一个轮询策略，等它发布完成重启以后它会接受新的规则。


那么对于 host 3 它就情况比较特殊，那么它现在我进行把规则推送也推送成功了，但是接下来马上 host 2 发布完成，要 host 3 就要发车发，那么 host 3 这个机器发的过程中，它已经把我们刚才推送的新的规则丢掉了，那么丢掉以后我们会发现如果说我们只是基于推送的话，我们的规则丢掉以后他没有感知他得不到最新的规则，所以这是一个问题。那么所以说我们只是推送是不能解决我们规则一致性的问题的。



那么如果要解决规则一致性的问题，我们需要做的是我们的 account 端会保持一个拉取的一个策略，所以说因为我们这些规则都是如果说已经我们有了我们的一个 push 状态，也就是我们 push 能做到近实时，那么对于 push 失败的情况，或者说系统重启的这种情况，我们需要基于一个拉的模式，基于拉的模式去把我们的规则拉取过来。


所以说这里面会要做一个什么事情？就是说我们的哨兵 client 端需要有一些类似于轮询操作，轮询操作去 check 我们规则的变化，但是这个轮询规则 check 我们可以做到优化，也就是说我们只 check 版本变化，如果说我发现当前我这个节点的我们的规则版本低于控制在的版本，那么我们就拉取新的规则，如果说我发现是相等的，那么我们就直接正常去再过段时间进行轮询就行。因为如果说我们已经有了pose，那么所以说我们定时轮询这个策略的周期可以放的长一些，我们可以放到分钟级或者说更长一些。这样即便我们的系统它存在了规则不一致的情况，它也在一个有限的时间范围里面。另外还需要保证的就是这个拉取是非常有必要的，因为我们的系统重启以后，它当时是没有规则的，所以说我们系统一定要具有拉取规则的能力。当我们系统发布完成重启以后，它需要从我们的控制台 DB 里面拉取最新的规则，同步到我们的信上，所以说这是我们对于控制台配置规则持久化需要做的一些事情。那么我们了解了我们控制台配置规则持久化的一些思路，那么具体要改造哪些内容，我们可以看一下。


对于我们控制台与数据交互的过程中，它分两部分，首先我们基于控制台我们设置，那么基于控制台操作，这里面是 degree 的control，也有我们基于这个 control 设置规律的。设置规则以后，它会首先对于规则进行 reposit 存储，当然默认的它是 in memory，也就是存在内存里面，那么我们改造完存储到数据库或存到 Redis 里面，那么它就可以做到一个词久化的过程，那么它持久化存储完成以后，它要注的一个三层 API 的client，也就是基于这个 API client 来进行规制推送，这个规则推送它是要推送到我们的哨兵的 call 对应的 client 端的这一些接收。那么接收的过程，其实我们的 client 端它嵌入到业务的系统里面，同时自己暴露出一个端口，用了这个端口用基于我们控制台和客户端的一个交互。


通常如果说默认情况，我们会引入的是 simple HTP command center，当然我们也可以改成对应的 ninety command center。所以说这个过程如果说我们只是默认的引用的话，它默认是基于一个 simple STP 协议，那么这个其实它并没有用到载体或唐姆开的默认暴露的端口，它是自己基于 Server shopping 的实现暴露出一个端口，那么它会通过 HT event task 包装，那么基于 activity even 的task，它会把我们这些请求包装成一个command，这个 command 比如说我们这里面是最直接的啥multiple，我们的Ruler，也就是说修改我们的规则的一个 command handler。


那么基于这个 command handler 呢？我们会接收我们的请求，那么最终会进行我们这个降级规则的一些修改，那么对于重新修改，那么这样的话整个这个流程就做到了我们规则一个修改的一个过程，那么这是我们修改的一个情况，那么我们可以从代码里面看，那么在这里面我们切换到这里面，首先是我们 degree control，它怎么去？对于这个 degree control 里面我们可以看到它里面有一些方法，这里面我们是有delete，我们可以看到我们像update，我们add，这是我们现在去增加我们的 rule 实时 post 的一些请求。


在增加的过程中我们可以看到它的请求，首先它的请求参数是一个对应的一个因体的对象，我们可以基于 request body 会接收过来，这里面我们可以看到它构造完成这个对象以后，通过 request 这些save，我们这个 entity seal 完成以后，它会进行我一个 publish Ruler，也就是把我们的 Ruler 分发出去。


其实这个分发的过程实现的比较简单，这里面分发的过程也就是我们调用了这里面 settle API client，这个 settle API client 可以理解为它就是一个 HTTP 的 client g r t p client 进行，把我们规则设置到对应的机器上面，我们可以看到在这里面会有一个逻辑，其实我们做到当我们在去操作的过程中，我们并不是说把我们前端对应的参数传入进来的，我们并没有传入 ENT 的一个参数。


我们这个 publish 这个 event 只是告诉我们哪个APP，哪个 IP 和哪个port，也就是哪个端口号发生了变化。至于变化的内容，一些规则它还需要通过我们的一个规则的库里面去获取出来，所以说这样能保证我们的规则跟我们分发出去的是一致的。假如说我们这个规则入库失败的话，那么我们这个规则推送的话，它也不会推送成功，它保证就是说这个规则库的数据跟我们推送出去的这个数据是保证一个一致性。


那么对于这个接口它是怎么触发这个接口的一个操作？我们可以在我们的控制台看一下，在我们控制台里面，这里面有对应的流控规则、降级规则，我们热点规则，那么在降级规则里面我们可以选新增规则，在这里面我们指定我们的资源名称和我们的熔断策略，这里面比如说调用比例、异常比例或异常数这样一个过程。把这些存储完成以后，我们点新增，那么规则就存储完成了。


那么新增的规则它首先会存储在 reposer 里面，接着就是执行我们这个 publish ruler，那么在这里面我们可以接着跟一下对于 set API client 它做的事情，在这里面其实也就是把我们这些操作，它会定义一个 i d p plan 的进行发送，这里面是 Excel 的command，就要真正执行我们的操作。


我们可以看到这里面会通过我们的对应 APP 和我们的 host IP 和我一个端口号，定义出我们这个URL，那么基于 URL 最终化进行我们一个操作，这里面是excommand，通过那我们调一个 HTP get 的一个对象进行发出我们的请求，这样的话我们这个请求就执行完成了。


那么执行完成以后对于我们的 Claude 端是如何接收的？这里面接收的过程其实需要我们参考对应的 MT event task，我们可以在这里面去跟一下，那么对于 even 的task，这里面我们要注意一下这个它其实最终会嵌入到我们客户端，在我们客户端里面它会进行一个执行这个 ITPEV 的task，它在这里面我们可以看到里面有一个操作是去获取我们的command，也就是说首先它会把我们这个请求的这些首先是我们的数据流，数据流它会 process query string 抽取出我们这些查询的参数，也就是我们的请求参数。通过我们的请求参数再去获取到我们的一个 command name。


那么基于这个对应的 handler 进行我们操作的一个处理。这里面的细节我们可以去看一下对应的哪些 handler 的存在，我们看这里面有很多，其实我们刚才提到我们这里面，这是一些跟数据抓取相关的一些信息，这里面去修改规则相关的一些信息，我们去更关注的是我们的 modify Ruler 投放的handler，其实在这里面去修改的过程中，因为我们知道我们的规则也很多，比如这里面需要去看一下我们规则的类型，规则类型这里面是我们的流控，这里面是一个权限，这里面是个降级等等。这是我们的系统规则，这样也就对应到我们的 multiple command handler。


那么在这里面我们具体操作的过程中，我们可以想象到这里面我们对于我们的 flow rule 的 type 的话，它处理的过程，其实我们可以跟一下这里面，首先它会把 flow rule 的 manager 进行load，也加载到我们的存储里面，同时这里面还有一个 read to d 的source，一个操作这里面 read to d 的 source 也就是说去做一个数据同步的一个操作。


底下我们可以看到对应了我们的 grade ruler type，它处理的逻辑也是首先把我们这些数据转换为对应的 grade Ruler，同时用 Digit Ruler manager 进行 load Ruler 进行我们当前这个节点内存集的规则进行更新。


当然这里面还会有一个操作，这里面像 red to data source，也就是说把我们当前内存的这些规则数据进行一个同步，这个它是同步到什么地方？我们可以跟一下，看一下这里面同步的词，其实它最终是同步到一个文件里面，我们可以看到对应的这个 write data source 它的一个实现，我们可以看到这里面有哪些实现，这里面默认的实现是一个 feel rate ratible data source，它是什么意思？就是说我们在业务系统里面把规则存储的内存以后，它同时会在我们当前这个机器的文件里面去备份一份，这样这个备份的文件它能做到重启的时候去把规则抓取过来。


但是对，如果说我们在数据传输的过程丢掉的话，其实用 feel writable data source 也是解决不了问题的，所以说最终我们还需要去克服的一个问题是什么？我们需要去克服我们定时轮询的操作，那么这里面也就是说我们对于哨兵 client 端的它停同步规则，它一定需要我们一个定时轮询，也就是说这个定时轮询的时间周期可以长一些，但只有它通过定时轮询才能保证我们的规则的最终一致性。那么关于三通的应用技巧的内容我们就先介绍这里，同学们我们下一章节再见。

