---
title: 4-7 OpenFeign面试题深度解析（0754）
---

# 4-7 OpenFeign面试题深度解析（0754）

同学们大家好，这一章节我们来介绍一下 swing Claude open FIN 对应的面试体深度解析。跟份或 open v 相关的面试体其实范围还是比较有限，通常我们会比较关注到比如份和 open fan 的一些区别是什么。这里面的份最早，它是 spring Claude 主键中一个轻量级的 restful 的 SP 客户端，它为了简化 HTP 的调用，可以把 HTTP 的调用转化为类似于 RPC 的调用。它让调用远程的这些接口服务像调用本地的方法一样简单。这样它就类似于比如说 double 和 RPC 的远程调用一样，通过动态代理实现接口的调用。phone，它通过封装包装这个请求体发发送 RTP 请求获取响应。接口响应结果通常把我们响应的结果通过序流化的方式得到我们这里面声明的这个对应的类型。


open fend 它是 spring Claude 在 phone 的基础上做了一层包装，包装的内容主要是支持了 string v seed 注解，比如像 request Mapping， get Mapping 和 post Mapping 等等。 open fend 它还实现了像以 load balance 相关程序的一个集合，最早它是默认集成的ribbon，现在它可以实施。现在最新的版本是基于 spring code load balance 进行了整合。对于服务提供者，我们需要是 API 的接口，而不是需要像 double 那样去需要强制实现某一个接口。对于 phone 的服务端，其实并不要求它是 phone 的，跟 phone 做什么相关？只要它是一个普通的 SPV seed 服务接口，它就可以通过 phone 的客户端进行连接。另一个问题，就比如说我们对 open phone 的一些运行原理，像像对于 open phone 的运行原理，我们源码解析的内容了解清楚，其实回答这个问题通常是不会困难的。


对于寸底层实验原理，我们知道 open phone 它通过自动装配和包扫描的方式，把我们的份 client 的注解接口扫描出来，并且为每一个接口注册一个份 client factory bin 的一个实例，它通过 affect 份 kind factory bin 的这里面的对应的 get object 方法去得到一个泛的一个动态代理类，这个动态代理里面实现的过程当然比较复杂，我们还是通过我们的源码解析的执行过程的方式去更好深入地去学习send。它会为每一个代理接口的每个方法生成一个 master handler，这个 master handler 放到一个 map 里面，通过方法的签名作为 k 对应的 mass handler，注意 value 去获取到。


当我们接口的 add 分 client 注解的 URL 属性配置服务，如果说它配置的是 URL 的话，这里面它就不会再通过 load balance 去，它就直接通过 URL 去请求。如果它配的是一个服务 ID 或者 service ID 的话，或者是它 name 的话，它会通过跟 NODE balance 的以及我们服务注册中心，相当于是我们通过这个服务名称去注册中心里面获取到我们的服务列表，再基于我们的负载均衡策略找到对应的实例ID，这样的话进行一个最后的一个执行，这样执行的过程中，也就是说选中对应的实例进行一个请求的发出。其实在执行过程中，对于比如说它真正执行的client，它有多种实现，比如说通常我们是用 client 实现类的默认实现类debug，那实例发出 USD 请求。当我们接口上有 iPhone client 详解的 UR 属性，如果说不配置，那么默认它都会走负载均衡逻辑，这就是需要与我们的负载均衡的 boot load balance 整合使用，这时候我们也不再使用默认的 client 接入掉，而统一使用的是 load balance phone client 接口调用，像 load balance 份 client 接口它的一个实现类最终还是使用 debug 排期请求，只是通过 load balance FIN client 注了一层包装而已。


这些 open FIN 它执行的过程介绍清楚，通常会得到面试官的认可，通常还会有一些更具体的一些案例，比如说像我们使用 FIN 开的一些配置方式，我们比如说怎么去具体配置这个呢？就把我们在用 FIN 跟 open FIN 开发过程中，比如说现在介绍，首先我们在做的过程中，我们首先是需要引入对应的 spring Claude，starter、 open fan，把这样一个依赖注入，强调我们的 palm 依赖引进去。


在开发的过程中，比如我们的入口类，在这么我可以就着我们这个程序去说，比如我们的入口类是需要去配置一下 enable FIN client，做上这个配置以后我们就可以去注册我们对应的 fend 接口，比如我们分的接口是指定一个接口的实现，是通常是服务跟服务的签名很像的一个 control 的方法。那么这里面也是通过 get Mapping 或者 quest Mapping 的方式去修饰，这样对应它的参数和想象内容都可以使我们服务方法对应的它的一bin。


这里面我们再通过用 iPhone client 的方式去描述一下这个接口，比如说这里面我们可以通过 name 标明一下对应的这个服务的ID，其实这个 Cid 就是通常我们是在 NICOS 服务注册中心里面的服务名称，这样我们就可以去找到服务的列表，从列表里面选择一个节点进行一个执行。


在这里面我们如果说没有使用到注释中心怎么办？没有使用注释中心，我们可以基于粪刊的里面，它这里面有对应的URL，比如说我们把 URL 配置进去以后，它就不会再使用我们的注册中心的负载均衡策略了，那么它就直接相当于通过 URL 调用，这是我们对于配置的过程。通传这个过程我们会使用到了 open 分跟我们默认的这些实现。


那如果说我们需要自定义实现怎么办？自定义实现我们知道有两种方式，一种方式我们是在当前的 phone client 里面，我们去创建一个，比如说对应的一个 my phone configuration，那么完成这个 my phone configuration 以后，我们看一下它的实现，其实在它的实现内容，其实我们可以构建我们所需要的这些地。


构建这些内容的过程其实是参考什么地方？是参考我们这里面引入的一个叫分 planters 一个configuration，这里面的配置其实都是参考这里面的内容。只要这里面有的这些内容，我们都可以通过自定义覆盖默认的一种策略把它去覆盖进来。比如这里面我们是直接简单实现了一个 decode 的一个 bin 的实现，这是我们使用比如说加配置的 Python 实验。另一种方式我们也可以基于 YML 配置的时间，比如我们可以看到这里面通过分 client config debug 的方式去配置我们默认的这些常用的属性，我们也可以指定一下当前的服务，指定一下这个服务，对这个服务进行一些特殊的设置，可以看到这里面的设置是非常丰富的，只要我们刚才 configure 里面涉及到了bin，几乎可以都在这里面去配置，比如这里面会涉及到对应的 ERRD code 或 retry 的一些策略等等。下面还要比如说我们的 request accept，一些拦截器等等一些内容。
其实能把这些介绍清楚的话，也能足以说明我们对于 open fan 就是泛刊的配置方式的一个熟悉程度，那么一般做到这些情况对我们的开发逻辑基本上没什么问题，并且我们也加上我们对于 open fun 在解析过程中源码的理解，我们知道为什么可以这样配，为什么这样配置是如何生效的？把这些能介绍清楚的话，通常面试官都可以得到一个满意的答复。


但有可能如果说有些面试官对份他特别熟悉，他在跟你沟通源码细节的过程中一定不要带便宜，应该站在自己的角度，因为对于 phone 的使用，我们最新的版本它是，比如它是 GE load balance，如果说你使用的是早期的版本，它那个负载均衡的策略它是基于 ribbon 的。所以说这些变化的过程我们一定要掌握主动权，站在我们的角度能给面试官一些合理的解释，得到他的认可。关于 open fun 的面试题的深度解析，我们就先介绍到这里。

