---
title: 6-2 Sentinel架构设计-2（1507）
---

# 6-2 Sentinel架构设计-2（1507）

接下来我们来看一下 settlement 核心概念，在这里面我们知道 settlement 最重要的概念就是资源和规则。对于资源的话，我们通常可以通过 settle resource 这个注解来定义资源，也可以通过我们手工编程的胖子定义资源。对于这个资源，为了简化我们的定义资源的一个复杂度，我们这里面是基于 settle 提供的depter，也就是说我们如何定义这些资源。


如果说根据我们不同的 RPC 框架进行 Adapter 的一些适配，就可以定义一个我们的资源，比如说对于像 Web Server Lite，对于 Web Server Lite，我们 Adapter 来定义资源的过程中，它会通过我们的 URL 作为我们的资源。对于 double 的话，它会对于我们方法，也就是我们接口 API 的 RPC 接口的声明作为我们的资源，比如说我们接口的全名称，包括我们对应的参数进行一个 double 的一个资源的一个定义。对于像我们这里面涉及到是 HTP 请求，像 OK HTP，它会把我们请求的URL，也就是我们对应的我们的 host 端口和我们对应的 URL 才能组合成作为我们的一个资源名称，这里面还要对应我们的 strong web v c。


那么 stream i m a C 我们怎么抽象成资源呢？它作为一个服务的提供方，对于我们 request Mapping 的内容，构建成我们对应的一些资源，包括我们这里面 spring close get away，它的原理也是一样的，这是我们资源的定义。


接下来我们看规则，对于我们 settlement 它的规则区分其实很多，比如说首先我们是限流规则，或者说我们的降级规则，以及我们的系统规则，以及还有我们的一些权限相关的一些规则。下面还有我们基于一些 parameter 一个参数类型的规则，对于我们这里面并没有把全部展开去介绍。


对于我们最重要的像流控，也就是说我们限流规则，限流规则通常会涉及到哪些属性这里面，比如像 resource 资源跟这里面是最重要的，对于任何规则一定要选定资源，我对哪个资源进行规则的设定。另外就是说我们对于资源进行一个阈值，比如说我达到一个什么样的阈值进行一些限流的方式？我们基于 grade 一个级别，是基于 QPS 限流还是基于线程限流？大家知道 QPS 和线程在这里面是其实所对应的逻辑是有些区别的。这里面还涉及的像 limit APP，我作为一个服务方，我是限制所有的请求，还是说我只限制某一部分请求，也就是我们部分来源的请求，这是我们的 limit APP。另外还有我们的策略，像 Sanal 提供的策略是在里面涉及到直接链路和关联，我们就不展开细讲了。


下面是我们的一些表现形式，对于我们的控制表现形式，这里面会有直接拒绝、排队等待和慢启动模式。直接拒绝是最容易理解的直接拒绝，也就是说当我们的请求量达到了我们的阈值，就相当于是直接把请求拒绝掉，也就是我们触发限流排队等待。如果说我们涉及到削峰填谷，那么我们的选项一定要选排队等待，也就是我们这一刻请求量特别多的时候，我并不是直接拒绝，我是给他一定的时间让他排队去等待，进行一个流量的消化，这样的话他就没有直接去解释一个排队等待另外一个慢启动模式。这个慢启动模式比较特殊，其实我们可以理解为是个预热，当我们一个新的接口上线以后，并不会直接让它的流量达到一定的一个均匀水平的一个情况，我是慢慢放量，给它一个爬坡的一个感觉，让我们的请求慢慢启动的过程当中也给系统一个预热的一个过程，这样避免我们的流量直接达到某个阈值的时候把我们的系统给压垮。


对于我们这里面降级规则也有一些对应的一些属性，其实跟这里面是类似的，但这里面其实还是有一些区别。接下来看一下核心的一些API。对于settle，其实我们在使用的过程中可能对我们的感知并不明显，因为我们如果说使用了 settle 合理的Adapter，其实对我们程序是无感的，也就是说我们并没有感觉到 settle 在我们编程的过程的存在。所以说对于 setonal 核心的API，我们这里面并没有过多的介绍。


如果说我们手工需要进行一些容纳线数编程的话，我们这里面会涉及到 s p h 和 s p h o 和 s c h u 这样一种用法，这里面的 s p h 很难理解，它对应的就是信号量 sphere 这样一个缩写。其实对于signal，它的开发者对这个也做了一些解释，也是因为一些历史的原因，这里面后面就没有再进行改动。


如果说我们在使用的过程中，我们通过 SPS entry，也就是作为进入的过程中，你就开启了我们整个限流容内策略的开始。这里面对于 SPHO 和 SPIU 它的区别，其实它们的方法内容很相似，只是对于返回值有区别。这里面对于 SPSO 它所有的返回值我们理解为不尔，如果抛异常以后会转成 false or 对于SPSU，它正常的返回值是entry，如果说进行限流以后，它会抛出一个 block exception。


对于这里面还有一个比较关键的信息，就是我们那个 slot chain builder，我们现在应该去了解整个settle，它处理的逻辑都是一些slot，我们可以理解一些插槽，在整个这个链上我们可以插入不同的 slotter 进行不同的业务处理。


默认的 slot 是这里面这几个，它的 slot chain builder 是构建这些 slot 的过程。这里面首先我们待会儿会详细的去介绍每一个 slot 来做的一些内容，但是大家知道我们默认的 settle 的 slot 就是这些就可以了。


接下来我们来看一下真正的处理的一个逻辑，在这里面我们可以看到整个一个请求过程中，其实就是一个请求通过这些 slot 进行一个执行的过程。这里面我们第一个我们会碰到的slot，也就是我们的 NODE select slot，这里面是记录了哪些事情？我们可以看到其实在一个请求链路的过程中，我们请求的方法会很多，不仅仅只会请求一个方法，这里面比如说从入口进来以后，通过 root 它会调用不同的方法这样一个路径，也就是说在调用的过程中它会有很多一个树状的分支，这里面会把这些节点的树结构记录下来，接下来他会统计一下触点结构的构建。


接下来其实最重要的这些其实我们关注的并不多，如果说我们涉及到在限流熔断的过程中，涉及到上下游不同来源的话，关注这里面通常对于我们来更重要的，也就是我们的 static slot，也就是说监控统一的一个slot，这是最重要的。其实我们可以看到这里面是有一个滑动窗口构建的这样一个宿主，其实在滑动窗口里面它会把我们每一个资源，当前的一定时间阶段的一些请求量来记录下来，这里面记录的内容会比较多，这里面我们看到我会记录当前的这个时间点，它会通过每秒钟划分两个窗口，也就是 500 毫秒进行一个窗口的一个记录。这这里面记录首先是一个时间戳，记录当前窗口的时间值，同时会记录当前的平均的RT，这里面我们通过的流量和主端的流量和当前比如说执行过程中抛出异常的量，也就是说这里面会把我们这些原始的这些请求信息在这里面记录下来。


接下来我们会进行一些 parameter flow slot，一些热点参数进行限流，这里面执行的过程中通过不同请求带来的参数进行一些区分，如果说需要的话，我们基于这些规则进行了删留。后面是我们的 system Flora，也就是系统负载相关的一些保护。


其实对于我们在整个执行的流程中最关注的是什么呢？是 flow slot 和我们的 degrade slot，也就是我们的流量，我们限流和熔断降级对于流量控制的限流，这里面其实它最终会用到我们这里面，对于我们可以看到是这里面 exception block of the pass。也就是说当我们通过的流量和我们当前设置限流的阈值进行比较，同一个时间窗口里面我们通过的流量已经大于我们的射击的流量，这会接下来的请求就不会正常通过了，它会进行 block 阻断。


对于我们的熔断降级通常跟什么相关的？跟我们这里面的平均时长， RT 和我们的exception。在我调用下游接口的时候，它的 RT 也就是响应时长变得越来越长，越来越长，达到我一个容忍的极限对它进行熔断。熔断的结果是后面的请求就不会再对它进行发出请求，而直接返回一个错误，这里面是一部分是RT，另一部分我们可以异常数，对于我们请求的时候，下游经常会抛异常数，我们可以通过一分钟的异常数或者说一段时间内的一个异常比例来进行设置我们熔断降级相关的一些内容。
这里面其实我们重点关注的是一个是我们的监控统一一个 static slot，它记录了我们原属的这些信息。另一个就是我们的 flow slot 和我们的 degrade slot，一个是流量控制和我们的熔断降级。接下来我们来看一下我们 settle 在运行过程中它一个初始化的过程，这里面初始化的过程其实单纯基于 settle core 来进行的一些逻辑，并不会包含像 string code 相关的内容。当我们进行初始化的过程，通常我们是通过 SPSU 进行entry，我也就是说当我们的程序执行到这段代码的时候， Sentinel 才会进行初始化。


其实这个初始化的过程中已经比较言辞，我们也可以理解为它是一种懒加载初始化的一个过程，也就是当我们系统启动的过程中 settle 的初始化的内容，这里面不包括 spend cloder 跟 settle 集成的内容，只是包括单纯的 settle call 它启动的过程，这里面它其实是个拦下来的过程，只有当它在执行 SPSU entry 进入的过程中，它才会进行一些逻辑操作。


这里面我们待会去通过源码去对应看一下，会涉及到像 env 这个环境变量会在一个静态代码块里面进行一个 init extrator， do init 也就是进行一个初始化的过程，其实这个初始化的过程 init accurate 注的事情也就是在静态代码块里面进行一些 SPI 信息的加载，加载的内容其实我们在执行 do init 进行一个 service loader，我们可以看到这个名称的话，可以理解为它是一个服务加载，加载的是对应的 init phone，也就是说我们所有配置的 init phone 相关的这些 SPI 的规则它就会加载过来进行一些初始化的执行。


这里面的配置内容也是放到 Meta info 下面，只是 Meta infer 下面，底下指一个 service 对应的一键名称，它会进行一个加载的过程，对于 Meta service 下面它可能会有哪一类的文件？像我们这里面 settleaccount 它当前提供了三种，一个是 init form，也就是说我们在 settle 加载的过程中要做的事情。


另一个是 process slot，也就是当前我们的系统定义了哪些slot，可以在这里面构建。另一个方式就是我们的 slot term builder，也就是说当前我们的系统默认提供的 slot term builder 是如何构建的？也就是说我们选择的内容是什么？当然这里面我们可以自定义替换我们自己实现的 slot chain builder，如果说我们没有去自定义的话，就会使用默认的打到 slot chain builder 以后，它会通过 process loader 去找我们当前配置的这些 slogan 的信息，这就是我们可以理解为这个 signal 初始化的一个过程。前提大家要理解这个其实是跟 spring code 或者 spring boot 都不相关的，纯粹的 settle 的core，它这里面即初始化的一个流程。


接下来我们来看一下 spring Claude 阿里巴巴相关的一些内容。在这里面我们看到对于 spring Claude 阿里巴巴对应的这个模块里面跟 seasonal 相关的，这里面涉及到四个模块。当然这里面像 spring code 阿里巴巴这个工程模块我们已经比较熟悉了，这里面我们重点关注哪些模块会涉及到 spring code 阿里巴巴signal、 spring code SQL breaker settle 以及我们的 spring code 阿里巴巴 settle Gateway，这里面还有 spring code 阿里巴巴 settle data source，其实对于我们来注意重要的，其实我们一看就能明白，肯定是对于这个 starter 对我们来是最重要的。


另外对于像阿里巴巴塞斯katelwin，大家知道这是跟我们 skin Claude kid 为集成提供的一个模块，另一种方式。我们这里面是 circle BREAKER 死机池，是提供了 spring Claude 熔断器的默认的实现，这个也是比较重要。


另外这里面是 spring code 阿里巴巴 settleted source，支持什么呢？其实我们可以不用花太多心思，它也就是说我们在定义 string code 这些配置规则数据源的过程中，我们提供的一些可选项主要会涉及到像ZK，Nicos，阿波罗等等这些我们的一些分布式配置工具，作为我们那个数据源的配置项，后面会涉及到比如像我们只要涉及到 spring code 或 spring boot 相关的一些组件，它都会涉及到我们的自动装配，包括我们初始化的流程和执行流程。


这里面的执行流程我们可以理解为跟原生的 settle 执行流程是类似的，因为最终它会执行到我们对应的 Sloter 初始化流程。对于我们自动装配初始化的流程大家其实已经相对是比较清晰的，其实我们重点去看一下这动装配有哪些内容，我们就可以粗略的知道我们初始化的内容是怎样的。我们先看一下我们 spring Claude 相关的一些自动装配的 auto configuration，这里面我们看到我们分了三块，三块儿跟我们这里面的这些模块儿也是有一个对应关系，唯独这里面的像 settle data source，它里面没有涉及到自动装配的内容，也就是我们现在上面这三块儿对应的内容。


首先我们看 starter 的对应的内容，对应的源码是在我们 speak close， Alibaba signal 对应的 Meta inforce，不用 factories 配置了几个 auto configuration，像 Sentinel Web auto configure 跟这里面是 Web Flex， auto communication 是一个对应的关系，也就是说我们对于我们的 stream Web Mac 进行一个适配，也就是说我们可以对应我们的 request mapping，对应的 URL 进行资源的定义等等。


下面是 settle endpoint auto configuration，这里面我们定义了一个endpoint，可以暴露出一些跟我们 settle 相关的一些信息。下面还有像 settle auto configuration 配置一些原生的一些 settle 相关的内容，主要会涉及到像对于 reset template 这个相关的一些包装，还有像 Sentinel 份 auto configuration。也就是说我们如果使用 fun 作为客户端的时候，跟我们在这种集成过程中是基于这里面的 auto configuration。


下面我们看我们 circle breaker，也就是说我们的熔断器，这里面我们的熔断器也会涉及到两个，一个是 signal circle breaker auto competition，另一个是基于Roactive，当我们看到 Roactive 这个命名的话，我们就可以知道它是基于一个可以理解为它的异步的一种操作方式，这是我们的 circle breaker 方式。


后面我们来看一下这个Gateway，其实主要还是提供了跟 spring code Gateway 集成的一种方式，这里面我们待会去看一下具体情况，所以说从这里面看，对于 Swift start 阿里巴巴 settle 主要是提供了一些限流的功能。


另外这里面对于 circle breaker 主要给我们提供了一个熔断器的一个实现，其实对于熔断器的实现，它其实是既可以用在限流程，也可以用在熔断层。总之它处理的逻辑就是说当我执行我正常请求的过程中，达到一定的阈值，会执行我一个 call back 操作，这就是它正常一个逻辑关于我们三次的架构设计的内容，我们就先介绍到这里，同学们，我们下一章节再见。

