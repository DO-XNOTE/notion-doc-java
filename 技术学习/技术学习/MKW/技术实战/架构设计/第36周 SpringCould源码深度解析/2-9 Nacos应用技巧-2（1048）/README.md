---
title: 2-9 Nacos应用技巧-2（1048）
---

# 2-9 Nacos应用技巧-2（1048）

那我们接下来看一下集成 Gecko s spring boot，那么集成 neck spring boot 跟 neck screen 是一个相对的，它也是在 neck group 下面的一个项目， neck stream boot protect，那么它相关的主要的依赖这里面是。
Nichols configures, remove the starter. Discovery Spring Buddha Starter.


这个在命名上跟我们的 spring two 阿里巴巴是很相似的，我们知道通过这个 start 可以集成 nicos config 功能，集成它通过集成 Nicos 的服务租车已发现的功能。
那我们来也来先看一下这个 nicos brain 布的product，它的项目项目源码是怎样实现的。我们可以看到在这里面我们克隆下来它的这个源码，在这里面它主要是分为三部分，一部分是configure，这里面三个模块，一个是辅助这一趴线，这里面还有一个base，狙击的base，这是一个part。还有一个是我们的一些案例。那么首先我们来看一下 config 的话，这里面我们依赖的也就是 nicos config 中部的starter，那么在 stream 部的 starter 它要依赖的 auto configure 和occur，这里面我们知道对 ACQ ATOR 的实现是它可以暴露一些Endpoint，对于 auto configure 它是来实现我们的一些自动装配的一些内容，那么我们可以看在这里面去依赖的过程。


在这里面 source 下面它配置了我们的 spring proper factories，我们基于 spring factories 的配置，我们知道对于它最主要的 auto configure，也基于是 Nicos configure auto configuration 这样去完成的。


对于 accuator 它其实提供一些Endpoint，可以让我们去辅助了解我们整个系统运行的一些信息，这里面我们可以看到是 Gecko configure endpoint auto configuration，其次也对应用缀里面的 endpoint 的一些信息，也就是通过这里面我们可以暴露出一些URL，可以进行一些访问。下面的对于服务发现它的逻辑也是一样的，这里面是 accuator 提供的一些Endpoint，来供我们去了解服务的一些信息。


还有这里面是 auto configure，对于我们这些 b 进行一个自动装备，主要是这些对于 starter 的话，这里面的实现逻辑， starter 它并没有去实现什么源码starter，它只是完成了一个 palm 文件的一个组装，也就是说在这里面它做一个间接的一个依赖。


这里面我们看到这个start，它依赖到这里面 nicos 不用context，这里面是 Nicos Corey spinboot auto configure，也就是它自动装配的内容。这里面还有 Nicos spring boot base，也就是对于这个 starter 它进行一些依赖的组装。那我们接下来看一下我们的用车的 study 工程里面，这里面我们建了一个 Nicos spring boot 这样一个项目模块。对于这个项目模块，我们先看到我们依赖了哪些内容。对于这个我们这里面依赖了 nicos discovery spring boot STARTER 和对应的 nicos config 中部的start。也就是说我们基于这两个 starting 引入了 Nicos 它的服务注册与发现，以及它的动态配置。


下面内容也就是我们常规的一些引入的一些信息项，那么这里面我们看到对于 application YML 这里面配置的信息上面我们就不用过度解释。这里面我们注意一下它对应的配置的这个 neck 的服务是 neck OS config server a d 点，也就是说这个跟我们在使用 spring closer 的方式的话，它是有一些不一样的，这里面我们直接是 nicos 作为前缀，对于我们在 string boot 这里面的话，它都是通过 string code 作为前缀，我们可以看到 string code 作为前缀，像后面是 nicos config s server 地址这样一个过程。那么我们再看一下对于 nicos application 它还配了哪些信息。


如果说我们基于 string 步骤的方式去依赖 nicos 的话，我们看这里面除了 string 部的vacation，它默认这个注解启动器，另外就是 nicos property source，在这里面我们约定了我们对于需要依赖的这个配置文件user，那么另外我们看跟我们的 spring 这个工程相比的话，这里面少配置了一个叫我们可以从这里面看一下，这边少配了一个 enable nicos configure，也就是说这个 nicos config 开关并没有配置。


我知道我们知道通过 stream boot 的话，它可以自动的去通过引领 auto configure 自动开启这个动态配置，所以说这里面我们就没有再配那个了，所以说这里面我们配上 my user，那么对于它所依赖的服务地址会通过我们这种方式去完成。


那么对于 next Controller 它实现的逻辑跟我们上面实现逻辑是很相似的，大家可以简单的去模拟去验证一下就可以了，这里面我们就不去再启动对于细节的一些验证了，接下来我们看一下使用 Gecko 的动态配置的实例，那么我们这里面我们如果说能让它实现动态的配置，那么我们 Gecko 必须支持一个动态刷新，那么这个动态刷新需要依赖什么呢？依赖我们对于 string Claude 我们定义的 refresh scope，基于这样一个 scope 它才能实现一个动态刷新的效果。


在这里面其实 spring Claude 它对这些整个配置进行一个高度的抽象，其实我们在使用 spring Claude 的过程中，我们根本不关心它底层的实现是 Nicos 还是其他的一些动态配置，所以说我们来看一下这个动态配置动态刷新的一个效果，在这里面我们还是切到我们对应的 next config 这样一个工程里面，在这里面我们是做几个对应的。
比较像我们这里面，对于不管是我们用 at value 这样的方式直接引入一个属性标明这个username，或者说另一种方案是我们把它引入到一个 bin 对象里面，我们通过 at value 注入一个属性，那么我们把 nicos configure 注成了一个bin，在这里面我们注释NB，那么我们用这种方式它都可以获取到对应的我们这个属性配置文件。那我们在 control 里面，这里面我们可以看到对它做了三种实现的方式。


首先我们去获取到我们通过 at value 注入的这个属性，另一种方式我们通过我们的上下文环境 environment 会获取到优势内幕这个属性。另一种方式，我们通过 Nike config 获取到对应这个 Nike config 的属性，我们获取这种用三种不同的方式获取到同一个对象的值，那么我们来去验证一下哪种情况的配置这个值是动态刷新的，哪种情况值它只是从原来获取一下，没有动态刷新。


那么如果说我们为了去验证它动态刷新的话，我们应该要具有一定的区分度，那么如果说我们首先现在用这种方式去执行的话，我们看到我们的 add refresh scope 都没有去执行，那么所以说这种情况下我们去先验证一下它在默认情况下是不会执行动态刷新的。


你想有没有拍上动态性？我们的方子怎么样？我们先访问这个control，我们看一下这三个对应不同对应词反馈的内容，那么我们同时修改对应 user name 的配置，那么我们再刷新看它有没有发生变化，那么好，这里面我们来看一下。首先我们来刷新，我们看到它的值是一样的，因为现在没有刷生什么变化，那么我们在这里面去修改它，那么我们把这里面的 C5 呢？我们把它修改成C6，那么我们相当于是提交发布，好，它有一定的言辞，配置完成，那么我接下来我们进行一个刷新看一下，OK，我们执行刷新以后，我们发现这三个属性里面只有一个发生变化，也就是我们只有从上下文获取的这个内容，它发生变化变成C6，那么我们不管是用 Nike config 这个对象里面获取的属性，还是我们 control 这个类里面获取的属性，它都没有发生变化。


那么这种情况下我们怎么处理？那么我们为了让它能动态发生变化，那么我们要做的事情是在这里面把这个 refresh scope 打开，让我们这个 control 变成一个 refresh scope，但 scope 由原来的单例 scope 变成我们一个支持刷新的scope，那么在这种情况下，我们需要对于我们的服务进行一个重启，我们重启完成以后看一下我们在这里面打开 repress scope 以后，看是不是这个属性对应的 user name 它也能做到一个动态的刷新。好，我们现在看现在应该是准启动完成，那么好我们现在再进行一个刷新，这会因为我们系统进行了重启，那么现在它的属性配置会统一恢复到C6，那么接下来我们要把 C6 改成C7，好，我们等它修改的状态返回好修改完成。那么我们在这里面去刷一下，看一下效果。


在这里面我刷新完成以后，发现我们的属性配置文件和我们的上下文环境变量都已经发生变化了，唯有我们使用 nicos configure 这种方式，它并没有发生变化。那么如果说我们想让 nicos 这个对象也发生变化，我们应该怎么做？那么我们来看一下这里面对于这个 refresh scope，它可以在两个地方去配置，一个是类型，另一个是方法，那么我们对于在这里面我们希望对 nicos config 进行一个配置，那我们是应该在类型上进行配置，还是应该在我们对应的这个 bin 的方法上配置？这个我们去想一下它是需要一个动态刷新的引用，那么如果说我们在这里面去配置的话，其实 spring 容器是并不知道它需要动态刷新的，所以说我们在这里面配置方式，我们应该是对应这个闭音的方法上，我们在这里面去定义一下 refresh scope，那么我们进行配置完成以后，我们再进行一个重启，我们进行一个验证。


好应该很快能启动完成，可以启动完成，那么我们在这里面再去刷新一下更新，现在已经都同步到C7，那么我们直接里面把它修改成C8，看一眼效果，好，修改完成。那么我们来我们在这里面再去看一下我们修改的效果，我们发现现在所有三个它都已经发生变化了，也就是说我们通过三种方式来获取的这个动态属性，它都在进行一个动态的一个更新。好，那么在这里面我们跟大家演示一个动态更新的效果就完成了。我们可以看到在我们使用 spring code 阿里巴巴的这种方式集成 NICOS Configure，整个我们的程序里面并没有体现 Nicos 相关的一些任何的依赖，也就是我们程序里面并没有直接依赖 Nicos 相关的内容。所以说我们在用这种方式去写代码的话，其实我们只依赖 spring code 它对应的一个抽象，并没有依赖 Nicos 具体的一些实现，这样跟我们后期切换不同的这些动态服务，注册与发现，或者 Nicos 的配置的话，或者其他的 in 配置的话，给我们提供了一个可能。好了，同学们， Nike 的英文技巧我们就先介绍到这里，同学们，我们下一节再见。

