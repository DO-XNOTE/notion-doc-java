---
title: 6-4 Sentinel核心源码解析-执行流程-1（1306）
---

# 6-4 Sentinel核心源码解析-执行流程-1（1306）

关于 settle 的执行流程，我们先看一下这里面程序模块里面的单元测试。首先我们来看一下对于我们的 settle store test，这里面刚才我们对于 test settle init，我们在跟的过程中，我们并没有通过 debug 的方式去运行，那么这里面我们先对于执行的过程进行一个测试，比如说我们这里面是 test flow ruler，也就是说如果说我们进行流控限制的话，整个执行的逻辑是怎样的？当我们在执行的过程中，我们通过单元测试，首先是单线程的去执行我们这个过程，首先我们需要初始化我们的流控规则，这里面我们看一下初始化流控规则的过程中，我们是在这里面我们先定义一个 flow ruler，一个列表，在这个列表里面我们添加了一条规则记录，这个规则记录定义的逻辑我们可以看起来相对比较简单，我们可以非常容易的理解。


首先我们定义那个group，先指定一下这个 Ruler 是为哪个资源定义的，这里面定义的资源就是对应的是 resource hello world，对于 hello world 这个资源，我们进行一个规则的定义，但其实我们只要理解它是个字符串就好，我们可以看一下在这里面执行的过程中， in tree 你就要指定跟这个字符串，只要它能对应上，那么就说明这个规则是匹配上的。


这里面我们看到在的greater，这个 greater 使用的是QPS，为什么我们使用QPS？ QPS 也是我们在业务指标里面经常使用到的一个指标，当然我们用单线程去验证的过程，我们只能选择按QPS。另外这里面设置的阈值，对于 QPS 的阈值是多少，我们这里设置是5，也就是说每秒钟我们这个请求的 QPS 不能超过5，超过 5 以后，后面就是处于一个 block 主段的一个状态，我们把这个规则，也就是说这个规则定义完成以后，把它放到我们的规则列表里面，同时进行一个规则的初始化。
这里面我们看到是 flow ruler manager load Ruler，其实这个 load rule 的过程，也就是把它上放到一个缓存列表里面，我们可以通过这个 k 找到规则列表，也就能完成我们当前的任务，这是我们初始化规则的过程，那么我们接下来看一下后面的逻辑。


初始化完规则以后，我们这里面是定义了一个循环，在这个循环里面执行我们的 same OKR， same OKR 我们的内容我们刚才已经注意到，我们是一个 s p s u entry 获得一个殷迟对象，这个里面我们去 cats 我们的 block excepson。


这里面可能有同学比较疑惑，为什么我们的 entry 并没有在 final 里面手工的关闭？这里面有一点跟大家去说一下，可以理解为是 JDK 6 后面支持的一些特性，它是我们可以看到在这里面我们对于entry，它其实实现了一个 auto global 的接口，对于 auto global 接口它有一个 close 操作对应，这里面我们可以看一下这里面的 close 操作，它做的 close 操作时间的内容是什么？这个 close 操作时间内容也就是退出，其实我们原来是手工调的一个退出，是通过 close 间接的去调用。


close 操作，其实可以理解为当我们 try 执行完成以后，对应的 finally 操作会对这个资源进行一个 close 操作，可以理解为是我们 Java 语法里面所天然支持的，这样的话让我们代码更简洁，这样就是我们这个 CMO 执行的过程，这样我们理解如果说我们没有处于限流，我们可以 try 得到对应的 entry 进行我们的 log info pause，也就是打印出我们这个日志输出。如果说它发生流控，也就是说进行流量达到一次限制的时候，那么就会打印lock， info block it。当然这里面我们打印了一个阻断以后，我会让它嗯睡眠一段时间，这样避免我们这个 block 进行一个死循环的一个打印。


再看一下这里面我会有一个时间差，这里面一个时间差是什么呢？我会让这个程序执行 5 秒钟，也就是在 5 秒钟的话执行，看一下这样的结果，大家看到这样的话就是 5 秒钟以后，它就会自动的就退出循环值执行完成。这里面也是如果说它能执行通过的话，它会打印POS，如果说它发生阻断的话会打印blog，因为这里面每一次 blog 它会sleep，也就是说 500 毫秒，也就是 500 毫秒，就是说一秒钟如果它 sleep 一次最多他 sleep 两次，他就相当于是可以跳出这个当前这个一秒钟这个窗口。


那么我们现在去执行一下，我们看一下他的日志。结果我们来进行一次一些分析，我们现在执行，因为它需要执行 5 秒钟，我们看一下的结果，现在执行完成了，执行完成，我们看一下的结果。首先是我们在执行的过程，我们可以看到我们是通过这里面 test flow ruler，首先初始化规则的过程，我们没有打印什么日志，这里我们就忽略。接下来我们去执行 same work， same work 我们可以看到在这里面是我们真正执行我们业务的日志。


首先是pass，这里面我们可以看到12345，OK，我们通过 5 个请求以后，下面是block，也就是主段它阻断的时候，我们可以看到如果说我们阻断没有这个休眠的话，它会通过死循环的方式快速去我们一个 block 会输出很大量的一个 block 的内容。那么我们这里面如果碰到block，会让他是休息 500 毫秒，如果说他休息 500 毫秒还在当前这一秒钟的时间窗口的话，他还会趋于继续的一个 block 状态，继续 blog 上的如果说连续两个 500 毫秒的休眠的话，它就已经跳出当前这一秒钟，这个时间窗口也就到了另一个第二个时间窗口，那么到第二个时间窗口它这个阻断的就已经放开了，也就是说因为在时间窗口里面它这个请求为0，通过的请求为0。


接下来应该要一个通过的过程。比如我们看到这里其实就是12345，在新的一秒钟的一个窗口里面通过了 5 个请求，通过完这 5 个请求，接下来它应该又要进行一个 block 状态，以此类推。我们可以这样去理解这样一个过程，其实它一个这样休眠的过程，也就是说每当它通过的请求达到阈值处以后，它就去是一个阻断状态，阻断完成以后，就是在阻断的过程中，它跳到新的一个磁线窗口，它的阻断自然就放开了。我们可以理解为时间窗口它在执行的一个过程，这样我们可以看到每次我们程序执行的过程中，刚好它是通过 5 个请求以后会阻塞两个block，两个 block 刚好跳到新的一个私信窗口，这样我们就理解了我们这个流控的一个逻辑是怎样的。那么我们在这里面理解完这个，这是一个单元测试以后看另一个单元测试，这个会更复杂一些。当然这个单元测试我是参考我们在这种官方的一个 demo 去简单改造了一下而成的。


那么我们先来看一下实现的过程，在这里面我们去简单介绍一下代码，这里面是执行的过程，是我们会在一个模仿一个多线程的环境去执行我们的一个资源的请求，请求的过程中我们每一秒钟我们去获取一下当前执行资源的一些状态，我们可以看到这里面我们订阅了像pass， block total，我们这个资源通过了多少个请求，阻塞了多少个请求？通过的总请求是多少？这里面我们会标记一下我们当前我们的线程数，指定三个线程数据执行，另外这里面会让它执行一定的时长，里面时间定的时间比较长，我们定的是 100 秒，这是我们的通过闷方法去执行，因为我们这里面会涉及到一个多线程的环境，使用单元测试的话会比较麻烦，这里应该使用 main 方法去执行。


main 方法执行的过程我们可以看，首先会初始化我们的流控的规则，流控规则初始化跟刚才的逻辑没有太大的区别，我们在里面简单去看一下，这样我们把规则初始化完成，这里面注意一下我们的每一秒的请求阈值是20，这里面对应的资源是k，我们把这个 k 公共抽象出来了，标明成一个我们的一个静态的一个常量。
接下来我们来看一下这是台词执行，在执行的过程中我们做的是什么事？我们是启动一个定磁器，这个定磁器是另一个线程机执行，我们看一下定磁器的逻辑，定磁器它做的事情，也就是我们可以看到它是每执行一次休息一秒钟，接着进行一个循环的去自强。在执行的过程中我们去获取到 total pass 和 block to 对应的这个数值，我们在这里面把数值打印出来，我们可以感受到当前这个状态对应数值的情况是怎样的？我们相当于是每秒钟的一个定时器去执行一些对应的内容，那么我们可以通过这个定时器来看到每秒钟执行的情况。


后面我们来看一下接下来是什么呢？这开始去模拟我们的流量，那我们看一下模拟流量的工作是怎样的？这里面我们可以看到它会同时会把我们整个线程启动起来，进行一个 star 的一个运行。我们看一下这里面我们会指定不同的一个线程名称，我们看下 rain task 注的事儿是什么？这里面我们看到它在启动的过程也是一个物料循环，通过这个 where 循环对我们资源进行请求，我们可以看到这是 SPSU entry 这个k，也就是说进入资源这里面如果说进入资源成功以后，我们这里面可以看到 pass 是 add and get，也就是说得到一个计数器加一。同时对于如果说触发了 block exception，那么我们这个 block 进行一个计数器的加1，这里面我们看到后面我们还有一个total，也就是在 final 里面一个 total 的计数器的加一。


同时我们看到如果说我们的 entry 不等于 null 的话，把我们当前 in tree 退出，这是我们这样一个执行的过程，如果这样执行的话，它可能会在一个 word 循环里面 32 个线程拼命的去跑，但是我们为了模拟的更相像一些，下面我们会对于这个里面线程进行一个短暂的休眠，这个短暂的休眠是基于一个水基数进行一个在 100 毫秒以内的一个休眠的一个时间，这样的话就可以模拟它这个执行的过程。


那么好，那现在呢？我们可以先执行一下程序，看一下它的表现是怎样的，这样我们通过我们的表现再去分析我们流入这样的过程。那么我们现在先执行，因为它执行的时间比较长，我们不会等它 100 毫秒执行完成，我们会把它给中断掉，我们去分析它的一些记录。那么现在执行这个时间段我们可以具有一定的一个参考性，我们可以现在终止我们的执行，我们可以看一些内容。


首先这里面是 began to do flow control，也就是开始我们的流控，嗯，这里面我们知道每个请求应该只有 20 个请求。通过现在我们可以看到，因为这里面有个秒的倒计时，这里面同 100 毫秒新的QPS，我们可以看到这里面是580。第一个我们是 total 是580，但是 pause 是52，这里面好像跟我们预期的不一样，但我们第一个请求我们先忽略它，不要对它有太多的考虑。那么我们看第二个，也就是说我们在99，也就是说在 90 秒，就是第 100 秒99，它是一个倒计的一个过程。后面我们看9897，下面这里面的pass，它都是对应的是20，也就是说后面它可以精确地控制到只有 20 个请求。通过我们可以看到这里面会有对应的一个 total 的数量和 block 数量，这里面为什么我们看到对应的请求每一次它可能通过的不一样？因为我们在这个循环的过程中会有一个随机的一个休眠的一个过程，这个休眠是小 100 毫秒的一个随机数，所以说它每次可能在每一个线程的整个执行的那个数是不一样的，就比方641、 625 629 等等一个过程最终我们要关注的是每次 pass 通过的内容，它都是20。
另一个我们可以看到这个里面有个计数器，这个计数器我们是相当于是每秒钟它更新一次，这里面我们看到它其实更新的过程中，我们看这里面是有 373839 这样一个加的过程，为什么我们能看到这样一个顺序的一个过程呢？这里面跟大家简单说一下，这里面有一个小小的一个技巧，也就是我们在执行 TIMER task 的时候，我们这里面会输出我们的时间，这里面时间是通过 time unit 去 control 的 time 获取到我们那个毫秒数，在这里面获取的毫秒数我们对于除以 1000 也就是得到一个相对于秒级的一个近似单位，这里面它也做了一些特殊的处理。


我们在获取 concurrent time 命令，其实并不是获取的 system concurrent time，也就是并不是我们获取的系统的毫秒时间，因为系统的毫秒时间它其实执行的过程也会有一定的消耗，但是它这个 time 悠久方法，这里面实现的逻辑就是说我们可以看到它每一毫秒休眠一下，他把当前的这个值获取出来，也就是说在每一毫秒区间内获取的值都是不变的。这样的话可以，如果说在毫秒之间重合的情况下，我们可以获取到一个相同的值，避免我们真正执行 system content time meanings 地进行一个时间的消耗。

