---
title: 1-8 服务流控降级（1119）
---

# 1-8 服务流控降级（1119）

里面我们怎么去启动 settle dashboard 控制台实例给大家简单看一下，首先我们如果说部署 settle dashboard，我们先需要通过 Docker 把我们这个镜像吐下来，然后我们通过这样的命令去启动。这里面大家注意一下，我们 settlebot 它本身默认的端口是8080，基于这个镜像它的端口是8858，所以说这个里面映射关系我们要改一下。把这个启动完成以后，我们接着需要通过浏览器访问就可以了。


那我们看一下 settle 模块的这个项目工程，在这里我们可以看到我们是新建了一个 settlement 模块，这个猜测模块它的代码实现跟这里面的 Gateway 模块是非常相似的，只是它相对 Gateway 的引入了 settle 相关的一些内容，这里面我们可以看到对应我们 Gateway 的 application 内容，跟我们 settle Gateway application 它的代码结构是一致的。不同点是我们先看一下 palm 引用，那么在 palm 引用里面，也就是说 settle 这个模块相对于 get way 添加了跟 settle 相关的内容。


我们看一下 settle 相关的内容是有哪些？这里面有是 spring Claude starter、阿里巴巴settle，也就是说这是跟 settle 相关的starter。大家注意一下这里面的 group ID 是，我们看是阿里巴巴Claude，其他的我们看一下，这里面是 spring framework Claude，所以说注意一下阿里巴巴提供的内容跟 spring framework 默认提供的内容是有一些区别的。


嗯，同时我们看除了这个 start 阿里巴signal，我们这里面还引入一个叫 spring 斯Claude，阿里巴巴 settle Gateway，也就是说在阿里巴巴 Claude 里面，它特意的针对 spring Claude Gateway 做了一个 settle 的适配，这个 SPA 就是对应的 settle Gateway 这样一个过程。那么如果说我们需要通过 negos 配置我们的持久化规则，我们还需要引入 center data source negos 这样一个依赖，那么通过它的话，我们可以在 negos 里面配置依赖规则，程序里面可以去读到它的规则进行引用。那么好，这是我们简单的这个 palm 文件，那么我们来看一下它的一些配置信息有哪些变化。


在震动看一下这个 Applix YML 相比 Gatev 来说，它多的内容就是这里面三种相关的节点，我们可以看到 settle 相关的节点的它的内容。这里面配置首先是 transport dashboard，它的作用是什么呢？也就是说我们这个 setting 这个工程，它作为一个客户端，也就是这个客户端它要需要向我们这个 settle dashboard 汇报信息，他们汇报信息的地址就是 settle 我们指定的这样一个URL，待会我们可以访问一下UR，看一下这个在脑袋 boss 的效果。


这里面其实我们可以设置一个心跳的时间间隔，这里面我们是定义的是 1 秒，后面还有一个是 data source，也就说这个 data source 是做什么用的？这个字号是我们基于网关的在配置这个哨兵的流控规则，它流控规则在哪配？是在对应的我们这个 Nicos 上面配置，对应一个叫配置文件，里面是 JSON 配的文件，它是基于 JSON 格子配的是降级规则这样一个过程。当然我们现在是先不考虑通过 nicos 配置规则，我们先让大家去感受一下通过 settle 流控的一个效果。同时我们还在 provider 里面也做了一些改造，我们只是重点看一下 pool 文件，在 nicos 里面也引入了跟 settle 相关的内容，这里面我们引入的是 spring Claude start、阿里巴巴settle，还有 settle deep sort、 nicos 这两个依赖。相比我们这个 settle 对应网关的依赖，它少了一 settle Gateway 这样一个依赖，也就是说我们通过 settle 保护我们的系统，既可以在网关层面保护，也可以在我们普通服务层进行保护。


大家了解完这一点以后，我们现在看一下三锁控制台的效果，在这里面我们可以看到这是我们登录完三层控制台的效果。如果说我们刚启动完成，以我们登录它的登录界面是这样一个登录界面先退出，在这里面我们使用 settle 和settle，用户名密码默认都是settle。


登录完成以后，在这里面可以看到注册在我们 settle 控制台的这些应用列表，其实这个应用列表跟我们在 Nicos 注册的服务列表是比较相近的。在这里面我们可以看到现在启动的服务列表有 nicos consumer、 Nicos provider 和 signal Gateway。


Nicos provider 和 settle Gateway 分别注入了我们的 settle dashboard，我们可以从这里面看一下这里面 next provide 和 settle gateway settle dashboard，它相当于是把自己本身注册到这个 settlement 控制台上面了，我们也可以看一下它这里面的一些实时监控，这里面就是机器注册的时候一些监控效果。


那么看到这样，我们可以打开我们的 nicos provided，我们看一下它的这些菜单都有什么，这里面菜单分别是实时监控，就是对应这样一个实时监控效果，这里面有触点链路，也就是说我们的触点链路。我们可以看到当我们在访问 ECSO 执行一个参数的时候，它会把整个这个流程的一个参数效果给我们传递过来，下面就是说我们需要配置流控规则，因为我们现在还没配，这里面降级规则、热点规则、系统规则、授权规则等等，下面还要支持集群流控，那么集群流控是需要一个 token Server，也就是因为集群流控它需要整体的对我们多个实例进行计数，它是需要一个 Server 能进行一个集中的一个计数。同时这里面有机器列表，我们可以看到，在这里面我们看到对应的机器列表，这里面健康状态是健康当前 4 天这里面还有它的一些实例总数，健康和它的一些失联效果。


这样的话我们来看一下，现在我们在实时监控，我们看不到什么监控的信息，那么我们可以通过请求来把这些监控信息的指标打出来，我们怎么打出来呢？可以看到这里面有两个页面，一个是 580818081 是对应我们的 provided 这样一个服务，那么我们这里面是 80218021 是对应我们网关的服务，我们在这里面访问 8021 的时候，请求会同时请求到 8081 上面，可以在这里面去请求，这样我们能看到整个流量变化的效果，就在这里面直接刷新就行，因为我们没必要照太多的流量通过这个变化去看一下塞塞托监控的效果。


好，那么我们现在切换到塞塞托控制台，稍等一下，因为它这个同步数据是有一点延迟，但是这个延迟还是秒机的，这样我们看到它已经监控已经出来了，在这里面我们看到的实体监控是对应 ECSO palm， palm 它这个请求，那这样是它的请求的流量。也就是说在这一分钟之内，我们看一下每一秒它的一个请求的 QPS 是多少，是我们刚才刷新的。


这里面我们看一下对于 settle 盖的位，同样我们也能看到这样一个请求，在这里面看到这个请求，它的资源 ID 字prodit，它用这种方式去请求这些数据。现在我们能看到当我们在做我们的一些请求的时候，因为赛特诺它对于资源进行拦截，进行收集，它已经采集到了我们请求的 QPS 相关的一些信息。它采集到 QPS 信息以后，那我们现在要做的事情是什么呢？我们去配置规则，配置规则让系统能触发限流，看一下限流的表现是怎样的，我们在这里面去配置流控规则，我们可以新政网关流控规则，我们先，我们可以在这里，嗯推出请求链路，在这里面我们去添加这个流控规则。这里面指定 provided 阈值类型，我们针对QPS，它的 QPS 值是我们可以限制为5。


接下来我们看间隔，这里面默认是秒的，当然我们是人为制造的流量，肯定没有线上真正流量那么高，所以说我们把这个秒改成分钟，这样我们可以更容易地触发这个流控的方式。我们这里面选的是快速失败，那么对于搜索类的业务的，我们快速失败是没什么问题的。那么对于交易类的业务，推荐大家去选匀速排队，因为交易我们不容易展示好，我们添加上这个规则，也看到这个规则的效果，可以进行一些编辑或删除。添加完这个规则以后，接下来我们要做的事情是什么呢？再去模拟我们的流量，在这里面我们模拟流量还是通过刷新的方式快速的去模拟。我们可以看到现在它已经触发流控了，这里面是 block ID by Sentinel，这里面是 parameter flow exception，现在的话我们已经触发流控。


既然触发流控，我们还可以接着请求的效果是怎样的？我可以看到settle，这里面我们先看这个四次监控，这里面我们看到监控信息也有两条线，这是通过的QPS，这是拒绝的QPS，因为它已经触发限流了，我们后面的这些请求它都处于一个拒绝的状态。


那么我们 provide it 这里面这个监控进行一个比较，这里面监控的比较效果我们看只能看到通过的请求，也就是说通过网关请求的过程中，因为我们在网关配的限流规则，直接在网关城就把这些请求拦截到了，所以说这些请求它并没有达到我们的 PRD 的成。


通常我们在做一些外部的请求拦截的时候，只要在网关上做完限流整体就可以做到相对比较好的一个效果。但也有一些情况是我们内部系统它可能还会被其他的一些不同的系统调用，所以说我们在这个内部的这些系统进行限流也是有必要的，当然它并没有我们的网关城这个更严格一些，现在我们体会到了流控的效果，我们在想一些问题，其实我们在使用 Setton 这个开源的scenario，本身它是比较原生的，它有些功能还需要我们进行一些二次开发。


这里面举个例子，比如我们的流控规则，那么对于流控规则来说，我们在这里面配置这个规则，它的数据存储在什么地方？大家有没有想过，其实默认它的数据应该是内存级的数据，它在表单把整个我们的数据信息相当于是存储在控制台，同时把这个数据推送到我们的 client 端，当控制台和 client 端同时重启以后，那么这个规则就丢失了。


这样肯定不是我们期望的，我们是希望 bug 这些流控规则持久化起来，持久化的一个方案，我们可以去自己实现对应 settle 的 data source， settle 本身也提供了一些实现，比如说它可以提供基于 nicos 进行一些实现。我们这里面看的是 nicos 的服务列表，看一下 next cost 的配置管理，在配置列表里面，这里面我们定义了一个规则，在这个规则我们可以看一下，这是 session 的规则，对于我们看一下它的详细内容，定了一个数组，现在有一个对象，这个对象它的针对内容是resource。


我们看 resource 针对 consumer ECSO test APP，也就是说当我们访问这个 URL 的时候，它会进行这样一个流控的一个进行处理，基于这个流控处理，它就可以去减轻规矩的效果，我们知道 nicos 的配置可以做到动态更新，所以说我们可以基于 nicos 去配置这些规则。


但还有一些问题就是说我们发现我们基于这种方式去配置规则，其实是没有通过控制台的，这种方式比较直观的，在这里面更直观一些。所以说我们还是希望能够通过控制台表单的方式去创建规则，并且要把规则实例化、持久化到我们相应的数据文件里面，不管是我们的 MySQL 数据库或者是动态配置文件都可以。大家看到这样一个问题以后，我们可以知道默认的 nicos 它可以去做样这个配置的一种方式，但是当然这种配置方其实并不是很友好。那么关于 Satin 我们先介绍到这里，下面会详细的去介绍一下各个组件的一些使用。

