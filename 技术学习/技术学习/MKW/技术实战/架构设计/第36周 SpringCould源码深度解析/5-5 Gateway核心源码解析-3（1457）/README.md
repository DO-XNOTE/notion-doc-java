---
title: 5-5 Gateway核心源码解析-3（1457）
---

# 5-5 Gateway核心源码解析-3（1457）

好，那么我们在这里面去处理这个 handler 过程，其实它的逻辑我们可以从这里面去简单去看一下，在这里面也是通过我们 exchange 里面找到我们的请求快速请求的信息，找到请求信息以后进行一些处理。那么其实这里面比较重要的一点就是说他找到请求信息以后，会得到我们对应的一个 root 相关的信息，那么这里面会有一些我们可以理解为 practice 的一个断言的一些校验，那么满只有满足这些断言的情况下，它才可以去执行进来，那么这里我们可以到了它，嗯， road predicate handle mapping 注意中的像 cup router，也就是获取到我们这个路由的过程。


在这里面我们可以看到对应的 root locator 它的一些内部结构，我们当前这个 root locator，它就是 cache root locator，它通过的第一层代理delegate，也就是 compose 的 root locate，那么我们可以从里面再展开看一下这个 comport 组合的里面，它会涉及到一个delegate，一个集合。对，这个集合里面它涉及到两个对象，这里面分别是 road locator builder，也就是我们刚才手工构造的。


另一个是 root definition road locator，它是通过我们的一些 root 一些描述文件去加载的，这里面描述文件可以是我们的配置 property 属性文件，也可以是我们一些动态的一些刷新的配置文件，那么这里面是我们的 road located 的对象的构造，那么我们可以看在这里面处理的过程中会涉及到一个对于一个 contact map 的一个转换，我们获取所有的这些 root locators 来进行通过 root 进行处理处理的过程，它的逻辑是这样逻辑我们可以首先可以看到 filter 问，也就是说当执行的过程中它会把所有的这些 Router 进行一个迭代。那么迭代的逻辑就是说它去查找这个 Router 对应的 party kid，也就是它的断言，去断言跟我们当前的这个请求的对象 Server Web extends 的对象。


这里面我们可能更多的关心的主要它还request，像 Server Web extends 里面 request 对象的一个情况，基于它 request 的一些情况，我们在这里面去判断断言是否满足成立？如果说断言成立的话，那么我们就把当前的这个 Router 再去返回过去，这里面我们可以看到对于这个 predicate 的一些处理的，我们这里面对于 Router 我们找到当前 Router 相关的一些内容，这里面主要还是 a sync predicate 这样一个信息，那么在这里面它如果断言匹配成功，那么就获取到我们的正确的一个结果。


好，那我们现在我们执行完成，后面我们对 root 进行一些 VDC 的一些校验，进行一些处理。好，我们进行下一步断掉，这里面去执行我们的一些处理好，这里面是在执行一个 invoker handler 过程一样，也就是说我们这会已经获取到了我们的 handler 对象，那么获取到我们 handler 对象以后进行一个处理，那么从这里面我们可以看到我们当前的 handler 对象的内容是什么？这里面我们看到我们的 handler 对象，也就是我们的刚才一直提到的我们必应的容器就是 filtering Web handler，那么 filter Web handler 其实这里面是我们需要关心的是这个filtering，也就是说我们基于过滤器的 Web Handler，那么它需要通过哪个 handle Adapter 进行处理？那么在这里面我们可以看到它的 handle Adapter 是也比较也是一个集合，它里面塞的也就是有四个磁线，那么我可以看一下那里面的实现是什么？这里面我们看到的分别是 map socket 的 handle Adapter 以及 request Mapping handle Adapter，这个 request Mapping Handler 也就是我们默认的webpack，他请求的对象还有是 Handler FACTS，也就是说我们基于嗯函数式编程的一些 e Adapter。


那么另一个就是 simple handle Adapter，我们刚才也介绍到我们对于网关的请求，它最终是通过 simple hand 丹来进行一些执行。那么我们这里面其实可以去跟一下迭代一看一下它的一个过程。我们之前我们可以看到当前获取的我们的 handler Adapter Server socket，那么肯定它这里面 support 是不支持的，那么跳过第二步，我们可以看到它是 request Mapping handler，那么也不支持，那么第三步，我们这里面是 handler function，也是不支持的。


那么第四步，现在我们到了 simple handle Adapter，那么它就可以进入进来，那么我们可以通过根入进来，可以看到对于这里面是 simple handle Adapter，它处理的逻辑其实比较简单，我们可以理解为它只是做了一层代理的适配，那么真正处理的逻辑还是我们 Web Handler 进行处理的，我们会把 handler 进行一个 handler 处理，那么我们跟进去跟进信息在这里面我们可以看到它首先去通过我们的 exchange 的请求获取到我们 request 请求对象，那么找到我们的Router。


也就是说我们的我们当前的 Router 信息也是还是存在它的一个变量里面，那么我们得到 Router 以后，我们做的事情是把 Router 相关的 filter 获取出来，那么这里面是跟 Router 紧密相关的，在这里面我们可以看到这里面跟 filter 相关的内容，我们也就是说跟当前这个路由指定相关的filter，它只有一个，我们看这一个 filter 是什么内容？这个 filter 是一个 Audit Gateway filter，我们刚才看到了我们这些 Gateway filter 都会经过这个 Audit 的，也就是基于一个排序的一个包装，那么它这里面的具体的实现内容，我们可以看到它其实是对于一个 lambda 表达式的一个主状表达式，具体的实现也就是我们在构建这个，我们可以从这边看到我们其实在构建这个路由的过程中，我们这里面定义的 filter 是 strip 的prepx，它其实它作用是什么呢？当我们在请求的时候，我们 provided 作为一个前缀，那么在映射的过程中，我们需要把这个前缀进行处理掉，也就是我们把这个前缀前面的内容截取掉，我们把后面的内容是跟我们对应的新的 URL 进行一个组装，是这样一个逻辑。好，那么我们继续看后面的逻辑，在这里面我们是这是我们获取的一个我们跟路由强相关的一个默认的 Gateway filter。


那么接下来我们来看一下它要处理的是我们的 global filter，那么 global filter 它有哪些内容？我们可以看一下，这里面是 combine 的，对于 global filter 我们可以看到当前我们的环境下，它去它得到了 10 个filter，这里面 filter 的内容我们还是可以简单看一下，我们比如说这里面是 remove cats 以及跟 cat 相关的内容，其实我们重点关注的是哪些？这里面是首先有 get with metric，也就是说我们涉及到一些监控信息的，是基于这里面去实现的。另外这里面像有 root to request，URI，以及就是说我们涉及到一个 UR 转换的一个过程。


这里面还涉及到一些比如说 load balance，我们这里面是默认获取到的是 no load banner，因为我们并没有引入 load balance 的 start 依赖，那么我们假如引入了 load balance start 依赖的话，可以得到一个 load balance 的一个client，那么这里面是有嗯， Websocket 的 routing filter 这里面还有 Natty routing filter，那么我们基于正常的我们当前的 HTP 请求，那么会用到的是 Netty routing filter。这面这几个 FILTER 我们在我们的 PPT 里面也有总结，我们可以从这里面去看一下。


这里面是我们 gload FILTER 一个过程，其实最终我们发出请求的是基于 Natty roading FILTER 的调用 IC behind connect 获取我们的外部的一个请求。那么好，我们现在可以看一下，接下来他处理的逻辑是把我们当前的我们跟这个路由强相关的比较自定义的。


这个 filter 跟我们的一些 global filter 进行了一个数据的一个墨制末日，完成以后再进行一个排序，就是一个统一的排序，这样一个过程，我们继续排序完成以后，我们可以从这里面去看一下 campaign 的它的整个一个顺序。现在我们看到组装起来的 filter 它成为 11 个，那么这里面我们看到我们特定的这个 filter 当前的位置是在从 0 到 10 的话，它的第五位，那么这样一个过程。好，我们接下来是处理的逻辑，就是它把我们这个 filter 集合构建成一个 defiltor get filter chain，也就是说一个 filter 一个链，这个链进行一个 filter 处理，那么现在我们要处理的也就是看我们这个默认那个链的 filter 处理的逻辑，也就是到这里面了。


那么我们可以现在我们跳舞断点，我们执行到这里面，那么在这里面执行的过程中我们可以看到它是一个，首先它是一个组装的过程，它执行到当前的过程，它就会获取到当前的filter，那么并没有对当前的 filter 进行处理，而是进行构建出来一个这样一个链的一个过程。


其实我们可以理解为这个螺丝套娃的一个过程，我一层一层一层把它套进去，最终在进行一个处理的解套的过程中进行一个 filter 一个处理，那么我们可以看这会，现在我们现在是进行一个套娃的过程，我们可以看到这里面是一个执行的过程，待会我们比如关键的一个地方，这里面我们看到这是我们自定义的这个 filter 它的一个底层的实现逻辑，看它的逻辑情况我们可以看到它首先是获取到我们的筷子请求，我们可以看到对请求进行一个处理。


这里面我们可以注重得到我们的 new pass，我们可以看到我们新的路径，那么我们输入的路径是 provide 的一口 APP name，也就是我们的路径，那我们看一下我们得到的一个新的路径值是什么呢？我们的 new pose 是 ECO APP name，这也就是说我们这个 filter 它处理的逻辑就是说把我们当前的这个路径指定的这个 provided 这个路由的前缀去掉，我们就是通过 Echo APU name 进行一个请求，也就是说把我们新的 new pass 去放到我们的请求里面，那么进行一个构建也就完成了，那么这就是我们对于这个 filter 在处理的逻辑，其实其他的 filter 也是类似的，它有当前专注的处理某一件事，那么我们跳过，那么现在我们看到哪？现在我们是到了，还是切换到我所有的处理的逻辑？那么我们接着向后跳跃，现在我们到了像 route to request URL 一个过程，我们看一下他处理的逻辑是怎样的。


其他的比如它的 host 会做一些什么处理，这样我们它最终得到的一个新的一个URL，那么它的新的 URL 是什么呢？我们可以看到 match 的URL，我们看一下它的内容就是我们最新要请求的，也就是说我们的 local my v m local 8081 e echo APU name 我们刚才那个filter，它处理了我们的URI，那么这个我们可以理解为它是把我们整个带 host url 处理出来，也就是我们接下来要请求的 URL 就是这个URL，所以说这样我们在执行的过程已经就比较清晰了。


我们最终这个路径它由我们网关的 URL 请求转换为我们下游真正服务器的一个请求，接下来进行我们的服务站的一个处理，我们跳过断点，那么现在我们到了一个叫 websocket routing filter，这里面处理的事情是什么呢？其实我们在讲解的过程中也提到，对于这个 Web shock 的 roading filter 和 net roading filter，其实它执行的过程中只有一个会生效，具体是哪个生效，它会看我们当前执行的协议，我们从这里面可以看到我们在执行的过程，我也会关注到我们会获取到 URI 的获取它的schema，我们看到它的 schema 当前是HTTP。


那么对于我们当前这个 Websocket routing filter，它支持的 scammer 是我们的 WS 或 WSS 这样一个过程，那么我们可以看到它明显是不符合的，因为我们是SCP，所以说他就把这个请求的逻辑直接跳过，也就是说当前这个 Web SOCKET road filter，它其实并没有执行它的业务逻辑，那么就相当于是跳过了。好，那么我们再继续执行，那么这会儿它执行到了我们的。 net loading filter，大家注意一下，这是我们注意 HCP 请求，那么这是我们整个 filter 的一个关键，也就是说执行的过程，它已经执行了这个filter，它要注的事情是需要发出请求，获取我们请求的一些信息。


那么在这里面，也就是首先我们看到它获取到对应的 request url 当前，嗯，我 request GR 我们已经处理完成了，我们是对应的 8081 的，嗯，对应的一个请求，那么看这里面也会去除判断当前的 scammer 是不是 H T E P？那么当前这会儿是已经满足我们的需求的，那么接下来去真正的去处理我们的一些数据，这里面我们可以看一下它处理的过程，这里面我们会把我们的 IC mass 的获取到，那么这里面的 xmass 的就是我们的 get 请求，那么 URL 我们也获取到， URL 就是我们已经确定的URL。那么接下来需要把head，也就是把我们的请求头信息进行一些拷贝复制，那么这样考虑复制完成以后，还有相关的一些数据，我们进行一些映射的一个转换，最终转化完成以后，这里面我们需要去 get SV hand，也就是获取到我们一个可以 SCV 的一个客户端进行一个请求，那么获取到客户端以后它会把我们的 header 进行一些处理等等一些信息。


最终我们的 request 设置一下，我们 mess 的设置一下 URL 进行把请求发出去，发出去以后我们最终会得到我们的请求体，那么进行一个数据的映射组装的过程，那么整个过程我们就完成了。那么好，那么在这里面我们如果感兴趣，大家可以去具体的去跟下面的过程，那这里面我们只是演示每个大的步骤，它里面这些逻辑是怎样去处理的。嗯，具体的这些执行的过程，其实我们基于 record 的方式在 debug 的过程确实会给我们带来一些困扰。好，那么那么我们执行到这里面，我们就知道我们执行下游的逻辑是怎样的了，那么我们现在跳过断点，我们整个程序就快执行完成了。


好，那这里面我们看到这是获取 get ATP client 的过程，这里面获取 ATP client 的过程，我们可以看到它获取的真正的，这是我们获取它的超时的一些设置。那么我们再向一下看它得到的这个 it client，真正的 client 实现，也就是 it client Connector 跟我们这里面是确定的一个 RTB client Connector 这样一个过程。好，那么我们现在是跳过这个断点，这里面我们我们可以看到它现在已经执行完成，那么我们回到这里面的话，这个请求已经获取到了我们的信息。


hello nicos discovery APP，其实这样它想要的结果可能我们的感触并没有那么明显。我们确实在我们源码跟踪 debug 的过程中能体会到整个执行的过程，涉及到比如我们这些，嗯， global filter，它执行处理的逻辑，每个 filter 做它相关的一些工作这样一个过程。那么如果说我们看到更全局的一个结构的话，我们是通过Router，predicate，handler， Mapping 以及处理的过程，最终获取找到我们的路由的定位，最终进行我们一些过滤器的一些处理。关于我们 KTV 的核心页码解析的内容，我们就先介绍到这里，同学们，我们下一章节再见。

