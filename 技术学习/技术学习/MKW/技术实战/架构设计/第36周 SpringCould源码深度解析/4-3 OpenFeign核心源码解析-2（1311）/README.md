---
title: 4-3 OpenFeign核心源码解析-2（1311）
---

# 4-3 OpenFeign核心源码解析-2（1311）

好，这是我们在构建 fan 的过程，那么还回到这里面的方法。当我们在通过 fan 构建出 builder 以后，这里面我们看它这是一些正常的一些处理逻辑。在咱们去判断一下当前指定的内容，它是不是有指定URL。如果说没有特殊指定URL，那么先判断一下我们命名的 name 它是不是基于对应 RTP 协议开头的。如果说它没有基于 RTP 协议开头，那么我们这里把 URL 改成基于 RTP 协议开头的类目。如果说其他的话，就直接把 name 附到 URL 里面。


我们看一下这里面，如果说它没有配置URL，我们会只用指定 load balance 对应的calendar。如果说它 URL 是指定了特定的URL，那么我们就不会使用 load balance，它就会有我们前面获取到一个 generate 对应的一个可选的一个client，这样一个执行的过程。那么对于我们来说，我们是基于 negos 注册服务中心的，我们配置的是一个服务名称，所以说我们最终执行的信息是执行到这里面去 load balance 对应的一个信息，这个方法它主要注的内容是获取到我们client，那么 client 不会 now 的时候进行通过 target 上来进行一个处理，我们看一下这个 target 的实现，它那个默认它有两个实现，一个是我们 Defi 的 target 来进行一个生成一个子目标对象。另一个是基于生成一个具有我们的快速熔断的一个target，那么我们这里面我们并没有去引入快速熔断相关的内容。到后面再讲 settle 一个熔断器的过程中的时候，它会生成对应的一个基于熔断器的target，那么这里面我们还是之前 debug target 底票target，它实现就比较简单，它其实就是表 FIN 默认的 target 在实现的，那么我们来跟进来的话，又执行到我们份的过程，那么在这里面我们看到对应 FIN 它里面 target 也就是首先是 build 出一个份对象，然后你有一个 instance 出来。


那么在这里面在 builds 的过程，我们看这里面其实我们则 capability 就是获取它对应相关的能力，这里面我们可以到一个retry，也就是说我们这个分开它是不是支持 retry 重试等等这些问题，下面我们看比较容易理解的，这里面相当于是一个日志记录器，这里面还有我们的编码器和解码器，最终我们可以到这里面还有一次我们的 invocase handle factory 生成代理的一个执行器，在这里面我们会最终它构建出一个 reflective 份。那么基于这个 reply 分我们也跟进来看一下。


在构建这个份，我们看一下 reflective 份，它是继承了这个泛对象，在这里面主要是它对instance，你有 instance 的过程进行一个覆盖，这里面我们可以看到它，其实这里面的内容就不用过头的去细讲了，它主要是来构建一个这个代理类的一个过程，这里面我们看它生成通过 invocase handler，那么 invocase handler 在这里面去通过我们加 DDK 的动态代理生成我们这样一个代理类。这样代理类最终把代理类返回过去，那么这样的话我们就构建出了我们关心的这个泛刊的一个对象，其实也就是说我们在定义的这个接口最终会生成一个 Java 的一个动态代理类。那么看到这里的话，大家应该是理解了这个 fan 或者 open fan 它的这个 string code 环境下初始化的一个整个过程。


好，那我们回来这里面我看整个初始化流程，我们最终是其实通过 reflect fun 来得到一个代理类，那么这个过程呢？我们就执行完成了，这是对于 open fun 的初始化的过程，那我们来看一下当我们获取到这个对应的代理类以后，它在执行的过程是怎样的。


对于执行的过程我们刚才也跟大家简单介绍过，最终还是通过我们这里面代理类里面对应的分一部 case handler 进行一个去定位，到我们对应的 master handler 的过程中进行一个 invoke 操作。其实这个 departure 的过程其实就是通过这个指定接口选择 master hand 指定方法的一个过程，那么对应这个 master handler 它的实现也就是我们这个 sync Notes 对应的 master handler。


对于这个 master handler 它在指定隐库方法的时候，其实构建了这些信息，那么我们现在还是通过跟代码的过程中我们去一块看一下，嗯，在执行这个过程，我们通过 debug 的方式是更比较方便的。那么我们刚才在讲代码初始化的过程中，我们是通过没有使用 debug 的方式，我们只是跟着把代码走进去。那么现在我们在执行这个执行过程，我们可以再通过 debug 的方式启动，启动的过程中我们先把我们初始化的过程快速过一下，接下来我们去深入 debug 我们整个执行的过程。


好，那么我现在我们开始启动debug，我们可以看到在 debug 过程这现在还是我们初始化的过程，在初始化的过程它首先会执行我们的份 client regist 里面的 regist being depending 的操作，那么我们整个在这里面我关注了几个节点，可以重点给大家去介绍。


首先回到这个对应的我们注册 BD 分析的方法，那么接下来我们跳过这个断点，我们看现在是到哪里来，现在是在 register client configuration，也就是说我们把一个指定的 configuration 注册到我们的定义容器里面，这样一个过程，那我们跳过接下来我们看这是 resist fine client 的过程。


那么在注册这个 fine client 的过程中，我们可是通过 enable fine clients 这里面会获取相关的一些信息，最终去判断一下我们的 client 有没有在我们的 enable 泛 client 这样注解里面配置，如果有注解配置的话，也就是下面的分子，它就是说把这个注解它通过 notisen generate being depending 包装起来注册到容器里面。


如果说我们的 kind 和 kind 紧急，若 kind 这个数组它是空的话，我们去通过构建一个scanner，也就是一个扫描器，通过扫描器去扫描所有这个bin，使用泛 client 进行包装了的这些bin，这是这样一个过程，那么接跳过这个断点，那么在执行的过程中我们可以看到刚才我们去介绍的。


这里面是我们得到这个 fine client 以后，其实把它注册进来，那么注册过程跟刚才我们其实是有介绍的。好，他接着又到这里面，我们接下来看，这是我们在具体注册这个 register 翻译 client 的一个过程，其实这里面大家注意其实最终它的关键环节，也就是我们构建非影刊的 factory b，也就是说我们通过 FIN client effect b 来构建出我们指定这个 FIN 接口对应的一个代理类，那么构建完这个代理类其实我们整个初始化的过程就完成了。


那么好，我们现在跳过下一步，其实我们刚才如果说通过 fine 刊的 factor bin，它在注册完成以后进行 bin 初始化的过程中，其实就会调用到 get back 的方法，那么通过该带拨代法间接的去调用该的target，这样我们可以看到在执行的过程中这是也是个初始化的过程，它这里面会涉及到对应我们的 FIN context 的构建，那么 FIN context 里面它会包含所有跟 FIN 相关的一些 configuration 的一个构建，这样我们看获取到我们的 get target 的方法。


在 get target 的方法，也就是我们在 get being 的过程中，我们可以这里面 get being 的过程中，如果说这个 thing context 它没有初始化的时候，它会进行初始化，那么初始化的过程其实也就是在执行了我们这里面的 FIN auto configuration 初始化的一个过程。好，这样我们就得到了 FIN context 的这个对象，那么这个对象我们再去进行构建出个这个 FIN builder 的这个对象，那么我们继续跳过。


那么在这里面我们构建 film builder 的过程中，我们刚才也介绍了，这里面通过把我们 builder 相关的像日志记录器，我们的编码器和解码器把它全注入进来，这里面如果说有必要的话，我们可以具有一些对应的一些费用， customer 自定义的一些改变。


好，跳过，在这里面我们看它会涉及到一个 configure bin，也就是说对于 bin 的一个配置注册一种方式。好，我们继续在这里面我们看到它会根据我们在配置翻译 class 的过程中，我们是有没有配置 URL 去选择，是通过 load balance 的方式还是通过默认的方式。好，今天我们接着继续。在这里面我们看到我们是构建了一个 thin blocking load balance 的一个client，也就是说我们在使用辅助对发现的过程中，我们可以通过我们的负载均衡的策略去执行 fun 的一些操作，好，继续，这是我们在通过创建一个 instance 对象，好，这样继续。


好，现在我们等执行一就应该到了我们的对应真正的执行的方法。那么这会呢？我们相当于是我们把初始化的 debug 过程跟大家跟了一下，那么我们看一下现在我们在执行的过程是怎样执行的，那么对于这个执行的过程中我们可以跟进去，我们跟进来以后其实就是 back to FIN 的一个 INFOG 操作，那么它的 INFOG 操作我们看这里面一些默认实现里面，首先对于像 equals 或者 has code 和 to string 这些方法我们都是忽略掉的，不是忽略就是说去执行对应的这个 reply 的份它的方法，那么除了这些方法我们会进行 departure 的一些操作。


那么我们看一下对于 discussion 操作。首先 get mess 的，也就是说对于一个接口，如果有多个方法，比如说我们这个接口指定了一个方法，那么当然我们这个 dispatch 的意义不是那么强烈。如果有多个方法的话，它通过方法的这个描述符获取到对应的这个方法的一个 invoke 的类。好，我们继续好。在这里面执行 invoke 的过程其实就相对来说是只是一个常规的一个执行流程，这个其实我们不用去做过多的一些解释。首先这里面我们会通过 build template from August，也就是说通过我们的参数构建出我们的这个 request template，其实也就是说我们所有的参数包装成一个模板的请求对象，基于这个请求对象进行后面的一些处理。在这个处理的过程中，我们看它是获取到像 options 和 retry 相关的一些内容，其实我们在这里面关注的就是 execute and decode，也就是说在这里面它就是把 template 和一些参数信息进行一些真正的执行的操作。那我们看其实它里面还是比较复杂的，我们跟进去在这里面执行并且解码的过程中，还是会从我们的这个 request template 里面去获取到我们 request 对象，最终在做一些其实获取到我们的通过 calendar 进行一个自行的操作。


那么根据你这 kind 它是怎么去处理这些内容的？在这里面我们看到它在对应的这个kind，也就是我们的 phone blocking load balance client，这里面首先去组装我们对应的UI，这里面我们去跟一下代码，这里面我们可以看到它是基于 load balance client factory 去获取对应的一个实例信息，也就是找到我们相关的一些 load balance 对应的一些服务实例。


那么这里面我们看到 1 比较关键的点， load balance client choice，也就是说获取到我们对应的一个 service license ID，这样进行执行，那么最终在这里面我们会去通过实例 ID 的判断，我们看是如何去执行这样一个效果，那么我们可以快速执行一下这样一个过程。


在这里面执行的过程我们看，首先我们去通过外均衡器去获取我们的私立ID，那么如果说我们的实例 ID 它等于 null 的话，它就执行下面的人，如果说不等于null，它来进行增强我们这些具体的操作，那么这里面是我们跟进去看一下，这是一个工具类，那么在这个对应的 load balance、 YouTube 的工具类里面它会进行引用这个 FIN client 来进行一些执行操作。


那么我们看一下根据你这个 FIN client 它做的一些事情，对于这个泛刊的它其实是一个底票的一个默认实现，那么在这里我们可以看一下，这是对应的我们一个底票的类，这个实现了一个client，这个过程其实就是涉及到 STB 请求的构建，我们 convert and send 就相当于我们得到一个 i t p u l 的一个connection，可以跟进来看一下。
在这里面我们把 URL 构建出来，同时去创建链接，创建链接的过程中设置一些我们 MTP 请求的一些常见的一些参数，最终我们会获取到我们对应的一个 LTP collection。这样的话我们看这里面会关心到我们一些输出的信息，那么得到这个输出的信息，把输出的内容进行写入这样一个过程，那么现在它执行到这里，这个执行其实因为它是一个异步操作，那么在异步操作的过程中它会去等着这个结果的返回，我们其实也是在我们在刊的执行的过程中，它其实因为我们在 debug 的过程它的时间比较久远的，这里面我们可能它会执行到一个异常节点。


好，我们看一下现在执行的效果，现在他就在这里面就是执行完成了，就是认为我们整个过程获取到了我们执行的效果，我们看这里面执行的结果，也就是说我们得到了我们的结果。 hello nicos discover test APP，这是我们在整个执行 debug 的这样一个过程，其实我们通过这个 debug 的跟踪的话，大家应该对于我们这个 open fan 它执行的这个流程应该是比较清晰了。那么回到我们 PPT 的话，其实我们简单总结一下，它还是经过我们获取到对应的代理，是基于份 invoke handler 进行，去通过 discussion 方法找到对应我们这个接口对应的 master handler，那么接下来一次这个 method handler 的实现就是 synchronize method handler 最终通过我们的 defile handler 来进行我们的 convert and sent，并且大于对于我们的 response 进行一个转化，得到我们对应的一个结果这样一个过程。好，那么关于 open fend 这些核心源码解析的内容，我们就先介绍到这里。

