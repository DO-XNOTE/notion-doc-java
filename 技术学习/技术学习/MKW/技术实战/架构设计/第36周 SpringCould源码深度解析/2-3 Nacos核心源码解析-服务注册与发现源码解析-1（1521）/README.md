---
title: 2-3 Nacos核心源码解析-服务注册与发现源码解析-1（1521）
---

# 2-3 Nacos核心源码解析-服务注册与发现源码解析-1（1521）

同学们大家好，这一章节我们来介绍 NICOS 的核心源码解析。首先我们来看一下，我们还是分三部分来介绍，第一，我们要介绍一下 NICOS 的源码说明，这里面我们主要介绍一下，我们在使用 NICOS 作为服务注册与发现的过程中，需要依赖你的一些第三方的包。第二，我们来介绍一下 NICOS 主要的服务，也就是服务注册与服务发现。第三部分，也就是 NICOS 的动态配置服务， NICOS 它主要提供了也就是服务注册发件它的动态配置，那么我们基于这两点作为我们最重要的源码解析的切入点。那么我们来首先看一下 nicos 的源码说明，从这里面我们看到在我们在分析 nicos 最重要的也就是对应的是阿里巴巴的nicos，它的源码，它这里面提供了整个 nicos 的控制在服务端以及它的客户端之间交互的方式。另一种 part 这里面我们看到是 spring code 阿里巴巴，我们为什么要使用 spring 阿里巴巴？因为它在我们通过 spring code 的方式集成 Nicos 相关的一些主键的时候，会非常方便。使用 nicos 的过程中，我们只需要使用对应 Gecko 的 starter 就可以快速的使用起来，这样我们就没必要再使用 nicos 原生的 kind 去构建我们跟 nicos 服务端的一些关系。


在这里面我们看到我们需要看到 Nicos 的源码和 spring code 阿里巴巴的源码，还有我们写的 study 的一个学习模块，那么在这里我们看，首先这是我们 spring Claude study，这里面我们要注意一下，我们版本做了一适当的调整，后面我们会简单说，在这里面我们依赖的 part 是 spring Claude builder，对应的版本是 3. 0. 1。
下面是我们的 spring boot 版本更新到 2. 4. 2，我们的 spring q 的版本是 2020. 0. 2，以及我们 spring q 的阿里巴巴的版本是 2021. 1。那么我们之所以去更新这个版本的话，主要是因为 spring code 阿里巴巴的版本更新到 2021. 1，所以说它可以让我们适当的提高 string code 版本和 string Boost 相关的版本。那么在这里面我们对版本的升级的过程中，如果我们做好了依赖的管理，我们整个版本升级完成以后，这里面代码的内容基本上是不需要我们做什么修改的，那么这是我们的 spring code started 的源码。


另一部分我们来看一下，这里面对应的是 spring Claude 阿里巴巴的源码。对于 spring code 阿里巴巴，我们主要关心的跟 Nicos 相关的内容，也就是这里面是 spring Claude start 阿里巴巴 Nicos discover 以及 spring Claude start 阿里巴巴 Nicos configure。那么基于这两个，一个是实现我们的服务注册与发现，另一个是我们的服务的动态配置。


对于 spring code 阿里巴巴，不管是 Nicos 的注册与发现还是动态配置，它都依赖到 NICOS 官方源码 NICOS 对应的Claude，那我们可以切换到 nicos 官方的代码，在这里面对于 NICOS 2 这样一个工程模块，这里面涉及到像 nicos client， nicos configure 以及这里面的 nicos naming，分别是对应我们基础的客户端服务、客户端操作以及我们的服务操作。对应 Nicode naming，也就是服务的注册与发现需要的功能。比如这里面我们可以看到它对应的 case country，也就是说当我们在服务进行注册的时候，它的一些 HCP 请求其实是请求到了这里面的一些方法。有这里面是beat，是一个心跳的操作，那么这里面是对应configure，也就是服务动态配置相关的一些服务提供漏放。


这里面我们看到 client 以这里面对应的 necos client，它其实提供了我们的服务注册与发现，以及动态配置的一些客户端操作，这些客户端操作最终是通过我们的 STP 请求跟我们这些服务端的一些 Controller 进行数据的交互。在这里面我们简单去了解一下大概的情况。


我们回到PPT，我们看一下当前我们的 neckles 和 spring code 阿里巴巴分别以使用的版本是什么。对于 Nicos 来说，当前已经发布了 2. 0 的版本，那么对于 spring code 阿里巴巴的最新版本是 2021. 1，基于 2021. 1，它依赖的 Nicos 版本依然是 1. 4. 1，所以说这里面我们的 nicos 版本还是 1. 1。


4. 那么对于最新的版本 spring code 阿里巴巴的 2021. 1，它依赖的 spring code 版本和 spring boot 版本也都有升级，那么我可以从这里面看一下，最初我们介绍的版本是比如说我们 Sprint 阿里巴巴 42. 5 对应的 Redis 版本，同此我们现在升级到二零二 1- 1 swing Claude build 版本也在这里面对应生成 3. 2.


0. 1。 string Claude 版本由原来的我们的 S R 9 版本生成，增升级到 2020 杠 0. 2。 spring 布的版本是依赖 spring code 对应的版本，也就是我们对应的是 2. 2。 4. 这里面我们要注意一下，我们可以看到对于 string Claude 的版本，它有一个明显的变化，它有原来的一些伦敦地铁字符，现转化成我们的一个日期的一个，让我们看起来会更容易理解，包括 stream boot 它的一些版本原来是有数字加 Redis 这样做一个正式版的发行，现在已经把 Redis 去掉了，它我们只是保证 2.


4. 2。对于 spring code 阿里巴巴它的这个版本发布，它其实原来是跟随 spring code 的一个版本的命名风格，现在因为 spring code 它已经不再采用这种方式了，所以说 spring code 阿里巴巴它也采用 spring code 最新的版本分隔，也就是日期分隔，我们这里面是 2021- 1，所以说这样我们在明白了这个对应关系，那么我们在使用的过程中就会比较方便，避免了一些我们的版本之间依赖一些冲突的问题。好，那么接下来我们看一下 NICOS 服务注册与发现源码解析的一大纲的一个内容。在这里面我们首先会跟大家介绍一下服务发现，那么服务发现它其实到于源码会涉及两部分，一部分是 nicos 客户端的操作，另一个是 NICOS 服务端的一些操作。对于 nicos 客户端的话，我们会使用到 spin code，阿里巴巴对应的一些STARTER，它这里面通过 STARTER 里面的制动装配去触发我们的服务注册与服务发现。那么在服务注册的过程中，它会最终转化成对应的 ICP 请求，这个请求会请求我们 next 服务端对应的Ctrl。这里面我们列出几个比较重要的，分别是服务的注册或者我们的服务的一些心跳，以及我们获取服务列表相关的一些操作这几个重要部分的源码。那么这里面我们先看一下我们 NICOS 客户端自动装配的源码，它的一个执行的一个结构。我们先看一下，对于这个自动装配，首先是需要依赖我们对应的一个STARTER，这个 start 就是 spring code STARTER，阿里巴巴 nicos discovery，那基于这个 spring boot 制动装配的原理，它会通过 Meta info 对应的 spring fanteries 去查找相关的一些 auto configuration。这里面我们列出了几个关键的 auto configuration，一个是我们的 necks discovery，也就是我们的 negos 的制动发现。另外我们这里面的 nicos service addressy，也就是说对于我们 NICOS 的一个服务注册制动出来，这里面还有 nicos service 的 auto configuration，也就是我们提供的 next Server manager 相关的一些操作，它是用来跟我们对于 necos client 一些包装进行 HTTP 请求的代理，这里面还有我们自对应 nicos discover client configuration，这里面最终也就是生成跟我们也是跟 Max 服务端交互的一些操作。这里面同时还要基于 reactive 的一个 disturver client，它跟是几个相对应的，一就是一个是我们理解一个主色的，一个是非主色的这样两种情况。在这里面我们看这里面有几个关键点。首先是我们通过这些 auto configuration 的创建出跟 nicos 相关的这些必应对象，那么我们通过这些必应对象跟 nicos 服务端进行交互，那在这里面比较重要的几个对象，我们这里面首先是 nicos 或者property，这是我们跟 Nike 相关的一些属性配置。同时这里面有一点就是大家在初始化的过程中，它会在特定情况下就是发布一个事件，这事件也就是 Nicos decovery info change event 这个事件的发布，它就是会触发我们写 Gecko discovery 的一些重新的一个配置。另外这里面是对于我们会使用到 Necos service discovery，它会依赖到 Nicos discovery property 和 Nicos service manager 这样一个过程。嗯，后面这是跟 Nike 的服务注册相关的一些内容，最终我们会通过 Nicos 的服务注册的方式，则我们会监听对应的我们 server 启动的事件来去初始化我们注册的一个过程。那么接下来我们来看一下我们服务注册的操作，对于服务注册，我们这里面 Nicos auto service registry，它是我们整个服务注册的入口，也就是当启动的过程中，它是怎么去触发服务注册。通常我们知道，当我们应用程序启动完成以后，我们的应用会注册到 Nicos 的服务列表上去，发现这台机器已经启动成功。那注册的过程其实有两种方式，一种方式，也就是现在默认的方式是监听这个 Web Server initial event，也就是说监听我们这个 Web 服务启动的这个事件。当我们监听到 Web 服务启动完成这个事件以后，他就开始去向 Nicos 服务中心去注册自己，那么注册完成，那么整个注册就搞定了。另一种方式是他去监听 on Nicos discovery info change event，也就是说当我们这些 Nicos 的一些信息，它的服务发现信息发生变卦的时候，我们进行它的一些操作，那么对于它来说，这里面我们监听到这个服务初始化完成的这个事件以后，它会进行 start 操作。那么对于 discoper info change 的话，也就是它注册与发现信息发生变更以后，它要进行一个 respond 操作，那么最终我们通过这里面作为整个服务注册的入口，那么它的启动的过程会执行对应的 nicos service registry，也就是说通过它去注册，注册的过程中它会依赖到 on nicos naming service，也就是说基于 nicos 的命名服务，那么我们可以看到对应这个 naming service 跟我们对于 nicos 它源码对应的服务，也就是 namings 对应这个模块的那种相关的。在这里面它初始化的过程中会构建 naming proxy，也就是一个代理一个，这里面 beat Rector 和 host Rector，一个是对于心跳进行服务，另一个进行对于一个数据同步进行服务。这里面的 naming pox，也就是我们那个命名服务的一个代理，这个命名服务代理它最终是对于我们这些 ITP 请求进行了一个包装。我们列出几个典型的请求，比如说服务注册，对于服务注册的话，它其实相当于做了一个这样一个请求， nicos VN s instance，心跳对应的，这里面是beat。那么对于我们要获取服务列表，它对应的方法是 query list，它对应的 URL 对应自 instance 的list，我们也可以理解为它是一个 rest 的风格的一个 URL 的组织关系，这是服务注册，那我们看一下服务发现，其实相对来说服务注册服务发现是比较简单的。服务注册我们可以理解为把当前节点注册写入到我们的 Nicos 服务端，同时要做到我们 Nicos 这些节点的数据同步，服务发现相对就比较简单了。那么对于服务发现的话，我们在客户端需要做好我们的 Nicos cover client 的一个构建，这这里面也是通过自动装配的方式得到我们的 nicos service cover kind 这样一个对象，这是我们孩子对应的 discover kind 和 active discover kind 这样有一个区分，最终他们的处理方式也都是通过 next survey discovery 去获取实例，获取instance，那么得到对应的 instance 通过 next service manager 进行一些获取我们的一些命名服务。最终我们还是通过 Nicos 的 naming service 去通过我们的 naming parks，也就是说我们的命名代理去调用服务列表 query list，也就是调用我们服务的对应的这个 control 的服务，获取到我们当前所依赖的服务里面它的一些实例的一些列表。现在我们介绍的跟不注册与发现相关的内容都还属于我们客户端的内容，客户端的内容是基于 spring Claude 阿里巴巴的 starter 对 nicos client 进行包装来完成的，接下来看一下 nicos 服务端所做的内容，对于这里面 nicos 服务端做的。首先我们看一下注册，那么对于注册的操作，当 client 端请求过来以后，它最终会访问到我们的 distance control，也就是一个实例的一个control，它进行一个注册操作能它的其实实现过程是比较复杂的，这里面最终我们的 control 会使用到我们的一个 service manager 进行一些操作，它会进行对于实例的注册的过程。那么注册的过程它其实会创建我们的一个实例对象，这个实例对象它会跟我们的 service 一个创建，并且去进行校验，最终会把我们的 seven 去进行一些铺的装载，最终会进当铺的装载完成以后，它会对这个 service 进行一个初始化，因为这个初始化的过程其实我们就相当于理解为它会通过 health check 来去 check 一下这个服务的心跳状态。注意一下这里面的状态的check，它并不会向客户端发出一个请求，他只是去校验一下当前这个 service 它最近获取到的一个心跳的情况。接下来它会进行一个此景，我们对于数据一致性的一个服务，它会进行这个数据的一个跟其他节点同步，这里面的同步它会分为一个，比如说我们临时节点和持久化节点的方式，这里面对于它的数据同步的方式也是值得我们去学习的一个重点，这是注册的内容。我们接下来我们看一下服务接收心跳的处理方式和它的获取服务列表的处理方式。


对于接收心跳，当时我们提到了它和 check 一下最近的心跳，那么心跳是怎么接收？这里面有通过 inspire control 对应的 beat control 操作会进行处理。这里面要注意一下它会通过心跳包传过来的这些信息去判断一下当前 service manager 存不存在这个实例。假如说这个实例它已经因为某些原因被服务端剔除掉了，那么心跳债过来的时候，它就会需要重新的对这个私利去走一些注册流程。如果说这实例是正常的话，他需要去做的更新一下最近处理的一些时间，这里面需要做的一些事情就是我们看一下它会通过嗯看到 b 的 process 去进行一些处理，最终其实这里面我们可以看到最终它要处理的时间，也就是说它会把这个实例最后的一个，它一个心跳时间去记录一下，同时它进行 service change 的一些操作的。当 service 对于服务这些一些属性状态改变完成以后，最终会发布出一个事件，这个事件就是 service change 的event，那么发布完这个事件以后，最终他要做的事情还是把我们心跳的数据向其他节点进行一些同步，那么这是我们接受心跳的一些逻辑。对于获取服务列表相对来说就是比较简单了，对于辅助列表，也就是对用 instance control list 操作，那么这个操作最终它会调用 do SRVIPXT 这样一个方法，对于这个方法里面它会去获取我们服务的一些信息。其实在这里面我们知道对于 nicos 它这些节点我们可以理解为它是具有一致性的，所以这个操作它并不涉及到一些跨服务的操作，它只需要对于最近节点的缓存 map 里面获取到这个服务相关的列表就可以了。

