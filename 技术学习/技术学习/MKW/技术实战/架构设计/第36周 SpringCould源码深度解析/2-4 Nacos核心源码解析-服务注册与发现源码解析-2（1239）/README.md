---
title: 2-4 Nacos核心源码解析-服务注册与发现源码解析-2（1239）
---

# 2-4 Nacos核心源码解析-服务注册与发现源码解析-2（1239）

我们先是通过 PPT 对于整个 NICOS 的服务注册与发现简要流程做了一个介绍，我们还是对应我们整个流程与我们源码的一个对应关系去重新再详细的去介绍一下。这里面我们还是切换到我们先是在看它的自动装配流程的一个操作，对于这里面的服务的注册与发现的它的自动装配的过程。我们来先切换到我们的学习模块，我们要看 nicos provided。


首先我们知道当我们的 provide 要启动的过程中，它会把自身的去注册到 nicos 上面，那我们看一下它在注册过程中做了哪些事情，在这里面我们要关注的它的是一些自动装配的一些信息，我们看到对于这里面，对于我们最重要的是 spring Claude，阿里巴巴 nicos discovery 一就是它服务发现的一个过程，那么对于它服务发现我们最好看源码的方式是切换到我们的 string Claude。阿里巴巴在这里面我们可以看到对应这个模块的源码通知，里面打开可以再进行一些详细的分析。其实我们最关注的还是 spring factory 里面它相关的一些内容，通过这里面我们可以看到对应的是 next discovery auto configuration，我们可以逐个点进去看一下。好在这里面我可以看到对于这个neckhouse，我们可以看到对于这个 auto configuring 它注册了哪些信息。


这里面首先我们看到首先有条件注解它是需要支持我们的服务发现，并且也支持我们 Nicos 的服务发现，它才会继续我们的一些 bin 的构建，对于 bin 构建的话，这里面当然它是一个默认条件，就是肯定是 unmissing bin 的，当这个 bin 不存在的时候，我去使用默认的bin。


这里面首先是构建 nicos discovery 的property，就是它的属性文件的构造，另外是 nicos service discovery 构建这个 nicos service 的一个发现的一个服务对象变，我们可以看到它在构建的过程是依赖的两个变，一个是我们上面的这个property，另外一个是 Nicole service manager，所以说在这里面它是需要我还依赖其他的一些信息。另外这里面我们可以看到是 next discovery Endpoint auto configuration，如果说对于 Endpoint 的话，我们知道它可以通过 Endpoint 获取到一些内部实例的信息，但是我们在关注整个 nicos 服务发现的注册流程的过程，这个 any point 可以暂时先不关注。


下面是 nicos service registry，也就是说它的服务注册最关键的一些地方，我们可以在这里面去看一下，在这里面我们可以看到它是构建了哪些内容，这里面它首先是 next service registry，也就是说 next 服务注册的注册器，这里面还有对应 next registry 及我们这里面是比较关注的就是 Nicos auto service。


其实对于我们来说这个是我们重点关注的，因为它注册完这个毕业以后通过去监听，也就是监听我们的服务启动完成的这个事件，那么来进行我们真正的注册的发起，这里面我们可以去简单看一下，在这里面最终我们看到它是继承了一个 abstract auto service reason，也就是说这个服务自动注册的一个方法。


在这里面它有哪些方法？其实最重要的方法也就是这里面是register，也就是说我们进行服务注册，它最终是怎么进行服务注册，它是通过调用 super 父类的，通过 service dressy 进行一个注册的操作，其实这个过程我们还是回到这里面去关注它的一些操作，这里面我们看一下它是怎么去发起这个注册的。在这里面能看到它是监听到 event listen 的，这是我们提到的是 Nicos discovery in free change event，那么其实这个支持是推进，它相当于是后来去支持的一种，是 20 年 9 月份去支持的一些功能。


其实在之前的话支撑的方案我们可以通过这种方式去看，它在这里面有对应的on，也就是事件监听，这里面是 on application event，我们进去看一下它关心的是哪一个，它监听的我们的 event 事件，也就是 Web Server in Server event，也就是当我们服务启动完成所处的操作，它这个操作最终会执行到哪里？执行到这里面调用starter，其实这个 starter 操作也就是我们去进行注册的一个操作，我们可以看一下在这个 starter 里面做的事情，也就是进行resist，其实在进行服务注册的过程中，我们可以看到它通常会有服务注册前的四件，服务注册后的四件，这是在服务注册前的四件，这是服务注册后的四件。当我们在服务注册完成以后，它进行这个进行数据的一个替换，也就是把我们的状态的置换一下。


这里面我们如果说有必要的话，我们可以监听 instance Pre restress 的和我们的 intense resisted，也就是说当我们注册之前的事件和注册之后的事件都可以去扩展一些我们自己的一些工作内容。看到这里面大概了解我们通过这里面的自动装配的一些信息，可以最终的引入到我们这里面的服务注册的入口，也就是我们通过监听 application event 对应的这种方式去触发我们整个注册的一些实现，看一下最终这个注册的流程，它是怎么执行到注册的过程的，在这里面其实也就是我们需要对应的这执行，我们需要跟一下 register 方法里面的内容。


如果说涉及到跟这里面的内容的话，我们最好的方式我们只通过断点，我们可以在这里面我们的 start 模块进行启动的过程，相应的地方加上断点可以进行我们的操作。在 nicos auto service resist 里面对应 resist 这个方法里面加上断点，大家注意一下，现在我们切换到我们的 study 模块，在这里面，因为我们依赖的跟这里面对应的是使用 Claude start，阿里巴巴 Nicole 后边它的版本是一致的，所以说我们在这里面看到的代码效果是一样的，现在我们去启动我provided，我们通过 debug 的判断去启动，这会已经执行到我们的断点所在处。


现在我们开始进行debug，只关心我们关注的内容，不不关注的内容我们就快速跳过，为了节约我们的时间，好，现在我们去进行，这里面我们可以看到对于这里面它会调用 super 它的request，那么我们跟进去。在这里面它需要首先去创建一些 registry 这样一个对象，这个对象是怎么获取到的？我们跟进去看一下。


在这里面我们点进去看到它的跟端口号的一些设置，另外我们看它的一些配置，就是如果说它进行，如果端口号必须要大于0，如果不大于 0 的话，它其实是当前如果我们没有设置，需要进行一些特殊的处理。好，我们现在去看，我们现在得到了，我们只在此对象，接下来我们进行一个注册的操作，现在还是 F7 跟进去，在这里面我们会去首先校验一下当前这个注册器，它是的 service ID 是不是正常的，如果设备 ID 不存在的话也会进行直接中断。接下来它在注册的过程中需要去创建 naming service，也就是我们的命名服务的注册器的一个service，我们看一下它做的事情是什么，我们根据 naming service，我们会通过 Nicos service manager 来构建这个 naming service。好，那么我们也跟进去看一下，也是去做了一些特殊的处理，它会根据 build naming service 通过 property 构建出来进行，我们返回。好，我们现在返回，在这里面我们得到了 naming service。


接下来我们通过 registring 获取到我们的 service ID，我们看一下这里面我们获取到的 service ID 是什么呢？从这里面去看一下它的 service ID 的值。对应的 service ID 就是 nickles provided，也就是我们对应 application YM 标明的是 stream application name，也就是我们的应用名称。接下来这里面是获取到 group 以后再进行一个，这一步是我们通过注册器的这个对象去生成我们 nicos intense，可以看到这个方法命名还是比较合理的。


get nicos intense from resist tracen，我们看一下它其实也就是一个 bin 的组装，我在这里面我们构建一个空的 empathy 的instance，去设置一下它的IP、 port 和它的一权重等等一些信息，这样我们最终会获取到这个 instance 对象。如果说关心这个值的话，我们可以在这里面去看一下这个 instance 值它的一些内容是什么。
好，我们看一下它这里面这个 IP 是本地IP，那么这个 port 也就是我们当前这个程序它的端口号。我们再看一下它的一些权重默认值、 1. 0、健康状态是处等等这些信息，它的集群名称是defailed，没有做一些特殊的处理好，那么我们现在把我们当前这个实例已经构建出来了，构建出来以后我们就需要对这个实例进行注册，这里面我们看指定了一些 service ID 和我们的group，还有这个实例对象进行它的一个注册。


这就来到了我们的一个关键操作，跟进去看一下，它会做一些特殊的一些处理，这个我不用关心，先看一下当前这个实例，它是一个临时的还是持久化的？当前是一个临时的，对于临时的这些节点，它需要进行心跳的一个心跳的构建，这里面首先它通过beat， react 去构建出我们那个心跳 bid 信息。


那么我们看一下构建的过程，是也是我们创建一个 bid info，把一些相关的信息的赋值进去，进来去做一些处理，得到一个心跳包信息以后，他会把这个心跳包信息添加到这里面进行，相当于是我们一个定时任务轮询去做的一些操作，我们可以跟进来看一下，在这里面会做一些处理，最终把这个心跳的信息构建一个背的task。
基于这个 task 通过 sky 的一个定时任务间隔的去进行一些心跳的操作，这是心跳操作，它已经也就是说当我注册过程中以后，我需要进行持续的心跳，向这个 Nicos 汇报我当前这个节点的健康状态。这个完成以后就开始进行我们节点的注册，我们再跟进去看节点注册需要做的事情。


在节点的注册过程中，它是把我们当前构建的这些 instance 信息都会组装成新的一个对象，这个对象就是一个map，这个 map 它命名parameter，就是跟我们在 ICP 请求对应的 KV 参数是一致的。整个这个过程它是把整个 parameter 构建完成以后，我们看一下这里面所有的相关的，像是端口号、权重都会转化成一个字符串操作。
我们可以看到这里面还有一个 Meta 信息， Meta day 的信息会通过 JSON 的方式去序列化进去。他要做的事情就是这里面是 IQ API，也就是做一个 API 的请求。对于 API 的请求的话，我们这里面应该关注的是什么？它的 URL 是什么？它的参数是什么？它请求的 it mass 的是什么？这个是通过 push 的请求向我们的 nicos UR instance 发出个请求，它的参数是parameter，可以看到这个请求对应的 UR 就是关注的是 nicos VENS instance 这样一个过程。


那么我们跟一下，看一下这个操作，跟到这里面其实我们的操作已经比较明了，这里面是获取我们的 Server list，也就是对于我们的 nicos 这个服务端，它的服务列表是什么？我们可以看到在这里面我们因为用的是单机模式，我们只获取到一个 Server list，从这里面进来看一下，我们能关注到这些参数分别是什么，可以看到对应的我们的 API 是我们刚才 Nicos 用V，E， N S instance，我们的 parameter 是刚才构造的parameter，这里面的一些 body 信息空的。我们的 sir 服务列表只有一个，就是我们的 nicos my VM local 88 bug，同时 post 帮助进行请求。


我们可以简单看一下，这里面会进行一些 head 的封装，最终会做一些处理，把我们这个 URL 构建完成。这里面构建完成以后，最终就是需要去做我们相关的一些操作了。这里面对应的是通过 rest template 去 exchange 咨询我们这个请求，最终获取到我们请求的一些操作。整个过程如果说我们的 request 也就是结果是 OK 的话，就把我们已获取的数据返回过去，这就是整个这个过程就已经完成了。

