---
title: 6-10 Sentinel面试题深度解析（0938）
---

# 6-10 Sentinel面试题深度解析（0938）

同学们大家好，这一章节我们来介绍一下 setonal 面试题深度解析，那么对于 session 面试题，我们更多的是去了解一下限流和熔断的一些规则，那么通常我们更多的会了解一下我们 settle 的工作原理。那么对于 Sensor 工作原理，这里面的内容是比较多的，我们待会可以再详细的回顾一下刚才我们介绍的内容。


第二个可能会被问到问题，就是 Seasonal 更详细的一些讨论，比如说对于三种它的负载降级的一些策略有哪些？那么简单来说，对于负载降级的策略，我们可以根据我们的 RT 的响应时长，或者说我们失败的次数或者失败的比例，那么这种方式，那么接下来我们来先看一下 settle 的工作原理，说到 settle 工作原理，我们最重要的也就是 settle 在这个工作原理图。在 settle 里面我们知道所有的资源都对应一个资源名称，也就是 resource name，我们每次资源都会去创建一个 entry 对象，那么 entry 可以通过主流框架适配自动创建，比如说我们通过double，我们通过 spram AC 都可以对应的一些方法名称去获取我们这个资源名称，也可以通过注解的方式调用 SPSEU API 显示的去创建entry，它创建的时候同时也会创建一系列功能的一些插槽。


我们可以看到对应下面的 NODE select 插槽和我们对应的 static slode 插槽，那么对于这个插槽我们可以来回顾一下，对于这边 NODE select SLOTA，它是负责搜集资源的一些路径信息，并将这些资源的调用路径用树状结构存储起来，用于根据调用路径来进行一些限流降级。那我们的 classer builders loader，它只用于存储资源统计信息以及调用者信息，例如这个资源的r，t，q，p， s 以及它的 third count，也就是在线程数。那么这些信息将就为多维度的一些限流降级的一些依据。其实这里面最重要的也就是 static slaughter，那么 STATICAL Slora，它是用于记录统计不同维度的 runtime 的一些指标。


STATIC slogan，它的计数器通过了一个滑动窗口，那么在这里面我们可以看到它像我们的 RT 的时间，我们的通过数，我们的主摄数及抛异常的一些情况等等的，这里面的数据都通过滑动窗口的方式记录下来。那么接下来 starting explorter，后面它记录了这些请求的词前，后面再去看我们的这些具体操作的一些内容。这里面向 parameter flow slotter，也就是根据热点参数进行一个限流，进行一些区分。另外还有就是对于 systemslot systemall，它会关心我们 CPU 和 load 当前的负载情况，会定义一些策略，当我们达到一个什么情况的时候触发限流。下面这是 authors lotter，这里面会进行一些白名单相关的一些设置，就是我们更像一些黑白名单或权限的一个设置。


其实后面是我们对于流控和降级两个，这两个是我们关注度比较高的，也就是首先是我们流动控制进行限流操作，另外我们是 degrade slot，也就是我们的一个降级操作。那么基于流量和相机，这是我们通常在做分布式系统的时候比较关注的一点。那么所有这些流量控制或熔断降级，它都会依赖前面的 STATIC SLOT 统计的一些信息，那么只有了这些信息，我们可以在这里面基于我们设置的一些规则的阈值进行触发，是否达到了流量降级的一些信息。


念完这些 slogan 以后，我们知道了这个 settle 它这个工作的一个全局的概念，那么其实有时候面子宽大会更关心你对 settle 的一些核心 API 的一些理解，那么这里面我们会涉及到我们首先构建这个 slot chain 的一个过程，里面我们会构建成一个对应的 process sloter chain，那么基于 process load chain，它是 settle 的核心的骨架，它将不同的 slot 也就是这些不同的 slot 组成起来，它构成一个直人链的模式，从而将不同的这些功能比如说像限流降级系统保护组合在一起。


那么所有的称它其实可以分为两部分，第一部分是统计数据，构建数据的部分，也就是这里面上面是我们是构建统计数据的部分，那么第二部分就是我们可以下面是这也就是判断部分，也就是我们通过规则去判断一下是否可以通过的这样部分，那么在整个过程它都会涉及到 context 的，那么 context 它代表整个链路的上下文。
我们贯穿一次调用的链路中所有的entry，那么 context 维持一个入口的节点，我们可以称为 entries node，那么每一次调用的 NODE 我们称为 car NODE，那么调用来源称为Orin，也就是 Orin NODE，那么 contact 和名称即为调用链路的入口。那么 context 的每一次的方式，它可以通过 seed local 的传递，只在一个 entry 的时候生效。


由于 context 通过 third local 传，所以因此对于异步调用的链路，我们线程切换的时候可能会丢到context，所以说在异步请求的时候，我们需要手工的去把我们的 context 传递一次，那么这里面我们说到这个entry，每一次一个资源调用都会创建一个 entry entry 它包含了资源名称，我们的 car NODE，我们当前统一节点以及我们的 Orin NODE，也就是来源统一节点。这里面普通的entry，它调用 SPU entry 的时候去创建，当逻辑执行完成的时候，我们通常在对应着 entry close 的方式把对应的 entry 关闭。


那么在 entry 执行的过程里面还涉及到一些NODE，我们看一下这里面的这些 NODE 信息，对于 NODE 我们可以它里面包含了一些统计的节点，通常我们这里面会涉及到 static NODE， defailed NODE， in class NODE，还有一些 entrance NODE，那么对于 static NODE 它是作为一个最基础的一个统计节点，包含秒级和分钟级两个滑动窗口的结构。那么在这里面我们看它是通过两个滑动窗口去记录这些数，那么 debug node，它就是我们的链路节点，用于统计调用链路上某个资源的数据，维持这个树状的结构。


pressure NODE 它是触点，我们可以为触点节点，它主要用于统计每个资源这些全局的数据，并且它会存放该资源按不同来源区分的一些调用数据，这里面也就是跳我们不同的 static NODE，那么在这里面我们知道整个 settle 的工作的这些过程，以及它相关的一些概念。


所以说这里面我们可以重点的把 static slot 里面它这个统计算法了解一下，也就是我们滑动窗口执行的过程，那么通常我们来只说到我们的熔断限流的过程中，大家也要去扩展一下台有的一些限流算法，最简单传统的我们的计数器算法，以及后面扩展的像我们的令牌桶和我们的漏桶的算法。


那么了解这些算法的工作原理，去跟我们的滑动窗口进行一些比较，了解一下他们各自的优缺点，这样有帮助我们对于 settle 的公路原理的一些进更深一步的一些了解。那么 settle 工作原理我们先介绍到这里，接下来我们看一下 settle 的服务降级的一些策略，那么通常我们对 settle 的服务降级，它可能会涉及到我们对于 RT 的一些平均响应时长，当我们作为 consumer 调用 provider 它接口的过程中，那么我们什么时候认为它是不可用的？其实最重要的一个指标，也就是我们的响应时长，假如说我们调用它的响应时间变得越来越久的时候，我们就应该对于进行一个触发熔断了。


那么如果说它形象字段变长，特别容易把我们 consumer 端的线程词资源拖垮占满，这样的话引起我们 consumer 端的一个不可用，那么第二个一个也就是我们的异常比例。那么除了我们的想象时长，那么另一个我们对于下游不可用的一个标志，也就是我们的异常比例。假如说我们调用请求的过程中异常比例非常高，那么我们对于一个服务的可用性也就是很低了，我们调 100 个请求返回百分之六七十的都是失败，那么对我们肯定是不可用的。


那么除了异常比例，还有一个更比较简洁的一种方式就是异常数。通常来说我们用异常数的比较少，因为异常的次数跟我们承担的 QPS 可能关系是比较高的。假如说我们设置这个异常数，异常 10 次，我们进行降级，那如果说 QPS 上千的话，异常 10 次，其实这个量并不是很大，那么我们通常会采用此 RT 的平均想象资产和我们的异常比例。那么基于 RT 的响应市场和异常比例，我们可以更友好地去做一些扩展和配置。


那么对于异常数的话，通常我们的流量上升的时候也会伴随着异常数的上升，那这个可能并不能直接说明我们系统的服务水平下降了，所以在这里面我们来看一下对于 RT 的平均响应时长，它通常是指 1 秒内的接口的访问响应时长超过某个指定值处触发熔断，那么调用这个符号的降级方式，比如说我们指定时间窗口内不能够再次访问 1 秒内。


如果说我们访问 5 次的平均响应磁场超过某个阈值，那么这里面我们要注意一下，如果说我们设置值它不会超过 5 秒，它的最大阈值是 4. 9 秒，也就是说如果说这个值它的响应时间超过 4. 9 秒，它也会通过 49 秒去计算的。所以说我们要限制一下阈值的方式去配置。另一方面是异常比例，它是指 1 秒内请求出现异常的比例，我们超过某个阈值，我们服务进行降级，那么这个时间窗口是一个秒级窗口。对于异常数是比较特殊的异常数，它是在分钟级的一个时间窗口，也就是说 1 分钟内请求出现的异常次数超过某个子丁的阈值，那么我们服务触发降级，那么这样的话，它的时间窗口是一个分钟级的。通常我们使用这三种方式也就满足我们一个服务降级策略。在这里面我建议大家使用 RT 的平均想要时长去做我们的一个降级策略的设置，因为我们对下而评估的一个最高标准，因为通常如果说异常比例，如果抛异常，它能快速失败的话，本身对我们调用的影响是比较小，因为它快速返回失败，我们可以进行失败的一些降级处理。那么如果说它返回的时间变长，我们不能及时的进行降级处理，我们只能去等待，那么这个等待的过程容易把我们的系统拖垮。关于 session 的服务降级策略的内容，我们就先介绍到这里，同学们，我们下一章节再见。

