---
title: 2-8 RocketMQ源码解析-Consumer消费消息流程-1（1332）
---

# 2-8 RocketMQ源码解析-Consumer消费消息流程-1（1332）

同学们大家好，这章节我们开始学习 rock MQ 核心源码解析 consumer 端消费消息流程的部分。前面给大家介绍了生产消息的流程，现在我们介绍消费消息的流程，这里面我们还是分三部分介绍。首先是 consumer 端启动拉取消息，这里面会包含 consumer 端客户端启动创建链接，以及我们拉取消息消费的流程。第二部分是 broker 端的处理拉取消息的请求，这里面我们会通过 pull message process 作为我们的入口，分析我们的 broker Server 接收到我们拉取消息的流程以后，我们怎么去处理。
第三部分依然是我们的代码演示，这里面我们已经知道我们采用了三种不同的方式实现，分别使用原生的 rock MU Calendar。第二是基于 rock MQ spin 坡的 starter 来继承，第三种是基于 spring code 阿里巴巴对 rock MQ 的包装。


接下来我们先看一下 consumer 启动拉取消息的过程。这里面我们列出了很多 consumer 启动的过程中，它会基于我们的 debug MQ push consumer 作为一个启动的入口，当它进行 stop 的操作的话，它会触发我们。这里面是 debug MQ push consumer MPL 去启动。其实我们通常在命名的时候，我们的 IMPL 通常是一个实现类，对应它前缀的内容是一个接口抓control，它实现的过程中反而没有遵循这个规律。我们这里面的 debug MQ puts consumer 和对应的IMPL，它们两个是没有直接相关的两个类，当它启用的过程中，这里面会发我们的 consumer message service 触发启动，同时这里面还会构造对应的 MQ client instance 这样一个 client 实例，它进行启动启动的过程中也会直接或间接地触发向 MQ client API。这里面我们的 pull message service 以及我们的 rebounce service，这里面我们要重点关注一下。其实最终我们在启动的过程中把我们的 poor message service 启动成功，这会他就开始通过一个 where true 的方式去轮询，监听我们消息的变更，有变更以后我们就拉取过来。


下面这里面的是 trace departure，是departure，它是一个消息诡异的转发器，我们可以理解它跟我们整个关注的启动消费流程关系不是很大，重点放在上面的这一些内容。通过源码来看一下 different MQ post consumer 它启动的过程。这里面我们还是回到我们的 Rocketing to study 这样一个模块，我们依然使用 Rockemin planter 作为最原始的一个消费的请求来发起。我们可以看到这里面的代码，我打了几个断点，方便我们理解我们项目其中的一个关键重点，比如说像我们 debug MQ push consumer，这里面我们构建出我们的 consumer 对象，我们去 set 我们的 name server 的地址，接着我们去订阅一下我们所关注的主题，以及是否有一些子的标签过滤。这里面我们对于我们订阅的主题是 sowcase topic，我们对于 Supper express，也就是我们一些过滤标签，我们是星级的，代表接收所有的消息，也就是接收所有这个 topic 下面的消息。


接下来我们这里面注册一个监听，这里面 register method listener，我们可以看到它的实现下是一个 my solution 的conkindly，这样第一种一个回调方法，基于这个回调方法，当我们有消息过来以后，它会调用我们的 consumer message 进行消息的消费。那么我们可以看到这里面，我们在这里面简单的进行了一个打印，表明我们对这个消息的一个正确消费。执行到这里面的话，我们的配置工作就已经完成了。


接下来我们是我们启动的工作，也就是说我们 starter 这些配置完成以后，我们就要开启我们的 consumer 端的工作，我们大概了解我们一个 consumer 端它的一个代码结构，那我们开始运行我们的程序，看一下每一步他做了哪些事情。我们现在通过第八个方式去启动，首先到我们第一个断掉，我们是创建我们的 debug MQ push consumer 对象，那我们跟进来看一下这里面做了哪些事情。我们可以看到在这里面我们是对应的默认方法，我们传入的是 consumer group，除了两个 null 以后还给我们补充了一个对象，那么这个对象是 allocate master Queen amazing，它是什么对象？它我们可以从这里面去看一下一个它的参数是什么，这里面我们可以看到它其实要求我们传输的是一个消费端的负载均衡的策略，对于这个消费盾的负载均衡的策略的话，我们这里面默认调用它创建的是基于一个平均的方式，需要平均分配的方式来完成这个负载均衡。


所以说这是我们在初始化的过程中跟我们构建了一个基于平均方式的负载均衡的策略，那么这里面我们其实并没有做什么操，这里面对于几个参数的一个赋值，我们看一下这里面是构造出一个 debug MQ push consumer i m p l，那么我们可以看到这里面是我们当前类似 debug MQ puts consumer，它里面依赖的一个实现类，也就是 defer 的 MQ consumer 和MPL。刚才也解释了其实 MPL 和 DQ 的 MQ push consumer 它们之间并没有继承的关系，这是我们创建对象的过程，把它程序执行下来，现在我们去设置我们的 name Server，我们都比较容易理解。接下来需要去做的是订阅我们的 topic 和我们关注的泰克标签。


进来以后，其实 subscriber 的操作，它其实在本身的 consumer 类里面并没有做什么操作，只是把这个操作代理给我们的 consumer MPR 去进行操作，可以看到他代理操作的内容，我们跟进来这里面，我们继续在这里面我们构造出来一个是 surprise bit，也就是说我们订阅的一些信息，这里面追踪这些订阅信息基于 robalance MPR 把内部的一个订阅信息的存储器就是一个 concurrent map。 hike map，它会把我们的订阅信息 put 进去，基于 topic 和 subscription data 这样一个对象传入进去，这一步它就把我们的订阅信息传入进去了。


我们现在回到我们的代码，接下来我们要注的事儿是我们的 reduced master listener，也就是说注册一下我们的一个消息监听，那么我们基于这个消息监听，也就是告诉程序当我们消息过来以后，我是怎么处理的。这里面我们是一个 master listener concurrently 的一个匿名类的实现，看一下这个类今天给它放到了什么位置，跟进这里面执行。得到这个渐进器以后，其实它也会把这个 method nation 也会转注册给我们对应的 consumer MPL 的实现。


接下来就开始我们 consumer 启动的工作了，看一下 consumer 启动带动了哪一些对象的创建启动。首先我们看这里面 set consumer group，那么 4S global 它会进行一层包装，这个过程我们就直接跳过，接下来是看到底吹的 MQ push consumer PL 的方式进行启动，这里面我们启动，我们跟进去启动的过程中它会首先判断一下我们当前的 service state，也就是说我们服务的状态。服务状态只有在 create just 就刚刚创建这个时间点，它才能进行启动，那么对于其他的一些死点启动的话，可以看到它会。其次抛出异常，也就是说这种程序状态是不合适的，那我们看一下它状态词 quit does 的情况下我们处理的逻辑。


这里面首先我们会把我们的 service state 设置为启动失败，可以看到基于使用启败进行我们的启动的过程，最终启动完成以后，它肯定会制造成一个running。这里面也是首先对于一个 configure 进行一个check，我们可以看一下它 check 的哪些内容。这里面 read Tech group ChatGPT 的内容，我们在介绍 producer 的时候也有介绍，其实它里面内容也就是再简单了解一下，其实也就是把不为空它的长度，还有它的一些制幅串，制幅级的一个情况给大家一个介绍。


那我们快速跳出接下来他要做的事是什么呢？是我们对应 consumer group 的一个校验，可以看到这里面校验的信息，当然他不能为闹，后面还会有一些，我们可以跟着断点走，在里面看一下它是否等于一些已经默认的 group 信息。这里面也是看我们当前的 master model，我们的疫情模式是不是被控，如果被控的话都直接抛出，这也是一些教研信息。我们继续，这里面是我们看一下我们的 located master Queen 的screads，也就是它是否有没有设，也就我们刚才知道它默认的是基于一个平均分配的策略。


这里面是我们的似乎有订阅信息，刚才已经特意订阅了信息，如果说没有订阅信息的话，你会抛intern，也就是说我们消费的内容不确定，就没必要去启动了，这里面去校验的是我们的 master listener，也就是说我们 message 我们得到我们消费消息以后需要怎么处理？如果说我们也没有配置 master mission 的话，也会抛出异常，需要配置 master mission 的，接下来我们这里面去判断，会判断一下当前我们的消息它是不是我们的，是我们看一下是不是顺序消费的，这里面还是我们是并行消费的，那么当然这两个是一个互斥的关系。


如果说我们顺序和并行它肯定只能选其一，如果说我们这里面看到它既没有设置顺序，也没有设置并行的话，那么也会抛出异常这样一个过程。接下来我们可以看到它对于消费线程数的一些设置，这里面我们可以看到它就是必须垂 1- 100 之降，我们可以看到它是 1- 100 自见。这里面我们看到它是一个 consumer 的一个线程数的最小的设置，最小设置我们是可以基于一个控制的一个设置值的，只要是在这个范围区间内就没问题。接下来这些我们校验逻辑，我们可以快速的跳过，看它后面的一些具体的一些工作内容，这里面也是对于一些阈值的一些校验，在这里面我们校验完成，接下来是拷贝我们的订阅信息，直接进行跳过我们继续。这里面我们看它通过 MQ client manager 是创建出一个 MQ client instance，这里面它的命名是 MQ client factory，其实这个命名其实并不是很容易理解。接下来是 rebless m，p， l 它的一些数据的一些初始化，我们的一些属性的注入，这里面构建出来一个 poor API wrapper，你就是把我们的一些请求信息包装起来，包装的对象主要是MQ， kind factory 以及我们的一些 consumer group 等等的一些信息。这里面我们会对于这些 poor API 包装器注册一下一个 filter method hope，我们去这里面我们会去获取一下我们的 offset store。


获取 offset store 的方式我们需要关心一下当前的模式是广播还是我们的集群模式，我们可以看到如果是它是广播模式的话，那么我们的 offset store 它是基于本地的方式去存储，如果是集群的话，我们需要基于远程的，也就是基于 BROKER 的方式去存储，我们继续这里面我们可以看到我们把 if MQ consumer 的 set of store 的方案设置进去，那么这里面对于 overstore 进行load，也就是加载我们的一些节点信息的过程。


这里面我们可以看到它是判断一下我们这个监听它是属于是否属于顺序的消息。当然我们这里面选的非数据的，非信非顺序的我们是并行的。并行的话这里面我们看到它会构建一个 consumer master service，这个 consumer master service 它两个实现，一个是基于顺序的一个实现，一个是一个并发的实现当前的发送信息是基于并行并发的一个消息。这里面我们把 consumer message service 进行启动，它启动的内容我们可以跟进来看一下，这里面启动的功能是起了一个定时任务，定期的去 clean 我们的一些过期的一些消息回来，这里面是我们会对这些 consumer 进行一个注射，接下来去判断一下注册的状态，如果说是注册状态不 OK 的话，在里面我们还会把它的状态回归为 create just，也就是说它一个初始化的状态。接下来它会到我们的 MQ kind factory 的一个启动的过程，看一下它启动做了哪些事情。在这里面启动过程我们看它其实对应的是 MQ kind instance 这一个客户端的一个实例。启动的过程我们看到首先会判断一下我们 service 的状态，这样状态是还是 create just，那么它如果是刚刚创建的一个过程，这里面它的逻辑也是先赋值为一个失败。


接下来我们去对于我们的 client API 的一个启动，以及对于我们 SUB studio 的 task 一个定任务的启动，以及我们的 Pro message service 的一个启动，这里面启动完成，对于我们跟客户端创建链接的过程就差不多完成了。


可以看到这里面其实还把我们的 MQ producer 也进行了一个启动，现在我们可以看到对应的 service state 设置为一个 running 状态，这里面是 MQ clinisense 的一个 service states，那么我们在这里面回到我们的 MQ push consumer m p l 在这里面它也会设置一个 running 的状态，那么这里面执行完成。

